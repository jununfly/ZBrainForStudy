# Handoff — 2026-08-08 16:49 GMT+8

> Compact handoff for a fresh agent to continue work on the zbrain TS→Rust migration.
> Created by session that closed: G85 (5 think test-impl desync) + E-option 8 pre-existing fail
> diagnosis + G72 (search telemetry) + G72 CLI wiring (8f790348). Repo state is clean and all
> explicitly requested work landed. The next concrete, scoped task is **closing out Part13
> minion handler wiring** (two roadmap flips + one render) — the handoff reader can pick that
> up in <10 minutes or pivot to a different track below.

---

## 1. Repo ground truth (verified at handoff time)

- **Branch / HEAD**: `rust-rewrite` @ `8761b316` (local) = `origin/rust-rewrite` HEAD
  - `git status` clean; `git rev-list --left-right --count HEAD...origin/rust-rewrite` → `0 0`
  - Working tree: no uncommitted changes
- **Last 4 commits on this branch** (most recent first):
  - `8761b316` chore(memory): log session-resume verification of commit+push + ls-remote propagation-delay rule
  - `7910195b` chore(memory): log G72 CLI writer wiring (commit 8f790348)
  - `8f790348` feat(zbrain-cli): wire default search telemetry writer at ctx construction
  - `35ede8e5` chore(memory): log G72 telemetry writer + --stats CLI (commit 02f5b11f)
- **Build/test state at handoff**: `cargo test -p zbrain-core --lib` → **2337 passed / 0 failed**
  - Caveat: that's the `core` lib only. `cargo test` workspace-wide may surface 0-2 transient
    `libsql_ffi` flakes on Windows native (see MEMORY.md "libsql FFI flake" rule); the CI is on
    `ubuntu-latest` so flakes don't gate.
- **Session kind**: Craft. ZBrain workflow uses `C:/Users/.../AppData/Local/Temp/zb_target_*`
  for cargo builds (monitor-lock workaround). Don't `cd` to `target/`, use `CARGO_TARGET_DIR=`
  with `C:/Users/...` Windows path (NOT `/c/` MSYS form).

## 2. Where to read project state (don't re-derive)

| What you need | Where to look |
|---|---|
| Project-level铁律 (Windows cargo / libsql / GAPS SSOT / 命名迁移 / port fidelity rule) | `C:\workspace\github\jununfly\zbrain\.workbuddy\memory\MEMORY.md` (~18 KB, exceeds 4k soft cap — historical debt, do not trim) |
| Today's working log | `C:\workspace\github\jununfly\zbrain\.workbuddy\memory\2026-08-08.md` (32 KB; full Part13 closeout + G72 + G1 + G85 + session-resume section) |
| Roadmap JSON (Part13 24 nodes) | `docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json` |
| Roadmap rendered md | `docs/plans/ZBRAIN_TS_TO_RUST_PART13_MINION_HANDLER_WIRING.md` (markers; render updates only the <!-- ROADMAP_SECTION_START/END --> slice) |
| Gaps SSOT | `docs/plans/KNOWN-GAPS.md` (G1/G72/G73/G84/G85 resolved; G74-G79 open + G80-G84 added) |
| TS residual inventory | `docs/plans/RESIDUAL-TS-INVENTORY.md` + `docs/plans/RESIDUAL_TS_AUDIT.md` |
| Migration prior parts | `docs/plans/ZBRAIN_TS_TO_RUST_PART{1..12}_*.md` (Part12 cycle = 48/48 done) |
| `roadmap_cli.py` | `C:\Users\victo\.workbuddy\skills\zj-roadmap-driven\roadmap_cli.py` — `link/render/decide/add/tree` first arg is JSON path, render from project root cwd |
| Skills base | `C:\Users\victo\.workbuddy\skills\` (see "suggested skills" §7) |

**Do NOT duplicate this content in the handoff target doc** — every line above is reachable
in 1 click. The remainder of this handoff is: what's *done*, what's *open*, what to do next.

## 3. Done in the closing session (2026-08-08, in order)

1. **G85 5 think test-impl desync (commit f4d19a3f)** — `entity.rs` tokenize flush on
   `STOP_WORDS || LEADING_VERBS`; `cite_render.rs` `(?i)` flag; `sanitize.rs` xml-attr-inject
   regex `(?:^|\s+)`; `gather.rs` graph-stream empty-guard; test 1 input → `"saw alice"`. All 5
   unit tests green.
2. **8 pre-existing fail (commit 67605362)** — 7 test-impl desync (think ×3 + search ×5) + 1
   real wiring bug: `ThinkOperation::execute` `chat: None` → `ctx.chat_provider.clone()`.
   New `OperationContext.chat_provider: Option<Arc<dyn ChatProvider>>` + `with_chat_provider`
   builder. MockChatProvider test rewritten for think tests requiring LLM.
3. **G72 search telemetry JSONL port (commit 02f5b11f)** — `crates/zbrain-core/src/search/telemetry.rs`:
   `SearchTelemetryEvent`/`SearchTimer`/`SearchTelemetryWriter` (eager-open
   `Mutex<Option<File>>`, no-op when `new(None)`) / `read_search_stats` / `StatsWindow`.
   9 unit tests green. Wired into `QueryOperation` + `SearchOperation` + `OperationContext.telemetry_writer` field.
4. **G72 CLI wiring (commit 8f790348)** — `zbrain query --stats [--stats-window hour|day|week|all]`
   + `parse_stats_window` + `format_search_stats`. `QueryArgs.query` made `Option<String>`.
   Default writer injected at `run_operation` in `crates/zbrain-cli/src/lib.rs`.
   End-to-end smoke: `zbrain init --pglite` → `zbrain query "database"` → 1 event
   `telemetry/search.jsonl` → `zbrain query --stats` reads count=1, p50=184ms.
5. **Session-resume verification + MEMORY.md rule added (commits 7910195b, 8761b316)** —
   Confirmed `commit+push` was intact despite `git ls-remote origin HEAD` showing stale SHA
   (bare-repo refs propagation delay). New rule in MEMORY.md: verify push via
   `git fetch origin` + `git rev-list --left-right --count HEAD...origin/<branch>` (target `0 0`),
   not via `ls-remote`.

## 4. Open / pending at handoff (in priority order)

### Priority 1 — Part13 收口 (estimated 10 min, recommend doing first)

19 minion handler 桩在代码层全部抵达终态（commit `906d2762` + 后续），但路线图 JSON 还有
2 个状态没翻，且 `render` 也没跑过。具体动作：

- 路线图 JSON 路径：`docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json`
- 现状（直接 `python` 跑就能复现）：
  - 根 `1`：`status = in_progress`, `mode = explore` → 应翻 `completed`
  - 容器 `1-3`：`status = pending`, `mode = exploit` → 应翻 `completed`（6 子节点已 `blocked`/wontfix 决策齐全）
  - 6 子节点 `1-3-1..1-3-6`：已 `blocked`（无需改）
  - 6 子节点 `1-3-1..1-3-6` 现状：lint→G80 / lint_fix→G81 / extract→G76 / integrity_auto→G82 / sync_retry_failed→G83 / repair_jsonb→G78（**1-3 容器决策已落盘**：`docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json` 的 `nodes['1-3'].decisions[0]`）

**步骤**（注意 `roadmap_cli.py decide` 键名坑）：
1. `python -c "import shutil,glob; [shutil.rmtree(d, ignore_errors=True) for d in glob.glob('docs/plans/*.json.lock')]"` — 清残留 lock（沙箱常见）
2. `python .workbuddy/skills/zj-roadmap-driven/roadmap_cli.py decide docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json 1` — 落 1 容器决策（"19 桩全终态"事实陈述），使用 `{"q","answer","note?"}` 键
3. 直接 Edit JSON 把 `nodes['1'].status` `in_progress`→`completed`、`nodes['1-3'].status` `pending`→`completed`（CLI 不一定支持此转换，验证后若不行就 `decide` 写一个新决策触发 render）
4. 跑 `python .workbuddy/skills/zj-roadmap-driven/roadmap_cli.py render docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json` —— **render 前先删 md 再 render**（marker 不符会追加成重复段）
5. `cargo check -p zbrain-core` + `cargo test -p zbrain-core --lib` 快速回归（应 2337 passed 不变）
6. `commit + push`（验证 = fetch + rev-list = `0 0`；别看 `ls-remote`）

### Priority 2 — G74-G79 收口（受 LLM seam 阻塞，估时 = 阻塞时长）

- **G74** eval 评测族 19 命令：缺 Rust verb，**blocked by G58 LLM seam**（Rust 缺 generateObject / structured chat seam 接 search）
- **G75** reindex 4 命令：`reindex-pages` 已接；`reindex-code/frontmatter/multimodal` 待独立切片
- **G76** extract 2 命令：blocked by G35/G60 LLM seam
- **G77** 图维护 4 命令：`backlinks` 已接；`backfill/edges-backfill/reconcile-links` 需新 core op（backfill_links/reconcile_links）
- **G78** 摄入/迁移 9 命令：Capture/ApplyMigrations 覆盖部分；`reinit-pglite/repair-jsonb` = pglite 死技术，建议 wontfix
- **G79** 杂项 16 命令：`recall` 已接；其余 15 TS-only（含 frontmatter 族、auth/providers/export/files/lsd/brainstorm/init-mode-picker 等）

解锁 G74/G76 需要 LLM seam（统一 ChatProvider + generateObject），看 `crates/zbrain-core/src/llm/` 当前覆盖面。

### Priority 3 — 516 悬空 `src/core` 引用（估时 1-2 h，大工程）

`tests 510/1078` + `src/eval 5/8` + `evals 1/3` + 若干 `scripts/check-*.sh` 还 import 死 TS。
两个选择：
- **A. 删/迁**：用 `git grep -l "from.*core/"` 全量列出，按文件分类删掉或迁到 `crates/zbrain-core` 对应模块
- **B. 留**：让 CI `bun test` 继续红（仓库已不再将 `bun test` 列入硬门槛——见 MEMORY.md "TypeScript 基线 gate 假阳性"），仅维护 `rust-tests.yml` 绿

推荐 B（仓库历史决策一致），除非外部 CI 真把 bun test 列入阻断。

### Priority 4 — 其他 Part11/Part12 残留

`TaskList` 显示 6 in_progress / 39 pending 大多是历史占位（如 #178 facts queue、#116/117 1-5-4/1-5-6），多数已被后续 commit 隐式完成但 task 没标 completed。建议接手时跑一遍 `TaskList` + `git log --oneline -- <path>` 交叉核对，单批关停。

## 5. Risks / sharp edges for the next session

1. **MEMORY.md 已 18 KB 远超 4k 软上限**：是历史欠账（不是本次会话造成的）。继续 append 可行但效率下降。如果接手 agent 打算主动瘦身，先备份再 edit，并保留所有「**铁律**」「**坑**」类条目。
2. **Part13 JSON shape 是 `dict[id] -> node` 非 list**：早期 python 脚本踩过 `KeyError: slice` 坑，要 `for nid, n in nodes.items():` 而非 `for n in nodes[:3]:`。
3. **`roadmap_cli.py decide` 键名**：用 `{"q","answer","note?"}`，用 `a` 键会 KeyError；只读焦点节点 (in_progress) 的 decisions。
4. **libsql FFI flake（Windows）**：见 MEMORY.md "libsql FFI flake" 段。libsql 集成测试有进程级 `OnceLock<Mutex<()>>` 降频。
5. **`.git/index.lock` 僵尸锁**：见 MEMORY.md。沙箱 `rm`/`del` 都被拦截，唯一绕法 `mv` 改名。
6. **`git stash` 灾难**：见 MEMORY.md。**别在沙箱用 stash** 探 baseline。
7. **TS 算法取证**：很多 Rust port 行为 = TS 历史 commit 决定，需要时 `git show <sha>^:src/core/<path>` 取回原文（不是 HEAD，HEAD 时常已删）。如 G72 telemetry 取 `bcafcafd^:src/core/search/telemetry.ts`。

## 6. Concrete first 30-minute action (if you take Priority 1)

```
# 1. 清 roadmap lock 残留（沙箱常见）
python -c "import shutil,glob; [shutil.rmtree(d, ignore_errors=True) for d in glob.glob('docs/plans/*.json.lock')]"

# 2. 核 Part13 当前状态
python -c "
import json
d = json.load(open('docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json'))
for nid in ('1','1-3'):
    n = d['nodes'][nid]
    print(nid, n.get('status'), n.get('mode'), n.get('label','')[:50])
"

# 3. 直接 Edit JSON（CLI 翻转状态不一定支持）
# 改 nodes['1'].status = 'completed'
# 改 nodes['1-3'].status = 'completed'

# 4. 落决策（用对的键名）
python .workbuddy/skills/zj-roadmap-driven/roadmap_cli.py decide \
  docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json 1
# q: "Part13 19 桩是否全部抵达终态？"
# answer: "yes - 12 接好 + 6 wontfix + 1 半接 (contextual_reindex G84 resolved)"
# note: "..."

# 5. 删 md 再 render（避免 marker 重复段）
rm docs/plans/ZBRAIN_TS_TO_RUST_PART13_MINION_HANDLER_WIRING.md
python .workbuddy/skills/zj-roadmap-driven/roadmap_cli.py render \
  docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json

# 6. 验证 + 提交
cd C:/workspace/github/jununfly/zbrain
cargo test -p zbrain-core --lib 2>&1 | tail -3
git add docs/plans/
git commit -m "chore(roadmap): close Part13 — 19 minion handlers all terminal"
git push origin rust-rewrite
# 验证 push：git fetch origin && git rev-list --left-right --count HEAD...origin/rust-rewrite  → 0 0
```

总时长 ~10 min。如果该动作不符合接手 agent 的兴趣，跳到 Priority 2-4 任一。

## 7. Suggested skills to load

When you continue, these skills are most likely relevant (in priority order):

- **`zj-roadmap-driven`** — 路线图驱动开发必备。CLI 在 `C:\Users\victo\.workbuddy\skills\zj-roadmap-driven\roadmap_cli.py`。铁律见 MEMORY.md "Roadmap 铁律" 段（marker / 单段 / lock / python 路径）。
- **`zj-grill-me`** — 如果用户给的是"模糊继续"指令，先用本 skill 逼出最小可执行问题。
- **`zj-tdd`** — 接 G75/G77 等新 verb 时，red→green→refactor loop。
- **`zj-diagnose`** — 如果遇到 hard bug 或 perf regression。
- **`zj-prototype`** — 任何超过 100 行 throwaway 探索代码前先 prototype。
- **`surgical-codebase-rewrite`** — 改 existing module 不破坏上下游时用。
- **`zj-improve-codebase-architecture`** — G74-G79 收口或 G75 multi-slice 决策时，可能需要重审架构。
- **`superpowers`** — 综合方法论（多 skill 复合时）。

**Skip for this handoff**：
- `tencent-pptx` / `wb-finance-skill` / `westock-data` / `pdf` / `docx` / `xlsx` — 与 zbrain 工程无关。
- `agent-browser` — 除非接手 agent 准备 e2e 浏览器验证（当前 zbrain e2e 走 `bun test` 已在 CI 红、本地不阻断）。

## 8. Open questions for the human (if any)

无阻塞问题。所有 open 路径都已在本 handoff §4 标出优先级 + 估时 + 第一步命令。
如果接手 agent 不想做 Part13 收口，需要 human 决策走 G74-G79 / 516 引用清理 / 其他方向。

---

**Handoff author**: closing session (G85 + 8 fail + G72 + G72 CLI + 收尾核验)
**Handoff time**: 2026-08-08 16:49 GMT+8
**Handoff location**: `docs/plans/2026-08-08-handoff.md`（用户指定放在 docs/plans/，非 skill 默认临时目录；与 ZBRAIN_TS_TO_RUST_PART*.md 同源）
**Next agent's first read**: this doc §6 ("Concrete first 30-minute action") → §2 ("Where to read project state") → §4 ("Open / pending")
