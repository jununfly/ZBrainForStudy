# 2026-07-20

## 完成 1-6-5-6 (routing-eval / Check 5 TS→Rust 全栈迁移)

续 2026-07-19 起点。本批把 routing-eval 整条栈从 TS 迁到 Rust，覆盖 grill 锁定的 7 子节点，全部完成。

**核心 (crates/zbrain-core/src/skill_resolver/)**
- 新建 `routing_eval.rs`：Section A(normalize/extract/slug/index) + B(lint, FixtureLintReason intent_copies_trigger/unknown_expected_skill/invalid_shape) + C(load JSONL, MalformedFixture) + D(run + structural_route_match + ALWAYS_ON_SKILLS={signal-detector,brain-ops,ingest})。29 单测全绿。
- 给全部公开类型补 `serde::Serialize` + rename（RoutingReport camelCase、RoutingOutcome snake_case、RoutingCaseResult matchedSkills、FixtureLintReason snake_case、RoutingFixture/RoutingCaseResult/MalformedFixture/FixtureLintIssue），供 `--json` 信封 parity。
- `check_resolvable.rs` 接 Check 5 为 warnings（仅当树含 routing-eval.jsonl 时跑 lint/missed/ambiguous/false_positive/malformed），2 个接入单测通过。
- `trigger_index.rs::parse_resolver_entries` 提 `pub`；`mod.rs` 加 `pub mod routing_eval`。

**CLI 外壳 (crates/zbrain-cli/)**
- 新建 `routing_eval.rs`：`RoutingEvalArgs`(--json/--llm/--skills-dir) + `run_routing_eval_command` + Envelope + resolve_skills_dir + render_human。退出码契约对齐 TS：0=clean / 1=任意失败 / 2=setup 错(no_skills_dir|no_resolver)。1 个信封序列化单测。
- `--llm` **拒绝**(exit 1，stderr 说明)，与 check-resolvable `--fix` 拒绝原则一致(诚实、不接収假 no-op)，落实 grill Q3/q-0 决策。
- `lib.rs`：`pub mod routing_eval` + `Commands::RoutingEval` + dispatch。

**TS 清理**
- 删 `src/commands/routing-eval.ts`（独立命令，已被 Rust 取代）。
- `src/cli.ts`：删 `routing-eval` dispatch 分支 + 从 `CLI_ONLY` 集合移除 `'routing-eval'`（注意：首次 Edit 因行首空格不匹配未生效，二次用 `'dream', 'routing-eval', 'skillify'` 精确匹配才真正移除，grep 已确认 cli.ts 全净）。
- `src/core/routing-eval.ts` 保留：仍被活的 `src/core/check-resolvable.ts`(export `parseResolverEntries` 供 mounts-cache/skill-trigger-index 消费) import，属 check-resolvable 核心清理节点(1-6-5-9)范畴，不在本切片内删。

**验证**
- `cargo test -p zbrain-core skill_resolver` 79 单测全绿；`cargo test -p zbrain-cli` 272 全绿。
- E2E（编译 `target/debug/zbrain.exe` 实跑）：`--help` exit0 / clean 树 `routing-eval: OK — 3 case(s), 100% top-1 accuracy` exit0 / `--json` 信封 camelCase 字段正确 / `--llm` exit1 / 不存在 skills-dir exit2 / miss exit1 / 无 resolver exit2，全部符合。

**roadmap 状态**：1-6-5-6-1~7 全部 completed（提交 `496c74d`）。

**待办**：提交 1-6-5-6（含 routing_eval.rs 双文件 + cli.ts 清理 + roadmap 渲染 + 本 memory）。typecheck baseline 门需在能跑 bun 的环境复核（预计 0 new，因删除文件只减错误数）。

## bun 环境已可用（修正"无 bun"记录）

用户要求"在本机安装 bun 环境"，但查证发现 bun 早已装好：`~/.bun/bin/bun` v1.3.14，node_modules 在。之前"本机无 bun"记录已过时。

**已验证（补齐 1-6-5-6-7 验证缺口）**
- `bun test ./tests/unit/routing-eval.test.ts` → 35 pass / 0 fail（TS 单测实跑绿）。
- `bash scripts/typecheck-baseline.sh` → exit 0，OK，64 inherited，**0 new**（routing-eval 删除未引入新 TS 错误）。
- 注意 bun 路径过滤需 `./` 前缀（`bun test ./path/to/file.test.ts`），裸路径会被当 filter。

正式结论：bun 环境本就存在，无需安装；已用其跑通 bun 单测 + typecheck baseline 两道门，本机现在可以完整验证 TS 分片。

## grill-me + roadmap-driven：定 1-6-5-7 下一刀

用户 `/zj-grill-me /zj-roadmap-driven 接下来应该做什么`。逐层拷问落定：
- 方向：留 1-6-5 轨道吃完（7/8/9 → 收口 1-6-6）。
- 顺序：1-6-5-7 filing-audit (Check 6) 先（独立自包含 port，与 routing-eval Check5 同构）。
- 范围：**仅核心 + 接 check_resolvable 警告，不含独立 CLI**（TS 端 filing-audit 只有 check-resolvable.ts 一个消费者，从无独立命令）。
- 子节点：细粒度 4 个（1-6-5-7-1 load / -2 run / -3 wire / -4 收尾），已 add+in_progress+render。
- fixture：inline 字符串（scratch 目录现造 _brain-filing-rules.json + SKILL.md）。
- 4 个 decide 已落盘 1-6-5-7。
- TS 探查事实：filing-audit.ts 仅被 check-resolvable.ts import；合约 loadFilingRules(_brain-filing-rules.json, 缺失=no-op/throw on malformed) + allowedDirectories + normalizeDir + runFilingAudit(writes_pages:true 技能查 缺失/未知目录→两类warning)。Rust 落 filing_audit.rs（镜像 routing_eval.rs），复用 parse_skill_frontmatter，IssueType::Filing* 已预留。TS filing-audit.ts 本切片不删，归 1-6-5-9 整删。

## 完成 1-6-5-7-1 (filing-audit 核心 load/normalize/allowedDirectories)

新建 `crates/zbrain-core/src/skill_resolver/filing_audit.rs`（镜像 routing_eval.rs）：
- 类型：FilingRule / SourcesDirEntry / FilingRulesDoc（serde Deserialize，rules 必填、其余 default 宽松）/ FilingIssueType(enum snake_case, as_str) / FilingSeverity(enum lowercase) / FilingIssue(camelCase, `type` rename, directory skip_serializing_if) / FilingReport(Default)。
- Section A：normalize_dir（去首尾/ + 尾斜杠，空/root→""）、load_filing_rules（缺失→Ok(None)，malformed→Err(String)）、allowed_directories（rules+sources_dir 合并→HashSet<String> 归一）。
- Section B（runFilingAudit）留注释占位，归 1-6-5-7-2。
- 10 单测全绿：normalize 4 态、load 缺失×2 / malformed×3（非对象/非数组/坏JSON）/ 正常解析、allowed 合并归一。

`mod.rs` 加 `pub mod filing_audit`。roadmap 1-6-5-7-1 completed、1-6-5-7-2 in_progress、render 完成。
注意：zbrain-core 编译有预存 warning `unused import: crate::error::Result`（admin_queries/calibration_queries/import/migration/oauth_queries/token_queries 顶层），与本次改动无关，非回归，本切片不处理。

## 完成 1-6-5-7-2 (run_filing_audit scan + 两类 warning)

`filing_audit.rs` Section B：实现 `run_filing_audit(skills_dir: &Path) -> Result<FilingReport, String>`：
- load_filing_rules 返回 None→空 report（no-op）；malformed→Err(String) 上抛（对齐 TS loadFilingRules throw）；read_dir 失败→空 report（对齐 TS catch）。
- 遍历子目录：跳过 `.`/`_` 前缀、非目录、无 SKILL.md；total_scanned++。
- parse_skill_frontmatter；仅 `writes_pages == Some(true)` 审计（D-CX-7：mutating:true/false/undefined 都跳过）。writes_pages_skills++。
- 缺 writes_to（空/None）→ FilingMissingWritesTo（directory=None）；否则逐目录 normalize_dir 比对 allowed，不在→FilingUnknownDirectory（directory=Some(raw)）。
- 复用既有 `parse_skill_frontmatter`，skill_name = fm.name ?? entry（message 用 skill_name、action 用 entry 目录名，对齐 TS）。

8 个 Section B 单测全绿（rules 缺失 / malformed Err / missing writes_to / unknown dir / clean allowed / 跳过非 writes_pages / 跳过 dot&underscore / 多 unknown 计数）。filing_audit 合计 18 测全绿。

踩坑：missing 分支误用 `skill: skill_name`（move），unknown 分支 `skill_name.clone()` 触发 E0382；统一改 `skill_name.clone()` 解决（missing 分支有 continue 但借用检查器跨分支报移动）。

roadmap 1-6-5-7-2 completed、1-6-5-7-3 in_progress、render 完成。

## 完成 1-6-5-7-3 (接 Check 6 进 check_resolvable 为 warnings)

`check_resolvable.rs` 在 Check 5 块后插 Check 6 块：调 `run_filing_audit(skills_dir)`（Result）。Ok→把每个 FilingIssue 按 type 映射为 ResolvableIssue（FilingMissingWritesTo/FilingUnknownDirectory，severity=Warning，复用 issue.skill/message/action，fix=None）。Err(malformed rules)→推一条 IssueType::FilingUnknownDirectory、skill='brain-filing-rules'、message='_brain-filing-rules.json failed to load'、action='Fix skills/_brain-filing-rules.json: ' + e（**对齐 TS catch 块 quirk**：TS 复用 filing_unknown_directory 表示 malformed，故 Rust 也复用，保证迁移后 issue 列表一致）。rules 缺失时 run_filing_audit 返回空 report → 不推 issue（"仅当 _brain-filing-rules.json 存在"自动满足）。

3 个接入单测全绿：check6_filing_missing_writes_to_warns / check6_skipped_when_no_rules_doc / check6_malformed_rules_doc_warns_unknown_directory。check_resolvable 合计 14 测全绿。

roadmap 1-6-5-7-3 completed、1-6-5-7-4 in_progress、render 完成。

## 完成 1-6-5-7 (filing-audit Check 6 全切片收口)

1-6-5-7-4 收尾：typecheck baseline 门 64 inherited / 0 new（本切片未引入新 TS 错误）。提交 `b6d4e3e`（7 files +916/-7：filing_audit.rs 新文件 + check_resolvable.rs + mod.rs + memory + roadmap；注释卫生修正 amend 进同提交）。roadmap 1-6-5-7-4 与 1-6-5-7 标 completed + render。

**整切片验证**：filing_audit 18 测全绿（Section A 10 + Section B 8）、check_resolvable 14 测全绿（含 3 个 Check 6 接入测试）、typecheck baseline 0 new。

**范围纪律落地**：仅核心 Rust 化 + 接 check_resolvable 警告，无独立 CLI（TS filing-audit.ts 保留，归 1-6-5-9 整删）；git 改动零 TS 删除。

**下一步轨道（1-6-5 收口）**：1-6-5-8 dry-fix（--fix 写回，撞"check-resolvable --fix 拒绝"原则需先定哲学）/ 1-6-5-9 重接 doctor+skillify-check 并删 TS check-resolvable.ts 核心栈。

## 完成 1-6-5-8 (dry-fix --fix 写回路径 TS→Rust 全切片)

grill 锁 3 决策：①--fix 留在 check-resolvable（Rust shell stub 本就留位，1-6-5-8 授权落地，不再是假 no-op）；②完整对齐 TS：--dry-run 仅预览 + --fix 真实写回，带完整安全闸门（working-tree 脏/非 git 仓库/代码 fence 内/已 delegate/多匹配 全 skip）；③REPLACE(跨切面 DRY)+INSERT(brain-first 合规缺失) 全移植。

**新建 crates/zbrain-core/src/skill_resolver/dry_fix.rs**
- 共享类型：FixOutcome / AutoFixReport(Default) / SkipReason(enum) / FixStatus(Applied/Proposed/Skipped) / AutoFixOptions{dry_run}。
- 安全闸门（slice 1）：is_inside_code_fence(字节偏移 fence 奇偶，对齐 TS isInsideCodeFence 严格前缀语义) / get_working_tree_status(git status --porcelain) / is_working_tree_dirty(非 git 仓库→false 不阻断)。
- REPLACE（slice 2）：detect_block_shape + expand_bullet/expand_blockquote/expand_paragraph + attempt_fix(多匹配→skip / code-fence→skip / delegation→skip / working-tree gates) + auto_fix_dry_violations REPLACE 循环，复用 check_resolvable::CROSS_CUTTING_PATTERNS / extract_delegation_targets / DRY_PROXIMITY_LINES。
- INSERT（slice 3）：find_insertion_line + MISSING_RULE_PATTERNS + attempt_insert_fix，接 analyze_skill_brain_first(来自 brain_first.rs) status==Warn 触发。

**新建 crates/zbrain-core/src/skill_resolver/brain_first.rs**
- analyze_skill_brain_first 完整合规阶梯（tier1 现有 callout / tier2 missing 且不在豁免 / tier3 typo 映射）+ 2 豁免（节点名豁免集合 / 无待迁移说明链路）+ strip_frontmatter / find_first_*_offset + 模式常量（EXTERNAL_LOOKUP_PATTERN / BRAIN_REFERENCE_PATTERN / CONVENTION_CALLOUT_PATTERN / PHASE_HEADING_PATTERN / DELEGATION_LINE_PATTERN / NODE_NAME_EXEMPTION / MISSING_RULE_EXEMPTION / NO_MIGRATION_EXEMPTION）。

**skill_frontmatter.rs**：加 format_brain_first_typo_hint（brain_first_typo 消费）。
**mod.rs**：接 `pub mod dry_fix;` + `pub mod brain_first;`。
**zbrain-cli/src/check_resolvable.rs**：删 --fix/--dry-run stub（原 exit1 拒绝），真实接 auto_fix_dry_violations 填 Envelope.autoFix；抽 run_check_resolvable_core（可单测，无 std::process::exit）；加 render_autofix_human；修 stale "NOT YET IMPLEMENTED" 注释。

**验证**：dry_fix 14 / brain_first 12 / CLI check_resolvable 5 单测；全 core 1813 / CLI 275 全绿；typecheck baseline 0 new（64 inherited）。
**提交**：`3a8e1b9`（8 files：dry_fix.rs + brain_first.rs 新 + skill_frontmatter/mod/check_resolvable.rs + memory + roadmap json/md）。roadmap 1-6-5-8-1~4 与 1-6-5-8 全 completed + render。

**铁律提醒**：TS 源 dry-fix.ts / skill-fix-gates.ts / skill-brain-first.ts 本切片不删（保留给 1-6-5-9 整删 check-resolvable.ts 核心栈时一并清），git 零 TS 删除。
**下一个轨道节点**：1-6-5-9（重接 doctor/skillify-check 并删 TS check-resolvable.ts 核心栈，capstone 收口整条校验栈）。

## 完成 1-6-5-9 (capstone：TS 校验栈整删 + brain-first 收口到 Rust) — 提交 62ee635

轨道 1-6-5-7→8→9 一路推进，本切片收口整条校验栈：
- 1-6-5-9-1/2（前序已完成）：抽 `src/core/resolver-entries.ts`（`parseResolverEntries`/`ResolverEntry`）重指 mounts-cache/skill-trigger-index 两活消费者；删 routing-eval.ts（已死）+ check-resolvable 栈源（check-resolvable/dry-fix/filing-audit/skill-fix-gates.ts + commands/check-resolvable.ts）+ 10 个 TS 测试；重定向 2 个混合测试（openclaw-compat→Rust `check-resolvable` 子进程、skill-trigger-index→resolver-entries.ts）。
- 1-6-5-9-3（本批）：Rust 新子命令 `zbrain check-brain-first <skill.md> --json`。`brain_first.rs` 加 serde（BrainFirstReason snake_case / BrainFirstStatus lowercase）+ `build_brain_first_summary_line`（对齐 TS 契约：`ok → {skill}: ok ({reason})`，`warn → {skill}: external lookup ({patterns}) without brain-first compliance [+formerly_exempt][+typo]`）+ `build_brain_first_fix_hint`。新建 `crates/zbrain-cli/src/check_brain_first.rs`（Envelope：skill/status/reason/external_patterns_matched/typo_hint/formerly_hardcoded_exempt/summary_line/fix_hint/error），ok→exit0 / warn|error→exit1。`lib.rs` 加 `Commands::CheckBrainFirst` + dispatch。
- 1-6-5-9-4（本批）：`src/commands/skillify-check.ts` Item 12 删 `skill-brain-first.ts` import + 本地 `checkBrainFirstCompliance`，改 spawn `zbrain check-brain-first <path> --json` 读 JSON 映射 passed/detail（ok→`{reason} ({skill})`，warn→`summary_line — fix_hint`）。删 `src/core/skill-brain-first.ts` + `skill-brain-first.test.ts`；audit 测试拆到 `tests/unit/audit-skill-brain-first.test.ts`（保留 audit 模块）。doctor 不动。
- **关键新约定**：新增 `src/core/zbrain-bin.ts` `resolveZbrainBin()`——解析 Rust 二进制顺序 `$ZBRAIN_BIN` → `target/debug/zbrain[.exe]` → `target/release/zbrain[.exe]` → PATH。TS 子进程必须用它，**不能** `bun src/cli.ts <rust-cmd>`（src/cli.ts 不代理 Rust 命令，会报 Unknown command）。已把 skillify-check 的 check-resolvable spawn 也切换到它（修 dev 解析）。顺带修了 openclaw-reference-compat e2e 测试（它之前错误地 spawn `bun src/cli.ts check-resolvable`，已坏）。

**验证**：cargo zbrain-core brain_first 14 / zbrain-cli check_brain_first 1 全绿；bun 21 测试全过（含新 check-brain-first 集成测试 ok/warn/error 三态 + 保留 audit 测试 + 修好的 openclaw 测试）；`bun src/cli.ts skillify check` 实跑 Item 12 走通（no_skill_md→best-effort pass）；typecheck baseline 0 new（64 inherited，7 stale error 消失）。
**提交**：`62ee635`（32 files，+815/−6012）。roadmap 1-6-5-9-1~5 全 completed + render。

**下一步**：1-6-5 父节点收口；残余 TS 待迁清单见 RESIDUAL-TS-INVENTORY.md（B=eval+schema-pack+skillify+doctor），brain-first/skillify-check 栈已清出。

## grill + roadmap-driven：operations.ts 收尾战略（本批）

用户 `/zj-grill-me /zj-roadmap-driven 接下来做什么`。四道决策落盘：
- **Q1**：1-6-6 是 1-6-5 已完九切片的逐字重复 phantom 节点（无子节点/无决策/无备注）→ 标 completed + render。focus 随后落 1-3（但其仅剩 1-3-3 blocked，故实际卡死）。
- **Q2**：下一步主线 = **operations.ts**（共享阻塞点，同时卡 1-4-2 output infra 与 1-10 schema-pack 删尾）。
- **Q3**：打法 = **替换式增量迁移**（Rust `OperationRegistry` 为继任者，非字面整搬）。
- **Q4**：第一步 = **先出覆盖审计 + 建 roadmap**（不写业务代码，先把地图画清）。

**探索关键发现**：
- `operations.ts` = 4243 行遗留 agent 工具注册表（107 唯一 op，含 ~3750 行内联实现），live 入口是 `src/cli.ts` + `src/mcp/dispatch.ts`/`tool-defs.ts`。
- Rust 继任者已就位：`crates/zbrain-core/src/operation.rs`（6354 行，已有 `OperationContext::remote_mcp`/`local_cli` 信任边界 + `OperationRegistry` + `Operation` trait），**已 port 10 个 Operation**（page CRUD 6 + query + think + takes_list + takes_search）；`zbrain-mcp` 注册这 10 个；`BrainEngine` trait ~120 方法覆盖绝大多数 op 域。
- 结论：剩余 ~97 op 绝大多数 = "把已有 engine 方法包成 Operation"（机械 wrap），仅 3 个 NET_NEW（file_upload 校验器 validateUploadPath / search_by_image / get_brain_identity）。

**交付物**：
- `docs/plans/MIGRATION.md`：107 op 逐一对齐表 + 领域 rollup（PORTED=10 / WRAP=87 / COMMAND_ELSEWHERE=7 / NET_NEW=3）。生成脚本 `scripts/ops_audit.py`（读 `scripts/ts_ops.txt`）。
- roadmap `1-6-7` 父节点（in_progress）+ 9 领域子切片（1-6-7-1 统一 live registry+tracer-bullet → … → 1-6-7-9 终局删 operations.ts）+ render。

**下一步实操**：开 1-6-7-1——汇总 operation.rs 碎片注册为全量 `register_all`，并以 page 剩余 WRAP（update_slug/rewrite_links/soft_delete/page timestamps）作第一批 tracer-bullet。

## 完成 1-6-7-1 (统一 live registry + page ops tracer-bullet) — 提交 11bfa15 + 6ea3583

用户「转为前台执行」：停掉后台 cargo（taskkill cargo.exe/rustc.exe），前台重跑 `cargo test -p zbrain-core page_ops_tests && cargo test -p zbrain-mcp && cargo build -p zbrain-cli`，全绿（core 6 passed / mcp 14 passed / cli Finished，exit 0）。

**代码改动**（operation.rs +430 行）：
- `pub fn register_all(registry: &mut OperationRegistry)` 汇总全量 live registry，替代 cli/mcp 三处碎片注册（cli.rs 1889/2200 两处 + mcp make_registry + cli 测试 7521）。
- 新增 4 个 page WRAP op：`SoftDeletePageOperation`(local_only=true,mutating) / `RewriteLinksOperation` / `GetPageTimestampsOperation` / `RefreshPageBodyOperation`（TypedOperation blanket，input_schema/local_only/mutating 齐）；各带单测断言 InMemoryEngine 真实实现。
- cli.rs 删 1864 孤儿 import 块（`use zbrain_core::operation::{...}` 重复集），3 live + 1 测试改调 `register_all`；mcp.rs `make_registry` 改调 `register_all` + 加 `get_page_timestamps` 经 MCP 暴露断言。
- 当前真实生产 op 注册数 = 10（原 6 page CRUD + query/think/takes_list/takes_search）+ 4 新 page = 14。

**提交**：`11bfa15`（10 files，+1114/-89：operation.rs + cli.rs + mcp.rs + 审计 doc + ops_audit.py + ts_ops.txt + roadmap json/md + memory）；`6ea3583`（roadmap 1-6-7-1→completed + render）。工作树干净。

**下一步**：1-6-7-2 标注/图域 wrap（tags 6 + links-graph 9 + timeline 3），engine 已有方法机械包成 Operation 并 register_all 接入。

## 完成 1-6-7-2 (tags/links/timeline 域迁移到 register_all) — 待提交

grill 5 题全收推荐 + 6 个 decide 落盘（发现并修正：审计 18 实为去重 10 op）。

**代码改动**（operation.rs）：
- 新增 10 个生产 op 并 register_all 接入（真实注册数 14 → 24）：
  - tags(3)：`AddTagOperation` / `RemoveTagOperation` / `GetTagsOperation`（纯 WRAP `add_tag`/`remove_tag`/`get_tags`）。
  - links(5)：`AddLinkOperation`(调 `add_links_batch` 包单条，from/to/origin_source_id=ctx.source_id) / `RemoveLinkOperation`(调 `remove_link`) / `GetLinksOperation` / `GetBacklinksOperation` / `TraverseGraphOperation`。
  - timeline(2)：`AddTimelineEntryOperation`(移植 TS 日期校验 `validate_timeline_date`，chrono 0.4.44 需 `use chrono::Datelike` 解私有 year 坑) / `GetTimelineOperation`(wrap `get_page` 读 `.timeline` TEXT 列按行解析 JSON)。
- **两处偏离（模块级注释已记录，不得引用 roadmap 编号）**：
  - `traverse_graph` 始终返回 `GraphPath[]`（`#[serde(transparent)]` 裸数组），不返回 TS legacy `GraphNode[]`。
  - `get_timeline` 读 Rust `page.timeline` TEXT 列（每行 JSON），非 TS 独立 `timeline_entries` 表——Rust 模型为 canonical。
- 踩坑修复：①`AddLinkOutput.added: u64` 但 `add_links_batch` 返回 `usize` → cast `as u64`；②`TimelineEntryOutput` 仅 derive Serialize 缺 Deserialize（get_timeline 需 `from_str`）→ 补 Deserialize；③chrono `year()` 私有 → 导入 `Datelike`；④测试 `res.unwrap()["entries"]` 临时值借用 E0716 → 先 `let out =`。

**验证**（复用 1-6-7-1 三道门）：`cargo test -p zbrain-core operation::tests::` 170 passed（含 10 新 domain 测试）/ `cargo test -p zbrain-mcp` 14 passed / `cargo build -p zbrain-cli` Finished。全绿。

**已提交**：`2688b53`（operation.rs + roadmap json/md，1-6-7-2→completed + render 已做）。

**下一步**：1-6-7-3…8 其余领域切片；终局 1-6-7-9 整删 operations.ts。

## 完成 1-6-7-3 (sources/facts/anomalies/health-stats 域迁移到 register_all)

grill 探查翻出审计 3 处硬伤（已落盘 1-6-7 decide）：①schema-pack(9) 实为 COMMAND_ELSEWHERE（Part10 Phase12 已 100% 完成）→ 1-6-7-6 摘除非 wrap；②recall 实为 TS-only（无 Rust CLI）→ 必须迁非摘除；③jobs/minions 仅 6/12 有 engine 方法。用户决策：**缺口 ops 就地补方法（不设 1-6-7-10）**；**一次推完 1-6-7-3…8，每切片三道门全绿后独立 commit，1-6-7-9 留待下次**。

**代码改动**（operation.rs）：新增 11 个生产 op 并 register_all 接入（注册数 24 → 35）：
- sources(4)：`SourcesAddOperation`(create_source) / `SourcesListOperation`(list_sources) / `SourcesStatusOperation`(get_source→404 转 invalid_params) / `SourcesRemoveOperation`(delete_source→removed:bool)。
- facts(3)：`ForgetFactOperation`(expire_fact 干净) / `ExtractFactsOperation`(简化包 insert_fact，接预抽取文本；Rust 无 LLM 抽取流水线) / `FindContradictionsOperation`(简化包 get_facts_health 作代理；Rust 无 eval 矛盾探测)。两简化 stand-in 已在 op 注释 + roadmap notes 标注，待专项切片补 LLM 流水线/矛盾探测。
- anomalies(1)：`FindAnomaliesOperation`(find_anomalies，by value 传 AnomaliesOpts)。
- health-stats(3)：`GetHealthOperation`(get_health→BrainHealth) / `GetStatsOperation`(get_stats→QueueStats) / `GetRecentSalienceOperation`(get_recent_salience→Vec<SalienceResult>)。**health/salience/stats 仅是 cliHints 别名，agent registry 不重复注册**（审计标 6 实为 3）。

**关键点**：`find_anomalies` 按值传 `AnomaliesOpts`（非 `&`）；`get_recent_salience` 的 `slug_prefix` 要 `Option<&str>`（`.as_deref()`）；`insert_fact` 的 entity_slug 借用+移动冲突→clone 解决；测试 `res.unwrap().as_array()` 临时值借用 E0716→先 `let out =`。

**验证**：`cargo test -p zbrain-core operation::tests::` 176 passed（含 1-6-7-3 新测试 + 注册计数 35）/ `cargo test -p zbrain-mcp` 14 passed / `cargo build -p zbrain-cli` Finished。全绿。

**下一步**：1-6-7-4 jobs/minions（12 op，其中 6 个缺 engine 方法需就地补：get_job_progress/replay_job/send_job_message/submit_job/submit_agent/subagent）。

---

## 1-6-7-5 ingestion+files+calibration+transcripts 完成 (commit 0b7c63a)

**范围澄清**：审计标 "ingestion(4)+files(5)=9 op" 又偏——实际 8 个 distinct op（审计不可信已成第四处硬伤）。8 op：get_chunks / log_ingest / get_ingest_log / file_list / file_upload / file_url / get_calibration_profile / get_recent_transcripts。

**实现**：
- engine 层 4 方法 list_files/get_chunks/log_ingest/get_ingest_log：InMemory + Libsql 实装，Postgres 走 trait 默认 Unsupported。双后端迁移 0019 content_chunks / 0020 ingest_log。
- get_calibration_profile：BrainEngine 加默认方法（返回 Unsupported），InMemory/Libsql 覆盖委托 `CalibrationQueries::get_latest_profile`；Postgres 默认 Unsupported（PostgresEngine 未 impl CalibrationQueries，不强制）。
- 8 个 TypedOperation 接入 register_all（46→54）。file_upload 复用 `validate_upload_path` 信任边界（remote=strict / local=loose）+ `upsert_file` 仅记元数据（与 TS 无 storage 后端时一致，不拷字节）。file_url 用既有 `get_file(source_id, storage_path)`。
- get_recent_transcripts 走环境变量 `ZBRAIN_DREAM_SESSION_CORPUS_DIR` / `ZBRAIN_DREAM_MEETING_TRANSCRIPTS_DIR` 兜底（Rust engine 无运行时 config kv 通道），移植 `is_dream_output`（LSD/DREAM 标记跳过）+ `build_transcript_summary`。

**三道门**：core operation::tests 195 passed（含 8 新 op 测试 + 注册计数改名 fifty_four）/ zbrain-mcp 14 passed / zbrain-cli build clean。

**决策**：TS operations.ts 未删（与前序 1-6-7-1..4 一致，删除推迟到 1-6-7-9）。"零 TS 残留"=Rust 侧无 TS 后端 shim，非删 TS。

**踩坑**：(1) types.rs 新 struct 未 re-export 到 crate root（lib.rs `pub use types::{...}` 漏加）→ E0432；(2) libsql log_ingest `input.xxx` 漏 clone → E0507；(3) libsql list_files match 两臂 `params![]`(=()) 与 `params![s]`(=[Result;1]) 类型不一致 → E0308，改字符串拼 + 单 bind 常量；(4) op execute 漏 `?` 导致 StructuredError 直返回 OperationError 类型不匹配 → E0308；(5) raw string 内含 `"` 终止 `r"..."` → 改用 `r#"..."#`；(6) 测试断言 camelCase 键（sourceType/sourceRef/dryRun）写错 snake_case。

**下一步**：1-6-7-6 schema-pack 摘除（9 op，多已迁命令→从 operations.ts 摘除，少数 wrap）。

---

## 1-6-7-6 schema-pack 摘除完成 (commit cb45716)

9 个 schema-pack TS op 从 operations.ts 摘除 + 专用单测删除（4 files, +11/-721）。验证：operations-descriptions(21) + mcp-tool-defs(9) pass；typecheck baseline 0 new errors（7 个 baseline 错误因删 op 不再复现）。关键前提：schema-pack op 经独立 `src/core/schema-pack/` 注册，与 `mechanical.test.ts`(`operationsByName`) 解耦，故可安全 per-slice 删。PGLite WASM init 在此 sandbox 挂死（brain-allowlist/operations-trust-boundary 60s timeout 退出码 124），与本改动无关。

## 1-6-7-8 第一批：commands-misc 5 个可行 op Rust 实现 (进行中)

**范围**：用户经 grill 选定「全量 Rust 实现」处理 1-6-7-7/8 缺口 op。本批挑 5 个 engine 方法已齐备的 commands-misc op：resolve_slugs / get_versions / revert_version / put_raw_data / get_raw_data（双后端 libsql+postgres 均已实现）。

**Rust 实现**（`crates/zbrain-core/src/operation.rs`）：插入 5 个 TypedOperation（Params/Output/impl）+ 注册到 `register_all`（54→59）+ 计数测试改名 `register_all_registers_fifty_nine_ops` + 5 个行为测试（seed_page helper + dry_run ctx 重建 via `OperationContext::local_cli()` + `ctx.engine.clone()`）。

**踩坑修复**：
1. `OperationContext.source_id` 是 **`String`**（非 `Option<String>`）——summary 假设错。修复：`ResolveSlugsOpts.source_id` 用 `Some(ctx.source_id.clone())`；engine 方法 `Option<&str>` 参数用 `Some(ctx.source_id.as_str())`（不能用 `.as_deref()`，因不是 Option）。
2. `PageVersion` / `RawData` 仅 derive `Serialize`、未 derive `Deserialize` → 测试不能反序列化为 typed struct。改为断言 `serde_json::Value`（`.as_array()`），不引入 `Deserialize`。
3. `put_page` **不创建** `page_versions` 行，version 仅由显式 `create_version` 快照。故 `get_versions` 测试须先 `engine.create_version(...)` 再读回，否则历史为空。

**关键决策（重要）**：本批**未删 operations.ts 中对应 5 个 TS op**。理由：(a) 迁移铁律——commands-misc 类 op 的 TS 删除推迟到终局 1-6-7-9「删 operations.ts」节点（1-6-7-1..5 一致保留，仅 1-6-7-6 schema-pack 因解耦才 per-slice 删）；(b) `tests/unit/e2e/mechanical.test.ts` 经 `import { operationsByName }` + `callOp` 直接派发 TS registry，删 op 会让 `callOp('get_versions'|'revert_version'|'resolve_slugs'|'put_raw_data'|'get_raw_data')` 失败；(c) TS `get_versions` 含 takes-fence 红挡逻辑（`stripTakesFence`），Rust `GetVersionsOperation` 当前**未移植**该隐私边界——删 TS 会丢行为。→ 本批只加 Rust，TS 留待 1-6-7-9。

**已知缺口（待 1-6-7-9 前补）**：Rust `GetVersionsOperation` 缺 takes-holder allow-list 红挡（TS 有）。切换前需把 `stripTakesFence` + `takesHoldersAllowList` 行为移植到 Rust 的 get_versions，否则 `takes-fence-read-ops.serial.test.ts` / `takes-mcp-allowlist.serial.test.ts` 覆盖的隐私边界在终局切换后失效。

**三道门状态**：core operation::tests 200 passed（含 5 新 + 计数 59）✅；zbrain-mcp test ⏳；zbrain-cli build ⏳。TS 侧因未改动，typecheck baseline 天然 0 new。

**下一步**：等三道门全绿后独立 commit 1-6-7-8 第一批（仅 Rust operation.rs 变更）。roadmap 1-6-7-8 第一批 marking + render。续 1-6-7-7（code-intel+search+search_by_image）与 1-6-7-8 余下（takes 4 op、get_brain_identity 待 AdminQueries 跨引擎接线）。

## 1-6-7-8 第二批：get_brain_identity (NET_NEW) Rust 实现 (commit 进行中)

**实现**：新增 `BrainEngine::brain_identity()` 默认方法（engine.rs，返回 `{version=CARGO_PKG_VERSION, engine=engine_kind_str(kind), page_count:0, chunk_count:0, last_sync_iso:None}`）+ LibsqlEngine override（调 `AdminQueries::get_full_stats` 填真实 page/chunk 计数）。`engine_kind_str()` 助手映射 EngineKind→"postgres"/"pglite"/"inmemory"。`BrainIdentity` struct（camelCase serialize）。`GetBrainIdentityOperation` 注册入 register_all（59→60）+ 计数测试改名 `register_all_registers_sixty_ops` + 行为测试 `get_brain_identity_reports_engine_and_version`（ingestion_ctx 用 InMemory→engine="inmemory"、计数=0）。InMemory/Postgres 走默认零计数。

**踩坑**：(1) engine.rs derive 用限定 `serde::Serialize`（非 `use serde::Serialize`）；(2) `BrainIdentity`/`engine_kind_str` 在 libsql.rs 须全限定 `crate::engine::...`；(3) `engine_kind_str` 返回 `&str` 但字段是 `String` → `.to_string()`。

**三道门**：operation::tests 201 pass；zbrain-mcp 14 pass；zbrain-cli build clean。TS operations.ts 中 get_brain_identity 仍保留至 1-6-7-9（同批决策）。

**剩余 1-6-7-7/8 缺口（准确审计）**：TS 98 op vs Rust 70 op，无 Rust 等效的 39 个含大量 cliHints 别名（history/revert/stats/health/salience/tag/tags/link/unlink/list/get/put/restore/graph/backlinks/timeline/transcripts/orphans/anomalies/delete/recall/whoknows）。真实 distinct 缺口：code-intel×7(code_blast/code_callers/code_callees/code_def/code_flow/code_refs/code_traversal_cache_clear，需新 engine 方法)、search/query/search_by_image(需 op 接线/图片嵌入 NET_NEW)、run_doctor(1-5 doctor 已迁，需 callable runner)、sync_brain(大子系统)、find_orphans(需 engine 方法)、takes_list/takes_search(需 engine 方法)、takes_calibration/takes_scorecard(被 1-3-3 阻断)。简言之：易摘的"engine 方法已备"批次已耗尽，下一步均需新建 engine 方法或触达被阻断/大子系统节点。

## 1-6-7-7：search op (Rust NET_NEW) + QueryParams boost/filter 轴 (commit 819a29c)

**关键发现**：Rust `query` op **早已存在**（`QueryOperation`，operation.rs ~1840，封装 `search_pages` + 向量嵌入 + rerank + 分页）。本切片真正缺口是 **`search` op 缺失**（TS `search` 走 `searchKeyword` 子串）。用户选的下刀是 "search + query ops"。

**实现**：
- `SearchOperation`（`name "search"`）：params `query`(req)/`limit`/`offset`/`source_id`(`__all__`→跨源)。execute 走 `search_pages(&SearchOpts{ keywords: vec![query], source_id, .. })`（整串作单 keyword→短语子串匹配，贴 TS `searchKeyword`）；source 默认 `ctx.source_id`（mirror `sourceScopeOpts`），offset/limit 内存切片分页。Output=`Vec<SearchResult>`。
- `QueryParams` 加 `salience`/`recency`/`min_score`/`types`（derive `Default`），execute 映射：`salience/recency=='off'`→`disable_*_boost=true`；其余轴 Rust 仍 pin 'on'（G13）；`min_score`/`types`→`SearchOpts`。`input_schema` 同步补。
- register_all 60→**61**；计数测试改名 `register_all_registers_sixty_one_ops` + 列表补 `"search"`；既有 3 处 `QueryParams {...}` 字面量补 `..Default::default()`。
- 新增 6 测试：search 匹配/空查询拒绝/源作用域排除/offset 分页 + query boost+filter 参数接受。

**三道门**：operation::tests **207 pass** ✅；zbrain-mcp **14 pass** ✅；zbrain-cli build clean ✅。

**roadmap**：1-6-7-7 标记 completed（标题收窄为 search-query，去掉捆在一起的 code-intel(7)）；1-6-7-8 关闭（两批已落）；**code-intel(7) 拆成新 pending 节点 1-6-7-10**（不与 search-query 吸收）；render 时发现 md 混有纯 `<!-- ROADMAP_SECTION -->` 死层复本（1-6-7-7/8 仍显示 `[ ]`），已删纯 marker 段只留受管 `⚠️` 段（叠层污染修复，符合 2026-07-16 踩坑手法）。

**范围取舍（明确留给后续）**：search_by_image（图片嵌入 NET_NEW）、code-intel×7（需新 engine 方法）、query 的 expand/detail/lang/symbol_kind/near_symbol/walk_depth/since/until/cross_modal 轴（需 code-intel/recency-decay 子系统，未移植）。TS `search`/`query` 定义仍保留至 1-6-7-9（与 1-6-7-1..8 一致：TS 删除推迟终局）。

<arg_key:6124c78e>replace_all</arg_key:6124c78e>
<arg_value:6124c78e>false
