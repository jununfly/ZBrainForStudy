# 2026-06-23

- 初始化 CodeGraph 索引：本地无 `codegraph` 命令，改用官方包 `npx -y @colbymchenry/codegraph init -i`；生成 `.codegraph/codegraph.db`，状态检查显示索引已更新（1,611 files, 18,154 nodes, 92,890 edges, 72.63 MB）。
- 用户纠正：当前工作应限定在 `zbrain` 项目内，不要与其他项目或跨项目路线图混淆；后续继续工作应优先读取本仓库上下文。
- 开始 `/zj-grill-with-docs` 项目整理：已探查到当前无 `ZJ-CONTEXT.md`/`docs/zj-adr/`，存在大量 `tests/unit/` 引用和少量 `tests/heavy/`，`docs/plans/20260526/（已删除）` 有连续 session/handoff 文档需要提炼合并；先进入边界确认，不直接删除。
- 用户明确选择 TS -> Rust 迁移主线：整个仓库语言统一迁到 ZBrain，所有 GBrain 品牌改名为 ZBrain。已创建路线图 `docs/plans/zbrain-ts-to-rust-roadmap.json（已删除）` + `docs/plans/ZBRAIN_TS_TO_RUST_ROADMAP.md（已删除）`，新增 `ZJ-CONTEXT.md`，新增 ADR `docs/zj-adr/ZJ-0001-zbrain-rust-rewrite-brand.md`。
- 用户确认目前没有线上用户，第一阶段品牌改名可以包含破坏性接口（CLI/bin/package/env/dotfile/config/public examples 等）。已更新 `ZJ-CONTEXT.md`、`docs/zj-adr/ZJ-0001-zbrain-rust-rewrite-brand.md` 与路线图决策；初步盘点 GBrain 命名命中非常广，需分批替换并验证。
- 用户确认旧 `CHANGELOG.md` 历史版本内容对未发布的 ZBrain 项目意义不大，可以直接删掉/reset。已将 `CHANGELOG.md` 重写为 ZBrain 空白起点，并更新 ADR/路线图决策。
- 用户确认命名迁移连文件名一起改：`gbrain.yml -> zbrain.yml`、`docs/GBRAIN_*.md -> docs/ZBRAIN_*.md`、package/bin/env/dotfile/path/docs/test 引用全迁；但 TS 代码部分先不直接动，随“完成 TS -> Rust”PRD 迁移成功一部分就删除一部分。路线图已新增 4 个分层执行节点。
- 已记录下个 PRD `docs/prd/complete-ts-to-rust.md`：TS 代码先不直接动；Rust 迁移成功一部分就删除对应 TS；不适合直接删的内容到时讨论并记录决策。路线图也记录了该决策。
- 按用户要求把 `docs/prd/complete-ts-to-rust.md` 纳入路线图，新增节点 `1-5 完成 TS 到 Rust PRD 实施`；当前品牌/文档/配置清理继续，TS 实现代码删除归入该 PRD 的逐切片迁移。
- 用户确认当前整理执行顺序：先品牌/文档/配置迁移，再 `tests/unit/ -> tests/`，最后做 plans/tests 清理；路线图已记录并渲染。
- 用户确认入口文档迁移策略：`AGENTS.md` 立即改成 ZBrain/Rust-first 入口协议；`CLAUDE.md` 是高风险大文件，先结构性改顶部总述、当前命令/路径/品牌，避免无脑全局替换，过时历史堆积后续随 plans 清理处理。
- 用户确认 package 层现在改：`package.json.name` 改 `zbrain`、`bin` 改 `zbrain`、scripts/postinstall/当前命令示例改 `zbrain`；TS CLI 内部硬编码不强制本轮全改，归入“完成 TS -> Rust”PRD 按切片处理。
- 用户确认配置文件迁移：`gbrain.yml` 直接改 `zbrain.yml`，不保留旧文件，不在代码层增加兼容读取；文档只说明 ZBrain 尚未发布，旧 examples 在 TS -> Rust reset 中重命名。
- 用户确认 `docs/GBRAIN_*.md` 策略：第一阶段只做重命名、当前引用修正、标题/当前品牌改名；不在命名迁移阶段合并内容。`GBRAIN_RECOMMENDED_SCHEMA`/`GBRAIN_SKILLPACK`/`GBRAIN_V0`/`GBRAIN_VERIFY` 分别改为 `ZBRAIN_*`，内容提炼合并留到后续清理。
- 用户确认 plans 清理策略补充：`docs/plans/2026-06-01-brainengine-contract-hardening（已删除）*.md` 合并为 `docs/plans/2026-06-01-brainengine-contract-hardening.md（已删除）`，保留计划、PR 结果和最终状态，删除 `*-pr.md`。
- 按用户要求从 `C:\workspace\github\jununfly\skills\skills\zj-roadmap-driven` 重新安装用户级 `zj-roadmap-driven` skill；安装前做了本地安全审计（仅 JSON/Markdown 读写，无网络/子进程/秘密读取），旧版本备份到 `~/.workbuddy/skills/.backup/zj-roadmap-driven-20260623-1433`，重装后 `roadmap_cli.py validate docs/plans/zbrain-ts-to-rust-roadmap.json（已删除）` 通过。
- 按用户要求从 GitHub `https://github.com/jununfly/skills/tree/main/skills/zj-roadmap-driven` 安装用户级 `zj-roadmap-driven` skill；克隆到临时目录并审计通过（仅 roadmap JSON/Markdown 读写，无网络/子进程/秘密读取），旧版本备份到 `~/.workbuddy/skills/.backup/zj-roadmap-driven-20260623-1437-github`，安装后 validate 当前 ZBrain roadmap 通过，tree 输出正常。
- q15 测试目录迁移收口：`test/` 已迁到 `tests/unit/`，runner/config 改为扫描 `tests/unit`，`run-slow-tests.sh` 增加 `--dry-run-list`；unit/serial/slow/e2e dry-run 分别发现 708/47/4/130 个文件；`check-test-isolation` 扫描 712 个 non-serial unit 文件通过，`package.json`/`openclaw.plugin.json` JSON 校验通过。当前阻塞是本机无 `bun`，无法执行 `bun run typecheck`、`bun test` 与 `scripts/test-shard.sh` 分片验证；路线图节点 `1-3` 已标记 blocked 并渲染。
- q15 plans 清理：将 `docs/plans/20260526/（已删除）` 提炼为 canonical 文档 `docs/plans/20260526-rust-rewrite-plan.md（已删除）`，覆盖目标范围、slice 列表、仍有效决策、当前状态/后续切片和废弃结论，并删除原连续过程目录；将 `docs/plans/2026-06-01-brainengine-contract-hardening-pr.md（已删除）` 的 PR outcome 合并进 `docs/plans/2026-06-01-brainengine-contract-hardening.md（已删除）` 后删除 PR 摘要文件；`RUST_REWRITE.md` 已改为引用 canonical 文档，路线图节点 `1-4` 已完成并渲染。
- Bun 阻塞复查：Windows 上已安装 Bun `1.3.14`，位置 `C:\Users\victo\.bun\bin\bun.exe`；重启 WorkBuddy 后 PATH 已恢复。`bun install` 的 `postinstall` 在 Windows/Bun 下因 POSIX redirect 解析失败，改用 `bun install --ignore-scripts` 完成依赖安装。
- q15 测试目录迁移完成验证：修复 `src/commands/claw-test.ts` 残留 `../core/claw-tests/unit/*` import 为 `../core/claw-test/*`；修复 `tests/unit/scripts/test-shard.slow.test.ts` 的 `REPO_ROOT` 层级；`bun run typecheck` 通过，`check-test-isolation` 通过，`scripts/test-shard.sh --dry-run-list` 有输出，迁移相关样本 23 tests 全部通过；路线图节点 `1-3` 已改 completed 并渲染。
- postinstall 跨平台修复：GitHub repo `jununfly/zbrain` 禁用了 issues，无法发布 issue；按 issue 正文在本地 TDD 完成。新增 `scripts/postinstall.ts`，将 `package.json` postinstall 改为 `bun run scripts/postinstall.ts`，新增 `tests/unit/scripts/postinstall.test.ts` 覆盖无 `zbrain` 时跳过、有 fake `zbrain` 时调用 migrations、失败码透传；`bun install`、postinstall 测试和 `bun run typecheck` 均通过。
- q15 公开入口文档品牌迁移：已将 `README.md`、`INSTALL_FOR_AGENTS.md`、`docs/INSTALL.md` 中的 GBrain/gbrain/GBRAIN/.gbrain/gbrain.yml 公开残留统一迁到 ZBrain/zbrain/ZBRAIN/.zbrain/zbrain.yml，并将安装仓库引用改为 `jununfly/zbrain`；记录路线图决策：公开入口先收口，历史设计/incident/legacy 文档残留后续分层判断，不做无差别全仓库替换。`package.json` JSON 校验、`bun run typecheck`、roadmap validate/render 均通过；`check-test-isolation` 已启动后台执行等待完成。
- q15 节点 `1-1-4` 已按 TDD 完成：新增 `tests/unit/zbrain-brand-guard.test.ts`，迁移源码/测试/脚本/CI 中公开命名引用到 ZBrain/zbrain（token/cookie/env/path/schema-pack/skillpack/root helper 等），重命名 schema-pack base YAML、生成脚本和相关测试/fixture 文件；`gbrain_cycle_locks` 与 `gbrain_tool_use_id` 作为 DB schema identifiers 暂留并在 guard 中全局 allowlist，后续如需改名应另开 DB schema migration slice。验证通过：brand guard、OAuth、skillpack manifest/registry tests、`bun run typecheck`；`serve-http-oauth` 因本机无 `DATABASE_URL` 全部 skip。路线图 `1-1-4` 已 completed 并 render。
- 复查 q15 节点 `1-1`：roadmap 原显示 `1-1-1`/`1-1-2`/`1-1-3` 仍是 in_progress，但实际 package/bin、env/dotfile/config path、docs 文件名与公开入口交叉引用已完成；验证 `package.json` name/bin 为 zbrain、无 `docs/GBRAIN_*.md`、公开入口无旧品牌残留、brand guard 通过。已补齐 `1-1-1`、`1-1-2`、`1-1-3` 为 completed，父节点 `1-1` 自动 completed，并 validate/render roadmap。
- 完整扩写 `docs/prd/complete-ts-to-rust.md`：以代码库事实记录 Rust workspace/current gaps、TS runtime surface、admin frontend TS 保留策略、迁移原则、目标 crate 架构、11 个迁移阶段、slice completion definition、deletion checklist 与验证策略；明确 Rust 替代成功后删除对应 TS，无法删除必须记录决策。发现 `docs/plans/zbrain-ts-to-rust-roadmap.json（已删除）` 已缺失，按 `zj-roadmap-driven` 规则重新 init canonical roadmap，生成 `docs/plans/ZBRAIN_TS_TO_RUST_ROADMAP.md（已删除）`，补齐 1-1 到 1-12 完整 TS->Rust 迁移树和关键决策，validate/render 通过。
- roadmap 节点 `1-2-4 Decide internal DB legacy identifier migration` 已完成决策/计划切片：确认首阶段直接改名 `gbrain_cycle_locks -> zbrain_cycle_locks`、`gbrain_tool_use_id -> zbrain_tool_use_id`，不保留兼容 alias/fallback；用户选择先写计划文档不改代码。新增 `docs/plans/2026-06-23-db-legacy-identifier-rename.md（已删除）`，覆盖 schema DDL、migration、runtime SQL、tests/e2e/heavy/eval/docs、brand guard、TDD 顺序、风险与 done criteria；roadmap 已记录决策并 render。
