# 2026-06-25 工作日志

## 继续 issue #19 → commit + close → issue #20 → commit + close

### issue #19 commit + push
- commit `6b4e7e2`: RawData/PageVersion types + 7 trait method signatures + 3 backend unimplemented!() stubs
- GitHub issue #19 关闭，roadmap 1-2-2-2 → completed

### issue #20 (InMemory 行为实现)
- 给 `InMemoryEngine` 添加 `raw_data_store` / `version_store` / `next_version_id` 存储字段
- 替换 7 个 `unimplemented!()` 为真实行为：
  - put_raw_data: 按 (page_id, source) 进行 upsert
  - get_raw_data: 支持 source 过滤，missing page 返回空
  - create_version: 快照 compiled_truth + frontmatter，分配单调版本 id
  - get_versions: 按 id DESC 排序（newest-first）
  - revert_to_version: 恢复 compiled_truth + frontmatter；未知 version_id 返回 Err
  - update_slug: 冲突检查 + 重命名；missing page 返回 Err
  - rewrite_links: 显式 Ok(()) no-op（integer page_id FK 设计）
- 新建测试文件 `tests/page_methods_advanced_writes.rs`，12 个 InMemory 专属测试
- commit `4fdc8b8` pushed，issue #20 关闭，roadmap 1-2-2-3 → completed

## issues #21–#23: libsql + Postgres + close 连续交付

### issue #21 (libsql 后端)
- commit `2306b9a`
- 新建 SQLite migration `0008_raw_data_and_page_versions.sql` + PG migration `0009_raw_data_and_page_versions.sql`
- 在 libsql.rs 中添加 MIGRATION_0008、更新 SCHEMA_VERSION 7→8
- 7 个 SQL 方法：put_raw_data (INSERT ON CONFLICT), get_raw_data (SELECT + optional source filter), create_version (RETURNING), get_versions (ORDER BY snapshot_at DESC), revert_to_version (UPDATE with rows check), update_slug (conflict check + UPDATE), rewrite_links (no-op)
- 14 个 libsql 专属测试 in `tests/libsql_engine_advanced_writes.rs`

### issue #22 (Postgres 后端)
- commit `5abee58`
- Migration 0009 通过 sqlx::migrate!() 自动加载
- 7 个 sqlx 方法：使用 `$1, $2` 参数、JSONB 绑定、chrono DateTime→to_rfc3339() 时间戳转换、rows_affected 检查
- 14 个 PG 专属测试 in `tests/postgres_engine_advanced_writes.rs`

### issue #23 (验证 + 关闭)
- commit `6d17dda`
- Roadmap 1-2-2 子树全部完成（1-2-2-1~6）
- 5 个 issues 全部关闭

## 1-2-3 Schema Migrations Ownership Audit

### Grill 决策
- Q1: Audit only, no implementation → A
- Q2: 100% parity target → A
- Q3: All migrations in Rust crates only → A
- Q4: Rust-specific versioning (reset to v1) → B
- Q5: Hard cutover (no dual runner) → A

### 1-2-3-1 Audit 完成
- Roadmap JSON: 1-2-3 parent + 6 children 已完整录入
- Roadmap MD: 当前施工状态 + 5 个决策已更新
- Canonical audit 文档: `docs/plans/2026-06-25-schema-migrations-audit.md（已删除）` 已创建
  - Full TS migration system spec + current Rust state
  - Parity gap analysis feature matrix
  - Explicit port/simplify/discard boundary
  - 6-slice architecture sanity check: ✅ verified

### 1-2-3-2 Grill 决策
- Q1: Module in dedicated `migration.rs` → A
- Q2: Dispatch via `dyn Migration` trait object → A
- Q3: Version type `i64` (align with SQLite PRAGMA) → B
- Q4: Handler/verify signatures deferred to 1-2-3-5 → B
- Q5: Full dedicated object-safety test file → A

### 1-2-3-2 实现完成
- `crates/zbrain-core/src/migration.rs`: 新增 240 行
  - Object-safe `Migration` trait: version/name/sql/sqlite_sql/postgres_sql/transaction/idempotent
  - `MigrationRegistry`: 排序迭代器、latest_version()、len()
  - `InMemoryMigration`: 测试用实现
  - handler/verify 全部 stub → `unimplemented!()`，签名推迟到 1-2-3-5
- `crates/zbrain-core/src/lib.rs`: 新增 `pub mod migration`
- `crates/zbrain-core/tests/engine_object_safety_migration.rs`: 新增 8 个测试
  - Trait object-safety compile-time assertions
  - Registry sorting/len/latest_version
  - Default overrides
- cargo fmt 通过，cargo check 无 Rust 错误

## 当前状态
- 1-2-2 subtree: ✅ completed
- 1-2-3-1: ✅ completed
- 1-2-3-2: ✅ completed
- 下一步：1-2-3-3 Integrate Rust runner into libsql backend (grill + 实现)

### 2026-06-25 - Slice #40 Completed
- **Issue #40: Operation trait and type foundation DONE
- Implemented: ErrorCode enum, OperationError struct, AuthInfo, CliOpts, Logger trait, OperationContext with full trust boundary fields, Operation object-safe trait
- 14/14 tests passing
- Commit: 206adee
- Node 1-4-1 status updated to in_progress
- Push: origin/rust-rewrite

