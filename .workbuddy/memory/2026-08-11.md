# 2026-08-11 工作日志

## G58 收口 fix 提交（zbrain-clean 仓库）
- 问题根因：`ca5ad2c2`（G58 longmemeval）推送时**漏提交** `InMemoryEngine::reset_for_benchmark()` + 完整 `find_trajectory` 实现（428 行，原在损坏的 zbrain 工作树里未提交），导致推送树 E0599 无法编译。之前"2505 测试全绿"是带 WIP 的工作树状态，非提交态。
- 修复：从 `C:/Users/victo/AppData/Local/Temp/zbrain_backup/engine.rs.bak` 合并回干净仓库 `engine.rs`，`git diff --numstat` = `428  3`（CRLF 安全，无整文件重写）。
- 提交：`6710c25b` fix(core): restore find_trajectory + reset_for_benchmark omitted from G58 commit（**不改写**已推 `ca5ad2c2`，追加 fix；rust-rewrite 协作分支）。
- 已 `git push origin rust-rewrite`，`ls-remote` 确认远程 → `6710c25b`。
- 同时把 `2026-08-10.md` 决策日志恢复进干净仓库（原未跟踪/未提交）。

## 验证结论
- `cargo` 完整编译 `zbrain-core` 通过（含合并后 engine.rs）→ 证伪"ca5ad2c2 可编译"。
- `cargo test -p zbrain-core --lib find_trajectory` → 5/5 通过。
- `cargo test -p zbrain-core --lib longmemeval` → 61/61 通过。
- 全量 `cargo test -p zbrain-core` 末尾 `extract_facts` 集成测试 `0xc0000005`（STATUS_ACCESS_VIOLATION）= 已知 libsql Windows FFI 间歇崩溃（工作记忆记载，CI 跑 ubuntu，代码层无解），与本次合并无关、非断言失败。

## 环境铁律（沿用）
- cargo: `C:/Users/victo/.cargo/bin/cargo.exe`；缓存 `CARGO_TARGET_DIR=C:/Users/victo/AppData/Local/Temp/zb_clean_target`。
- 活动仓库已切换为 `zbrain-clean`（健康 clone）；原 `zbrain` 因坏对象 `0e0d0a2d`(missing tree) 损坏，仅作备份保留。
- mingw git 绝对路径 `/c/...` 会被错拼为 `C:\c\...` → clone 用**相对路径**。
