# 2026-07-30

## 1-3-4 synthesize 大件启动 + 1-3-4-1 dream_verdicts 完成 ✅

续 1-3-4（synthesize.ts 1247行 fan-out 大件）。先 Explore 勘察拆 6 子节点；roadmap JSON 1-3-4 `in_progress` + 拆 1-3-4-1..6，render 同步 ⚠️ 段 + 手动同步顶部孤儿纯段（时间戳 + 1-3-4 `[ ]→[~]` + 子节点树）。

### 1-3-4-1 dream_verdicts migration + engine 方法（已 push 前的 working tree）
- 双 dialect migration：`migrations/0026_dream_verdicts.sql`（PG: JSONB + TIMESTAMPTZ DEFAULT now()）+ `migrations-sqlite/0026_dream_verdicts.sql`（SQLite: TEXT(strftime默认) + BOOLEAN DEFAULT 0；JSONB→TEXT）。注册 `LIBQL_MIGRATIONS`/`POSTGRES_MIGRATIONS` version=26（libsql.rs:145/295、postgres.rs:264/405）。
- engine.rs：新增 `pub struct DreamVerdictInput{worth_processing:bool, reasons:Vec<String>}` + `pub struct DreamVerdict{worth_processing:bool, reasons:Vec<String>, judged_at:String}`；trait 默认 `get_dream_verdict`/`put_dream_verdict` Unsupported；InMemory 用 `dream_verdicts_store: Mutex<Vec<InternalDreamVerdict>>` 实现（upsert，`judged_at=chrono::Utc::now().to_rfc3339()`）。
- libsql.rs：`get` 用 `row.get::<i64>(0)!=0` 读 bool + `serde_json` 解析 reasons TEXT；`put` 用 `ON CONFLICT DO UPDATE` + `strftime` 刷新 judged_at。
- postgres.rs：`get` 用 `try_get` bool/jsonb/timestamptz（`$reasons` 用 `try_get::<Option<serde_json::Value>>`）；`put` 用 `$4::jsonb` 转换 + `now()` 刷新（参考 postgres.rs:7739 的 JSONB 绑定风格：绑字符串然后在 SQL 里 cast）。
- tests：`libsql_engine_migrations.rs` 的 `EXPECTED_VERSION` 25→26；新增 `dream_verdict_round_trip_inmemory`（patterns.rs）+ `dream_verdict_round_trip_libsql`（libsql_engine_migrations.rs）验证 miss/写/读/upsert/异 hash 独立。

### 踩坑（已修，复盘用）
1. **漏加 InMemory 结构体字段**：只改了 `new()` 的初始化列表，忘了在 `InMemoryEngine` struct 定义里加 `dream_verdicts_store` 字段 → 编译 E0560/E0609。补在 `take_grade_cache_store` 字段之后。
2. **跨 crate 类型未 import**：libsql.rs / postgres.rs 用到 `DreamVerdict`/`DreamVerdictInput` 但没在 `use crate::engine::{...}` 里导入 → E0425/E0422。补进两个 import 块。
3. **WSL echo 掩盖真实退出码**：`cargo test ... > log; echo EXIT=$?` 的 `EXIT` 其实是 `echo` 的退出码（恒 0），需用 `grep "test result"` 或 `grep "^error"` 看真实结论。第一轮编译失败被误判为通过。

### 验证
- WSL Linux：`--lib dream_verdict_round_trip_inmemory` → 1 passed；`--test libsql_engine_migrations` → 8 passed（含 `fresh_db_runs_all_migrations_ends_at_expected_version` 验证 0026 应用+幂等）。Windows libsql FFI 崩溃照例绕开（WSL 验证）。
- 全 lib 编译通过（postgres.rs 在 `pub mod postgres` 无条件编译，已覆盖）。

### 下一步
1-3-4-2 transcript-discovery 模块（纯 fs+sha256，无引擎依赖，可独立先行）；后续 1-3-4-3 模型上下文预算纯函数、1-3-4-4 扇出编排、1-3-4-5 磁盘双写、1-3-4-6 配置/冷却。等用户 `commit+push`（feat 实现+接线 与 chore roadmap/memory 分离）。

## 1-3-4-2 transcript-discovery 模块 完成 ✅

移植 TS transcript-discovery.ts(295行) 为纯 fs+crypto 模块（无引擎依赖）。
- 新文件 `crates/zbrain-core/src/autopilot/phases/transcript_discovery.rs`，`mod.rs` 注册 `pub mod transcript_discovery`。
- API：`DiscoveredTranscript{file_path:PathBuf, content_hash:String(sha256 hex), content, basename, inferred_date:Option<String>}` + `DiscoverOpts{corpus_dir, meeting_transcripts_dir?, min_chars?(默认2000), exclude_patterns, date?, from?, to?, bypass_guard}`；`discover_transcripts(opts)` 递归扫 .txt/.md，下降时 prune（隐藏目录`.`开头 / node_modules/.raw/ops / .raw 结尾 / 子模块 .git 文件），过滤 日期范围 / min_chars / dream_generated 自消费标记(LSD 永跳过即使 bypass) / excludePatterns(词边界自动 \b 包裹、大小写不敏感、坏正则 stderr 警告不崩)；结果按 file_path 排序。`read_single_transcript` 支持 --input ad-hoc。`is_dream_output`/`is_lsd_output`/`is_brainstorm_output`/`compile_exclude_patterns`/`hash_content` 导出。
- 三个 marker regex (DREAM/LSD/BRAINSTORM) 用 `lazy_static` + `r#"..."#`（raw string 含内部 `"` 不能用 `r"..."`）。
- 验证：WSL Linux 10 个 hermetic 单测全绿（discovers_txt/md、min_chars、date 单/范围、dream marker 跳过、lsd 永跳过(含 bypass)、exclude 词边界、prune node_modules/.git、meeting 合并、read_single、hash 确定性）。

### 踩坑（已修）
1. **raw string 提前结束**：marker regex 用 `r"..."` 但内含 `"`（(?:"|'|)）导致 raw string 提前结束、`'` 被当字符字面量 → 改用 `r#"..."#`。
2. **&str < &String Ord 报错**：`is_in_date_range` 比较 `date < from` 其中 from 是 `&String` → E0277；改 `from.as_str()`（!= 因 PartialEq<str> for String 不需改）。
3. **pruneDir 误杀根目录**：`tempfile` 临时目录名以 `.` 开头，被"隐藏目录"规则 prune 掉 → `filter_entry` 对根(depth==0) 永不 prune（对齐 TS pruneDir 只过滤子目录，不对 corpus 根）。
4. **WSL 9P fs 双 TempDir 竞态**：meeting 测试用两个独立 TempDir 在 WSL 并发/时序下偶发丢文件（非确定性 fail）→ 改单 TempDir 双子目录（与 prunes 测试同构），稳定 10/10。

### 下一步
1-3-4-3 模型上下文预算纯函数（computeChunkCharBudget / splitTranscriptByBudget / rewriteChunkedSlug + 单测；纯函数可独立）；随后 1-3-4-4 扇出编排 / 1-3-4-5 磁盘双写 / 1-3-4-6 配置冷却。等 `commit+push`（feat 与 chore 分离）。

## 1-3-4-3 模型上下文预算纯函数 完成 ✅

移植 TS synthesize.ts 三纯函数（无引擎依赖，独立先行）。
- 新文件 `crates/zbrain-core/src/autopilot/phases/context_budget.rs`，`mod.rs` 注册 `pub mod context_budget`。
- `compute_chunk_char_budget(model, Option<config_max_prompt_tokens>)->u32`：常量 MODEL_CONTEXT_TOKENS(opus 1M/sonnet 200K)/CHARS_PER_TOKEN=3.5/HEADROOM_RATIO=0.9/UNKNOWN_MODEL_BUDGET_TOKENS=180K；config 优先（不在此 floor，MIN_PROMPT_TOKENS=100000 由调用方负责）/ model 查表×0.9 / 未知回退 180K，均 floor 成 char 数。**去掉 TS 全局 Set 'warn once' stderr 副作用**以保纯函数。
- `split_transcript_by_budget(content, content_hash, max_chars)->Vec<String>`：三级切分(`\n## Topic:` → `\n---\n` → `\n`)，hash 派生 searchStart 抖动(0.5×~0.6×budget) 保证 (content,hash,maxChars) 确定性；无边界硬切到 max_chars。用**字符索引(Unicode scalar)**而非 TS 的 UTF-16 length；Rust 无 surrogate，safeSplitIndex 退化为 char 边界硬切。max_chars==0 panic!。
- `rewrite_chunked_slug(slug, hash6, idx)->String`：已正确后缀/以 `-hash6-cN` 或 `/hash6-cN` 结尾→不变；裸 hash6(前导 start/`-`/`/`)→追加 `-cN`；未知形状→passthrough。用手动前缀检查替代 TS 的 RegExp（规避 hash6 正则元字符转义坑）。
- `DEFAULT_MAX_CHUNKS=24` / `MIN_PROMPT_TOKENS=100000` 以 pub const 暴露供 1-3-4-4/6。
- 验证：Windows 直跑 `cargo test -p zbrain-core --lib context_budget`（纯函数无 libsql FFI，无需 WSL），**19 测试全绿**。纯新增、未改既有代码。

### 踩坑（已修，重要）
- **roadmap render marker 认知纠错**：源码 `roadmap.py:531/622` 写入/查找的是 **`<!-- ROADMAP_SECTION_START -->`（无 ⚠️）** 标记块——**根本不存在 ⚠️ 变体**。此前记忆/总结说的"skill 只维护 ⚠️ 段"是误读。后果：在**已有 md** 上 render 时，若 md 的标记块与工具预期不符，工具找不到自己的 marker → **追加**新块 → md 出现**重复段**（旧块成孤儿 + 新块）。要得单一干净段，**先删 md 再 render**。已据此修正 MEMORY.md 两条相关记录。
- **误删 JSON 数据文件**：render 前清理命令 `rm -f "$JSON" ...` 把 roadmap 数据 JSON 也删了（未提交改动随之丢失）。修法：从 `git checkout HEAD -- <json>` 恢复已提交版，再重做未提交的 completed+notes 编辑。教训：删 md 时只删 `.md`，绝不同时删 `.json`。

### 下一步
1-3-4-4 扇出编排 run_phase_synthesize（复用 patterns 的 MinionQueue+wait_for_completion；接 cycle 真实臂；需 1-3-4-1/2/3 + ChatProvider 裁决入参）；随后 1-3-4-5 磁盘双写 / 1-3-4-6 配置冷却。等 `commit+push`（feat 与 chore 分离）。

## 1-3-4-4 扇出编排 run_phase_synthesize 完成 ✅

忠实移植 TS runPhaseSynthesize 扇出核心（351 行函数）。
- 新文件 `crates/zbrain-core/src/autopilot/phases/synthesize.rs`，`mod.rs` 注册 `pub mod synthesize`；`cycle.rs` 加 `CyclePhase::Synthesize =>` 真实臂（镜像 Patterns 臂，结果映射 PhaseResult）。
- 流程：`load_synth_config`（TS 默认，corpus_dir=None→not_configured） → `discover_transcripts`(1-3-4-2) → 逐 transcript `judge_significance`（传入 `chat: Option<&dyn ChatProvider>`，8K 截断 head+tail，解析 JSON `worth_processing`/`reasons`，不可解析回退 `worth=false`，None→跳过并记"no configured provider"）结果缓存 `dream_verdicts`(1-3-4-1 get/put) → `split_transcript_by_budget`(1-3-4-3) 切分 → 逐 transcript/chunk `MinionQueue.add` 提交 `subagent` 作业（`idempotency_key=dream:synth:<filePath>:<hash16>[:c<i>of<n>]`、`max_stalled=3`、`on_child_fail=Continue`、`timeout=30min`、`allowed_slug_prefixes=["wiki/"]`）→ `wait_for_completion` 轮询（patterns 同，无 worker→timeout 不硬失败）→ 最佳努力 slug 收集（`execute_raw` 走 `subagent_tool_executions`，Rust 未镜像→0）。
- `judge_significance` 用 `ChatOpts{model, system, messages, max_tokens:200}`，JSON 提取用手写 first '{'..last '}'（不引 regex，`regex` crate 虽可用）。`normalize_subagent_model` 保证 `provider:model`（claude-*→anthropic:）。`build_synthesis_prompt` 忠实移植 TS（含 chunk banner / slug 规则 / D 任务）。
- 验证：Windows 直跑 `cargo test -p zbrain-core --lib autopilot::phases::synthesize` **15 测试全绿**（not_configured / no_transcripts / dry_run 缓存+重跑 cache-hit / 不值得跳过 / 无 chat 跳过 / 分块多作业）；`cargo test -p zbrain-core --lib autopilot::cycle::tests::run_cycle_` **7 测试全绿**（含空脑，skipped 计数保持 15，Synthesize 真实臂仍 Skipped 因 corpus_dir=None）。纯新增+1 真实臂，无回归。

### 偏差（记入后续子节点，符合 patterns 先例）
- **config(1-3-4-6)**：硬编码 TS 默认，`corpus_dir` 取 opts 覆盖（ad-hoc 路径），无覆盖则 `not_configured`；engine `getConfig/setConfig` + cooldown 未做。
- **磁盘双写(1-3-4-5)**：`reverseWriteRefs`/`writeSummaryPage` 未做（DB-canonical，subagent 直写引擎）。
- **slug 收集**：`subagent_tool_executions` 表未镜像→`pages_written` 最佳努力=0（同 patterns）。
- **allowed_slug_prefixes**：默认 `["wiki/"]`（TS 读 filing-rules，缺失→NO_ALLOWLIST fail；worker 侧仍强制真实 allow-list）。
- **prior contradictions block**：省略（仅信息性）。
- **测试语义修正**：`VerdictRecord.cached` 语义=“是否来自缓存读取”，dry-run 时是新判定（已写入缓存）故 `cached=false`；重跑 cache-hit 才 `cached=true`。

### 下一步
1-3-4-5 磁盘双写（reverseWriteRefs / writeSummaryPage；受 Q1 决策：复刻 or DB-canonical）与 1-3-4-6 配置/冷却（loadSynthConfig 接 engine.getConfig/setConfig + cooldown + subagent_tool_executions 表）。等 `commit+push`（feat 实现 与 chore roadmap/memory 分离）。

## 1-3-4-5 磁盘双写（复刻 TS）完成 ✅

Q1 决策：用户选**复刻 TS 磁盘双写**（非 DB-canonical）。在 1-3-4-4 的 synthesize.rs 上补磁盘镜像。
- 新增 page→markdown 反向序列化器（Rust 原缺失）：`serialize_markdown(frontmatter, compiled_truth, timeline?, tags)`（port TS serializeMarkdown，serde_yaml 序列化 frontmatter + 可选 `<!-- timeline -->` 块）+ `render_page_to_markdown(page, tags, overrides)`（port TS serializePageToMarkdown，叠 dream_generated/dream_cycle_date 戳）。
- `source_id_re()`（OnceLock 缓存 `^[a-z0-9](?:[a-z0-9-]{0,30}[a-z0-9])?$`）+ `validate_source_id`（非 default 源写 `brainDir/.sources/<id>/<slug>.md`）。
- `reverse_write_refs(engine, brain_dir, &[(slug,source_id)])->u64`：逐对 get_page+get_tags→render→写盘，non-fatal stderr 继续。
- `write_summary_page(...)`：put_page 引擎 canonical summary（`dream-cycle-summaries/<date>.md`）+ brain_dir 有值时 mirror 磁盘。run_phase_synthesize 末尾接 reverse_write_refs（brain_dir 有值）+ write_summary_page（始终引擎写）。
- `SynthesizePhaseResult.disk_files_written:u64`；`cycle.rs` Synthesize 臂加该字段。
- 验证：Windows 默认 target/ 直跑 `cargo test -p zbrain-core --lib autopilot::phases::synthesize` **20 测试全绿**（含 5 个新增磁盘双写用例：default 源镜像 / .sources 子目录 / 缺页跳过 / summary putPage+磁盘 / run_phase 镜像 summary 索引）。测试用 InMemoryEngine，不碰 libsql 运行时。

### 踩坑（已修）
- **target_alt2 全新目录触发依赖重编失败**：为绕开被孤儿进程占用的默认 target/ 锁，曾用 `CARGO_TARGET_DIR=target_alt2` 跑测试，但全新目录触发 `libsql-ffi`（Permission denied）/ `aws-lc-sys`（缺 NASM、`lib.exe` 1114）完整重编 → EXIT=101，且完成通知因会话中断未送达。教训：**Windows 验证 zbrain-core 测试应复用默认 target/ 缓存**（libsql-ffi/aws-lc-sys 已编好），勿用全新 target dir 触发重编；孤儿锁进程退出后默认 target/ 锁即释放。
- **PageInput 漏字段**：`PageInput` 派生了 `Default`（必填仅 page_type/title/compiled_truth，余 14 字段均 Option）。手写构造漏 `content_hash`/`effective_date`/`chunker_version` 等 → E0063；改 `..Default::default()`。`disk_files_written` 计数 `usize` 赋 `u64` 字段 → E0308；改 `as u64`。

### 下一步
1-3-4-6 配置/冷却（loadSynthConfig 接 engine.getConfig/setConfig + cooldown 时间戳 + subagent_tool_executions 表；受 Q2 决策：完整复刻 or patterns 先例硬编码+KNOWN-GAP）。等 `commit+push`（feat 实现 与 chore roadmap/memory 分离）。

## 1-3-4-6 配置/冷却 完成 ✅ → 1-3-4 / 1-3 簇整体收口 🎉

Q2 决策：用户选**完整复刻（加引擎方法）**。
- migrations 双 dialect：`0027_config`（key/value TEXT）+ `0028_subagent_tool_executions`（TS schema.sql:809 镜像；FK 到 minion_jobs 故意省略——写入端未落地，SQL 注释说明）。libsql.rs/postgres.rs 注册 version 27/28；`EXPECTED_VERSION` 26→28。
- `BrainEngine` 加 4 trait 方法（默认 Unsupported）：`get_config`/`set_config`（upsert）/`unset_config`（返回 affected u64）/`collect_child_put_page_slugs(&[i64])->Vec<(String,String)>`。三后端全实现：InMemory（`config_store` HashMap + `subagent_tool_executions_store` Vec）；libsql（`params_from_iter` 动态 IN + `json_extract(input,'$.slug')`/`'$.input.slug'` 双重编码回退）；postgres（链式 `.bind()` + `input->>'slug'`/`input->'input'->>'slug'`）。
- synthesize.rs：`load_synth_config` 改 async 读 `dream.synthesize.*` 8 键（enabled "false"/"0" 才 false；cooldown 默认 12h；exclude_patterns JSON 数组）；`check_cooldown` 读 `last_completion_ts` RFC3339，within → `Skipped("cooldown_active")`；run_phase 成功后 `set_config` 写回时间戳；slug 收集改走 `engine.collect_child_put_page_slugs`（删 execute_raw free function）。
- 验证：lib 全量 **2062 passed 0 failed**（Windows 默认 target/）；`libsql_engine_migrations` **10 passed**（新增 `config_round_trip_libsql` + `collect_child_put_page_slugs_empty_and_missing_children` 集成测试）。
- KNOWN-GAPS 登记 **G63**（subagent_tool_executions 写入侧：minion tool loop 不落 execution 记录；双向指针=0028 SQL 头注释 + synthesize.rs 模块 doc）。
- roadmap：1-3-4-6 completed（decisions 记 Q2）→ 1-3-4 completed（6 子全绿）→ **1-3 synthesis 簇 completed**（4 phase 全迁）。删 md 重 render 单一 ROADMAP_SECTION 确认。

### 踩坑（已修）
1. **async 化 load_synth_config 丢 opts 覆盖**：从引擎读 config 时把 `opts.min_chars/max_prompt_tokens/max_chunks_per_transcript` 三个覆盖弄丢 → 既有 `chunked_transcript_submits_one_job_per_chunk` 回归。修法：`opts.X.or_else(|| stored.parse().ok()).unwrap_or(default)` 三层。教训：接真实 config 时 **opts 覆盖优先级必须逐字段保留**。
2. **测试断言字段错位**：`SynthesizePhaseResult::ok(summary)` 把文案放 `summary`，`reason` 恒 None（skipped 才有 reason）——新测试误断言 `reason=Some("no transcripts to process")`。
3. **E0609 幽灵字段**（编译期已修）：`SynthesizePhaseOpts` 无 `meeting_transcripts_dir` 字段（该值只从引擎 config 读），误写 `opts.meeting_transcripts_dir` 一行删除即可。

### 下一步
1-3 簇收口完毕。Part12 剩余：**1-4 anomaly-transcript 簇**（下一 pending）、1-5 auto-think、1-6 orchestration 主循环（runCycle 2057 行）。

### commit+push（本会话末）
已分离提交并推送到 `origin/rust-rewrite`：
- `6109131` feat(zbrain-core): wire synth config + cooldown via real engine methods (1-3-4-6)（9 文件 +728/-82）
- `38dd1f6` chore(roadmap): close 1-3-4-6 and roll up 1-3 synthesis cluster（5 文件 +66/-29）
工作区清洁。

## 1-4 anomaly-transcript 簇 完成 ✅（mode=explore 调研+收口）

重大发现：1-4 的 Rust 实现其实早已完整存在（之前会话落地，不在 Part12 跟踪视线内）——`anomaly.rs`(429行，compute_anomalies_from_buckets 已逐行比对 TS 等价) + `transcript_discovery.rs`(即 1-3-4-2 产物) 均已实现并接线（find_anomalies 三后端 + op dispatch + CLI）。TS 源文件 src/core/cycle/anomaly.ts / transcript-discovery.ts 仍在，删除受 1-6 TS 引擎迁移阻塞（pglite-engine.ts/postgres-engine.ts 仍引 TS anomaly；extract-atoms.ts:255 仍引 TS transcript-discovery）。

用户选 A「验证+完整收口」：
1. **合并重复 dream-guard**：operation.rs:7626 手抄私有 `is_dream_output`（LSD_RE/DREAM_RE OnceLock）删除，调用点 8546 改为复用 `transcript_discovery::is_dream_output(&raw, false)`（单一真相源，语义等价：LSD+dream 永远跳过）。
2. **补 brain_find_anomalies minion tool**：brain.rs 照 ToolDef 模式新增 FindAnomaliesTool（input 收 since/lookback_days/sigma，execute 调 `engine.find_anomalies(opts)` 按值传参——trait 签名按值收 AnomaliesOpts），注册 register_all；顶部 doc 表格加行 + Deferred 列表移除过期 `brain_find_anomalies` 条目；加 optional_f64 helper + 2 接线测试 + register_all 计数 8→9。
3. **验证**：zbrain-core lib 全量 2064 passed（基线2062+2新）；anomaly lib 13 passed；get_recent_transcripts 1 passed。

### 踩坑（已修）
- **E0308 按值传参**：find_anomalies trait 签名 `opts: AnomaliesOpts`（按值），初写 `&opts` 报错；改按值传 `opts`。lib.rs 仅 `pub mod anomaly`，无 crate 根重导出 → 用全路径 `crate::anomaly::AnomaliesOpts`。
- **E0382 moved value**：新测试 `result.unwrap_err()` 移动后 `result.err()` 复用 → 改 `result.as_ref().err()` 借用。
- **register_all 测试硬编码 8**：加第 9 个 tool 后该测试失败 → 改名 `_nine_tools` + 计数 9 + 加名断言。
- **brain.rs 同文件并行 Edit 互相干扰**：一次消息发多 Edit 同文件致快照过期（struct 插入/doc 表格失败）→ 改单点 Edit + Python 精确插入；doc 表格列对齐空格用 Python 跳过。

### 下一步
1-4 收口。Part12 剩余 pending：**1-5 auto-think**、1-6 orchestration 主循环（runCycle 2057 行；TS 引擎 pglite/postgres 迁移也在此，anomaly/transcript-discovery 的 TS 删除受此阻塞）。

### 已 commit+push（2026-07-30 晚）
- `38a9e6c` feat(zbrain-core): close 1-4 anomaly-transcript — add brain_find_anomalies tool + dedup dream guard（2 文件 +98/−20）
- `eaa7104` chore(roadmap): close 1-4 anomaly-transcript cluster（4 文件 +30/−6）
- 已 `git push origin rust-rewrite`（a884781..eaa7104）。工作区清洁。
