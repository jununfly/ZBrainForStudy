# 2026-07-27 工作记录

## eval seam 收口 (方案 B) 收尾 + Task tracker 清理
- 上一轮（2026-07-24）eval Rust seam 收口（方案 B：接受 TS 断裂、两 eval seam 返回 honest error + 审计 IO 落地）已验证完成。
- 今日清理探索阶段残留 task：`#31`(quality-probe 审计读写) 标记 completed（实际 7 单测已落地）；`#32`(bun spawn eval 适配器) 删除（方案 A 已被用户否定，属废弃条目）。
- nightly_probe 27 测试（20 原 + 7 新审计）全绿，G58 已登记，无残留 src TS 改动。

## zbrain TS→Rust 迁移总进度盘点（用户要求"先汇报总进度"）
- 残留 TS：src 非测试 433（2026-07-15 为 529，净删 ~96）、tests/*.test.ts 568（原 765）、helpers 11。Rust 测试套件 ~2200+ 全绿。
- KNOWN-GAPS：open 42 / blocked 1 / resolved 12。
- 已完成 Part：1-10 + Part8 eval 处置 + Part9 Phase11 package cutover + Part11 主体（doctor 11 检查、孤儿命令、operations.ts 删除、search/facts/think core、schema-pack 迁移 G4）。
- **当前最前沿 = Part11 calibration 簇 (1-3)**：1-3-1 纯函数 / 1-3-2 engine-read 子集已 done；1-3-3 Phase2 引擎/LLM 基建 in_progress，roadmap 记录"当前施工 1-3-3-1 calibration schema 迁移（5 表 version 0023 双后端）"。
- **BLOCKED**：1-4-2 output infra（BrainWriter 撞逃生舱禁令 + integrity/operations 消费者）；1-12 cycle 大迁移（runCycle 2057 行 + 20 phase 全 stub）→ 拆成 Part12。
- **Part12 cycle 迁移**：6 簇全 `[ ]` 未启动（facts-extraction/emotional-calibration/synthesis/anomaly-transcript/auto-think/orchestration），决策"operations.ts 删完后启动"，现 operations.ts 已删，可启动但 roadmap 仍 not-started。
- **Task tracker 与 roadmap 已不同步（待 reconcile）**：`#4` 删 operations.ts 实际已完成（git 331331f/45fe955 + roadmap 1-6-7 done）；`#26` "skillpack 迁移" 实际 CLI 接线已完成（git 5bb7ec3），core 26 模块早迁移完；`#21-#24` book-mirror 移植实际已完成（git db3dd7b + G56 resolved）。真正仍 pending 的实线是 `#2/#3`（code-intel 簇 find_orphans/run_doctor + code_flow/blast/cache_clear 的 clap 薄包装）。
- 用户决策（AskUserQuestion）：本轮只汇报，不实现。下一步待用户选线（skillpack 收尾 / cutover #2#3 / book-mirror 复核 / calibration 续推 / Part12 cycle 启动）。

## 1-3-3-5 takes_calibration op 实现（用户"选4+1"：reconcile tracker + 续推 calibration #1）
- 目标：对齐 `get_calibration_curve` 到 canonical TS 形状并接入 `takes_calibration` op。
- 关键实现：
  - `calibration_queries.rs`：`CalibrationBucket` 改为 canonical `{bucket_lo, bucket_hi, n, observed, predicted}`（去 camelCase rename）；新增 `CalibrationRow` + 后端无关纯函数 `aggregate_calibration_curve(rows, bucket_size)`（整数 scale 分箱 `floor(weight*scale)`, scale=round(1/bucketSize)，复刻 TS Postgres `numeric` 精确性，修复 0.7→bucket7 的 FP 边界）；新增 `CalibrationCurveQuery{holder, bucket_size, holders_allow_list}` + `for_holder`。
  - 三后端统一委托 `aggregate_calibration_curve`，SQL 只做作用域（holder + allowList 空 list→`AND 0=1` fail-closed + `resolved_quality IN ('correct','incorrect')`）：`engine.rs`(InMemory 内存迭代) / `libsql.rs` / `postgres.rs`。旧 `confidence`/`resolution` 列 + `bucket_label`/`accuracy` 形状已弃。
  - `BrainEngine` 新增 `get_calibration_curve` 默认方法（Err Unsupported）+ InMemory override 委托（对齐 `get_scorecard` 桥接模式），使 op 能停在 `&dyn BrainEngine`。
  - `operation.rs`：`TakesCalibrationOutput` 扩 `curve: Vec<CalibrationBucket>` 字段（保留 profile+domain_scorecards）；execute 构造 `CalibrationCurveQuery{holder, bucket_size:None, holders_allow_list: ctx.takes_holders_allow_list.as_deref()}` 调 `get_calibration_curve`，从 resolved bets 直接算 curve（独立于 profile）。op 早已注册（解锁 1-6-7-16）。
- 测试：纯分箱（含 0.7→bucket7 FP-edge、observed/predicted、bucketSize 越界回退 0.1）+ InMemory holder/quality 过滤 + allowList fail-closed（空 list 返回空）+ 2 op dispatch 测试（curve 非空 / allowList 排除）。共 8 calibration_queries + 2 op 测试全绿。
- 验证：`cargo check` / `cargo build -p zbrain-core -p zbrain-cli` 通过（libsql/postgres 非 feature-gated 常编）。
- roadmap：`1-3-3-5` → completed；MD "当前施工" 推进到 `1-3-3-6`（gateVoice+runAbTrial）。

## skillpack 测试模块预存 bug 全套修复（用户"先解决 skillpack 测试模块有预存 bug"）
- 该模块仅在 `--all-features` 下编译测试代码，默认 feature 不编（故之前默认测试跑不到）。
- 编译期 bug（3 处，已在 skillpack 测试代码内修复）：
  1. `skillpack/copy.rs:304` + `skillpack/installer.rs:355`：`use std::tempfile::tempdir;` → `use tempfile::tempdir;`（std 无此模块）。同目录 `doctor.rs:146` 已正确。
  2. `skillpack/doctor.rs:171` 测试 `assert_eq!(result.total, 6)` → `result.score`（生产 `DoctorResult` 字段已改名 score，line 129 `score: score.total`）。
  3. `skillpack/rubric.rs:382` 测试构造 `SkillpackManifest {..}` 字面量缺 3 必填字段 → 补 `e2e_tests/ eval_schema_version/ runbook_schema_version: None`（均为 Option）。
- 运行时 bug（4 个失败，编译通过后暴露，逐一定位根因）：
  - `apply_hunks.rs:354` 测试 `hunk.lines.len()` 断言 3 但解析器把上下文+增删全计入 = 4 → 改测试为 4（测试数错）。
  - `apply_hunks.rs` **生产 bug**：`original.lines()` 会丢弃尾换行，打补丁会静默删掉文件尾换行。修复：函数开头记 `trailing_newline = original.ends_with('\n')`，join 后若原带尾换行则 `output.push('\n')` 还原。`apply_hunks` 仅被本模块测试调用（未 re-export），改行为安全。测试 `test_apply_simple_diff` 期望保留尾换行（正确）。
  - `rubric.rs:408` 测试空 manifest 期望 tier=Experimental 实际=Blocked：tier 公式要求 `core_passed==5` 才非 Blocked，而空 manifest `core_passed=1`（同测试 `total==1`/`core_passed==1` 断言已通过，公式自洽）→ 改测试为 Blocked。
  - `doctor.rs` `test_minimal` 期望 score=6/tier≠Blocked：实际 minimal pack setup 漏建 LICENSE 文件 → core dim `license_exists` 失败 → core_passed=4/score=4/Blocked。根因是**测试 setup 不完整**（不是生产 bug）。修复：补 `std::fs::write(pack_root.join("LICENSE"), "MIT")`，使 minimal pack 真正合法 → core_passed=5/score=5/Experimental（非 Blocked）。断言 score 改为 5。
- 验证：`cargo test -p zbrain-core --lib skillpack --all-features` 10/10 全绿；整个 lib 测试目标 `--all-features` 编译通过（无其它模块被波及）。
- 结论：skillpack 测试模块之前因编译错误从未运行，暴露的 4 运行时失败中 1 个是真实生产 bug（apply_hunks 尾换行），3 个是测试过时/setup 不完整（已分别修正，未弱化任何有意义断言）。

## 1-3-3-2 undoWave 完成（TDD，8 测试全绿）
- **交付**：`CalibrationWaveQueries` trait（calibration_queries.rs，4 方法 + `UndoWaveOpts`/`UndoWaveResult`）+ `undo_wave` 自由函数（calibration.rs，停在 `&dyn BrainEngine`）+ BrainEngine default Err + 三后端桥 override。libsql/postgres 真实 SQL（dry_run 走 COUNT、实跑走 UPDATE/DELETE 返回 affected），InMemory 返回 0（无 wave 表）。
- **测试选型（用户确认）**：真实 libsql 临时库（`tests/libsql_undo_wave.rs`，NamedTempFile + init_schema + raw Builder conn seed，范式抄 code_symbol_query.rs）。8 测试：4 步各自 wave 隔离行为 + dry-run 不写 + 幂等 + 自定义 resolved_by_label + gstack warning。
- **阻断性发现修复**：migration 0023（calibration 5 表）此前**从未注册**（两后端 MIGRATIONS 停在 22）——1-3-3-1 标完成但表从不创建；本次注册 version 23 双后端。另补 libsql/postgres `BrainEngine::get_calibration_curve` 桥 override（1-3-3-5 遗留缺口，op 经 dyn 调用会命中 default Err）。
- **语义要点**：Step1 经 take_grade_cache(applied=true+wave) 定位 takes，`resolved_by` 交叉核对保护手动改判；Rust takes 无 `resolved_source` 列（TS UPDATE 有），只 NULL 现存 6 列（代码注释已说明）；Step1b UPDATE applied=false 审计行保留；gstack scrub 不 spawn 二进制→登记 **G59**（warning 指路手动命令）。
- **踩坑**：libsql 测试并行首跑出现过一次 STATUS_ACCESS_VIOLATION（冷启动 FFI 竞态，已知类型），重跑 3 次并行全绿，非本次代码问题。
- roadmap 1-3-3-2 completed + render；KNOWN-GAPS 登记 G59。未提交（等用户指令）。

## 1-3-3-4 queryAcrossBrains 收口完成（grill-me + roadmap-driven 决策落盘）
- **边界**（grill Q1–Q4 用户均选 q-0）：补 D18 四用例行为测试 + 接 op handler + 本次做生产 MountResolver（吸收原 roadmap "生产 resolver 为后续小节点"）。Q5 local_brain_id 来源 = `ctx.brain_id` 优先 + fallback `'host'`。
- **审计发现**：`query_across_brains` 在 Rust 已实现（`calibration.rs:579-622`，4 规则 parity 对齐），roadmap 标 pending 是状态滞后；缺口仅为行为测试/op 接线/生产 resolver。
- **关键改动**：
  - `calibration.rs`：`query_across_brains` 第一参数 `&dyn CalibrationQueries` → `&dyn BrainEngine`（用 `BrainEngine::get_calibration_profile` 桥委托 `get_latest_profile`，零新增 trait 方法）；`CrossBrainProfileResult` 加 `#[derive(Serialize)]` + camelCase（op Output 必须）；新增 `NoMountsResolver`（ctx 无 resolver 时降级返回空）。
  - `operation.rs`：`OperationContext` 加 `mount_resolver: Option<Arc<dyn MountResolver>>`（serde skip）+ `with_mount_resolver` builder；新增 `GetCrossBrainProfileParams` + `GetCrossBrainProfileOperation`（name=`get_cross_brain_profile`，Output=`Option<CrossBrainProfileResult>`，从 ctx.brain_id/can_read_mounts 算）并注册。另补 lib 级 dispatch 冒烟测试 guards 注册 + Serialize derive。
  - `mounts.rs`：新增 `ProductionMountResolver` + `build_mount_engine`（读 mounts.json，`Pglite`→database_path / `Postgres`→database_url，含 `engine.connect(&EngineConfig)`；单 mount 失败 `tracing::warn` + skip，返回成功子集——急切构造+容错跳过，Q2 q-0）。
  - `zbrain-cli/src/lib.rs:2902`：唯一 ctx 构造点注入 `ProductionMountResolver`（若 `~/.zbrain/mounts.json` 存在）。
  - `libsql.rs` **附带修复真实生产 bug**：`get_latest_profile` 行映射索引整体错位 + `published` 用 `get::<bool>` 读 INTEGER（libsql 0.9.30 崩）。修正：published=`get::<i64>(5)!=0`；JSON 列 domain/pattern/bias 索引 11/12/15；voice_gate_passed(13)/attempts(14)/model_id(16)/cost_usd(17)/judge_agreement(18) 全部修正。`get_calibration_profile` op 读取任何真实 profile 都会崩，undoWave 测试只计数一直没暴露。
  - `Cargo.toml`：dev-deps 加 `async-trait`（D18 测试需定义 async stub resolver）。
  - `tests/libsql_cross_brain.rs`（新建）：D18 四用例（local-first / mount-fallback / published=false 跳过 / subagent 禁）+ 真实 libsql 临时库 + `StubMountResolver`。
- **验证**：cross_brain 4/4 绿（串行稳定；并行偶发 FFI 竞态 STATUS_ACCESS_VIOLATION 已知 flake）；新增 op dispatch 冒烟 1 绿；回归 calibration lib 62 / undoWave 8 均绿。两 crate 编译干净（仅预存 dead-code warnings）。
- roadmap 1-3-3-4 → completed + render（⚠️ 标注的 "子节点" checkbox 列表为手动维护层，render 只重写非 ⚠️ 的树段落，故手动同步勾选 1-3-3-4）；无新 KNOWN-GAP。
- **未提交**（延续 1-3-3-2 口径，等用户指令；改动文件：calibration.rs/operation.rs/libsql.rs/Cargo.toml/mounts.rs/lib.rs + 新测试 + roadmap json/md）。

## 1-3-3-6 gateVoice + runAbTrial 完成（grill-me 7 决策落盘 + 实现 + 测试）
- **grill 决策（7 问全 q-0）**：两者都移植；`calibration/` 下 `voice_gate.rs`+`think_ab.rs` 两子模块；三独立 DI trait（VoiceGenerator/VoiceJudge/ThinkRunner，Arc<dyn> 入参）；生产 backing 包裹 ChatProvider（LlmBackedVoiceJudge+ChatBackedVoiceGenerator，haiku）；source_id 调用方提供（FK 失败返 Err，G52）；buildAbReport cutoff Rust 算 ISO8601 绑定；D 风格行为测试。
- **voice_gate.rs**：VoiceGateMode(5 rubric)/VoiceVerdict/parse_judge_output（鲁棒解析，乱码→academic）/gate_voice 重试+模板兜底/LlmBackedVoiceJudge/ChatBackedVoiceGenerator + 内联 11 断言单测。
- **think_ab.rs**：AbPreference/ThinkRunner/PreferenceResolver DI + run_ab_trial 编排 + build_ab_report（net_negative：decisive≥20 且 win_rate<0.45）+ format_ab_report。Rust 无 runThink，生产 think 接线延后（仅 DI 接口）。
- **关键架构修正（execute_raw 禁区）**：初版用 `engine.execute_raw` 失败——LibsqlEngine 未实现 `BrainEngine::execute_raw`（这是刻意设计，libsql.rs:9811 的 execute_raw 属 TokenQueries）。改为 `CalibrationQueries` trait 新增两 typed 方法：`insert_think_ab_result(&ThinkAbInsert)->Option<i64>`（默认 impl 返 Unsupported——写不静默丢）+ `think_ab_preference_counts(cutoff_iso)->Vec<(String,u64)>`（默认返空——读降级），libsql/postgres 显式实现（pg 侧 `$1::timestamptz` cast；libsql 侧 ran_at 是 ISO TEXT 字典序比较）。InMemory/测试 Fake 走默认，4 处 impl 零破坏。
- **think_ab.rs 入参**：`AbRunInput.queries: &dyn CalibrationQueries`（非 BrainEngine）；build_ab_report 同。
- **测试**：`tests/think_ab.rs` 4 用例全绿（insert+read-back via raw libsql conn / 聚合+net_negative+时间窗排除旧行 / 无 decisive 不 flag / 未知 source FK 报错）；lib calibration 回归 71/71 绿；`cargo check --workspace --tests` 通过。
- roadmap 1-3-3-6 → completed + render。**1-3-3 簇剩 1-3-3-7**。**未提交**（等用户指令；改动：calibration.rs/calibration_queries.rs/libsql.rs/postgres.rs + 新 calibration/ 目录两文件 + tests/think_ab.rs + roadmap json/md）。

## 1-3-3-7 calibration-profile 循环 runPhaseCalibrationProfile 完成（收口 1-3-3 簇）
- **grill 决策（8 问全 q-0，前已落盘）**：独立编排函数 `run_calibration_profile` 落 `calibration/calibration_profile.rs`；PatternStatementsGenerator/BiasTagsGenerator 两 async trait DI（生产 ChatBacked 包裹 ChatProvider，patterns=Sonnet，judge=Haiku LlmBackedVoiceJudge）；复用 `pattern_statement_template`/`PatternStatementSlots`；BudgetGate trait DI 默认 PermissiveBudgetGate（总放行，真实累加器归 Part12 cycle 集成）；复用 `aggregate_domain_scorecards(engine,holder,domains,source_id)`（domains=None→`{}`）；typed `insert_calibration_profile` 守 execute_raw 禁区（CalibrationQueries 方法）；返回 `ZbResult<CalibrationProfileResult>`（不接 op，归 1-3-3-5）；D 风格测试（纯单测 parsers+pick_fallback_slots + 集成 tests/calibration_profile.rs）。
- **代码改动（#66/#67/#68）**：
  - `calibration_queries.rs`：新增 `CalibrationProfileInsert` 结构体 + `CalibrationQueries::insert_calibration_profile` 默认 Unsupported 方法。
  - `calibration.rs`：注册 `mod calibration_profile` + glob 重导出。
  - `calibration/calibration_profile.rs`（新建）：完整移植。const 版本戳/模型 id（patterns=`claude-sonnet-4-6`，judge=`claude-haiku-4-5-20251001`）/cold-brain 阈值 5；`parse_pattern_statements_output`/`parse_bias_tags_output`/`pick_fallback_slots`；三 trait（PatternStatementsGenerator/BiasTagsGenerator/BudgetGate）+ ChatBacked impls + `PermissiveBudgetGate`；`PatternStatementsVoiceAdapter`（PatternStatementsGenerator→VoiceGenerator 桥）；`run_calibration_profile`：解析 generators/judge（chat 缺失且未注入→硬错）→get_scorecard→cold-brain<5 skip(insufficient_data)→budget gate→gate_voice::<PatternStatementSlots>→bias tags(best-effort)→aggregate_domain_scorecards(fail-soft)→insert_calibration_profile；3 纯单测。
  - **三后端桥**（关键修复：原 #67 漏了 libsql 的 BrainEngine 桥）：`CalibrationQueries::insert_calibration_profile` 的 libsql 真实实现（RETURNING id，pattern/bias/domain 三 JSON 字符串，generated_at/published/cost_usd/judge_model_agreement 走 DEFAULT/NULL，不引 chrono）；postgres/InMemory 走默认 Unsupported（PG 无 calibration_profiles 表，G52 不伪造 source）。`impl BrainEngine for LibsqlEngine`（libsql.rs:645 块）此前缺 `insert_calibration_profile` 桥接 → 走 trait default Unsupported；补上后全链路打通（测试抓出）。
- **测试（#69）** `tests/calibration_profile.rs`（新建，4 用例全绿，串行，全 stub 无 LLM）：cold-brain 跳过（空库 resolved=0）；完整写入+read-back（PRAGMA foreign_keys=OFF 宽 seed 6 条 resolved takes，断言 profile_written/voice_gate_passed/pattern_statements/active_bias_tags/total_resolved=6 + raw conn 读回 source_id/holder/model_id=`claude-sonnet-4-6`/JSON 数组/`{}`）；budget-exhausted 跳过（DenyBudgetGate，Warn+skipped=budget_exhausted+无行）；未知 source FK 报错（no-such-source→assert 含 foreign key 或 insert_calibration_profile）。
- **验证**：`cargo test -p zbrain-core --lib calibration` 74/74 绿（含 calibration_profile 3 纯单测 + voice_gate 全）；`cargo test -p zbrain-core --test calibration_profile -- --test-threads=1` 4/4 绿；`cargo check --workspace --tests` 仅预存 unused-import warnings 无 error。
- **踩坑**：① `CalibrationProfileOpts` 含 trait object 字段不能 derive(Debug)，改手动 impl Debug（对齐 voice_gate 的 VoiceGateOpts 不 derive Debug）；② libsql 测试 seed 时 raw conn FK 是开的，须 `PRAGMA foreign_keys=OFF` 才能 page_id=0 seed resolved takes（引擎连接仍 FK ON，calibration_profiles insert 的 source_id='default' 走 seeded FK 通过）；③ migration 0012 已给 takes 加全列，测试无需 ALTER（初版 ALTER 报 duplicate column）。
- roadmap 1-3-3-7 → completed + render；**1-3-3 簇 7 子节点全 completed，父 1-3-3 同步 completed（簇收口）**。
- **未提交**（延续 1-3-3-2/4/6 口径，等用户指令；改动：calibration_queries.rs/calibration.rs/calibration/calibration_profile.rs(新)/libsql.rs/postgres.rs/engine.rs + tests/calibration_profile.rs(新) + roadmap json/md）。

## Part12 cycle 大迁移启动：1-1-1 extract-facts phase 落地（未提交）
- **1-3-3-7 批次已提交并 push**（e7d7ffe feat + 0e93162 chore → origin/rust-rewrite）。
- **roadmap 簿记**：Part12 JSON 节点 1-1（facts-extraction 簇）in_progress + 6 sub-node（1-1-1~1-1-6）+ 7 条 grill 决策（Q1 六 sub-node / Q2 每 phase=execute_phase 真实臂 / Q3 ChatProvider（本 phase 实际无 LLM）/ Q4 底向上 / Q5 stub 测试 / Q6 范围仅 6 函数 / Q7 消费者映射归 1-6）；1-1-1 已 completed + render。
- **关键发现**：extract-facts.ts 是 fence 解析 + DB 对账（wipe-and-reinsert per page），**不调 LLM**；仅 embeddings（fail-open 降 NULL）与 phantom-redirect 预通行（需磁盘+锁）跳过 → 登记 KNOWN-GAPS **G60/G61**（含代码双向锚点）。
- **改动**：`types.rs` NewFact/FactRow 加 `row_num`/`source_markdown_slug`（v51 fence 列，表已有列但未暴露）；libsql `insert_fact` 写 ?18/?19 + `row_to_fact`/SELECT 对齐；trait 默认方法 `delete_facts_for_page`(Unsupported)/`count_legacy_fact_rows`(0)，libsql/inmemory 真实实现（libsql 走 inherent `_impl` + trait 委托，**不能开第二个 impl BrainEngine 块——E0119**）；postgres FactRow 硬编码 None；`time.rs` 加 `today_utc_date()`；新模块 `autopilot/phases/extract_facts.rs`（FENCE_SOURCE_DEFAULT/METRIC_NORMALIZATION_MAP/normalize_metric_label/extract_facts_from_fence_text/run_extract_facts + 3 单测）；`cycle.rs` ExtractFacts 真实臂（guard/warnings→Warn）+ `run_cycle_empty_brain` 断言 skipped 18→17 + extract-facts=Ok。
- **测试**：lib autopilot 201/201 绿；新集成 `tests/extract_facts.rs` 6/6 绿（fence 对账 read-back、无 fence 零插入、空库 Ok、重跑幂等 wipe-and-reinsert、dry-run 零写、legacy guard 拦截）；facts_crud 41/41 回归绿；zbrain-cli 重建成功。
- **踩坑**：① FactRow/NewFact 扩字段后需全量补字面量（engine.rs 测试 11 处 + operation.rs + postgres.rs + facts_crud.rs `nf()` + zbrain-cli lib.rs）；② `get_all_slugs` 带 `Option<&str>` 参数且返回 HashSet（需 collect+sort 保确定性）；③ `GetPageOpts` 从 `crate::GetPageOpts` re-export 导入（types 里没有）。
- **未提交**（等用户指令）。下一步：1-1-2 extract-atoms。

## Part12 1-1-2 extract-atoms phase 落地（未提交）
- **续 1-1-1**：facts-extraction 簇第二个子节点。extract-atoms.ts 与 extract-facts 不同——**它调 LLM（Haiku）**，写入 `type:"atom"` pages；TS 有两条源（transcripts 文件系统路径 + brain pages），**1-1-2 仅迁 pages 路径**，transcript 路径登记 **G62**（磁盘+config loader 复杂，与 G61 phantom-redirect 同类）。
- **grill 决策落盘（Q8~Q12）**：① ChatProvider 注入用方案B——`CycleOpts` 加 `chat: Option<Arc<dyn ChatProvider>>` 字段（不改 run_cycle/execute_phase 签名，7 处测试零改动）；execute_phase ExtractAtoms 臂 `match &_opts.chat { None=>Skipped("no chat provider wired"), Some(chat)=>run_extract_atoms(...) }`；空 brain chat=None→Skipped，skipped_count 保持 17（ExtractAtoms 仍计 skipped）。② transcript 路径不迁→G62。③ 幂等用新 trait 方法 `discover_extractable_pages(source_id, affected_slugs: Option<&[String]>)` + `atom_exists_for_hash(source_id, hash16)`；libsql/postgres 各自真实 SQL（NOT EXISTS 幂等子查询 + 内联 IN 列表避开 libsql 数组参数坑），inmemory 走 default Err(Unsupported)→run_extract_atoms fail-soft 包成空。④ Haiku 预算 `DEFAULT_BUDGET_USD=0.3`，`estimated_spend+= (usage.input*0.8+output*4.0)/1e6`，超限 halt+Warn+部分结果。⑤ atom slug=`atoms/{today_utc_date()}/{slugify(title)}`；atom_type/virality_score/emotional_register/source_quote/lesson 映射 frontmatter；source_hash=content_hash[..16]；source_slug（页面源）。
- **改动**：
  - `types.rs` 加 `DiscoveredPage{slug,content,content_hash}`。
  - `engine.rs` trait 加 `discover_extractable_pages`/`atom_exists_for_hash` 默认 Err（注：`PageInput` 在 crate 根 `pub use`，不在 `crate::types`）。
  - `libsql.rs` + `postgres.rs` 两方法真实实现（NOT EXISTS 幂等 + 内联 IN 列表）。
  - `autopilot/phases/extract_atoms.rs`（新建）：常量/EXTRACT_PROMPT(raw string 避转义)/`parse_atoms_response`(容错：去 fence、找首 `[`、失败回退 rfind(']')、过滤无效 atom_type、source_quote 截 500/title 截 200，改 `pub(crate)` 修 visibility warning)/`slugify`/`run_extract_atoms`（transcripts 恒空 G62，pages fail-soft，逐 item 超预算 skip，chat 调 Haiku，写 atom page，dry_run 仅计数）+ 3 单测（parse 变体/slugify/inmemory 无工作 chat 不被调）。
  - `phases/mod.rs` 加 `pub mod extract_atoms;`；`cycle.rs` 加 `chat` 字段 + ExtractAtoms 真实臂 + `run_cycle_empty_brain` 断言（extract-atoms=Skipped，skipped 保持 17）。
  - **chat 注入范式提炼**：LLM-heavy phase 经 `CycleOpts.chat: Option<Arc<dyn ChatProvider>>` 注入，execute_phase 臂 `None→Skipped`；后续 propose-takes/grade-takes 同款。
- **测试**：`tests/extract_atoms.rs`（新建，4 用例全绿）：pages 路径→mock Haiku 返回 atoms→写 2 atom pages + get_page read-back 校验 atom_type/source_hash(=content_hash[:16])/source_slug；重跑幂等（NOT EXISTS 拦截 0 新写）；dry-run 零写；空库零工作。
- **踩坑**：① libsql 集成测试并行跑 `STATUS_ACCESS_VIOLATION` 0xc0000005（Windows FFI 竞态）——加 `OnceLock<Mutex<()>>` 进程级锁 `libsql_test_guard()` 串起本文件 4 测试（串行 0.15s，并行稳定）。② 测试 `list_atom_slugs` 须 `async`（初版 sync 调 `.await` E0728）。③ `MockChatProvider`（chat.rs:349，`#[async_trait::async_trait] impl ChatProvider`，`new`/`queue_text`/`queue_result`/`queue_error`，usage 默认 input:10/output:20）直接复用。
- **KNOWN-GAP G62 登记**（docs/plans/MIGRATION.md，双向锚点：extract_atoms.rs 模块 doc + `let transcripts = Vec::new()` 处注释 `registered in docs/plans/MIGRATION.md (G62)`）。
- **roadmap**：1-1-2 → completed + render（注：当前 `roadmap_cli.py render` 实际只重写 `⚠️ ROADMAP_SECTION` 段，顶部纯 `ROADMAP_SECTION` 段已成孤儿——tool 行为与记忆文档相反；已手动把顶部 1-1-2 行同步为 `[x]` 保持两份树一致）。
- **验证**：`cargo build -p zbrain-core` 0 error；`cargo test -p zbrain-core --lib autopilot` 204/204（含 extract_atoms 3 + cycle 13）；`cargo test -p zbrain-core --test extract_atoms` 4/4（串行 0.15s，并行因锁稳定）。zbrain-cli 无需重建（仅 cycle.rs 加字段，TS 不调）。
- **未提交**（延续铁律，等用户指令）。改动文件：`types.rs/engine.rs/libsql.rs/postgres.rs/cycle.rs/phases/mod.rs/phases/extract_atoms.rs(新)/tests/extract_atoms.rs(新)` + `KNOWN-GAPS.md` + `zbrain-ts-to-rust-part12-cycle-migration.json`(md_file 经 link 写入) + `ZBRAIN_TS_TO_RUST_PART12_CYCLE_MIGRATION.md`。下一步：1-1-3 extract-takes（或用户要求提交）。
