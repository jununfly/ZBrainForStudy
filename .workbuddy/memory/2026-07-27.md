# 2026-07-27 工作记录

## eval seam 收口 (方案 B) 收尾 + Task tracker 清理
- 上一轮（2026-07-24）eval Rust seam 收口（方案 B：接受 TS 断裂、两 eval seam 返回 honest error + 审计 IO 落地）已验证完成。
- 今日清理探索阶段残留 task：`#31`(quality-probe 审计读写) 标记 completed（实际 7 单测已落地）；`#32`(bun spawn eval 适配器) 删除（方案 A 已被用户否定，属废弃条目）。
- nightly_probe 27 测试（20 原 + 7 新审计）全绿，G58 已登记，无残留 src TS 改动。

## zbrain TS→Rust 迁移总进度盘点（用户要求"先汇报总进度"）
- 残留 TS：src 非测试 433（2026-07-15 为 529，净删 ~96）、tests/*.test.ts 568（原 765）、helpers 11。Rust 测试套件 ~2200+ 全绿。
- KNOWN-GAPS：open 42 / blocked 1 / resolved 12。
- 已完成 Part：1-10 + Part8 eval 处置 + Part9 Phase11 package cutover + Part11 主体（doctor 11 检查、孤儿命令、operations.ts 删除、search/facts/think core、schema-pack 迁移 G4）。
- **当前最前沿 = Part11 calibration 簇 (1-3)**：1-3-1 纯函数 / 1-3-2 engine-read 子集已 done；1-3-3 Phase2 引擎/LLM 基建 in_progress，roadmap 记录"当前施工 1-3-3-1 calibration schema 迁移（5 表 version 0023 双后端）"。
- **BLOCKED**：1-4-2 output infra（BrainWriter 撞逃生舱禁令 + integrity/operations 消费者）；1-12 cycle 大迁移（runCycle 2057 行 + 20 phase 全 stub）→ 拆成 Part12。
- **Part12 cycle 迁移**：6 簇全 `[ ]` 未启动（facts-extraction/emotional-calibration/synthesis/anomaly-transcript/auto-think/orchestration），决策"operations.ts 删完后启动"，现 operations.ts 已删，可启动但 roadmap 仍 not-started。
- **Task tracker 与 roadmap 已不同步（待 reconcile）**：`#4` 删 operations.ts 实际已完成（git 331331f/45fe955 + roadmap 1-6-7 done）；`#26` "skillpack 迁移" 实际 CLI 接线已完成（git 5bb7ec3），core 26 模块早迁移完；`#21-#24` book-mirror 移植实际已完成（git db3dd7b + G56 resolved）。真正仍 pending 的实线是 `#2/#3`（code-intel 簇 find_orphans/run_doctor + code_flow/blast/cache_clear 的 clap 薄包装）。
- 用户决策（AskUserQuestion）：本轮只汇报，不实现。下一步待用户选线（skillpack 收尾 / cutover #2#3 / book-mirror 复核 / calibration 续推 / Part12 cycle 启动）。

## 1-3-3-5 takes_calibration op 实现（用户"选4+1"：reconcile tracker + 续推 calibration #1）
- 目标：对齐 `get_calibration_curve` 到 canonical TS 形状并接入 `takes_calibration` op。
- 关键实现：
  - `calibration_queries.rs`：`CalibrationBucket` 改为 canonical `{bucket_lo, bucket_hi, n, observed, predicted}`（去 camelCase rename）；新增 `CalibrationRow` + 后端无关纯函数 `aggregate_calibration_curve(rows, bucket_size)`（整数 scale 分箱 `floor(weight*scale)`, scale=round(1/bucketSize)，复刻 TS Postgres `numeric` 精确性，修复 0.7→bucket7 的 FP 边界）；新增 `CalibrationCurveQuery{holder, bucket_size, holders_allow_list}` + `for_holder`。
  - 三后端统一委托 `aggregate_calibration_curve`，SQL 只做作用域（holder + allowList 空 list→`AND 0=1` fail-closed + `resolved_quality IN ('correct','incorrect')`）：`engine.rs`(InMemory 内存迭代) / `libsql.rs` / `postgres.rs`。旧 `confidence`/`resolution` 列 + `bucket_label`/`accuracy` 形状已弃。
  - `BrainEngine` 新增 `get_calibration_curve` 默认方法（Err Unsupported）+ InMemory override 委托（对齐 `get_scorecard` 桥接模式），使 op 能停在 `&dyn BrainEngine`。
  - `operation.rs`：`TakesCalibrationOutput` 扩 `curve: Vec<CalibrationBucket>` 字段（保留 profile+domain_scorecards）；execute 构造 `CalibrationCurveQuery{holder, bucket_size:None, holders_allow_list: ctx.takes_holders_allow_list.as_deref()}` 调 `get_calibration_curve`，从 resolved bets 直接算 curve（独立于 profile）。op 早已注册（解锁 1-6-7-16）。
- 测试：纯分箱（含 0.7→bucket7 FP-edge、observed/predicted、bucketSize 越界回退 0.1）+ InMemory holder/quality 过滤 + allowList fail-closed（空 list 返回空）+ 2 op dispatch 测试（curve 非空 / allowList 排除）。共 8 calibration_queries + 2 op 测试全绿。
- 验证：`cargo check` / `cargo build -p zbrain-core -p zbrain-cli` 通过（libsql/postgres 非 feature-gated 常编）。
- roadmap：`1-3-3-5` → completed；MD "当前施工" 推进到 `1-3-3-6`（gateVoice+runAbTrial）。

## skillpack 测试模块预存 bug 全套修复（用户"先解决 skillpack 测试模块有预存 bug"）
- 该模块仅在 `--all-features` 下编译测试代码，默认 feature 不编（故之前默认测试跑不到）。
- 编译期 bug（3 处，已在 skillpack 测试代码内修复）：
  1. `skillpack/copy.rs:304` + `skillpack/installer.rs:355`：`use std::tempfile::tempdir;` → `use tempfile::tempdir;`（std 无此模块）。同目录 `doctor.rs:146` 已正确。
  2. `skillpack/doctor.rs:171` 测试 `assert_eq!(result.total, 6)` → `result.score`（生产 `DoctorResult` 字段已改名 score，line 129 `score: score.total`）。
  3. `skillpack/rubric.rs:382` 测试构造 `SkillpackManifest {..}` 字面量缺 3 必填字段 → 补 `e2e_tests/ eval_schema_version/ runbook_schema_version: None`（均为 Option）。
- 运行时 bug（4 个失败，编译通过后暴露，逐一定位根因）：
  - `apply_hunks.rs:354` 测试 `hunk.lines.len()` 断言 3 但解析器把上下文+增删全计入 = 4 → 改测试为 4（测试数错）。
  - `apply_hunks.rs` **生产 bug**：`original.lines()` 会丢弃尾换行，打补丁会静默删掉文件尾换行。修复：函数开头记 `trailing_newline = original.ends_with('\n')`，join 后若原带尾换行则 `output.push('\n')` 还原。`apply_hunks` 仅被本模块测试调用（未 re-export），改行为安全。测试 `test_apply_simple_diff` 期望保留尾换行（正确）。
  - `rubric.rs:408` 测试空 manifest 期望 tier=Experimental 实际=Blocked：tier 公式要求 `core_passed==5` 才非 Blocked，而空 manifest `core_passed=1`（同测试 `total==1`/`core_passed==1` 断言已通过，公式自洽）→ 改测试为 Blocked。
  - `doctor.rs` `test_minimal` 期望 score=6/tier≠Blocked：实际 minimal pack setup 漏建 LICENSE 文件 → core dim `license_exists` 失败 → core_passed=4/score=4/Blocked。根因是**测试 setup 不完整**（不是生产 bug）。修复：补 `std::fs::write(pack_root.join("LICENSE"), "MIT")`，使 minimal pack 真正合法 → core_passed=5/score=5/Experimental（非 Blocked）。断言 score 改为 5。
- 验证：`cargo test -p zbrain-core --lib skillpack --all-features` 10/10 全绿；整个 lib 测试目标 `--all-features` 编译通过（无其它模块被波及）。
- 结论：skillpack 测试模块之前因编译错误从未运行，暴露的 4 运行时失败中 1 个是真实生产 bug（apply_hunks 尾换行），3 个是测试过时/setup 不完整（已分别修正，未弱化任何有意义断言）。
