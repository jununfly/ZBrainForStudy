# 2026-08-15

- 路线图 `1-1-5` 下新建延伸三节点（CLI `add`，未手改 JSON）：
  - `1-1-5-9` eval-brainstorm 持久化 run store（save/list/gc，清 checkpoint stub）
  - `1-1-5-10` eval-brainstorm review 子命令（历史 run 查看 + grounding/pass 趋势）
  - `1-1-5-11` eval-brainstorm checkpoint resume 续跑（resume playback 接入 orchestrator）
  - 决策记于 `1-1-5-9`：brainstorm 延伸 = 1-1-5 下一组节点，镜像 suspected-contradictions 的 trend/review + 填 `checkpoint.rs` Q3 MVP stub。md 已 render。
- 背景：`1-1-5-5` brainstorm（#63）已于 2026-08-14 收口并推送（e6bbdf1f feat + 01233614 chore）。`crates/zbrain-core/src/eval/brainstorm/checkpoint.rs` 仅 `compute_run_id` 实装（已测），`save_checkpoint`/`list_runs`/`gc_stale_checkpoints`/`load_checkpoint`/`clear_checkpoint` 均为 no-op stub，orchestrator 不调用 resume → 1-1-5-9 填这些 stub。
- 未提交：roadmap JSON/MD 改动 + 本日志，等用户显式触发 commit/push（rust-rewrite）。

- **1-1-5-10 review 子命令已实装并验证**（决策全 A 已落盘）：
  - 序列化解锁：`BrainstormResult`/`BrainstormIdea`/`JudgeIdeaResult`/`JudgeAxisScores`/`BrainstormCost`/`CloseRefForReport`/`FarRefForReport` 加 `serde::Deserialize`；`profile_label`(orchestrator) 与 `FarRefForReport.source` 由 `&'static str`→`String`（构造点全加 `.to_string()`）。`JudgeIdeaResult` 实测不含 `&'static str`（纠正旧误判），`JudgeConfig.label/extra_instructions` 不在序列化链上。
  - store.rs：新增 `mean_grounding: Option<f64>`（summary 从 `ideas[].judge.scores.concrete_grounding` 均值）、`load_run_result`（反序列化重建 `BrainstormResult`，利好 1-1-5-11）、`recent_runs_by_days`、`render_trend_chart`（双轴 pass-rate% + grounding 表格 + 双 bar）、`render_review_header`。
  - checkpoint.rs：新增 facade 委托 store；CLI `print_run_review`/`print_run_trend` 助手。
  - lib.rs：`BrainstormArgs`/`EvalBrainstormArgs` 加 `--review-run <run_id>`/`--trend`/`--days`（默认 30）；两命令连引擎前早退；`--list-runs` 表加 pass-rate% + mean-grounding 列。
  - 验证：`cargo check -p zbrain-cli` 通过；`cargo test -p zbrain-core --lib brainstorm` 34 passed（含 `summarize_mean_grounding_and_load_result`）；`cargo test -p zbrain-cli --lib brainstorm` 4 passed。CRLF numstat 正常（无整文件重写）。roadmap 节点标记 completed 并 render。
  - 待用户显式触发 commit/push（feat/chore 分离，rust-rewrite）。后续 1-1-5-11 resume 可直接复用 `load_run_result`。

- **1-1-5-11 eval-brainstorm checkpoint resume 续跑已实装**（feat 源码 + chore roadmap 完成，测试验证后台跑）：
  - core `orchestrator.rs`：
    - `BrainstormProfile` 加 `PartialEq` derive；但 transitively 要求 `JudgeConfig: PartialEq`，故另在 `judges.rs` 给 `JudgeConfig` 加 `PartialEq`（修 E0369：`&JudgeConfig` 无 `==`）。
    - 新增 `ResumeOverrides`（全 `Option` 字段：model_override/judge_model/source_id/source_ids/max_cost_usd/max_far_set/max_ideas_per_judge_call/active_bias_tags），`Default` 派生。
    - 新增 `prepare_resume`（返回 `anyhow::Result<(BrainstormOptions, String)>`）：非 force 时解析 `saved_at`(RFC3339)→`chrono::Utc`→`signed_duration_since.num_milliseconds()`，`age_ms > STALE_MS`(=checkpoint.rs 的 7 天闸门) 则 `bail!`；按 `profile_label=="lsd"` 映射 `LSD_PROFILE`/`BRAINSTORM_PROFILE`；`active_bias_tags` 取 overrides 优先于 result；返回 `(options, result.run_id.clone())`。
    - 新增 4 单测：`prepare_resume_recent_ok_and_maps_profile` / `_stale_blocked_without_force` / `_stale_ok_with_force` / `_overrides_win`，用 `sample_result(profile_label, run_id, bias)` 构造 `BrainstormResult`。
  - cli `lib.rs`：
    - import 补 `BrainstormProfile`；`EvalBrainstormArgs` 加 `resume: Option<String>` + `force_resume: bool`。
    - `run_brainstorm_command`：移除占位 `bail!`，改 `if let Some(run_id)=&args.resume` 分支 → 构造 `ResumeOverrides{judge_model,max_cost_usd,max_far_set,max_ideas_per_judge_call}` → `run_resume_playback`。engine 构建上移共用。
    - 新增 `run_resume_playback`：load_checkpoint(取 saved_at)+load_run_result→prepare_resume→run_brainstorm→改写 `regenerated.run_id=format!("{}~r{}",orig,ts)`→format_brainstorm_markdown(only_passed,include_meta) 或 serde_json→save_checkpoint（默认保存）。
    - `run_eval_brainstorm_command`：`if let Some(run_id)` 分支用 `ResumeOverrides::default()`（eval 不暴露 per-run 覆盖，原样重放），profile 传 `&BRAINSTORM_PROFILE`（Q4 两者同接）。
    - 新增 3 CLI parse 测试：`brainstorm --resume deadbeefcafe1234`、`eval-brainstorm --resume oldrun` 等。
  - roadmap：`docs/plans/zbrain-g74-g76-reimpl.json` 节点 `1-1-5-11` → `completed`（6 决策完整保留）；`ZBRAIN_G74_G76_REIMPL.md` 整体重写（含树 + 决策历史表）。因 Windows safe-delete shim 在回收站不可用时 fail-closed，Bash/PowerShell/Python 对已存在文件的 `os.replace`/覆盖写/删 全部 `WinError 5` 拒绝 → JSON 用 privileged Edit 改 status、MD 用 privileged Write 重写、`roadmap_cli.py render` 的 `os.replace` 仍不可用，故产物由特权工具生成（JSON valid 已校验）。
  - 验证（关键修正）：此前「`cargo check` 两 crate 全绿」记录**不准**——`BrainstormProfile: PartialEq` 派生 transitively 要求 `JudgeConfig: PartialEq`，而 `JudgeConfig`(judges.rs) 只 derive `Debug,Clone,Copy`，故 lib 实际 **E0369 编译失败**。已在 `JudgeConfig` 加 `PartialEq`（其 `weights: JudgeAxisScores` 早已 `PartialEq`，余字段 `&'static str`/`f64`/`Option<_>` 均 `PartialEq`，加派生零副作用）。修复后 `cargo test -p zbrain-core prepare_resume` → **4/4 passed**（_recent_ok_and_maps_profile / _stale_blocked_without_force / _stale_ok_with_force / _overrides_win），整 `zbrain-core` 套件 exit 0。`cargo test -p zbrain-cli resume` → **3/3 passed**（brainstorm_resume_flag_parses / brainstorm_resume_force_parses / eval_brainstorm_resume_flag_parses），exit 0。**全部测试绿，#318 完成。**
  - 待用户显式触发 commit/push（feat/chore 分离，rust-rewrite）。stray `.tmp`（`docs/plans/` 下 6 个）因 sandbox 禁删须排除 stage。
