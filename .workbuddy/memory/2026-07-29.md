# 2026-07-29

## WSL Linux 验证 libsql 测试（Part12 验证线）
- WSL 后台任务（原 j2mrj4）隔夜后：Rust 工具链已手动装好（curl 绕 rustup TLS 坑），但 `cargo` 从 crates.io 直连下载依赖撞房东网络慢到超时（`transfer too slow: 0 bytes in 30s` 反复 fail），`/root/.cargo/registry/cache` 0 crate，Phase 3/4 一直空转重试失败。
- 配 cargo **rsproxy sparse 镜像**（`/root/.cargo/config.toml`：`replace-with=rsproxy-sparse` + `sparse+https://rsproxy.cn/index/` + `git-fetch-with-cli=true, retry=10`），`cargo fetch` 115s 下 477 crate/428MB 验证可用。注意：之前记的"rsproxy 走不通"是指 rust 组件 dist 路径，cargo registry 镜像另回事、完全可用。`cargo` 执行须用**固定 WSL PATH**（含 `/root/.rust/bin`），勿用 `$PATH`（Windows 侧会展开成带括号的 Windows PATH 致 WSL bash 语法错）。验证脚本 `cargo test | tail` 会截断编译错误，须完整输出。
- 首次在 Linux 真正编译 zbrain-core 暴露 **E0433**：`schema_pack/pack_lock.rs` 的 `default_is_pid_alive` unix 分支 `unsafe { libc::kill(pid,0) }`，但 `libc` 仅 `zbrain-worker` 声明、`zbrain-core` 未声明，且 `zbrain-core/Cargo.toml` 有 `unsafe_code = "forbid"`。Windows 上该分支被 `#[cfg(not(unix))]` 剔除从不编译，故**只 Linux 暴露**（CI ubuntu 同理会红）。用户选 `/proc` 方案修复：unix 分支改 `std::path::Path::new(&format!("/proc/{}", pid)).exists()`（无 libc、无 unsafe，契合 forbid）。host 侧 `pack_lock.rs` 已改（working tree，未提交，等指令）。
- 同步到 WSL `/root/zbrain`，重写验证脚本（完整输出、去 tail），后台重跑（task **kiai95**）：`Compiling zbrain-core` 中、无 E0433，修复生效。等后台完成通知，届时汇报 `--lib` + 52 libsql target 在 Linux 绿/红。
- 经验已写入 MEMORY.md（rustup 避坑条后追加 cargo 镜像坑 + zbrain-core Linux 编译坑两条）。临时脚本 `.workbuddy/_tmp_wsl_run2.sh`、日志 `.workbuddy/wsl_libsql.log`（未提交）。

## Linux 验证最终结果（kiai95 RERUN2 + 单独重跑交叉确认）
- `cargo test -p zbrain-core --lib`：**1955 passed / 0 failed ✅**（pack_lock.rs /proc 修复生效，Linux 核心库编译通过）。libsql FFI 在 Linux **不崩溃**（Windows segfault 是 Windows 原生 FFI 特有，CI 用 Linux 正确）。
- 22 个 target FAILED，细分：
  - **94 个 panic 在 `pg_fixture.rs:94` = `PgInitFailure`**（postgres 测试，本环境无 pg-embed）→ 预期失败，正是 rust-tests.yml 应排除的部分。
  - **5 个真实失败（非 postgres，单独跑也失败=真 bug/漂移）**：
    1. `libsql_engine_migrations` ×2：`assert left:23 == right:21` → 测试常量 `EXPECTED_VERSION=21`（注释"through 0021_code_edges"）过期；实际 migration 已到 **0023**（`0001`–`0023` 共 23 个文件，0022_image_search_spend_log / 0023_calibration_tables 后来加）。属测试常量过时，一眼能修。
    2. `libsql_token_queries` ×1：`valid_oauth_token_returns_auth_info` 查到 `None`，期望 `Some("My Agent")` → 真 bug（token 查询/存储路径在 Rust 端口可能漏字段或条件）。
    3. `code_symbol_disambiguation` ×2（inmemory_*）：disambiguation 查到 `[]`（左空）→ 真 bug（in-memory engine 的 disambiguation 实现/测试数据）。
  - 这 5 个失败**均属更早集群债务**（schema-pack / search / code-graph），**与 Part12 1-1-3 extract-takes 正交**（roadmap JSON 无 token/disambiguation/migration 词）。
- **rust-tests.yml 缺陷（实测坐实）**：脚本一次性 `cargo test -p zbrain-core --lib --test T1 ... --test T52`（不是逐 target 循环），且**无 `--skip postgres`** → 会被 94 个 postgres PgInitFailure 弄红，且复现任何跨 target 串扰。须改：逐 target `for t; do cargo test --test "$t" -- --skip postgres; done` + `--lib` 单独跑。
- host 侧 `pack_lock.rs` 的 /proc 修复**仍 working tree 未提交**（验证前置，CI 编译 Linux 必需）。

## grill 决策（zj-grill-me + zj-roadmap-driven，用户问"接下来应该做什么"）
- 路线图加节点 **1-7「验证线打通：rust-tests.yml修复 + pack_lock提交 + 5测试失败清算」**（in_progress, exploit）。
- Q1：Linux 验证暴露 CI 框架缺陷 + 5 真实失败（均非 Part12），与 1-1-3 正交 → 用户选 **全清闸门**：先修 CI（pack_lock 提交 + rust-tests.yml 逐 target+skip postgres）+ 全修 5 失败（含 3 真 bug 根因），CI 全绿后再回 1-1-3。
- Q2：3 真 bug 根因未知可能很深 → 用户选 **设兜底**：若某真 bug 根因是 Rust 端口功能缺失/需新设计（非测试笔误），登记 KNOWN-GAP + 建 1-7 子节点跟踪，闸门其余照常推进并回 1-1-3；pack_lock/CI/常量属一眼能修不在此列。
- 验证方式：CI 仍 dormant（需用户在 GitHub UI 启用），本地 WSL（已装 Rust+rsproxy 镜像+pack_lock 同步）重跑即是代理验证；另可启 CI 让绿 CI 真实生效。

## 闸门 1-7 执行完成（全清，Q2 兜底未触发）
- 5 个失败全部**直接修复**、无一需转缺口，且诊断确认全是移植笔误/cfg-gated 漏编译（非功能缺失）：
  1. `pack_lock.rs`：`/proc/<pid>` std-only 检查（去 libc + unsafe，契合 crate 级 `unsafe_code=forbid`）。
  2. `libsql.rs::verify_access_token`：SELECT 列索引按 JOIN 布局对齐（0=client_id 1=scopes 2=expires_at 3=client_name 4=source_id 5=resource 6=federated_read）——之前 scopes 读 3、client_name 读 4… 整体偏移致 client_name=None。
  3. `engine.rs::InMemoryEngine::disambiguate_symbol`：从遍历 `code_edges_store`（图边）改为 `chunk_store`，按 source 作用域、匹配 `symbol_name`/`symbol_name_qualified`，镜像 libsql 查询（精确 + 无精确时 fuzzy，truncate 25/5）。
  4. `libsql_engine_migrations` 常量 `EXPECTED_VERSION` 21→23。
  5. `rust-tests.yml`：改**逐 target 循环** + `-- --skip postgres`（pg-embed runner 不可用），每步保留 3× flake 重试；避免 94 postgres PgInitFailure 弄红 + 跨 target 串扰。
- **WSL Linux 验证全绿**：`--lib` 1953 passed、`libsql_engine_migrations` 7、`libsql_token_queries` 7、`code_symbol_disambiguation` 4，**0 failed**（gate_verify.log，EXIT=0）。
- 提交分离：代码+CI 修复 = **commit 868eb49**（fix，5 文件）；memory+roadmap = 单独 chore commit。roadmap 1-7 标 completed + 决策落盘。
- 焦点回 **1-1-3 extract-takes**（Part12 facts-extraction 簇下一叶子）。

## 1-1-3 extract-takes 实现完成（未提交，等指令）
- 新建 `crates/zbrain-core/src/autopilot/phases/extract_takes.rs`：port `src/core/cycle/extract-takes.ts` 的 **cycle db 路径**（枚举 `list_all_page_refs` → 每页 `get_page` 取 `compiled_truth+timeline` → `parse_takes_fence` → 逐页 `add_takes_batch` 增量 upsert）。**不 port `fs` 路径**（需磁盘 walkMarkdownFiles，超 cycle 范围）与 `--rebuild`（`BrainEngine` 无 `executeRaw` 逃逸，delete 走不了）。按页 flush（Rust `add_takes_batch(page_id, &[TakeInput])` 单 page 批量，与 TS `addTakesBatch(buffer)` buffer 自带 page_id 跨页不同）。
- 接 `execute_phase`：在 `cycle.rs` 加 `CyclePhase::ExtractTakes` 真实臂（枚举 + ALL + label + scope(Source) + 真实臂 + 清掉 stub `_=>` 里已死的 `ExtractFacts` 匹配）。空脑 → 0 pages → status Ok；有 warning → Warn。
- **关键偏差**：TS `ALL_PHASES`（20 项）**无**独立 `extract-takes` phase（它是 v0_28_0 orchestrator 消费者，非 runCycle phase）。Rust 提为第 21 项（与 ExtractFacts/ExtractAtoms 先例一致，暴露给 run_cycle）。已在 `extract_takes.rs` module doc + roadmap 1-1-3 notes 注明此 taxonomy divergence。
- `run_cycle_empty_brain` 测试：phase 数 20→21，`extract-takes` 断言 Ok（skipped 仍 17）。`all_phases_count_matches_ts` 20→21。
- 验证：Windows 本机 `cargo test -p zbrain-core --lib` 相关 + `autopilot::` 203 passed / 0 failed；2 个 extract_takes 单测（db upsert 2 takes + 空脑）通过。跨引擎：`libsql.rs:3873` / `postgres.rs:2858` 均已实现 `add_takes_batch`，故 cycle 在 libsql/postgres 也通，无功能缺口（不需 KNOWN-GAP）。
- 改动已提交并 push（用户指令 "commit + push"，feat/chore 分离）：
  - **1423330** feat(zbrain-core): implement extract-takes cycle phase（extract_takes.rs + cycle.rs + phases/mod.rs）
  - **ab639fe** chore(part12): mark 1-1-3 in_progress in roadmap & memory
  - **30c5513** chore(part12): flip 1-1-3 → completed（已 push）
  - 远端 `a454d99..30c5513`。roadmap 1-1-3 现为 completed（与 1-1-1/1-1-2 先例一致）。
  - 未跟踪保留：`.workbuddy/_tmp_gate_verify.sh` / `_tmp_wsl_run.sh` / `_tmp_wsl_run2.sh` 三个历史临时脚本（不提交）。
- 待用户指令：是否继续 **1-1-4 propose-takes**（需 LLM chat provider，与 ExtractAtoms 同范式）？
