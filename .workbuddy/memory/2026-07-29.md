# 2026-07-29

## WSL Linux 验证 libsql 测试（Part12 验证线）
- WSL 后台任务（原 j2mrj4）隔夜后：Rust 工具链已手动装好（curl 绕 rustup TLS 坑），但 `cargo` 从 crates.io 直连下载依赖撞房东网络慢到超时（`transfer too slow: 0 bytes in 30s` 反复 fail），`/root/.cargo/registry/cache` 0 crate，Phase 3/4 一直空转重试失败。
- 配 cargo **rsproxy sparse 镜像**（`/root/.cargo/config.toml`：`replace-with=rsproxy-sparse` + `sparse+https://rsproxy.cn/index/` + `git-fetch-with-cli=true, retry=10`），`cargo fetch` 115s 下 477 crate/428MB 验证可用。注意：之前记的"rsproxy 走不通"是指 rust 组件 dist 路径，cargo registry 镜像另回事、完全可用。`cargo` 执行须用**固定 WSL PATH**（含 `/root/.rust/bin`），勿用 `$PATH`（Windows 侧会展开成带括号的 Windows PATH 致 WSL bash 语法错）。验证脚本 `cargo test | tail` 会截断编译错误，须完整输出。
- 首次在 Linux 真正编译 zbrain-core 暴露 **E0433**：`schema_pack/pack_lock.rs` 的 `default_is_pid_alive` unix 分支 `unsafe { libc::kill(pid,0) }`，但 `libc` 仅 `zbrain-worker` 声明、`zbrain-core` 未声明，且 `zbrain-core/Cargo.toml` 有 `unsafe_code = "forbid"`。Windows 上该分支被 `#[cfg(not(unix))]` 剔除从不编译，故**只 Linux 暴露**（CI ubuntu 同理会红）。用户选 `/proc` 方案修复：unix 分支改 `std::path::Path::new(&format!("/proc/{}", pid)).exists()`（无 libc、无 unsafe，契合 forbid）。host 侧 `pack_lock.rs` 已改（working tree，未提交，等指令）。
- 同步到 WSL `/root/zbrain`，重写验证脚本（完整输出、去 tail），后台重跑（task **kiai95**）：`Compiling zbrain-core` 中、无 E0433，修复生效。等后台完成通知，届时汇报 `--lib` + 52 libsql target 在 Linux 绿/红。
- 经验已写入 MEMORY.md（rustup 避坑条后追加 cargo 镜像坑 + zbrain-core Linux 编译坑两条）。临时脚本 `.workbuddy/_tmp_wsl_run2.sh`、日志 `.workbuddy/wsl_libsql.log`（未提交）。

## Linux 验证最终结果（kiai95 RERUN2 + 单独重跑交叉确认）
- `cargo test -p zbrain-core --lib`：**1955 passed / 0 failed ✅**（pack_lock.rs /proc 修复生效，Linux 核心库编译通过）。libsql FFI 在 Linux **不崩溃**（Windows segfault 是 Windows 原生 FFI 特有，CI 用 Linux 正确）。
- 22 个 target FAILED，细分：
  - **94 个 panic 在 `pg_fixture.rs:94` = `PgInitFailure`**（postgres 测试，本环境无 pg-embed）→ 预期失败，正是 rust-tests.yml 应排除的部分。
  - **5 个真实失败（非 postgres，单独跑也失败=真 bug/漂移）**：
    1. `libsql_engine_migrations` ×2：`assert left:23 == right:21` → 测试常量 `EXPECTED_VERSION=21`（注释"through 0021_code_edges"）过期；实际 migration 已到 **0023**（`0001`–`0023` 共 23 个文件，0022_image_search_spend_log / 0023_calibration_tables 后来加）。属测试常量过时，一眼能修。
    2. `libsql_token_queries` ×1：`valid_oauth_token_returns_auth_info` 查到 `None`，期望 `Some("My Agent")` → 真 bug（token 查询/存储路径在 Rust 端口可能漏字段或条件）。
    3. `code_symbol_disambiguation` ×2（inmemory_*）：disambiguation 查到 `[]`（左空）→ 真 bug（in-memory engine 的 disambiguation 实现/测试数据）。
  - 这 5 个失败**均属更早集群债务**（schema-pack / search / code-graph），**与 Part12 1-1-3 extract-takes 正交**（roadmap JSON 无 token/disambiguation/migration 词）。
- **rust-tests.yml 缺陷（实测坐实）**：脚本一次性 `cargo test -p zbrain-core --lib --test T1 ... --test T52`（不是逐 target 循环），且**无 `--skip postgres`** → 会被 94 个 postgres PgInitFailure 弄红，且复现任何跨 target 串扰。须改：逐 target `for t; do cargo test --test "$t" -- --skip postgres; done` + `--lib` 单独跑。
- host 侧 `pack_lock.rs` 的 /proc 修复**仍 working tree 未提交**（验证前置，CI 编译 Linux 必需）。

## grill 决策（zj-grill-me + zj-roadmap-driven，用户问"接下来应该做什么"）
- 路线图加节点 **1-7「验证线打通：rust-tests.yml修复 + pack_lock提交 + 5测试失败清算」**（in_progress, exploit）。
- Q1：Linux 验证暴露 CI 框架缺陷 + 5 真实失败（均非 Part12），与 1-1-3 正交 → 用户选 **全清闸门**：先修 CI（pack_lock 提交 + rust-tests.yml 逐 target+skip postgres）+ 全修 5 失败（含 3 真 bug 根因），CI 全绿后再回 1-1-3。
- Q2：3 真 bug 根因未知可能很深 → 用户选 **设兜底**：若某真 bug 根因是 Rust 端口功能缺失/需新设计（非测试笔误），登记 KNOWN-GAP + 建 1-7 子节点跟踪，闸门其余照常推进并回 1-1-3；pack_lock/CI/常量属一眼能修不在此列。
- 验证方式：CI 仍 dormant（需用户在 GitHub UI 启用），本地 WSL（已装 Rust+rsproxy 镜像+pack_lock 同步）重跑即是代理验证；另可启 CI 让绿 CI 真实生效。

## 闸门 1-7 执行完成（全清，Q2 兜底未触发）
- 5 个失败全部**直接修复**、无一需转缺口，且诊断确认全是移植笔误/cfg-gated 漏编译（非功能缺失）：
  1. `pack_lock.rs`：`/proc/<pid>` std-only 检查（去 libc + unsafe，契合 crate 级 `unsafe_code=forbid`）。
  2. `libsql.rs::verify_access_token`：SELECT 列索引按 JOIN 布局对齐（0=client_id 1=scopes 2=expires_at 3=client_name 4=source_id 5=resource 6=federated_read）——之前 scopes 读 3、client_name 读 4… 整体偏移致 client_name=None。
  3. `engine.rs::InMemoryEngine::disambiguate_symbol`：从遍历 `code_edges_store`（图边）改为 `chunk_store`，按 source 作用域、匹配 `symbol_name`/`symbol_name_qualified`，镜像 libsql 查询（精确 + 无精确时 fuzzy，truncate 25/5）。
  4. `libsql_engine_migrations` 常量 `EXPECTED_VERSION` 21→23。
  5. `rust-tests.yml`：改**逐 target 循环** + `-- --skip postgres`（pg-embed runner 不可用），每步保留 3× flake 重试；避免 94 postgres PgInitFailure 弄红 + 跨 target 串扰。
- **WSL Linux 验证全绿**：`--lib` 1953 passed、`libsql_engine_migrations` 7、`libsql_token_queries` 7、`code_symbol_disambiguation` 4，**0 failed**（gate_verify.log，EXIT=0）。
- 提交分离：代码+CI 修复 = **commit 868eb49**（fix，5 文件）；memory+roadmap = 单独 chore commit。roadmap 1-7 标 completed + 决策落盘。
- 焦点回 **1-1-3 extract-takes**（Part12 facts-extraction 簇下一叶子）。

## 1-1-3 extract-takes 实现完成（未提交，等指令）
- 新建 `crates/zbrain-core/src/autopilot/phases/extract_takes.rs`：port `src/core/cycle/extract-takes.ts` 的 **cycle db 路径**（枚举 `list_all_page_refs` → 每页 `get_page` 取 `compiled_truth+timeline` → `parse_takes_fence` → 逐页 `add_takes_batch` 增量 upsert）。**不 port `fs` 路径**（需磁盘 walkMarkdownFiles，超 cycle 范围）与 `--rebuild`（`BrainEngine` 无 `executeRaw` 逃逸，delete 走不了）。按页 flush（Rust `add_takes_batch(page_id, &[TakeInput])` 单 page 批量，与 TS `addTakesBatch(buffer)` buffer 自带 page_id 跨页不同）。
- 接 `execute_phase`：在 `cycle.rs` 加 `CyclePhase::ExtractTakes` 真实臂（枚举 + ALL + label + scope(Source) + 真实臂 + 清掉 stub `_=>` 里已死的 `ExtractFacts` 匹配）。空脑 → 0 pages → status Ok；有 warning → Warn。
- **关键偏差**：TS `ALL_PHASES`（20 项）**无**独立 `extract-takes` phase（它是 v0_28_0 orchestrator 消费者，非 runCycle phase）。Rust 提为第 21 项（与 ExtractFacts/ExtractAtoms 先例一致，暴露给 run_cycle）。已在 `extract_takes.rs` module doc + roadmap 1-1-3 notes 注明此 taxonomy divergence。
- `run_cycle_empty_brain` 测试：phase 数 20→21，`extract-takes` 断言 Ok（skipped 仍 17）。`all_phases_count_matches_ts` 20→21。
- 验证：Windows 本机 `cargo test -p zbrain-core --lib` 相关 + `autopilot::` 203 passed / 0 failed；2 个 extract_takes 单测（db upsert 2 takes + 空脑）通过。跨引擎：`libsql.rs:3873` / `postgres.rs:2858` 均已实现 `add_takes_batch`，故 cycle 在 libsql/postgres 也通，无功能缺口（不需 KNOWN-GAP）。
- 改动已提交并 push（用户指令 "commit + push"，feat/chore 分离）：
  - **1423330** feat(zbrain-core): implement extract-takes cycle phase（extract_takes.rs + cycle.rs + phases/mod.rs）
  - **ab639fe** chore(part12): mark 1-1-3 in_progress in roadmap & memory
  - **30c5513** chore(part12): flip 1-1-3 → completed（已 push）
  - 远端 `a454d99..30c5513`。roadmap 1-1-3 现为 completed（与 1-1-1/1-1-2 先例一致）。
  - 三个历史临时脚本（`.workbuddy/_tmp_gate_verify.sh` / `_tmp_wsl_run.sh` / `_tmp_wsl_run2.sh`）已于 15:20 经用户指令删除（`rm`），工作区恢复完全干净（无未跟踪）。
- 待用户指令：是否继续 **1-1-4 propose-takes**（需 LLM chat provider，与 ExtractAtoms 同范式）？

## 1-1-4 propose-takes 实现完成（未提交，等指令）
- 新建 `crates/zbrain-core/src/autopilot/phases/propose_takes.rs`：port `src/core/cycle/propose-takes.ts` 的 **cycle db 路径**——枚举 `list_pages(UpdatedDesc, limit)` → 每页取 `compiled_truth+timeline` → `run_propose_takes(engine, chat, opts)`（`chat` 即 extractor，注入 `&dyn ChatProvider`，测试用 `MockChatProvider`，与 extract_atoms 同范式）→ 解析 verdicts（kind 集合 fact/take/bet/hunch）→ 逐页 `add_take_proposal` 幂等写 `take_proposals` 队列表。**不 port `--rebuild`**（无 executeRaw 逃逸）。
- **关键架构决策：typed 方法而非 raw SQL**。TS 用 `engine.executeRaw` 读写 `take_proposals`；Rust `BrainEngine` 设计意图（engine.rs:2505）反对 execute_raw 逃逸，故新增 typed trait 方法 `add_take_proposal` + `take_proposal_exists`（默认 Unsupported，三引擎实现）。InMemory 加 `take_proposals_store` + `next_take_proposal_id` 字段 + `InternalTakeProposal` 结构（镜像 `add_takes_batch` 范式）。
- **真缺口补表**：`take_proposals` 表定义在 `src/schema.sql`（TS 权威），但 Rust migration 0023 注释明示「Rust does not yet have this table」——即 libsql/postgres 运行时缺表。补 **migration 0024（双 dialect：migrations/ + migrations-sqlite/）** 移植全列（含 `wave_version`/`model_id`/`dedup_against_fence_rows` 等），postgres 的 `dedup_against_fence_rows` 用 `CAST($14 AS jsonb)` 写 JSONB 列。`EXPECTED_VERSION` 23→24。
- **踩坑（重要）**：migration 用 `include_str!` 编译期嵌入 + `MigrationRegistry`（libsql `LIBQL_MIGRATIONS` / postgres `POSTGRES_MIGRATIONS`），仅新增 `.sql` 文件不会自动应用——**必须**同时在 `libsql.rs`/`postgres.rs` 加 `const MIGRATION_00NN: &str = include_str!(...)` 并在 registry 加 `version: NN` 注册项，否则 `init_schema` 停在旧版本（首次跑 migration 测试 `left:23==right:24` 失败即此因）。已记入 MEMORY.md 工程铁律。
- 接 `execute_phase`：在 `cycle.rs` 加 `CyclePhase::ProposeTakes` 真实臂（枚举变体已先于本节点存在，本轮补真实臂）；无 chat → `PhaseStatus::Skipped`（与 stub 行为一致，skipped 计数仍 17）。`propose_takes.rs` 单测 4 个（多页各自 1 proposal / 空脑 0 / parser 变体 / 已存在幂等）。
- **偏差**：TS `parseExtractorOutput` kind 集合为 `fact/take/bet/hunch`，但 TS PROMPT 写 `prediction|judgment|bet`（自身不一致）；忠实移植 parser，prediction 归一化入 take（我的测试初版误断言 kind=="prediction" 已修正）。
- 验证：Windows 本机 `cargo test -p zbrain-core --lib autopilot:: -- --skip postgres` **207 passed / 0 failed**；`libsql_engine_migrations` **7 passed**（含 0024 应用到 v24，idempotent 二次跑 0 应用）。改动未提交（铁律）。
- 改动文件：`M cycle.rs / phases/mod.rs / engine.rs / libsql.rs / postgres.rs / libsql_engine_migrations.rs / roadmap json+md`，`?? propose_takes.rs + migrations/0024 + migrations-sqlite/0024`。roadmap 1-1-4 标 `in_progress`（实现完成待 push）。
- 待用户指令：commit+push？还是继续 **1-1-5 grade-takes**（消费 take_proposals 表，需 chat provider + 解析 grade 反馈）？

## 1-1-4 已 commit + push（用户指令 "commit + push"）
- feat: `d9162a4` —— propose_takes.rs + engine/libsql/postgres 三引擎 typed 方法 + cycle.rs 真实臂 + phases/mod.rs + migration 0024 双 dialect + EXPECTED_VERSION 23→24（9 files, +955/-4）。
- chore: `687a4d9` —— roadmap json/md 1-1-4→completed（render 同步 ⚠️ 段与顶部纯段）+ 今日 memory + MEMORY.md migration 铁律。
- push：`90af814..687a4d9` → origin/rust-rewrite。工作区干净。
- 下一步：1-1-5 grade-takes（消费 take_proposals 表，需 chat provider + 解析 grade 反馈）或继续 1-1 簇后续。

## 1-1-5 grade-takes 实现完成（未提交，等指令）
- 新建 `crates/zbrain-core/src/autopilot/phases/grade_takes.rs`：port `src/core/cycle/grade-takes.ts` 核心循环——`list_takes(resolved=false,active=true)` 按 `since_date` 升序 → 逐 take `take_is_old_enough`(默认 6 月, 无 since_date 计 too_recent) → 默认 evidence retriever 占位(取 claim) → `evidence_signature`=SHA-256(judge_id|evidence) → `take_grade_cache_exists` 幂等 → chat judge 解析 verdict(correct/incorrect/partial/unresolvable, 容错围栏包裹) → `add_take_grade_cache` 写缓存(D17 auto_resolve 默认 false, 仅 high-confidence 经 `resolve_take` 写回 canonical takes)。纯函数 `aggregate_ensemble` 实现 T5 三模型陪审团(3/3 一致 + min conf>=0.85 才 auto-apply)。**未移植**: gstack-learnings 外部耦合(默认关闭, 非 zbrain-core 范围)。
- **typed 方法非 raw SQL**：engine trait 加 `add_take_grade_cache` + `take_grade_cache_exists`(默认 Unsupported，三引擎实现：InMemory 加 `take_grade_cache_store` 字段 + `InternalTakeGradeCache` 结构；libsql/postgres 走 `take_grade_cache` 表，PK 复合冲突安全)。`take_grade_cache` 表已在 migration 0023 存在 → **无需新迁移 / 不 bump EXPECTED_VERSION**。
- 接 `execute_phase`：cycle.rs 加 `CyclePhase::GradeTakes` 真实臂(无 chat → Skipped)。编译 + 单测全绿(Windows `cargo test -p zbrain-core --lib autopilot:: -- --skip postgres` → **217 passed**, 含本节点 10 测试；完整 lib test 编译通过含 libsql/postgres 路径)。
- 改动文件：`M engine.rs / libsql.rs / postgres.rs / cycle.rs / phases/mod.rs / roadmap json+md / MEMORY.md`，`?? grade_takes.rs`。roadmap 1-1-5 标 `in_progress`（实现完成待 push）。
- 待用户指令：commit+push？还是继续 **1-1-6 conversation-facts-backfill**？

## 1-1-5 已 commit + push（用户指令 "commit + push"）
- feat: `2566c75` —— grade_takes.rs + engine/libsql/postgres 三引擎 typed 方法 add_take_grade_cache/take_grade_cache_exists + cycle.rs GradeTakes 真实臂 + phases/mod.rs（6 files, +1229/-2）。
- chore: `dff29e4` —— roadmap json/md 1-1-5→completed（render 同步）+ 今日 memory + MEMORY.md 进度行。
- push：`687a4d9..dff29e4` → origin/rust-rewrite。工作区干净。
- 下一步：1-1-6 conversation-facts-backfill（1-1 facts-extraction 簇最后一个叶子）。

## 1-1-6 conversation-facts-backfill 实现完成（待 push）
- 新建 `autopilot/phases/conversation_facts_backfill.rs`（忠实移植 TS `conversation-facts-backfill.ts` + `extract-conversation-facts.ts` core）：消息 parse/分段/渲染、checkpoint 编解码(fingerprint=sha8)、`extract_facts_from_turn`(LLM 抽取 4 策略容错)、`process_page`(body cap→分段→逐段抽取→budget 超预算即中止不写刚抽 facts→`insert_fact` 带 row_num+source_markdown_slug→终态 EXTRACTION_COMPLETE 审计行→checkpoint 追加)、`run_extract_conversation_facts_core`(brain-wide/local BudgetTracker，按 type 分页续传)、`run_phase_conversation_facts_backfill`(enabled 默认 false→Skipped)。6 测试全绿。
- 补 Rust 缺口：migration 0025 双 dialect 建 `op_checkpoints` 表；engine.rs 4 trait 默认方法 + InMemory 字段/实现；libsql.rs/postgres.rs `MIGRATION_0025`+registry+4 impl；`EXPECTED_VERSION` 24→25。
- cycle.rs 接 `CyclePhase::ConversationFactsBackfill` 真实臂（无 chat→Skipped；有 chat 传默认 enabled:false→Skipped，同 TS 默认）。
- 验证：autopilot 223 pass（含 run_cycle_empty_brain skipped 计数断言）；libsql_engine_migrations 7 pass 确认升 v25。
- roadmap json 1-1-6→in_progress；MEMORY.md 进度行更新。改动未提交（铁律等用户 commit+push）。

## 1-1-6 已 commit + push（用户指令 "commit + push"）
- feat: `ee106d7` —— conversation_facts_backfill.rs + migration 0025 双 dialect + engine/libsql/postgres 三引擎 4 个 trait 方法 + cycle.rs ConversationFactsBackfill 真实臂 + phases/mod.rs + libsql_engine_migrations EXPECTED_VERSION 24→25（9 files, +1538/-2）。
- chore: `38ca1df` —— roadmap json 1-1-6→in_progress + 今日 memory + MEMORY.md 进度行。
- chore: `9fa4412` —— roadmap json 1-1-6→completed + push 记录。
- push：`dff29e4..9fa4412` → origin/rust-rewrite。工作区干净。
- 1-1 facts-extraction 簇（extract-facts/atoms/takes/propose-takes/grade-takes/conversation-facts-backfill）全部 push 完成。下一步：回到 part12 主迁移剩余节点（1-2 … 等）。

## 1-2 emotional-calibration 簇迁完（working tree，待 commit+push）
- 4 子节点全部实现（继承前置 1-2 勘察结论：calibration_profile 函数逻辑已在 Part11 迁好，1-2-4 只接线不重写）：
  - **1-2-1** 新建 `autopilot/phases/emotional_weight.rs`：port `emotional-weight.ts` `computeEmotionalWeight` 纯函数（`HIGH_EMOTION_TAGS` 15 项、`DEFAULT_USER_HOLDER="garry"`、tagBoost 0.5 / takeDensity min0.3 / takeAvgWeight*0.1 / userHolderRatio*0.1，clamp01）；复用 engine `EmotionalWeightTake`。9 测试全绿。
  - **1-2-2** engine 加 trait 必需方法 `batch_load_emotional_inputs` + `set_emotional_weight_batch`（trait+InMemory+libsql+postgres 三引擎；`set_emotional_weight_batch` 只写 `emotional_weight`+`salience_touched_at`，**不写** `emotional_weight_recomputed_at` 归 1-6；`get_config` 走 opts override 对齐 1-1-6）。
  - **1-2-3** 新建 `autopilot/phases/recompute_emotional_weight.rs`：`run_phase_recompute_emotional_weight`（batch_load→compute→可选 dry_run→set_emotional_weight_batch）+ cycle.rs `RecomputeEmotionalWeight` 真实臂（确定性、无 LLM，无论 chat 与否都跑）。5 测试全绿。
  - **1-2-4** cycle.rs `CalibrationProfile` 真实臂：无 chat → Skipped（同 extract-atoms 范式）；有 chat 注入 `Arc<dyn ChatProvider>` 调 `run_calibration_profile`，映射 `CalibrationProfileStatus`→`PhaseStatus`。**关键修正**：calibration 原已归 catch-all Skipped，接真实臂后仍 Skipped → `run_cycle_empty_brain` 的 skipped 计数**保持 16**（不是 16→15，前置摘要预测有误），仅加 calibration 状态/summary 断言。
- **验证**：Windows 本机 `cargo test -p zbrain-core --lib autopilot::` **239 passed / 0 failed**（autopilot 全量回归）；calibration_profile 既有单测仍绿。libsql/postgres 三引擎实现编译通过。
- **改动文件**：`?? emotional_weight.rs / recompute_emotional_weight.rs`、`M engine.rs / libsql.rs / postgres.rs / cycle.rs / autopilot/phases/mod.rs / roadmap json+md / MEMORY.md`。roadmap 1-2 + 4 子节点均已标 completed（render 已同步 ⚠️ 段；**手动**同步了顶部孤儿纯段 1-2 展开 + 1-1-3..6 标完成）。
- **铁律**：改动未提交，等用户显式 "commit + push"（feat 含 1-2-1/2/3/4 实现+接线；chore 含 roadmap/memory）。
- 下一步：**1-3 synthesis 簇**（synthesize/synthesize-concepts/patterns/schema-suggest）。
