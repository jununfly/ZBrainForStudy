# 2026-08-08

## Part13 启动：minion handler 接线（Part12 收口后的「cycle 后续」）

用户意图「转入 Part12（cycle 大迁移）」经 zj-roadmap-driven + zj-grill-me 纪律核查，发现 Part12 (`zbrain-ts-to-rust-part12-cycle-migration.json`) 已全 48/48 completed（autopilot 20 phase + `cycle.rs` 48 个 `CyclePhase` 臂全覆盖 + CLI `zbrain dream` 接 `run_cycle` + TS `dream.ts`/`cycle.ts` 已删 + 验证线 CI 绿）。故「转入」与「已关闭」冲突 → 重定向为「补 minion handler 桩」= 新建 **Part13** 路线图跟踪。

**19 个 `not_implemented` 桩**分 4 类（不在 Part12 范围，需新建 Part13）：
- 1-1 cycle-phase（7）：autopilot_cycle/consolidate/extract_facts/recompute_emotional_weight/resolve_symbol_edges/synthesize/extract_conversation_facts → 经 `run_cycle`
- 1-2 现有 verb（3）：integrity→Integrity verb / reindex→Reindex verb / sync→Sync verb
- 1-3 无 verb（6）：lint/lint_fix/extract/integrity_auto/sync_retry_failed/repair_jsonb（命令未迁或死技术，需处置决策）
- 1-4 LLM·embedding-seam（3）：embed/embed_backfill/contextual_reindex（需 infra 注入，阻塞）

路线图 `docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json` 经 `init`+`add`×23+`decide`+`render` 构建（CLI 在 `.workbuddy/skills/zj-roadmap-driven/roadmap_cli.py`，**非** `docs/plans/`）。

## Part13 1-1 簇收口（7 handler 接 run_cycle）

用户 grill 选「1-1 cycle-phase 整簇（推荐）」先攻。重写 7 文件（`crates/zbrain-core/src/minions/handlers/`）：
- 统一模式：从 `ctx.data` 读 `dir`/`dry_run`/`pull`/`source_id`（fallback `brain_dir`），构造 `CycleOpts`，调 `run_cycle(engine.as_ref(), &opts)`，返回 `CycleReport` 序列化 JSON。
- `autopilot_cycle`：`phases:None`（完整 cycle），测试 `json!({"dry_run":true})` 防重副作用。
- 其余 6 个：`phases:Some(vec![特定 CyclePhase])`（Consolidate/ExtractFacts/RecomputeEmotionalWeight/ResolveSymbolEdges/Synthesize/ConversationFactsBackfill）。
- `extract_conversation_facts` 注释：LLM 依赖子步骤在无 ChatProvider 时按 run_cycle 既有行为 Skipped，与 CLI dream 一致。

**坑**：`crate::Result`=`Result<T,StructuredError>`（非 anyhow），`serde_json::Error` 不能经 `?` 直转 → 7 文件初 build 报 `E0277`，逐文件改 `serde_json::to_value(&report).map_err(|e| crate::Error::new("SerializationError","cycle_report",&e.to_string()))?` 修复后绿。

**验证**：`CARGO_TARGET_DIR=C:/Users/victo/AppData/Local/Temp/zb_target_cycle`（非 `/c/`），从 `target/debug/{deps,.fingerprint,build,incremental}` 经 `robocopy ... /XF *.lock` 复制缓存绕监视器锁；`cargo build -p zbrain-core` 绿、`cargo test -p zbrain-core --lib minions::handlers` 7 新测试全 ok。
**caveat**：同批报 `52 passed; 1 failed`，失败的是无关模块（operation::tests::dispatch_json_think_*、search::dedup/fusion/intent、skillify、think::* 约 14 个），本分支预存负债（未碰相关文件），**待 baseline checkout 证实**。

路线图：1-1-1..1-1-7 + 1-1 标 completed、记 1-1 决策、`render`。stats：24 节点 / pending 15 / in_progress 1 / completed 8 / blocked 0 / decisions 2。

**下一步（未做，待 grill）**：① 落盘 7 handler + 2 Part13 路线图文件 + memory；② 1-2 现有 verb 簇（integrity/reindex/sync）或先决策 1-3/1-4 阻塞项。

## Part13 1-2 簇部分收口（integrity + sync，reindex 推迟）

用户 grill 选「integrity + sync」（1-2 中真正机械的两桩）。落盘：commit `906d2762`(memory) + `9625c76f`(1-1)，push `b6b4c5db..9625c76f`。

重写 2 handler：
- `integrity.rs` → `zbrain_core::integrity::scan_integrity(&dyn BrainEngine, &IntegrityScanOptions)`（读 `ctx.data` dir/source_id 构造 opts），返回 `IntegrityScanResult` 序列化 JSON。
- `sync.rs` → 构造 `OperationRegistry` + `register_all`，`OperationContext::local_cli().with_engine(engine).with_source_id(id)`，`registry.dispatch_json("sync_brain", &ctx, params)`（返回 `OperationResult<Value>` → `map_err` 到 `crate::Error`）。

**关键发现（inspection）**：1-2 非均匀机械。`reindex` 的 CLI 逻辑（`run_reindex_pages`）完全在 `zbrain-cli`，且需 `EmbeddingClient::from_env()`（embedding seam）→ 实际属 1-4，故 1-2 只接 integrity+sync，`reindex`(1-2-2) 保持 pending 推迟到 embedding-seam 组，不半接线。
`SyncBrainOperation` 是私有 struct，只能经 registry 名派发（`registry.dispatch_json`）；`OperationContext::local_cli()` 给最小本地 ctx。

**验证**：`cargo build -p zbrain-core` 绿；`cargo test -p zbrain-core --lib minions::handlers` **53 passed / 0 failed**（新增 `integrity_smoke`、`sync_handler_dispatches_to_sync_brain`）。
路线图：1-2-1 + 1-2-3 标 completed、记 1-2 决策（reindex 推迟到 1-4）。`render` 后 md 显示两桩 `[x]`。stats：completed 10 / decisions 3。

落盘：commit `83ff40f9`(1-2) + push `9625c76f..83ff40f9`。

**下一步（待 grill）**：剩余 1-3（无 verb 6 桩：lint/lint_fix/extract/integrity_auto/sync_retry_failed/repair_jsonb，多属死技术/无 Rust 替代，需 wontfix-vs-新 core op 决策）与 1-4（embedding/LLM-seam 3 桩 embed/embed_backfill/contextual_reindex + 已推迟的 reindex，阻塞于 EmbeddingClient/ChatProvider infra）。

## Part13 1-3 triage + 1-4 embedding 接线（用户选「both tracks now」）

用户 grill 选「both tracks now」→ 让全部 19 桩到达终态（接好 or 显式 wontfix）。

**1-3 死技术 6 桩 → 显式 wontfix（roadmap 状态 `blocked`，无 `wontfix` 状态枚举）**：
- 新增 `handlers/util.rs::unsupported_job(name, gap_id)` 共享 helper；6 文件（lint/lint_fix/extract/integrity_auto/sync_retry_failed/repair_jsonb）从 `Ok(json!({"status":"not_implemented"}))` 改为 `Err(unsupported_job(...))`，指向 KNOWN-GAPS。
- 重叠已有 id：extract→G76、repair_jsonb→G78（不新建，避免 SSOT 重复）。新建 G80(lint)/G81(lint_fix)/G82(integrity_auto)/G83(sync_retry_failed)；contextual_reindex LLM 推迟→G84。

**1-4 embedding-seam + 推迟的 reindex → 接好（feature-gated behind `embedding`）**：
- 关键坑：`EmbeddingClient::from_env()` 仅在 `embedding` feature 下存在（zbrain-core 默认无此 feature，仅 zbrain-cli 启用）。handler 内全部 embedding 逻辑用 `#[cfg(feature="embedding")]`，否则返回 `Unsupported` 错误 → 默认 feature 也能编译+测试。
- 新增 `handlers/embed_util.rs::embed_pages(engine, client, pages, dry_run)`（list→embed_batch→put_page_embedding，little-endian BLOB 编码 `encode_embedding_le`）。
- `reindex`：list_pages（含 source_id/dry_run/batch 循环）→ embed_pages（等价于 CLI `run_reindex_pages`，把推迟的 1-2-2 接好）。
- `embed`/`embed_backfill`：list_stale_pages（NULL-embedding 页）→ embed_pages。
- `contextual_reindex`：embed 部分接好；per-chunk LLM 上下文合成推迟（需把 ChatProvider 注入 minion handler ctx，当前 worker 只给 subagent 注入）→ 返回 `llm_context_augmentation:"deferred"`，记 G84。

**验证**：`cargo build -p zbrain-core`（默认 feature）绿；`cargo check -p zbrain-core --features embedding` 绿（确认 gated 块类型正确）；`cargo test -p zbrain-core --lib minions::handlers` **53 pass / 0 fail**。默认 feature 下 embedding handler 测试断言 `is_err()`（无 provider/无 feature → 明确错误），对生产（LibsqlEngine+provider）路径靠结构正确性+feature 编译保证。

路线图：`render` 后 1-1/1-2/1-4 全 `[x]` completed，1-3 六叶 `[!]` blocked，根 in_progress。stats：16 completed / 6 blocked / 6 decisions。落盘 commit `05f2f9ce` + push `a5fa5b52..05f2f9ce`。

**Part13 已全收口**：19 个 `not_implemented` 桩全部终态（12 接好 run_cycle/verb/embedding + 6 显式 wontfix + 1 contextual_reindex 半接 LLM 推迟）。后续仅剩 G84（ChatProvider 注入 seam）为开放缺口。

## G84 收口：contextual_reindex 接 LLM 上下文增强（用户「开始 G84」）

把 `ChatProvider` 经 **registry DI seam** 接入（与 `subagent` 同一机制，非 `MinionJobContext` 字段）：`register_builtin_handlers` 已收 `chat_provider: Arc<dyn ChatProvider>`，原 line 127 把它 move 给 subagent，现改为 `Arc::clone(&chat_provider)` 后同时注册 `ContextualReindexHandler::new(Arc::clone(&chat_provider))`。

- `ContextualReindexHandler` 新增 `chat_provider` 字段 + `new(chat_provider)`。
- 新增 `synthesize_page_context(chat, page, model)`：构造 prompt（含 page.title/compiled_truth）→ `chat.chat(ChatOpts{model, system, messages:[User], tools:[], max_tokens:None, cache_system:false})` → 返回 `result.text` 截断。
- `handle`：在 `#[cfg(feature="embedding")]` 块内，对每页调用 LLM 合成上下文，嵌 `context + compiled_truth`；**LLM 失败按页回退裸 `compiled_truth`**（`llm_failures++`，report 标 `partial`，绝不中断整批）。
- 新增 `embed_util::embed_pages_augmented(engine, client, items: Vec<(slug,source_id,text)>, dry_run)`，复用 `put_page_embedding`，与 `embed_pages` 平行但吃调用方提供的文本。
- `model` 经 `ctx.data["model"]` 可覆盖，默认 `None`（provider 默认）。
- KNOWN-GAPS G84：`open`(blocked) → **resolved (2026-08-08)**，更新描述指向现载体 `contextual_reindex.rs`。Part13 1-4 追加 decision 记录 G84 实现。

**验证**：`cargo build -p zbrain-core`（默认）绿；`cargo check -p zbrain-core --features embedding` 绿（gated chat+embed 块类型正确）；`cargo test -p zbrain-core --lib minions::handlers` **54 passed / 0 failed**（新增 `contextual_reindex_fails_without_embedding_feature` + `synthesize_page_context_calls_llm_with_page_content`）；`cargo build -p zbrain-cli` 绿（下游 register_builtin_handlers 消费者未破）。
**坑**：① `Arc<dyn ChatProvider>` 在 subagent 注册处被 move，需在 line 127 先 `Arc::clone`；② `PageType` 是 `pub type = String` 别名（非 enum），测试构造 `Page` 不能用 `PageType::Note`，改用 `..Default::default()`；③ `PageKind`/`PageType` 在 `crate::engine` 是私有再导出，测试须从 `crate::types` 直接导入。

## G73 — search eval IR 指标（"先删后补"缺口）已收口
- 取回已删 TS 原文 `git show bcafcafd^:src/core/search/eval.ts` 对账。
- 新增 `crates/zbrain-core/src/search/eval.rs`：4 纯指标 `precision_at_k`/`recall_at_k`/`mrr`/`ndcg_at_k` + 类型 `EvalQrel`/`EvalQrelFile`/`EvalConfig`(`EvalStrategy`)/`QueryResult`/`EvalReport` + `parse_qrels`（内联 JSON 或文件路径，支持 array 与 `{version,queries}` 两形式）+ `run_eval` 编排 + `keyword_search_slugs` 便利函数。`search/mod.rs` 加 `pub mod eval` + 导出。
- **关键设计**：`run_eval` 用**泛型异步 query 闭包** `F: Fn(&str) -> Fut` 注入检索策略（keyword/vector/hybrid 由调用方决定）。原因：Rust `search_pages` 单一入口靠 `query_embedding: Option` 选 keyword/hybrid，而 `EmbeddingClient` 仅在 `embedding` feature 存在 → 若把 embedding 塞进 run_eval 会引入 feature-gate。闭包注入使 eval 模块**零 embedding 依赖、无 feature-gate、可纯单测**。测试用 `Arc<InMemoryEngine>` + 闭包内 `to_string()` 持有数据，使 future 为 `'static` 以匹配 `F: Fn(&str)->Fut`（泛型闭包不能把返回 future 生命周期绑定到 `&str` 形参，HRTB+`BoxFuture` 实测不收敛，最终用「闭包持有数据」绕过）。
- **验证**：`cargo test -p zbrain-core --lib search::eval` **13 passed / 0 failed**（含真实 InMemoryEngine keyword 集成 + parse_qrels 文件往返 + 4 指标 golden case，如 nDCG 非理想排序 0.7968、P@5=2/5、R@5=1.0）。默认 feature 下 `cargo build -p zbrain-core` 绿（gated 块无）。
- **注意**：全仓 `cargo test -p zbrain-core --lib` 现 2307 passed / 13 failed，但 13 失败分布在 `operation.rs`/`search::dedup|fusion|intent`/`think::*` 等**未改动模块**，且**隔离运行同样失败** → 系本分支既有失败（之前仅跑 `minions::handlers` 过滤未发现），与本次纯新增 eval 模块无关。
- KNOWN-GAPS G73：`open` → **resolved (2026-08-08)**；MEMORY.md 1-7 bullet 同步（G73 resolved，剩 G72 open）。

## G1 — Think/evidence 检索丢失 rerank（核心病根：分层错位）已收口
- 推荐路径落地：`crates/zbrain-core/src/rerank_client.rs` 新增未 feature-gate 的 `pub fn rerank_document_of(&SearchResult) -> String`（snippet → compiled_truth → title 投影）+ `pub fn rerank_stamp(&mut SearchResult, f64, i64)`（stamp `rerank_score`/`reranker_delta`）+ `pub async fn rerank_results(query, Option<&RerankSettings>, Vec<SearchResult>) -> Vec<SearchResult>`（核心 primitive）+ `pub async fn retrieve_and_rerank<F,Fut>(query, settings, retrieve) -> Result<Vec<SearchResult>, Error> where F: FnOnce()->Fut`（HRTB-safe 闭包 wrapper，规避 G73 教训）。
- `operation.rs` `QueryOperation::execute` 内联 rerank 段（2028–2071）替换为 `rerank_results(&query_text, ctx.rerank.as_ref(), results).await`，**行为不变**（同 doc_of/stamp/enabled/audit_dir/model）。删除 "Think/evidence internal retrieval intentionally does NOT rerank" 注释（本刀推翻）。
- `think/synthesize.rs` `ThinkSynthesizeOpts` 新增 `pub rerank: Option<RerankSettings>`，从 `operation.rs ThinkOperation::execute` 透传 `ctx.rerank.clone()`。
- `think/gather.rs` `ThinkGatherOpts` 新增 `pub rerank: Option<RerankSettings>`（手写 `Default` impl 因为 `RerankSettings` 非 `Default`），`run_gather` Stream1 改 `match hybrid_search(...).await { Ok(results) => rerank_results(&opts.question, opts.rerank.as_ref(), results).await, Err(e) => { eprintln; Vec::new() } }`（保留 fail-open 语义 + 借 `match` 解决 `&opts.question` borrow 冲突）。
- **关键设计**：G73 教训复用——`retrieve_and_rerank` 闭包无 `&str` 形参（`F: FnOnce() -> Fut`），闭包体内捕获 `engine_ref` + 自有 opts，future 为 `'static` 避开 HRTB 纠缠。实际写入用的是 `match` 早解 hybrid_search 错误的 `rerank_results` primitive（更短、更显式、不需闭包）。
- **坑（已踩）**：
  1. `cargo check` 第一次 E0505（`&opts.question` + 闭包内 `&question` 双借），改 `match` 早解 OK。
  2. **5 个 G1 unit test 第一次未出现在 `--list`**——根因：`rerank_client.rs` 顶部 `mod tests` 闭括号被 `#[cfg(all(test, feature = "rerank"))] mod ze_wire_tests { ... }` 紧接，我 Edit 的 `old_string` 末尾 `}` 误把 G1 块塞进了 `ze_wire_tests`（feature-gated），默认 build 编译不到。**修复**：`git checkout HEAD -- rerank_client.rs` 重来，把 G1 块改插到 `mod tests` 闭括号前（`#[cfg(test)]` 块，line 823）。这是「同一文件两个测试模块」的 grep 盲点教训。
  3. 新测试用 `Arc` 但 `mod tests` 顶部 `use` 只导 `Mutex`/`TempDir`，加 `use std::sync::Arc;`。
  4. `Page` 26 字段，用 `Page::default()` + override 3 字段构造 test stub（无需逐字段填）。
  5. build 路径仍是 `CARGO_TARGET_DIR=C:/Users/...` + robocopy 缓存。
- **验证**：`cargo test -p zbrain-core --lib rerank_client::` **19 passed / 0 failed**（14 旧 + 5 新：rerank_results_none_is_passthrough / rerank_results_some_stamps_score_and_delta / rerank_results_some_with_only_title_falls_back / retrieve_and_rerank_runs_closure_then_reranks / retrieve_and_rerank_propagates_retrieve_error）；`cargo test -p zbrain-core --lib query_operation_tests` **16/16 绿**（含 `query_reranker_*`）；`cargo test -p zbrain-core --lib think::synthesize` **8/8 绿**（含 `run_think_uses_injected_chat_and_resolves_citations`）；`cargo test -p zbrain-core --lib think::gather::` **5/6 绿**（`anchor_triggers_graph_stream_without_panic` pre-existing 失败：graph stream 代码（gather.rs 182-203）byte-identical to HEAD，`InMemoryEngine.traverse_paths` 对不存在的 slug 返回 `Ok([])`，`run_gather` 在 `Ok` 分支无条件 `slugs.insert(anchor.clone())` 致 `graph_slugs` 非空，与 G1 改动无关）；`cargo test -p zbrain-core --lib search::engine` 2/2 绿；`cargo build -p zbrain-cli` 绿。
- KNOWN-GAPS G1：`open` → **resolved (2026-08-08)**，主表行折叠入「详情已删」+ 留索引指针；G1 详情段（含分层错位病根 + 否决 engine 下沉/operation 复制论证）折叠为「已 resolved 索引」一行保历史可追溯。
- `git status`：4 modified files (operation.rs / rerank_client.rs / think/gather.rs / think/synthesize.rs) + KNOWN-GAPS.md + memory log。
- **`.git/HEAD` "corruption"实为 missing tip（已修复）**：`git status`/`git diff HEAD` 报 `fatal: bad object HEAD`，`git fsck --no-reflogs` 报 28 missing blobs / 2 missing trees / 18 dangling commits。诊断后定位：local `refs/heads/rust-rewrite` 与 `refs/remotes/origin/rust-rewrite` 都指向 `0015ad182f9cb8fa0461cd500ab58e30643b419f`（即 `feat(search): port IR eval harness (G73) — metrics + parse_qrels + run_eval`），但 local object store 缺这个 commit 及其 28 个 blob + 2 个 tree。`git fetch origin` 一次性把这些 object 拉下来（exit 0 静默），`git fsck` missing 数从 30 降到 0，HEAD 变 valid，HEAD/branch 一致 (`Your branch is up to date with 'origin/rust-rewrite'`)。**根因不是磁盘/object store 损坏，是 sandbox 重启后本地没 fetch 最新 tip**——误判为「infra issue」多绕半天。教训：ts-anomaly-class 失误前先 `git cat-file -t <HEAD-sha>` 探类型 + `git fetch origin` 试恢复，**不要**直接套 `git hash-object -w` 重建。
- 落盘 commit `3e31805a` + push `0015ad18..3e31805a` 成功。
