# 2026-08-08

## Part13 启动：minion handler 接线（Part12 收口后的「cycle 后续」）

用户意图「转入 Part12（cycle 大迁移）」经 zj-roadmap-driven + zj-grill-me 纪律核查，发现 Part12 (`zbrain-ts-to-rust-part12-cycle-migration.json`) 已全 48/48 completed（autopilot 20 phase + `cycle.rs` 48 个 `CyclePhase` 臂全覆盖 + CLI `zbrain dream` 接 `run_cycle` + TS `dream.ts`/`cycle.ts` 已删 + 验证线 CI 绿）。故「转入」与「已关闭」冲突 → 重定向为「补 minion handler 桩」= 新建 **Part13** 路线图跟踪。

**19 个 `not_implemented` 桩**分 4 类（不在 Part12 范围，需新建 Part13）：
- 1-1 cycle-phase（7）：autopilot_cycle/consolidate/extract_facts/recompute_emotional_weight/resolve_symbol_edges/synthesize/extract_conversation_facts → 经 `run_cycle`
- 1-2 现有 verb（3）：integrity→Integrity verb / reindex→Reindex verb / sync→Sync verb
- 1-3 无 verb（6）：lint/lint_fix/extract/integrity_auto/sync_retry_failed/repair_jsonb（命令未迁或死技术，需处置决策）
- 1-4 LLM·embedding-seam（3）：embed/embed_backfill/contextual_reindex（需 infra 注入，阻塞）

路线图 `docs/plans/zbrain-ts-to-rust-part13-minion-handler-wiring.json` 经 `init`+`add`×23+`decide`+`render` 构建（CLI 在 `.workbuddy/skills/zj-roadmap-driven/roadmap_cli.py`，**非** `docs/plans/`）。

## Part13 1-1 簇收口（7 handler 接 run_cycle）

用户 grill 选「1-1 cycle-phase 整簇（推荐）」先攻。重写 7 文件（`crates/zbrain-core/src/minions/handlers/`）：
- 统一模式：从 `ctx.data` 读 `dir`/`dry_run`/`pull`/`source_id`（fallback `brain_dir`），构造 `CycleOpts`，调 `run_cycle(engine.as_ref(), &opts)`，返回 `CycleReport` 序列化 JSON。
- `autopilot_cycle`：`phases:None`（完整 cycle），测试 `json!({"dry_run":true})` 防重副作用。
- 其余 6 个：`phases:Some(vec![特定 CyclePhase])`（Consolidate/ExtractFacts/RecomputeEmotionalWeight/ResolveSymbolEdges/Synthesize/ConversationFactsBackfill）。
- `extract_conversation_facts` 注释：LLM 依赖子步骤在无 ChatProvider 时按 run_cycle 既有行为 Skipped，与 CLI dream 一致。

**坑**：`crate::Result`=`Result<T,StructuredError>`（非 anyhow），`serde_json::Error` 不能经 `?` 直转 → 7 文件初 build 报 `E0277`，逐文件改 `serde_json::to_value(&report).map_err(|e| crate::Error::new("SerializationError","cycle_report",&e.to_string()))?` 修复后绿。

**验证**：`CARGO_TARGET_DIR=C:/Users/victo/AppData/Local/Temp/zb_target_cycle`（非 `/c/`），从 `target/debug/{deps,.fingerprint,build,incremental}` 经 `robocopy ... /XF *.lock` 复制缓存绕监视器锁；`cargo build -p zbrain-core` 绿、`cargo test -p zbrain-core --lib minions::handlers` 7 新测试全 ok。
**caveat**：同批报 `52 passed; 1 failed`，失败的是无关模块（operation::tests::dispatch_json_think_*、search::dedup/fusion/intent、skillify、think::* 约 14 个），本分支预存负债（未碰相关文件），**待 baseline checkout 证实**。

路线图：1-1-1..1-1-7 + 1-1 标 completed、记 1-1 决策、`render`。stats：24 节点 / pending 15 / in_progress 1 / completed 8 / blocked 0 / decisions 2。

**下一步（未做，待 grill）**：① 落盘 7 handler + 2 Part13 路线图文件 + memory；② 1-2 现有 verb 簇（integrity/reindex/sync）或先决策 1-3/1-4 阻塞项。

## Part13 1-2 簇部分收口（integrity + sync，reindex 推迟）

用户 grill 选「integrity + sync」（1-2 中真正机械的两桩）。落盘：commit `906d2762`(memory) + `9625c76f`(1-1)，push `b6b4c5db..9625c76f`。

重写 2 handler：
- `integrity.rs` → `zbrain_core::integrity::scan_integrity(&dyn BrainEngine, &IntegrityScanOptions)`（读 `ctx.data` dir/source_id 构造 opts），返回 `IntegrityScanResult` 序列化 JSON。
- `sync.rs` → 构造 `OperationRegistry` + `register_all`，`OperationContext::local_cli().with_engine(engine).with_source_id(id)`，`registry.dispatch_json("sync_brain", &ctx, params)`（返回 `OperationResult<Value>` → `map_err` 到 `crate::Error`）。

**关键发现（inspection）**：1-2 非均匀机械。`reindex` 的 CLI 逻辑（`run_reindex_pages`）完全在 `zbrain-cli`，且需 `EmbeddingClient::from_env()`（embedding seam）→ 实际属 1-4，故 1-2 只接 integrity+sync，`reindex`(1-2-2) 保持 pending 推迟到 embedding-seam 组，不半接线。
`SyncBrainOperation` 是私有 struct，只能经 registry 名派发（`registry.dispatch_json`）；`OperationContext::local_cli()` 给最小本地 ctx。

**验证**：`cargo build -p zbrain-core` 绿；`cargo test -p zbrain-core --lib minions::handlers` **53 passed / 0 failed**（新增 `integrity_smoke`、`sync_handler_dispatches_to_sync_brain`）。
路线图：1-2-1 + 1-2-3 标 completed、记 1-2 决策（reindex 推迟到 1-4）。`render` 后 md 显示两桩 `[x]`。stats：completed 10 / decisions 3。

落盘：commit `83ff40f9`(1-2) + push `9625c76f..83ff40f9`。

**下一步（待 grill）**：剩余 1-3（无 verb 6 桩：lint/lint_fix/extract/integrity_auto/sync_retry_failed/repair_jsonb，多属死技术/无 Rust 替代，需 wontfix-vs-新 core op 决策）与 1-4（embedding/LLM-seam 3 桩 embed/embed_backfill/contextual_reindex + 已推迟的 reindex，阻塞于 EmbeddingClient/ChatProvider infra）。
