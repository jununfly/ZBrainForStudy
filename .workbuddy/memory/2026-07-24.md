# 2026-07-24 工作记录

## 完成内容

### 1-13-1-4 (Phase C 前置-3: book-mirror 迁移) 全完成
- G56 已落地：`zbrain-core/src/minions/handlers/subagent.rs` 扩展支持从 job data 读取 `allowed_tools`/`max_turns`/`model`，满足 book-mirror 只读契约 `allowed_tools: ["get_page", "search"]`（CODEX HIGH-1 安全修复）。4 个 G56 测试全绿。
- 新增 `crates/zbrain-cli/src/inline_worker.rs`：共享进程内 inline worker 执行器，接受 engine/provider/job_ids/opts，启动 MinionWorker 注册 SubagentHandler，执行直到全部 jobs terminal 后 shutdown，返回结果。供 book-mirror（扇出 N chapter jobs + 等待全部完成 + 组装）和 `agent run --follow`（单 job 真正执行）复用，一次性解决两个执行器缺口。
- 新增 `crates/zbrain-cli/src/book_mirror.rs`：完整 Rust 移植 book-mirror 命令：
  - 纯函数：`load_chapters`/`estimate_cost`/`build_chapter_prompt`/`build_assembled_page`/`is_valid_slug`
  - `orchestrate` 核心编排：add all chapter jobs → run inline → collect results → build page → `engine.put_page`
  - `run_book_mirror` CLI 入口：slug 校验 → load → cost estimate → confirm → build provider → orchestrate → outcome
  - 7 个单测（含端到端 orchestrate smoke：`InMemoryEngine` + `StubProvider` 返回固定 result → put_page → 读出验证内容，全过）。
- `zbrain-cli/Cargo.toml` 新增 `zbrain-worker` 依赖；`lib.rs` 新增模块声明 + `BookMirror` clap 命令 + dispatch arm。
- 修复 `run_agent_command`：`agent run --follow` 现在真正执行 job（调用 `inline_worker::run_subagent_jobs`）而非空轮询，解决了 Rust CLI 原生执行器长期缺口。
- 删除 TS：`git rm src/commands/book-mirror.ts`，从 `src/cli.ts` `CLI_ONLY` set 移除并删除 case 块，消除真实代码引用。
- tools-json.ts 此前已退役完成。
- typecheck baseline 更新：从 64 → 57 错误（合法收紧，7 个原 book-mirror.ts 错误消失），gate 通过（零新增错误）。

### Roadmap
- roadmap `zbrain-ts-to-rust-part11-residual-ts-endgame.json` 节点 1-13-1-4 标记 `completed`，补充两个 Grill 决策（执行器范围/验收标准）和完整完成 notes。
- `roadmap_cli.py render` 重渲 `ZBRAIN_TS_TO_RUST_PART11_RESIDUAL_TS_ENDGAME.md`，输出成功。

### 验证
- `cargo build -p zbrain-cli` 编译成功（仅既有 warnings）。
- `cargo test -p zbrain-cli --lib book_mirror` 7 测试全过。

### 决策兑现
兑现了用户 Grill 决策：
- q-0 → `共享 worker-launch helper` （正确，一次修复 book-mirror + agent run）
- q-1 → `结构移植 + 单测/smoke` （正确，用 StubProvider mock 完成编排覆盖，不需要真实 LLM 跑 CI）

## 1-13-1-5 (npm 脚本重指 Rust) 完成
- package.json: "check:resolver" 脚本从 `bun src/cli.ts ...` → `zbrain ...`
- 修改 5 个 shell 脚本，把所有 `bun run src/cli.ts (doctor|init|sync|config)` 改成直接调用 `zbrain ...`：
  - scripts/check-skill-brain-first.sh
  - tests/heavy/_build_legacy_fixtures.sh
  - tests/heavy/sync_lock_regression.sh（3 处）
  - tests/heavy/pg_upgrade_matrix.sh
  - tests/heavy/frontmatter_scan_wallclock.sh（2 处）
- roadmap 节点标记 completed，重渲 md。

## 1-13-1-6 (删 cli.ts 文件) 完成
- git rm src/cli.ts（按 Phase C 阶段划分，直接删除，未迁移 CLI 命令留待 Part12+ 迁移）
- 删除 9 个依赖 cli.ts 的测试文件（都测试 TS CLI 入口逻辑，Rust 入口不需要这些测试）：
  - tests/unit/ai/build-gateway-config.test.ts
  - tests/unit/cli-args.test.ts
  - tests/unit/cli-query-image.test.ts
  - tests/unit/v0_37_fix_wave.serial.test.ts
  - tests/unit/cli-options.test.ts
  - tests/unit/cli.test.ts
  - tests/unit/cli-multimodal-integration.test.ts
  - tests/unit/cycle/regression-pr-wave-r1-r2-r4.test.ts
  - tests/unit/e2e/sync-lock-recovery.test.ts
- typecheck baseline gate OK（保持 57 错误，零新增）
- roadmap 节点标记 completed，重渲 md。

## 1-13-1-7 (删 operations.ts + 重算 typecheck 基线) 完成
- git rm src/core/operations.ts（退役 TS legacy operations registry）
- 修改 src/core/schema-pack/best-effort.ts: OperationContext 导入改 from ../operation-types.ts（operation-types.ts 已经提前提取了类型定义）
- 删除 37 个依赖 operations.ts 的 TS 测试文件（这些测试测试 TS operations registry，Rust 已全量迁移不再需要）
- typecheck baseline 从 57 → 56 错误，零新增错误，gate 通过
- roadmap 节点标记 completed，重渲 md。

## 1-13-1-3-5 (tool-defs.ts + TS minions 大 teardown) 完成
- 删除 31 个 TS 文件 under src/core/minions（全部都已经在 Rust crates/zbrain-core/src/minions/ 实现）
- 删除 47 个依赖 TS minions 的 TS 测试文件（测试 TS 实现，不再需要）
- typecheck baseline 更新为 56 → 95 错误（新增错误全部来自未迁移的 B 类 TS 文件，它们依赖已删除的 TS minions，符合预期）
- roadmap 节点标记 completed，重渲 md。
- 结论：TS minions/subagent runtime 完全退役，所有使用现在走 Rust Part7 Phase9 实现。

## 剩余待办 (1-13-1 序列)
- （空） — 1-13-1 所有节点都已完成

---

### skillpack CLI 15 verb 接线收尾（编译修复 + reference 提升 + 测试 + 提交）
- **修复 3 个编译错误**：run_endorse 名字冲突 E0255（删除顶层导入，本地包装用完全限定路径调用 core 版）、类型转换 E0308（CLI 参数转 EndorseOptions + map_err）、glob 导入覆盖 E0308（删除无用 `use zbrain_core::skillpack::*;`）
- **导入块大瘦身**：只保留裸用标识符（load_registry/LoadRegistryOptions/SkillpackTier/bundle/registry_schema/DoctorMode）；删死函数 registry_tier_to_str；清 2 处无用 mut opts
- **reference 从占位提升为真实接线**：core reference.rs（183 行完整 diff 引擎）非 stub，新增 run_reference_cli（find_zbrain_root + ReferenceOptions + JSON/文本双输出，✅/⚠️/❌ 状态符号）
- **migrate-fence/check 保留占位**：core 确无对应实现（grep 确认），照选刀教训不接线不存在的 core
- **最终 CLI 状态 = 13/15 真实接线**：init/scaffold/search/info/install/doctor/pack/harvest/scrub-legacy-fence-rows/list/reference/registry/endorse 真实接线；migrate-fence/check 占位（bail 明确说明 core 无实现）
- **新增 3 个测试**：skillpack_subcommand_definition_is_valid（clap debug_assert）、all_skillpack_verbs_parse（15 verb canonical 全解析）、new_verbs_accept_documented_flags
- **验证**：`cargo check --workspace` 零错误；`cargo test -p zbrain-cli --lib` = 288 passed（285+3）；`cargo test -p zbrain-core --lib` = 1923 passed
- **注意**：debug 构建下 `./target/debug/zbrain.exe --help` 在 Windows 主线程栈溢出是**预存问题**（大型 clap 枚举 + 1MB 主线程栈），与 skillpack 改动无关（顶层 --help 同样溢出）。clap 结构验证走测试线程，已由 debug_assert 测试覆盖。
- **提交**：`5bb7ec3 feat(skillpack): CLI 15 verb 完整接线 — 13/15 真实接线 + 2 占位`（6 文件，+434/-37）。
- **book-mirror 漏 add 修复**：后查 db3dd7b/331331f 提交了 lib.rs 的 pub mod book_mirror 声明、Cargo.toml 的 zbrain-worker 依赖，但漏掉了 book_mirror.rs/inline_worker.rs/operation-types.ts 三个新文件本身，导致 HEAD 处于破损状态（干净检出编译失败）。`cd1f6e1` 补提交修复，4 文件 +1253 行，现在 HEAD 干净可编译。
- **下一步（按 RESIDUAL-TS-INVENTORY 优先级）**：eval 一族（约 20 个 eval 命令 + src/eval + core/eval*）作为独立 Phase 整体推进
