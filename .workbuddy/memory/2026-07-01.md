# 2026-07-01

## 1-6-4-5 magic-link scope grilled + issues published

- Grilled 1-6-4 scope: discovered Rust already has /token handler (token.rs) — 1-6-4-4 marked completed, new 1-6-4-6 created for DB-backed OAuthQueries

## 1-4-1-5 Sync Engine grilled + issues published

- 初始化路线图 `zbrain-ts-to-rust.json`（8个Phase节点）
- Grilled 7个决策问题，全部确认：
  - Q1: 范围聚焦 sync 管道本身（不含 chunking/embedding）
  - Q2: 模块位置 zbrain-core/src/sync.rs
  - Q3: 并发模型 B（Postgres 多 worker / PGLite 串行）
  - Q4: Git diff 用 std::process::Command（非 git2）
  - Q5: 文件遍历用 walkdir + inode 循环检测
  - Q6: Anchor 写入 sources 表，failures 写 JSONL 文件
  - Q7: import_one_path 走到 putPage（不含 chunking/embedding）
- 拆解为 8 个 AFK issues，按 triage 规范发布到 GitHub：
  - #94 import_one_path
  - #95 sync_manifest
  - #96 sync_walker
  - #97 sync_failures
  - #98 sync_anchor
  - #99 sync_concurrency
  - #100 sync_core (blocked by #94-#99)
  - #101 sync_cli (blocked by #100)
- 全部标签: enhancement + ready-for-agent
- Execution order decided: magic-link → DB OAuthQueries → MCP dispatch → webhook → TS cleanup
- Magic-link grilled fully (6 questions), 3 design decisions:
  - Q1: 1-6-4-4 completed → new 1-6-4-6
  - Q2: execution order confirmed
  - Q3: MagicLinkAuth independent struct (方案B) — not embedded in AdminAuth
  - Q4: Rate limiting 10/min/IP ported (不跳过)
  - Q5: Session TTL: create_session_with_ttl(), 7d for magic-link, 302 to /admin/
  - Q6: 5-slice TDD plan confirmed
- Published 5 sub-issues + updated parent #86:
  - #89: MagicLinkAuth core state machine + rate limiting (enhancement, ready-for-agent)
  - #90: AdminAuth::create_session_with_ttl (enhancement, ready-for-agent)
  - #91: POST /admin/api/issue-magic-link handler (enhancement, ready-for-agent)
  - #92: GET /admin/auth/:token handler (enhancement, ready-for-agent)
  - #93: TS cleanup - delete magic-link handlers (enhancement, ready-for-agent)
- Roadmap updated: 1-6-4-4 completed, 1-6-4-6 added, 1-6-4-5 in_progress with full decisions
- Next: implement #89 (MagicLinkAuth core) via TDD red-green-refactor

## Magic-link admin auth (#89-#93) 全部完成 + 提交

- #89 MagicLinkAuth: async nonce lifecycle (issue/redeem/prune/LRU), 10 req/60s/IP rate limit, 9 tests
- #90 create_session_with_ttl: 2 tests, backward-compatible
- #91 issue-magic-link handler: Bearer auth, 3 integration tests
- #92 auth/:token handler: nonce validation→302+Set-Cookie(7d), 4 integration tests
- #93 TS cleanup: removed magicLinkNonces/consumedNonces/pruneExpiredNonces/adminAuthRateLimiter + both handlers from serve-http.ts; updated E2E tests
- All 57 Rust tests pass, cargo check clean
- Committed: 076588a (25 files, +2349/-245) on rust-rewrite

## OAuth/Token parity + PostgresEngine OAuthQueries/TokenQueries (#252-#255)

- Audit LibsqlEngine OAuth/Token vs TS parity → found 3 gaps:
  1. RegisterClientRequest 缺 source_id + federated_read → 补齐 trait struct + INSERT + admin handler
  2. AuthInfo 缺 resource + allowed_sources → 补齐 struct + verify_access_token query
  3. OAuthQueries trait 缺 sweep_expired_tokens() → 加入 trait + 双引擎实现
- Created Postgres migration 0010_oauth_tables.sql (oauth_clients, oauth_tokens, oauth_codes)
- Implemented full PostgresEngine OAuthQueries + TokenQueries (~300 lines sqlx):
  - register_client, update_client_ttl, revoke_client, get_client
  - exchange_client_credentials, verify_confidential_client_secret
  - exchange_authorization_code, exchange_refresh_token
  - sweep_expired_tokens, verify_access_token (with legacy fallback)
- All test suites pass (zero failures)
- Roadmap updated: 1-6-4-6 marked completed, 1-6-4-3 unblocked
- Committed: fd9e3e7 (10 files, +738/-10) on rust-rewrite
- Next: #83 OAuth scope hierarchy (unblocks MCP dispatch 1-6-4-3)

## OAuth scope hierarchy validation ported to zbrain-core (#83)

- Added to `crates/zbrain-core/src/scope.rs`:
  - `is_allowed_scope(scope)` — checks membership in ALLOWED_SCOPES
  - `assert_allowed_scopes(scopes)` — validates all scopes, returns InvalidScopeError on first unknown
  - `InvalidScopeError` — struct with invalid_scope + Display/Error impls
  - `normalize_scopes_input(raw)` — full TS parity: None/null→"read", string→split+dedup+sort+validate, array→validate each element (rejects whitespace-in-element, empty strings, non-string types, unknown scopes)
- Moved normalize_scopes_input from zbrain-web to zbrain-core (was web-layer helper with no validation)
- Updated admin/oauth.rs to use core normalize_scopes_input with error handling (400 BAD_REQUEST on invalid scopes)
- 30 scope tests total (17 new), all passing
- All integration tests pass (7 admin OAuth + 7 oauth_queries_contract + 4 token_queries_contract + 6 libsql_oauth + 4 libsql_token)
- Roadmap updated: 1-6-4-3 unblocked (both OAuthQueries DB impl + scope hierarchy done)
- Committed: f6a80db (4 files, +253/-27)
- Next: #85 — MCP HTTP dispatch (1-6-4-3), the final blocker before webhook + TS cleanup

## MCP HTTP dispatch (1-6-4-3) — #85 completed

- Added `required_scope()` to `TypedOperation` trait (defaults to "read"), propagated through `Operation` + blanket impl
- Created `crates/zbrain-web/src/mcp.rs`:
  - POST /mcp: JSON-RPC 2.0 dispatch with bearer auth, scope enforcement, tools/list + tools/call
  - GET /mcp: 405 Method Not Allowed
  - AuthInfo conversion: token_queries → operation context
  - Scope enforcement via `has_scope()` before `dispatch_tool_call()`
- Added `ValidateParams` blanket impl for `serde_json::Value` (test utility)
- Fixed `InMemoryEngine`: added `token_scopes` HashMap, exchange_client_credentials stores scopes, verify_access_token looks up by token
- Made `JsonRpcRequest`/`JsonRpcResponse` public in zbrain-mcp
- Added `OperationRegistry` to `AppState`; created `build_operation_registry()` shared helper in zbrain-cli
- 9 MCP integration tests pass (scope pass/fail/implied/admin + unknown op + parse error + auth error)
- 66 total zbrain-web tests pass
- Committed: 2549abc (12 files, +697/-23)
- Roadmap status: 1-6-4-5 ✅, 1-6-4-6 ✅, #83 ✅, 1-6-4-3 ✅
- Next: 1-6-4-2 (webhook ingestion) or 1-6-4-1 (TS cleanup). 1-6-4-4 (/token handler) also pending.

## Webhook ingestion endpoints (1-6-4-2) — #87 completed

- Created `crates/zbrain-core/src/ingestion.rs` (~330 lines):
  - `IngestionEvent` struct: source_id, source_kind, source_uri, received_at, content_type, content, content_hash, untrusted_payload, metadata
  - `compute_content_hash()` — SHA-256 hex
  - `validate_ingestion_event()` — non-empty strings, ISO 8601 received_at, 64-char hex hash, content type taxonomy check
  - `detect_content_type()` — X-Zbrain-Content-Type overrides Content-Type; text/* recognition; binary pass-through
  - `is_allowed_ingest_content_type()` — v1 allowlist: text/markdown, text/plain, text/html, application/json
  - 27 unit tests
- Created `crates/zbrain-web/src/webhook.rs` (~893 lines):
  - POST /ingest: bearer auth → write scope check → content-type detection → IngestionEvent validation → put_page → 202
  - POST /webhooks/github: X-Hub-Signature-256 verification (HMAC-SHA256, constant-time), push event filter, source lookup by github_repo, branch ref matching → 202 with placeholder job_id
  - `WebhookState` struct: engine + token_queries (decoupled from AppState)
  - 13 integration tests (10 GitHub + 3 ingest)
- Added `SourceRow` struct + `get_source_by_github_repo()` to BrainEngine trait
  - InMemoryEngine: matches config.github_repo
  - LibsqlEngine: json_extract(config, '$.github_repo')
  - PostgresEngine: config->>'github_repo'
- Added `engine: Arc<dyn BrainEngine>` to AppState, propagated to all 8 test files
- MinionQueue integration deferred (TODO comments for future async job submission)
- All 40 new tests pass; full workspace tests pass (1 pre-existing Windows permission issue in page_methods_update_cr_state, unrelated)
- Committed: 708c2e3 (22 files, +1605/-16)
- Roadmap: 1-6-4-5 ✅, 1-6-4-6 ✅, #83 ✅, 1-6-4-3 ✅, 1-6-4-2 ✅
- Next: 1-6-4-1 (TS cleanup — delete serve-http.ts and remaining TS web modules)

## /token handler error handling improvements (#84)

- 1-6-4-4 was already marked "completed" (handler existed in token.rs). This session enhanced it:
- Added `error_description` to ALL error responses (RFC 6749 compliance)
- Added `client_secret` required validation in client_credentials (matches TS: "client_id and client_secret required")
- Error mapping: invalid_client → 401, invalid_grant → 400 (matches TS serve-http.ts:604-611)
- 6 new tests: client_credentials missing/empty client_secret, error_description presence in auth_code/refresh_token/unsupported grant_type
- 19 token tests pass (up from 13)
- Committed: 2c51121 (3 files, +194/-15)
- All workspace tests pass (0 real failures)

### 1-6-4 complete status:
- 1-6-4-5 magic-link ✅
- 1-6-4-6 DB OAuthQueries ✅
- #83 scope hierarchy ✅
- 1-6-4-3 MCP dispatch ✅
- 1-6-4-4 /token handler ✅ (enhanced)
- 1-6-4-2 webhook ingestion ✅
- 1-6-4-1 TS cleanup ✅ (completed below)

## 1-6-4-1 TS cleanup — serve-http.ts and all web-backend deleted

- Comprehensive audit: mapped all serve-http.ts imports → consumer analysis → safe-to-delete classification
- Deleted 4 source files: serve-http.ts, timing-safe.ts, svg-renderer.ts, admin-embedded.ts
- Deleted 2 build scripts: build-admin-embedded.ts, check-admin-embedded.sh
- Deleted 12 test files: all serve-http related tests + admin-embed-spawn + e2e/webhook/oauth/meta tests
- Modified 3 files: cli.ts (remove 'serve' from CLI_ONLY), package.json (remove admin-embedded scripts), fix-wave-structural.test.ts (delete admin-embed + admin PKCE describe blocks)
- Preserved: oauth-provider.ts (used by auth.ts), scope.ts (used by tests), ingestion/types.ts (used by capture.ts)
- Full Rust workspace test suite passes (0 regressions)
- Committed: 1c48e21 (23 files, +34/-4991)
- Slice 1-6-4 fully COMPLETED. TS web-backend retired; Rust zbrain-web is sole HTTP server.
- Next: 1-6-5 or next slice per roadmap.

## 1-7-1-1 Source CRUD API routes — completed

- Created DB migration 0011: added `config`, `chunker_version`, `contextual_retrieval_mode`, `trust_frontmatter_overrides`, `archived`, `archived_at`, `archive_expires_at` columns to `sources` table
- Extended `BrainEngine` trait: `add_source`, `get_source`, `list_sources`, `update_source`, `delete_source` (5 new methods)
- Implemented in `InMemoryEngine`, `LibsqlEngine`, `PostgresEngine`
- Created `crates/zbrain-web/src/admin/sources.rs`: GET /sources, POST /sources, GET /sources/:id, PUT /sources/:id, DELETE /sources/:id
- Source ID validation: lowercase, no spaces, 1-128 chars, ascii alphanumeric + `-_.`
- delete_source → soft-archive (sets archived=true + archived_at), not hard delete
- 5 integration tests pass (list, create, get, update, delete)

## 1-7-1-2 Source import/clone with git operations — completed

- Added Rust dependencies: `url` (workspace), `rand` (zbrain-core), `tokio process feature` (workspace + zbrain-core + zbrain-web)
- Created `crates/zbrain-core/src/url_safety.rs` (~280 lines): SSRF-safe URL parsing ported from TS
  - `parse_octet()`, `hostname_to_octets()`, `is_private_ipv4()`, `is_internal_url()`, `parse_remote_url()`
  - Covers: IPv4 private ranges, IPv6 ULA/link-local, IPv4-mapped IPv6, hex/octal/single-int encoding bypasses, metadata hostnames, CGNAT 100.64/10 (Tailscale)
  - 25 unit tests
- Created `crates/zbrain-core/src/git_remote.rs` (~320 lines): SSRF-defensive async git operations
  - `GIT_SSRF_FLAGS` (http.followRedirects=false, protocol.file.allow=never, protocol.ext.allow=never)
  - `GIT_SSRF_SUBCOMMAND_FLAGS` (--no-recurse-submodules)
  - `GIT_ENV` isolation (GIT_TERMINAL_PROMPT=0, GCM_INTERACTIVE=never, etc.)
  - `clone_repo()`, `pull_repo()`, `validate_repo_state()` (6-state: Healthy/Missing/NotADir/NoGit/UrlDrift/Corrupted)
  - Uses `tokio::process::Command` (not git2 crate) for precise flag control
- Created `crates/zbrain-core/src/sources_ops.rs` (~280 lines): atomic clone-then-rename pattern
  - `add_source()`: parse+validate URL → clone to temp dir → INSERT DB → rename to final path (rollback on failure)
  - `remove_source()`: symlink-safe path deletion (canonicalize + prefix check + is_symlink guard)
  - `get_source_status()`: validate_repo_state wrapper
  - `reclone_if_missing()`: re-clone when Missing/NoGit
  - `count_pages()`: placeholder for future
  - `is_path_contained()`: separator-suffixed prefix containment check
  - 7 unit tests
- Extended `AppState` with `zbrain_home: PathBuf`; updated all 10+ construction sites across tests
- Created `POST /sources/import` admin route with `ImportSourceRequest` (id, url, name, depth, branch, federated)
- Wired `depth` and `branch` fields through: ImportSourceRequest → AddSourceOpts → CloneOpts
- Added `SourceRow` import to sources.rs tests (was missing)
- **Full workspace test suite: 288+14+34+90 = 426 tests, ALL PASSING (0 failures)**
- Only pre-existing warnings, no new issues
- 1-7-1-2 COMPLETE. Next: 1-7-1-3 Capture pipeline.

## 1-7-1-3 Capture pipeline — completed

- Added workspace deps: `serde_yaml = "0.9"`, `unicode-normalization = "0.1"`
- Modified `ingestion.rs`:
  - `normalize_for_hash(s)` — strip BOM → CRLF→LF → trim → NFKC Unicode normalization (mirrors TS normalizeForHash)
  - `compute_content_hash` now calls normalize_for_hash before SHA-256 (decision Q5: A)
  - `compute_raw_hash(data: &[u8])` — raw SHA-256 without normalization
  - 39 tests (9 normalize + 5 hash + 25 existing ingestion)
- Created `crates/zbrain-core/src/capture.rs` (~450 lines):
  - `detect_binary(data)` — scan first 8KB for NUL byte, returns Option<usize>
  - `derive_title(body)` — first non-empty non-fm-delimiter line, strip #, cap 80, default "Capture"
  - `parse_frontmatter_from_body(body)` — YAML frontmatter extraction (BOM-tolerant, ---\n start, serde_yaml parse, returns (Option<Value>, body))
  - `merge_capture_frontmatter(existing, body, opts, now)` — user-wins precedence: type (CLI>user>'note'), title (user>derived), captured_via (user>opts.source>'capture-cli'), captured_at (user>opts.captured_at>now), other keys pass through
  - `capture_content(raw, opts)` — full pipeline: binary check → UTF-8 decode → empty check → parse fm → merge fm → compute hash → CaptureResult
  - `chrono_now_iso()` — Howard Hinnant civil_from_days algorithm (no chrono dep)
  - 38 tests (8 binary + 6 title + 8 parse_fm + 7 merge_fm + 9 capture_content)
- 6 key decisions recorded in roadmap JSON (Q1-Q6)
- **Full workspace: 338+32+14+34+90 = 508 tests, ALL PASSING**
- 1-7-1-3 COMPLETE. Next: 1-7-1-4 Content extraction.

## 1-7-1-4 Content extraction — completed

- Roadmap restructure: chunking/embedding split from 1-7-1-5 into new 1-7-1-6, CLI commands moved to 1-7-1-7
- Created `crates/zbrain-core/src/markdown.rs` (~330 lines):
  - `ParsedMarkdown` struct: frontmatter, compiled_truth, timeline, type_, title, slug, tags
  - `split_body(body)` — 3 delimiters: <!-- timeline -->, --- timeline ---, ## Timeline
  - `infer_type(body_path, fm, path_prefixes)` — 3-tier: fm.type → BASE_PATH_PREFIXES (17 built-in) → user pack mapping → "note"
  - `infer_title(fm, body, body_path)` — fm.title → first h1 → filename humanized (only first word capitalized)
  - `infer_slug(fm, body_path)` — fm.slug → filename stem slugified → full path slugified
  - `infer_tags(fm, body_path)` — fm.tags (array or comma-string) → directory segments → empty
  - `parse_markdown(body, body_path, path_prefixes)` — main entry wiring all above
  - 50 tests
- Created `crates/zbrain-core/src/file_classify.rs` (~250 lines):
  - `FileType` enum: Markdown, Code(String), Image(String), Unknown
  - `detect_code_language(path)` — 31 languages by file extension
  - `detect_image_format(path)` — 7 image formats (png/jpg/jpeg/gif/svg/webp/bmp)
  - `is_markdown_path(path)` — .md/.mdx/.markdown
  - `classify_file(path)` — priority: markdown > image > code > unknown
  - `file_extension(path)` — handles Dockerfile/Makefile as extensionless special cases
  - 35 tests
- 7 key decisions recorded (Q1-Q7)
- **Full workspace: 423+32+14+34+90 = 593 tests, ALL PASSING**
- 1-7-1-4 COMPLETE. Next: 1-7-1-5 Sync engine.

## 1-7-1-5 Sync engine (#94-#101) — all 8 issues completed via TDD

- **Round 1** — low-level modules (RED→GREEN→REFACTOR per module):
  - #97 sync_failures: JSONL recording + acknowledge (5 tests)
  - #98 sync_anchor: get/set_sync_anchor via BrainEngine SourceRow (7 tests)
  - #96 sync_walker: walkdir + inode cycle detection + is_syncable filter (5 tests)
  - #95 sync_manifest: git diff --name-status parsing → SyncManifest {to_sync, to_delete} (7 tests)
  - #99 sync_concurrency: Postgres→Parallel(8), Libsql/InMemory→Serial, user overrides (7 tests)
  - #94 import_one_path: read→capture→parse_markdown→put_page→add_tag (4 tests)
- **Round 2** — pipeline layer:
  - #100 sync_core: perform_full_sync + perform_sync (incremental via git diff) + needs_full_sync (4 tests)
- **Round 3** — CLI layer:
  - #101 sync_cli: `zbrain sync` subcommand with --source-id, --repo-path, --full-sync, --chunker-version, --max-file-size, --failures-dir, --parallelism (9 tests)
  - Added `SyncConfig` to zbrain-cli Config (default_repo, chunker_version)
  - Implemented `run_sync_command()`: resolve config→engine→source→sync→print result
  - Helper: `ensure_source_exists()`, `get_git_head_commit()`
- **Total: 48 new tests across 8 modules + 9 CLI tests = 57 sync tests, all green**
- **Full workspace: 432+32+14+34+90+57 = 659 tests, ALL PASSING (0 failures)**
- Code placed in `crates/zbrain-core/src/sync/` (7 modules) + `crates/zbrain-cli/src/lib.rs`
- Dependencies added: `walkdir` (zbrain-core)
- Key decisions: no chrono (used crate::time), no git2 (used std::process::Command), Windows path handling via Path comparison
- 1-7-1-5 COMPLETE. Next: 1-7-1-6 Chunking/Embedding or 1-7-1-7 CLI commands.
