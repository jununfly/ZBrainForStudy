# 2026-07-14

## 1-4-2 & 1-4-5 Grill (7/7 A)

- Q1: 1-4-2 = 15 handlers, extract-conversation-facts+ingest_capture → 1-4-5
- Q2: 手写 struct + #[async_trait], 不用宏
- Q3: 每个 handler 独立文件
- Q4: 内联核心逻辑, 直接调 ctx.engine()
- Q5: 1-4-5 单节点, shell v1 基础 spawn, contextual-reindex v1 不做 LLM
- Q6: 分层测试 — 5 InMemory 完整 + 10 smoke
- Q7: register_builtin_handlers(engine, chat_provider)

## 1-4-2 实现完成 (75b2b44)

- register_builtin_handlers 改为双注入 (engine + chat_provider)
- 15 handlers: orphans/purge/backlinks/import/subagent_aggregator (5 InMemory 完整)
  + sync/sync-retry-failed/embed/lint/lint-fix/extract/integrity/integrity-auto/reindex/repair-jsonb (10 smoke)
- PurgeResult 加 Serialize derive
- 69/69 minions tests, 995/995 zbrain-core lib tests 全绿

## 1-4-3 + 1-4-5 Grill (5/5 A) + 实现 (0c2bdb6)

- Q1: 1-4-3 = 7 smoke handlers, runCycle 留独立 cycle port 节点
- Q2: 1-4-5 = ingest_capture + shell 完整, 其余 smoke
- Q3: 1-4-5 精确 5 个（去掉不存在的"辅助"）
- Q4: shell v1 = command + timeout + stdout/stderr, cwd 固定, env 透传
- Q5: 1-4-3 纯 smoke, 不做输入解析

### 1-4-3 完成：7 smoke handlers
- autopilot-cycle, synthesize, patterns, consolidate, extract_facts,
  resolve_symbol_edges, recompute_emotional_weight
- 全部返回 not_implemented JSON

### 1-4-5 完成：2 real + 3 smoke
- ingest_capture (REAL): 解析 slug/title/content/tags/source → import_from_content
- shell (REAL): tokio::process + tokio::time::timeout, {stdout, stderr, exit_code, timed_out}
- embed-backfill, contextual-reindex, extract-conversation-facts (smoke)

### 基础设施变更
- ImportResult 加 Serialize derive (import.rs)
- tokio 加 time feature (zbrain-core Cargo.toml)
- Registry 注册 28 handlers (was 16)

## 1-4 完成 (5/5)

| 节点 | 状态 | Tests |
|------|------|-------|
| 1-4-1 | ✅ Brain tools (22) |
| 1-4-2 | ✅ 低复杂度 handlers (15) |
| 1-4-3 | ✅ Autopilot + phase (7 smoke) |
| 1-4-4 | ✅ Registry + Subagent v1 (9) |
| 1-4-5 | ✅ 中复杂度 handlers (2 real + 3 smoke) |

89/89 minions tests, 1015/1015 zbrain-core lib tests 全绿

## 1-5 Grill (6Q) + 1-5-1 实现

### 1-5 Grill 决策
- Q1: 两条路径都做(minions-dispatch + inline fallback), 拆 7 子节点(6 exploit + 1 deferred)
- Q2: 1-5-3 cycle phase 分层: 编排器完整+琐碎真实+中等/LLM skipped stub
- Q3: 1-5-2 扩展含 BrainHealth + getHealth() (三后端)
- Q4: 1-5-4 含 no-worker probe + federated v2, nightly probe → 1-5-7 deferred
- Q5: computeRecommendations 用 Option B 硬编码 4 provider, G29 登记 recipe 注册表未迁移
- Q6: 1-5-5 daemon 包含 OpenClaw 集成 + 4 平台目标

### 1-5-1 完成: Autopilot fanout 纯函数 + dispatchPerSource
- 新建 crates/zbrain-core/src/autopilot/{mod.rs, fanout.rs}
- 5 个公开函数: resolve_fanout_max / read_last_full_cycle_at / is_source_stale / select_sources_for_dispatch / dispatch_per_source
- 16 tests 全绿, lib 1031 tests 无回归
- Rust 差异: resolve_fanout_max 接受 Option<&str> override(无 getConfig); list_sources+filter 替代 listAllSources({localPathOnly}); 跳过 maxWaiting; 跳过 emit/log callbacks
- G29 登记: AI recipe 注册表未迁移(provider 元数据硬编码子集)

## 1-5-2 BrainHealth + brain-score-recommendations 完成

- 新建 crates/zbrain-core/src/autopilot/brain_score.rs
- BrainHealth struct + get_health() trait method (InMemory 实现, SQL 后端用 default 返回 unsupported)
- compute_recommendations / classify_checks / max_reachable_score / estimate_anthropic_cost
- embedding_provider_configured (Option B: 硬编码 6 provider: openai/zeroentropyai/voyage/ollama/llama-server/google)
- 静态 pricing 表: ANTHROPIC_PRICING (6 model) + EMBEDDING_PRICING (7 model)
- canonical_json + idempotency_key (sha8 of canonical JSON)
- RemediationStep / Check / RecommendationContext / CheckClassification 类型
- InMemoryEngine 新增 store_for_test/chunk_store_for_test/links_store_for_test 测试辅助方法
- 35 tests 全绿 (29 纯函数 + 6 get_health InMemory 集成), lib 1066 tests 无回归

## 1-5-3 Cycle 模块完成

- 新建 crates/zbrain-core/src/autopilot/cycle.rs
- CyclePhase enum (20 phases) + ALL 数组 + label/needs_lock/scope 方法
- PhaseScope/PhaseStatus/PhaseError/PhaseResult/CycleStatus/CycleReport/CycleTotals/CycleOpts 类型
- run_cycle 编排器: 顺序执行所有 phase + derive_cycle_status 派生总体状态
- orphans phase: 真实实现 (engine.find_orphan_pages)
- purge phase: 真实实现 (engine.purge_deleted_pages(72), dry_run 跳过)
- 其余 18 个 phase: skipped stub (分 "needs orchestration" 和 "LLM-heavy" 两类原因)
- 13 tests 全绿, lib 1079 tests 无回归

## 1-5-4 Autopilot 主循环 + 模式判定 + reconnect 分类 完成

- 新建 crates/zbrain-core/src/autopilot/runner.rs
- 11 个纯函数: classify_reconnect_error / should_spawn_autopilot_worker / resolve_autopilot_mode / check_lock_freshness / compute_adaptive_interval / should_full_cycle / should_sleep / update_no_worker_probe / update_error_counter / handle_reconnect_failure / is_source_freshness_stale
- run_autopilot_tick: 单次循环迭代 (MinionsDispatch path: health→score gate→fanout/targeted/sleep; Inline path: run_cycle→adaptive interval)
- AutopilotState / AutopilotOpts / TickEvent / TickResult 类型
- Nightly quality probe: skipped stub (1-5-7 deferred per grill Q4)
- 63 tests (52 pure + 11 integration), lib 1142 tests 无回归
- commit 2185deb

## 1-5-5 Daemon install/uninstall 完成

- 新建 crates/zbrain-core/src/autopilot/daemon.rs
- 纯生成函数: generate_wrapper_script / generate_launchd_plist / generate_systemd_unit / generate_crontab_line / generate_ephemeral_start_script / generate_openclaw_snippet
- 检测函数: detect_install_target (runtime OS/env) / detect_open_claw / should_inject_openclaw
- 路径辅助: plist_path / systemd_unit_path / ephemeral_start_script_path / wrapper_script_path
- resolve_zbrain_cli_path: which→current_exe→argv[1] fallback chain
- show_status: plist/crontab 存在性 + last log line
- OpenClaw 集成 per grill Q6
- 38 tests (全 cross-platform safe), lib 1180 tests 无回归
- commit ac3a9ea

## 1-5-6 CLI 接线 完成

- 在 crates/zbrain-cli/src/lib.rs 添加 AutopilotArgs + Commands::Autopilot + handler
- AutopilotArgs: --repo / --interval / --json / --inline / --no-worker / --install / --uninstall / --status / --once
- Handler 分发: daemon module (install/uninstall/status) + runner::run_autopilot_tick (once + continuous loop)
- 连续循环含 5-strike circuit breaker (update_error_counter)
- --once 模式: 单 tick + 事件输出 (JSON 或 human-readable)
- 5 CLI arg parsing tests, CLI 130 tests 全绿
- commit 80448d8

## 1-5 完成 (6/7, 1-5-7 deferred)

| 节点 | 状态 | Tests |
|------|------|-------|
| 1-5-1 | ✅ fanout 纯函数 + dispatchPerSource | 16 |
| 1-5-2 | ✅ BrainHealth + brain-score-recommendations | 35 |
| 1-5-3 | ✅ Cycle 模块 (types+phases+runCycle) | 13 |
| 1-5-4 | ✅ 主循环 + 模式判定 + reconnect 分类 | 63 |
| 1-5-5 | ✅ Daemon install/uninstall | 38 |
| 1-5-6 | ✅ CLI 接线 | 5 |
| 1-5-7 | ⏳ Nightly quality probe (deferred) | — |

autopilot 模块总计 165 tests (lib) + 5 tests (CLI)
lib 1180 tests, CLI 130 tests 全绿

## 1-6 Remote execution (commit 1b69d8a + 88ac83e)

- TS `remote.ts` (250行) → Rust CLI `zbrain remote ping` + `zbrain remote doctor`
- Ping 轮询退避曲线: <30s→1s, <5m30s→5s, else→10s. 默认超时 15min
- 复用 McpClient (OAuth/token cache/401 retry/timeout)、config::is_thin_client、SSRF 防御
- `--max-wait` 替代 `--timeout` (避免与全局 CLI --timeout 冲突)
- 纯函数: parse_duration, compute_poll_interval, unpack_tool_result, is_terminal_state
- 20 tests (4 CLI + 6 parse_duration + 3 poll_interval + 2 terminal_state + 5 unpack_tool_result)
- CLI 150 tests 全绿

## 1-7 jobs/agent CLI 命令层 (commit e1b8bf7 + b4d5c34)

- TS `jobs.ts` (1575行) + `agent.ts` (333行) → Rust CLI 子命令
- JobsAction: 8 subcommands (Submit/List/Get/Cancel/Retry/Prune/Stats/Work)
- AgentAction: Run with --follow polling
- MinionQueue::new(&engine) 直接引用 LibsqlEngine (非 trait object)
- MinionJobStatus 用 as_str()/parse() (非 Display/FromStr)
- MinionJob 用 error_text (非 failed_reason)
- Work 为 stub (worker 在 zbrain-worker crate)
- 15 tests (10 jobs CLI + 2 agent CLI + 3 parse_relative_duration)
- CLI 165 tests 全绿

## 1-8 G7 收口: webhook→MinionQueue (commit 4706c1e + 8ad8111)

- **POST /ingest**: 替换 `engine.put_page()` 为 `MinionQueue::add('ingest_capture', {slug, content, source, title})`
  - Worker 拾取后调 `import_from_content` 分块存储（行为对齐 TS，之前是直写单页）
  - 返回真实 `job.id` (i64→string) 替代 `uuid::new_v4()`
- **POST /webhooks/github**: 替换 placeholder UUID 为 `MinionQueue::add('sync', {sourceId, repoPath, auto_embed_backfill, embed_reason})`
  - priority=-10（push-triggered sync 抢占 autopilot 的 0）
  - idempotency_key=`sync-trigger:{sourceId}:{30s bucket}`
- **MinionQueue API**: `MinionQueue::new(state.engine.as_ref())` — 接受 `&dyn BrainEngine`，可从 `Arc<dyn BrainEngine>` 解引用
- **ingest_capture handler 数据格式**: Rust 用 `{slug, content, source, title?}` (非 TS 的 `{event, slug?}`)，调 `import_from_content`
- **sync handler**: 已注册但为 stub (`{status: "not_implemented"}`)，sync pipeline 待 CLI 迁移
- KNOWN-GAPS G7 标记 resolved
- zbrain-web 92 tests 全绿 (90 原有 + 2 新增验证 job 在队列中)

## Part7 Phase9 Roadmap 总结

| 节点 | 状态 | 说明 |
|------|------|------|
| 1-1 | ✅ | MinionQueue + job 持久化 (3/3) |
| 1-2 | ✅ | MinionWorker + supervisor (5/5) |
| 1-3 | ✅ | Budget + rate leases (3/3) |
| 1-4 | ✅ | Minion handlers + tools (5/5) |
| 1-5 | ✅ | Autopilot + fanout (7/7) |
| 1-6 | ✅ | Remote execution |
| 1-7 | ✅ | jobs/agent CLI 命令层 |
| 1-8 | ✅ | G7 收口: webhook→MinionQueue |

Part7 Phase9: 35/35 nodes completed 🎉

## 1-5-7 Nightly quality probe (completed)

- TDD 6 slices: S1 should_run_nightly (7 tests), S2 trait+types (4 tests), S3 core state machine (10 tests), S4-S5 audit+stubs, S6 wiring
- `nightly_probe.rs` (671 lines): should_run_nightly 纯函数 + NightlyProbeDeps async trait + run_nightly_quality_probe 8-outcome 状态机
- 接线到 runner.rs: TickEvent::NightlyProbeCompleted(outcome) 替换 NightlyProbeSkipped stub
- AutopilotOpts 新增 enable_nightly_probe: bool (default false)
- Eval 适配器 stub（runLongMemEvalForProbe/runCrossModalBatchForProbe），真实实现后续独立节点
- lib 1201 tests, CLI 165 tests 全绿
- commit: 9cddd4a

## TS 删除安全性审计（决定删除策略）

- **核心结论**：删除会不会破坏 KNOWN-GAPS / Phase 10-11，完全取决于**删哪批 TS**，不是"删/不删"的二元问题。
- **KNOWN-GAPS 依赖 TS 作 spec**：G4/G5/G13/G23/G26/G29 等的"TS 权威来源"列直接指向具体 TS 文件（src/core/search/hybrid.ts、ai/gateway.ts、ai/recipes/、commands/schema.ts@5d5b404~1 等），是补齐时照抄的规范。这些文件**大部分仍在工作树**（hybrid/expansion/intent-weights/gateway/recipes/queue/attachments 全活），仅 schema.ts 已删但可从 git 5d5b404~1 恢复。
- **Phase 10 eval 源全部还在工作树**：src/core/eval*、cross-modal-eval、claw-test、calibration 都活着——Phase 10 要决定 port 哪些、删哪些，删早了就失去 port 素材。
- **git 兜底**：本仓 TS 全程版本追踪，任何删除都能 `git show <sha>:<path>` 恢复。schema.ts 已验证在 5d5b404 被删、历史可取。所以"删了失去 spec"不成立——只是恢复成本 = 多一步 git 考古 + KNOWN-GAPS 的 commit 锚点必须精确。
- **PRD 删除铁律**（complete-ts-to-rust.md）：删除必须逐 slice 走 Deletion Rule checklist；Replacement implies deletion，但**仅删"已被 Rust 验证替换"的 slice**；未替换/被 KNOWN-GAPS 依赖的不能删。
- **安全删除分层**：(A) 已 100% 替换且无 KNOWN-GAPS 引用的 TS impl+test（Phase 1-5、7、9 大部分存储/CLI/minions）→ 可删；(B) 被高影响 GAP 引用的（hybrid.ts/gateway.ts/recipes/schema.ts）→ 保留到 GAP 关闭；(C) Phase 10 eval 源 → 保留到 Phase 10 决策。
- **建议顺序**：先补齐 3 个高影响 GAP（G23 PG search / G24 libsql embedding 写 / G25 chunk embedding）解除 search/hybrid 依赖 → 再分层删 A 类 → Phase 10 决策后删 eval → Phase 11 final cutover。

## G23/G24/G25 补齐完成（用户批准顺序 step1）

- commit f6ea28c。G23 postgres search_pages override + 6 集成测试；G24 libsql/postgres put_page 持久化 embedding（COALESCE-preserve）+ InMemory 本就写；G25 import_from_content 加 embedding_client 参数 fail-open + 3 测试。lib 1204 passed，clippy 无新 error。新增 G30（minion 路径 chunk embedding follow-up，MinionJobContext 无 client）。KNOWN-GAPS G23/G24/G25 标 resolved。

## A 类删除清单探查（用户批准顺序 step2）——关键发现：机械删 A 类不安全

- **roadmap 事实**：Part1-9 全 completed（JSON 是 SSOT）。Part3 节点 1-3「TS 入口整体退役」JSON 标 completed，但 `src/cli.ts`/`scripts/postinstall.ts`/`check-cli-executable.sh` **仍物理存在**——`bin.zbrain` 已切 `bin/zbrain-rs.js`（纯 Rust pass-through wrapper，不依赖 TS），故 TS 已非发布运行时，删除风险是"丢参考 spec"而非"破坏运行时"。`package.json main` 仍指 `src/core/index.ts`，`typecheck=tsc --noEmit` 仍在。
- **TS 规模**：1461 个 TS（896 tests/unit + 436 src/core + 117 src/commands + 7 src/eval + 2 src/mcp）。admin/ 11 个 TSX 是 Category B 保留。
- **两 Explore agent 结论分歧暴露真相**：CLI agent 极端保守（0 A-DELETABLE，因 Rust CLI 实际接线子命令集远小于假设——extract/search/embed/import/storage/reindex 等**根本无 Rust CLI 子命令**；sources 仅 4/~20 子命令）；core agent 找出 ~26 候选（mcp/tool-defs+dispatch、ai/capabilities+model-resolver+types、facts/extract+decay、ingestion/**、chunkers/**、search/rerank+recency、minions/backoff+stagger+rate-leases+worker+types+handlers/*）。但两者都只做**模块名匹配**，非行为等价核实。
- **决定性阻塞 = TS 是紧耦合依赖图**：`src/core/minions/queue.ts`（G28 KEEP-SPEC）`import { rowToMinionJob,... } from './types.ts'`——而 types.ts 被 core agent 列为 A-DELETABLE。删 types.ts → 保留的 spec 文件 queue.ts 断 `tsc --noEmit`。同理 minions handlers 被 queue.ts/gateway.ts(G26)/多个 audit+eval 文件 import。**结论：不能按"叶子模块已迁"删——必须先切断 KEEP-SPEC/KEEP-EVAL 对候选的 import 边，否则 typecheck 崩。**
- **修正策略**：机械删 A 类会破坏 typecheck；正确做法要么(a)按 PRD Deletion Rule 逐 slice 行为等价核实 + 连同 importer 一起处理，要么(b)重新定义"A 类" = 整条 import 闭包都可删的子图（如整个 minions 运行时层一起删，但其中 queue.ts/attachments.ts 是 KEEP-SPEC → 无法整体删）。待与用户对齐是否接受"暂缓机械删、改为先给 KEEP-SPEC 文件解耦/或推进 Phase 10 决策解除 eval 保留"。

## 用户选 ①「整子系统切片删」——建依赖闭包工具求 sound 删除集

- 工具 `scripts/ts_dep_closure.py`（解析 src/tests/scripts 全部 .ts 的相对 import + `@/*`→src 别名，建正/反向图，1482 文件/3767 边；`**` 递归 glob 自实现，`fnmatch` 不支持）。命令：`graph`/`importers <path>`/`closure <seedfile>`/`tests <seedfile> [del|keep|orphan]`。
- **核心算法**：从 KEEP 种子（KNOWN-GAPS spec + Phase10 eval 源 + package 入口 src/core/index.ts + doctor 子系统，见 `scripts/_ts_keep_seeds.txt`，153 seeds）求**下游传递闭包 R**，`D=ALL-R` 即安全删除集。**关键修正**：初版只折叠 impl→impl，漏了「保留的 test 仍 import D-impl」——删 D 会让 kept test 断 tsc。加**迭代 fixpoint**：凡 import ≥1 个 R-impl 的 test 也 pin 它 import 的一切进 R，重复到不增长。
- **sound 结果 D=135**（R=1347）：22 个纯死 impl（src/core/search/keyword.ts+vector.ts[零 importer]、minions/index.ts+batch-projection.ts、context-engine.ts、chunkers/llm.ts+semantic.ts、supabase-admin.ts、transcription.ts、data-research.ts、enrichment-service.ts、backoff.ts、fail-improve.ts、openclaw-context-engine.ts 等）+ 18 dead scripts（postinstall.ts、build-*.ts、sharding.ts 等）+ 95 tests（对应上述死 impl 的 orphan/slice 测试）。
- **踩坑教训（决定性）**：`src/mcp/`（dispatch.ts+tool-defs.ts）看似最干净第一刀（Rust zbrain-mcp 已替换），但 dispatch.ts 被 **9 个 KEEP test**（facts/takes spec 测试）当共享 harness import → 删了断 tsc。证明"整子系统闭包"必须连 kept-test 边一起算，`src/mcp` 当前**不可删**（还是 facts/takes 测试的活 seam）。
- **诚实信号**：D 从 201（仅 impl 折叠）→135（加 test 折叠）暴露真相——kept tests 传递 import 了大部分 impl，**不退役测试就删不动多少 impl**。第一刀只能是这 135 个真死代码（无任何 kept impl/test 依赖），是无损清理死残留，非"删已迁子系统"。真正削减已迁子系统需 Phase 10 eval 决策先解除 eval 对 impl 的 pin。
- 工具产物：`scripts/ts_dep_closure.py`（保留可复用）+ `scripts/ts_keep_seeds.txt`（KEEP 种子 rationale 文档）；D/R scratch 输出写系统 temp。

## 第一刀死代码删除执行完成（用户批准：判断2 先删、判断1 测试退役独立 slice）

- **tsc 基线本就不绿**：删前 `node_modules/.bin/tsc --noEmit` 已有 **101 errors / 29 files**（多为指向已删 `doctor.ts`/`init.ts` 的悬空 TS2307 + 隐式 any TS7006），是之前 TS 退役刀的残留。故目标 = **删后不新增错误**（非"保持绿"）。用 `file(line,col): TSxxxx` 签名集做精确 diff。
- **删除 134 个真死代码**（原 135，见下 .d.ts 教训）：`git rm`（注意 delete-set 文件有 CRLF，需 `tr -d '\r'` 否则 pathspec 带 `\r` 报 fatal）。删后 tsc = **76 errors，NEW regressions=0，25 个基线错误因删掉带错文件而消失**。Rust `cargo build -p zbrain-core` 仍绿（TS 删除与 Rust 完全隔离）。
- **决定性教训 — ambient `.d.ts` 盲区**：首删 135 含 `src/types/image-decoders.d.ts`，导致 kept `src/core/import-file.ts` 冒 4 个新错误（heic-decode/@jsquash 类型丢失）。根因：`.d.ts` 是**隐式 ambient 引用**，无 import 边，闭包图看不见其消费者，误判死代码。已 `git checkout HEAD --` 恢复 + 给工具加 `is_ambient()` 守卫（D 永久排除 `*.d.ts`）。**通用规则：import-graph 闭包对 .d.ts / 隐式全局类型无效，删 TS 前 .d.ts 一律强制 KEEP。**
- **下一刀（判断1，独立 slice）**：测试退役/重写——把 9 个 facts/takes KEEP-test 从 import TS `src/mcp/dispatch.ts` 改为依赖 Rust seam 或退役，解除对已迁 impl 的 pin，才能真正削减 mcp/minions/commands 实现层。

## 判断1 前提被 Rust 覆盖核实推翻（决定性发现，暂缓退役）

- **目标**：退役 14 个 import `src/mcp/dispatch.ts` 的 TS 测试以解锁删 `src/mcp/`（dispatch.ts 286 行 + tool-defs.ts）。
- **dispatch.ts 确认 0 活实现引用**：头注释自称"only retained for src/commands/serve-http.ts"，但 **serve-http.ts 已不存在**（早被删）。Rust `zbrain-web/src/mcp.rs` 注释"Mirrors the TS MCP HTTP dispatch in serve-http.ts"，`build_mcp_router` 已 merge 进 web router，`ServeHttp` 由 Rust CLI 处理。→ dispatch.ts 运行时层确已被 Rust 替代，仅被 14 个测试钉死。
- **tool-defs.ts 不可整删**：还有活实现 `src/core/minions/tools/brain-allowlist.ts` import 其纯函数 `paramDefToSchema`（第29行）。
- **决定性核实（Explore agent，实读+grep）**：14 个 TS 测试里**仅 3 条**（#1 auth-permissions、#10 facts-visibility、#12 sourceId-required）Rust 有等价覆盖可安全退役；**11 条 Rust 覆盖缺失或部分缺失**。高危完全缺失 4 条隐私/安全不变量：**#11 summarizeMcpParams 日志脱敏（Rust 零实现零测试）、#8 _meta brain_hot_memory 注入（dispatch_tool_call 硬编码 meta:None）、#14 per-token takes-holder allowlist 过滤（字段只写不读，引擎无 list_takes/search_takes）、#13 takes-fence read redaction（fence 解析器未接入 GetPage 读路径）**。另 facts MCP op 层在 Rust 完全未移植（#3 forget_fact/#6 anti-loop dream marker/#7 put_page backstop/#9 facts ops 注册，extract_facts handler 是 not_implemented 桩）。
- **结论**：判断1「退役测试解锁删 src/mcp」的前提不成立——退役这 11 条测试 = 静默放弃 Rust 尚未实现的安全/隐私行为验证，是真实回归风险，**不是纯粹的测试退役而是实现缺口**。src/mcp 当前**仍不可删**。正确路径是先补 Rust 实现+测试（尤其 #11/#8/#14/#13），而非删测试。待与用户对齐：是把这些缺口转为 Rust 实现节点（在 KNOWN-GAPS 登记），还是跳过判断1 直接推进 Phase 10 eval 决策。

## G31-G35 登记 + Phase10/删除策略量化（决定性心智模型修正，commit 5a2f0c7）

- **G31-G35 已登记进 KNOWN-GAPS**（commit 5a2f0c7）：G31 summarizeMcpParams 日志脱敏(privacy,零实现)、G32 _meta brain_hot_memory 注入(blocked on G34)、G33 per-token takes-holder allowlist 过滤(死字段,安全)、G34 takes-fence 读遮蔽(未接 GetPage,安全)、G35 facts MCP op 层(extract_facts 是桩)。src/mcp/dispatch.ts 正式标记"暂不可删"。
- **量化实验推翻"eval 是删除主线大头"的假设**：用 ts_dep_closure.py 对比三组种子的闭包 D：
  - 含 eval 种子(当前 153 seeds)：D 极小
  - **去掉全部 eval 种子块**：D 只多释放 **10 个文件**（4 tests + 4 commands + 2 core/eval）
  - **只留 package 入口 `src/core/index.ts` 一个种子**（理论删除上限）：R 仍 = **1337 文件，D 仍只有 10**
- **决定性根因**：`package.json` `main: "src/core/index.ts"`（仅 4 行），第 2 行 `export { PostgresEngine } from './postgres-engine.ts'`——`postgres-engine.ts` 是 **4780 行完整 TS 后端**，import 21 个核心模块（engine/takes/migrate/schema/vector-index/search/cycle/anomaly/db/connection-manager...），**这一条 re-export 边把整个 src/core（1337 文件）钉进 R**。
- **心智模型彻底修正**：真正 pin 住海量已迁 impl 的**既不是 eval、也不是测试**，而是 **package 仍把完整 TS PostgresEngine 当公共 API 入口导出**。只要 `index.ts → postgres-engine.ts` 这条边在，src/core 几乎整体不可删——删 eval / 退役测试都只能释放边角（各 ~10 文件）。
- **正确删除主线（修正后）**：要真正削减 src/core，必须处理 **package 入口契约**——即 Phase 11 final cutover 的核心动作：把 `package.json` main/exports 从 TS `src/core/index.ts` 切走（或指向 Rust 产物/napi 绑定/彻底移除 JS 库入口，只保留 `bin: bin/zbrain-rs.js`），解除对 postgres-engine.ts 的公共 API pin。这是**一次性大 cutover**，不是渐进 slice。在此之前，渐进删除只能清理已证明的死代码残留（e643d86 已清 134 个）。
- **待用户决策**：(A) 直接规划 Phase 11 package 入口 cutover（切断 index.ts→postgres-engine 边，是解锁 src/core 整体删除的唯一钥匙）；(B) 补 G31-G35 的 Rust 实现（安全/隐私补齐，独立价值，但不解锁删除）；(C) 暂停删除线，转其他 roadmap 工作。

## Part8 Phase10 eval 处置 roadmap + Part9 Phase11 cutover roadmap（2026-07-15，grill+roadmap 驱动）

- **用户依次选：Phase10 定位=只决策不 port(1)、树=7 子系统节点(1)、7 子系统 P/K/D 认可、1-7 整体判 K(A)；Phase11 目标=彻底移除 JS 库身份(A)、节奏=分两刀先 cutover 再删(1)、1-1=纯配置 cutover typecheck 留作验证(1)。**
- **Part8 Phase10 roadmap（commit cfd3700）**：`docs/plans/zbrain-ts-to-rust-part8-phase10-eval-disposition.json` + 同名大写 md。7 eval 子系统处置全 completed：P=3（1-1 longmemeval 有 seam+CI job / 1-2 cross-modal 有 seam / **1-6 core/eval 共享层=唯一被生产 import，绑 G5 doctor 优先 port**）、K=4（1-3 contradictions 3040行 / 1-4 takes-quality / 1-5 code-retrieval / 1-7 CLI 命令层 16 文件全在 cli.ts 注册无死命令）、D=0。**净结论：eval 完全不阻塞 Phase11 cutover，删 eval 仅释放 ~10 文件**。
- **Part9 Phase11 roadmap（本轮建，pending 施工）**：`docs/plans/zbrain-ts-to-rust-part9-phase11-package-cutover.json` + md。4 个一级节点：
  - **1-1 package.json cutover（纯配置零删除）**：删 main + 整个 exports(20条) + openclaw.extensions(悬空) + scripts.postinstall(悬空) + scripts.dev；删 scripts/check-exports-count.sh(EXPECTED_COUNT=20 唯一硬 guard，配套 public-exports.test.ts 已不存在)+从 check:all 移调用；typecheck 保留降级非 gate；验证 cargo build 绿 + zbrain --help 冒烟不看 tsc。
  - **1-2 重算 KEEP 种子 + 闭包 D**：cutover 后新种子仅剩 KNOWN-GAPS spec + Phase10 判 K 的 eval + doctor 子系统。
  - **1-3 分批删 src/core 已迁 impl**：tsc 基线 diff 逐 slice 验证（复用 e643d86 手法）。
  - **1-4 final 收尾**：typecheck 脚本+tsconfig 处置 + CHANGELOG + 残留 TS 决策。
- **cutover 零消费者前提已核实（agent-78d088de）**：admin(name=gbrain-admin 不依赖 zbrain)/src/tests 无实 import(仅 2 处 JSDoc 注释)；未发布 npm(无 private/publishConfig/files，走 clawhub)；openclaw.extensions 指向的 src/openclaw-context-engine.ts 已在 e643d86 删除=悬空，实际 openclaw.plugin.json 走 bin/zbrain-rs.js serve-mcp。**package.json 有 5 个悬空引用**(openclaw-context-engine/postinstall/minions-index/transcription/backoff)。
- **决定性心智模型（务必记住）**：pin 住整个 src/core(1337 文件闭包)的是 `package.json main→src/core/index.ts→export PostgresEngine(4780行完整TS后端)` 这一条 re-export 边，不是 eval 也不是测试。切断它是解锁 src/core 删除的唯一钥匙 = Phase11 1-1。
- **Phase11 1-1 已执行完成（commit 6230c95，TDD GREEN）**：以 /zj-tdd 落地——先写 RED 断言脚本 `scripts/_phase11_cutover_assert.sh`（7 断言 A1-A7，cutover 前 6 FAIL），执行 package.json cutover（删 main + exports 20条 + scripts.dev/postinstall/check:exports-count + openclaw.extensions 悬空，check:all 移除 guard 引用，保留 bin.zbrain + openclaw.compat）+ `git rm scripts/check-exports-count.sh`，cutover 后断言 7/7 PASS。**回归护栏 = cargo build -p zbrain-cli 0.41s 绿 + zbrain --help exit=0**（证明改 package.json 未影响 Rust CLI），按 1-1 决策原文不看 tsc（基线本就 76 errors，delta 验证留 1-3）。断言脚本标记 throwaway，1-4 删除。**下一步 = 1-2：cutover 后重算 KEEP 种子 + 依赖闭包 D**（新种子仅剩 KNOWN-GAPS spec + Phase10 判 K 的 eval + doctor 子系统，理论删除上限已被量化为 D 从 10 跃升至海量 src/core）。
