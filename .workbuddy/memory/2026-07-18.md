# 2026-07-18

## check-update 命令迁移完成 (1-6-4-12 全 4 切片)

- slice 3 (网络层) + slice 4 (CLI 接线) 完成并提交 `b43497d`。
- `crates/zbrain-cli/src/update_check.rs`：
  - `fetch_latest_release`(GitHub releases API, UA `zbrain/{CARGO_PKG_VERSION}`, 10s timeout, 失败静默 None) + `fetch_changelog`(raw CHANGELOG) + `build_update_client()`(10s) + `CheckUpdateResult`(serde snake_case, error 用 `skip_serializing_if` 仅错误支出现) + `build_check_update_result()` 纯 builder + `run_check_update(json)` 编排。
  - 24 单测全绿 (slice1/2 的 18 + slice3 的 6: no_release error 形 / minor-bump / patch 不升级 / 去 v 前缀 / json 省略 error / json 含 error)。
- `lib.rs`：`Commands::CheckUpdate(CheckUpdateArgs)` + dispatch `update_check::run_check_update(args.json)`。
- 删 TS `src/commands/check-update.ts` + `src/cli.ts` dispatch/CLI_ONLY/SELF_HELP/help 清理 + 删 `tests/unit/check-update.test.ts` 与 `tests/unit/e2e/upgrade.test.ts`。
- PARITY_GATE 通过：`bun run typecheck` 绿 (64 inherited, 0 new)；二进制 E2E smoke `--help`/`--json`(沙箱无网→`error:"no_releases"`)/human 与 TS wire 对齐 (current_source 恒 `'package-json'`, 自动识别 Binary 安装)。
- 经验：clap 子命令参数 struct 用 `#[derive(Debug, Parser)]`（与 AnomaliesArgs 同款，lib 内 Parser derive 在 enum 字段场景满足约束）；`--help` 走 clap 自动，无需 TS 式手动拦截。
- 1-6-4-12 父节点 auto-completed。残留孤儿命令树仍活跃：1-6-4 下剩 1-6-4-5(done) 等；后续候选 check-resolvable(仍阻塞静态分析 core) / integrity G51 auto 借 resolver SDK。

## models 命令迁移完成 (1-6-4-13 全 4 切片)

- 新建 `crates/zbrain-cli/src/models.rs`（~900 行，27 单测全绿）。4 切片：1 只读路由表纯函数 / 2 doctor 纯函数(validate_embedding_dims + ProbeStatus) / 3 doctor 探测编排(AiProbeExecutor trait + LiveProbeExecutor，chat/embed/rerank 真客户端，fail-open) / 4 CLI 接线 + 删 TS。
- `Cargo.toml`(zbrain-cli)：启用 zbrain-core `anthropic`/`openai`/`google` feature（仅加 dep:reqwest，让 doctor chat 探测真可用）+ 加 `async-trait` dep（workspace key 是连字符 `async-trait`，曾误写下划线导致 E0432）。
- `lib.rs`：`Commands::Models(ModelsArgs)` + `ModelsMode`(clap::ValueEnum: Read/Doctor 作 positional，default read) + dispatch `models::run_models_command(args.mode, args.json, args.skip, cli.config.as_deref())`。
- **可复用洞察（ConfigLookup 来源）**：Rust 类型化 `Config` 无 `models.*` 命名空间，但主 `Config` 有 `#[serde(flatten)] extra: BTreeMap<String, serde_yaml::Value>`（config.rs:66）捕获所有未知键含 `models.*`。`config_to_lookup` 拍平 `extra` 成点号键喂给 `build_models_report`/`run_models_doctor`，等价于 TS `engine.getConfig()`。未来迁任何读 `getConfig` 覆盖的 TS 命令都用此手法。
- doctor live 探测：chat=`resolve_recipe_strict`+`instantiate_chat`；embedding=`EmbeddingClient`；reranker=`ZeroEntropyRerankClient::from_env()`（门控 `search.reranker_enabled`，Rust 无 `resolveSearchMode` → reranker 恒 `DEFAULT_RERANK_MODEL`，已知 fidelity gap，未登记 KNOWN-GAPS）。
- 删 TS `src/commands/models.ts`(646 行) + `tests/unit/models-doctor-reranker.test.ts` + `cli.ts` 清理（CLI_ONLY / CLI_ONLY_SELF_HELP / `case 'models'` dispatch 三处）。
- PARITY_GATE 通过：`bun run typecheck` 绿（0 new, 64 inherited）。E2E：--help / read(路由表) / --json / doctor(embedding_config ok + reranker_config ok + embedding_reachability fail-open) 全验证。
- 保真度细节：`validate_embedding_dims` 对无 `provider:` 前缀的本地模型（如默认 `all-minilm-l6-v2`）返回 None(ok)，对齐 TS（TS 仅 voyage/zeroentropy 做 dims 校验）；embedding_reachability 因 Rust 无本地 embedding 客户端而 fail-open（正常降级，不崩）。

## 下一步规划 grill（zj-grill-me + zj-roadmap-driven 双 skill）

用户问"接下来做什么"，用 grill 逐层烤 + roadmap 落盘，最终锁定 `apply-migrations` 为下一刀（roadmap 节点 1-6-4-14，3 切片，已建 + 4 决策已记）。

**关键探查结论（改变难度判断）**：
- 剩余孤儿命令（recall/forget/providers/check-resolvable/code-intel/extract-conversation-facts/brainstorm/auth/migrate-engine）**全是硬 port，没有干净快刀了**。code-intel 依赖 Rust 没有的 ~2900 行静态分析核心；extract-conversation-facts 的 Rust handler 仅 50 行 stub，1115 行逻辑在 TS；recall/forget/providers/auth 各纠缠 thin-client/ai-recipes/web-admin。
- **Rust 已有完整迁移 machinery**：`libsql.rs:154 LIBQL_MIGRATIONS` + `postgres.rs:237 POSTGRES_MIGRATIONS`，都在 `init_schema()` 连接时自动迁移。`MigrationRegistry` 公开 `latest_version()`/`len()`/`iter()`(按版本升序)/`add()`。
- **决定性发现**：`src/core/migrate.ts:12` 注释 "100% to engine.initSchema(). All migration decision logic lives in Rust." —— TS 的 `runMigrations` 早已直接委托 Rust `initSchema`。`apply-migrations.ts:335/346` 的 apply 路径也是 `runMigrations(eng)`。那 20 个 `src/commands/migrations/*.ts` 文件（v0_11_0→v0_32_2）**现在只用于 `--list` 展示 + `--force-retry` 查找，不参与 apply**。
- → `apply-migrations` 在 Rust 里是**薄 CLI 壳**（罩现有 registry + init_schema），根本不用搬那 20 个 TS 文件。是剩余孤儿里最接近"快刀"的。
- `migrate-engine` 与 schema 迁移**无关**：实为脑间数据迁移工具（copy pages 源→目标 engine，处理 slug 碰撞）。独立未来节点，不打包。

**已定决策（roadmap 1-6-4-14）**：
- Q2 下一命令 = apply-migrations；Q3 范围 = 薄 CLI 罩 Rust registry + `--list` 保真 TS 20 命名版本（展示元数据），apply 委托 init_schema，不建平行 apply 系统；Q4 migrate-engine 不打包；`--force-*` 因 Rust init_schema 单事务全有/全无（无 TS 式半途卡死）降级为"强制重跑 init_schema"/no-op，并文档说明偏差。
- **已知 parity gap（待登记 KNOWN-GAPS）**：Rust `init_schema` 可能不含 TS 的数据回填类迁移（如 v0_18_0-storage-backfill）。因选"保真输出不建第二套 apply"（非"增强 Rust registry"），这些 data backfill 在 Rust 不跑。fresh brain 无影响；老 brain 升级路径需注意。
- 切片规划：1-6-4-14-1 展示元数据+纯函数（静态 `TS_LEGACY_MIGRATIONS` 列表 + 格式化，TDD）/ 1-6-4-14-2 引擎集成（读已应用 schema 版本 + init_schema 委托，可能加 `BrainEngine::schema_version()` trait 方法两后端）/ 1-6-4-14-3 CLI 接线 + 删 `apply-migrations.ts` + cli.ts 清理 + PARITY_GATE + E2E(--help/--list/--dry-run/--yes)。

## apply-migrations 迁移反转 → C' 务实重建 orchestrator 框架 (1-6-4-14)

- **重大探查反转（推翻前一轮 Q2/Q3）**：apply-migrations 实际是 **upgrade orchestrator 系统**（`src/commands/migrations/*.ts` 的 15 个 `Migration` 带 `featurePitch`+`orchestrator`，写 `completed_migrations` 表），**不是 DB schema 迁移**。Rust 的 `LIBQL_MIGRATIONS`/`POSTGRES_MIGRATIONS` 是 schema DDL（`init_schema`），两者**不对应**。Rust 无任何 upgrade orchestrator 系统。
- grill 重议链：Q2 选 apply-migrations（误判为快刀）→ 发现前提崩（Rust 无 orchestrator 对应物）→ Q3 拟"薄 CLI 罩 Rust registry"亦错（只覆盖 `--force-schema` 子路径）→ 用户选 C 字面完整重建 → 证据（explore 测绘 14/15 是 old-brain-only 升级胶水、项目无线上用户 memory 铁律、Rust `zbrain init` 已 10+ 处调 init_schema 覆盖 schema 阶段）→ 收敛为 **C' 务实重建**：完整框架 + `completed.jsonl` 状态 + 15 声明(`--list` 完整) + `v0_11_0` 真实实现 + 其余 14 fresh-brain 自动 complete 薄壳。roadmap 1-6-4-14 + 方向决策已更新渲染。
- **状态机制真相**：TS 用 `~/.zbrain/migrations/completed.jsonl`（JSONL 追加文件，非 libsql 表），idempotency guard（complete 后重复 complete 跳过），`MAX_CONSECUTIVE_PARTIALS=3` wedged 判定。Rust 用 `config::zbrain_home()`（honor ZBRAIN_HOME）拿根。
- **slice 1 完成（12 测绿）**：`crates/zbrain-cli/src/apply_migrations.rs` = `ORCHESTRATORS`(15 静态声明, version+headline+category) + `CompletedMigrationEntry` + `load/append_completed_migration`(JSONL+idempotency+malformed skip) + `compare_versions` + `status_for_version`(complete wins/wedged) + `build_plan`(applied/partial/pending/future/wedged) + `format_list/format_dry_run`（对齐 TS `printList`/`printDryRun`）。`lib.rs` 已 `pub mod apply_migrations;`。
- **slice 2 探查（已定边界）**：v0_11_0 的 4 能力——autopilot install(Rust `zbrain autopilot --install` 已有) + minion_mode(Rust `autopilot/runner.rs` 有概念)；**AGENTS.md 注入 + cron `jobs.json` 重写 Rust 无等价**（grep 仅在注释出现）。进一步确认：Rust 的 `zbrain autopilot --install` **只 generate 配置（plist/unit/crontab/wrapper）并打印，零真实 I/O**（daemon.rs 只有 `generate_*` 函数，无 write/install/persist）；Rust **无** `preferences.json`/`AGENTS.md`/`cron-jobs.json` 写模块（TS 有 savePreferences/loadPreferences + AGENTS.md 注入 + cron 重写，Rust 完全没有）；minion_mode 只是 runner.rs 里"从 preferences 读"的概念，无写 API。
- **slice 2 完成（6 测绿，apply_migrations 共 18 passed）**：`OrchestratorRunOpts` + `OrchestratorRunResult` + `run_orchestrator(decl, opts)` + `run_apply_migrations(entries, installed, opts, ledger_path)`（mirror TS run loop：pending+partial 顺序跑，append complete，dry_run 不写）。C' 务实落地：v0_11_0 真实跑 = 调 `daemon::detect_install_target()`+`generate_wrapper_script`+对应 `generate_*` 生成配置并记录 `autopilot_installed=true`（Rust 能做的真实部分）；其余 3 个 host-touching phase（minion_mode 写/AGENTS.md/cron）因 Rust 无机制而**有意 out-of-scope**（Rust autopilot daemon 自管）；其余 14 orchestrator = fresh-brain 自动 complete 薄壳（fresh brain 已在目标状态，old-brain-only 升级胶水，项目无老 brain）。roadmap 1-6-4-14-2 completed。
- **slice 3 完成（apply-migrations 节点 1-6-4-14 全 completed，roadmap 已 render）**：CLI 接线 `Commands::ApplyMigrations(ApplyMigrationsArgs)` + `run_apply_migrations_command`(async, ledger-only 路径不连 engine；`--force-schema`/`--force-all` 才 `load_config`+`LibsqlEngine::init_schema`)。`installed` 版本用 `registry_ceiling_version()`(=0.32.2，ORCHESTRATORS 最大值) 锚定——因 Rust `CARGO_PKG_VERSION`(0.0.1) 对该历史 registry 无意义，fresh install 视为全部 applicable（无 future）。v0_11_0 orchestrator 的 `generate_wrapper_script` 仅返回字符串不写文件，故 `--yes` 只写 ledger 不污染 cwd。
- **TS 删除策略（关键反转）**：探查发现 `src/commands/migrations/index.ts` 是**共享 registry**，被 `upgrade.ts`(读 `migrations[]` 做 feature pitch) + `thin-client-upgrade-prompt.ts`(用 `compareVersions`) 依赖。故**不能整删 migrations/ 目录**。改为：删 15 个 `v0_*.ts` orchestrator + `apply-migrations.ts`；把 `index.ts` 瘦身为**纯元数据 registry**（`MigrationMeta{version,featurePitch}` + `migrations` 数组 + `compareVersions`/`getMigration`，去掉 `orchestrator` 字段）；`upgrade.ts` 的 `runApplyMigrations(['--yes'])` 改为 `execFileSync('zbrain', ['apply-migrations','--yes','--non-interactive'], {stdio:'inherit'})`；`cli.ts` 删 dispatch 块 + THIN_CLIENT_REFUSED 条目 + hint。`src/core/migrate.ts` 自有的 `Migration` 接口(空 `MIGRATIONS: Migration[]`)与 orchestrator `Migration` 完全无关，不受影响。
- **PARITY_GATE 通过**：`bash scripts/typecheck-baseline.sh` → "no new errors above baseline (64 inherited)"。删 19 个孤儿 TS 测试（`apply-migrations.test.ts`/`migration-resume.test.ts`/`migrations-registry.test.ts`/`migrations-v0_*.test.ts`/`migration-orchestrator-v0_*.test.ts`/`storage-backfill.test.ts`/`e2e/{frontmatter-migration,migration-flow,multi-source}.test.ts`）—— 它们 import 了已删的 `v0_*.ts`/`apply-migrations.ts`，Rust 侧 19 单测已覆盖等价逻辑。另删 `migrate.test.ts` 里读 `apply-migrations.ts` 源码的过时 describe 块（PR #356 预检）。
- **E2E 全绿（隔离 ZBRAIN_HOME）**：`--help`/`--list`(15 pending 展示完整 headline)/`--dry-run`/`--yes`(写 15 条 complete)/幂等重跑("All migrations up to date")/`--force-orchestrator`(对 wedged 写 retry 后 --yes 恢复)。注意：Windows 二进制把 `ZBRAIN_HOME=/tmp/x` 解析为 `C:\tmp\x`，Git-bash 的 `/tmp` 不同，但均临时目录，真实 `~/.zbrain` 未被污染。
- **apply_migrations.rs 现状**：19 单测（18 + registry_ceiling 1）。整个模块 = 纯状态/计划表面 + CLI 接线；v0_11_0 真实(autopilot config 生成) + 14 fresh-brain 薄壳。C' 务实重建收口。
