# 2026-07-09

- **1-2-2 progress reporter 提交（95c0c03）**：cli disagrosis 完 + clippy 修复（6处）+ KNOWN-GAPS G14-G20 登记 + roadmap 标 completed。
- **1-3 TS 入口退役 completed（9c2fa24）**：Grill Q1-Q4 全 A。4 动作：(1) package.json 删 `zbrain-ts` bin；(2) postinstall.ts 重写为纯 Rust 路径(cargo build + init --migrate-only)，TS fallback 全删；(3) check-cli-executable.sh 检查目标 `src/cli.ts` → `bin/zbrain-rs.js`+修复 mode bit 100644→100755；(4) KNOWN-GAPS 登记 G21(apply-migrations 未迁)。`src/cli.ts`/`src/commands/` 代码不动。验证：build clean + core 654 + cli 125 全绿。
- **#484 REFACTOR + #108 验证 completed**：`cargo test --workspace` 全量通过。chunking 50/50、cli 125/125、core lib 659/659、所有集成测试(in_memory/libsql/postgres/oauth/token/engine/page_methods) 全绿。唯一失败 `page_methods_update_cr_state` 是 Windows 权限问题(os error 740)，与 #108 无关。Roadmap Part4 Phase 6 全部节点标记 completed (1→1-2→1-2-2)。

- **Phase 7A (1-1-1~1-1-7) 全部 completed**：
  - 1-1-1~1-1-5 由上一轮已完成 (DB migration, types, fence parser, trait methods, 3 backend impls)
  - 1-1-6: salience wiring — 验证发现实际上在 slice 6c-takes-salience 已完成。Libsql: LEFT JOIN takes + Rust ln(1+N); Postgres: SQL ln(1+COUNT); InMemory: ln(1+distinct_takes)。plan 中的 red test 从未创建，现有 7+6 个 salience 测试全通过
  - 1-1-7: 新建 `takes_crud.rs` — 19 个测试 (13 InMemory + 6 Libsql) 覆盖 round-trip、weight clamping、resolve、scorecard (empty/single/multiple/holder-filter/unresolved)
  - Bugfix: libsql + postgres `resolve_take` not-found 错误统一为 `not_found` code (之前是 `engine`)
  - Roadmap Part5 标记 1-1-1~1-1-7 + 1-1 全部 completed
  - 下一步: 1-2 (7B: Backlinks + Facts) 或 1-3 (7C: Graph + Salience 收尾 + CLI)

- **1-2-4: Links integration tests completed**：
  - 新建 `crates/zbrain-core/tests/links_crud.rs` — 26 个测试 (16 InMemory + 10 Libsql)
  - 覆盖: add_links_batch (round-trip/dup suppression/link_type distinct/origin fields/source_id filter), remove_link (basic/nonexistent noop/link_source filter), get_links (deleted page filter), get_backlinks (symmetry/multi-source), get_backlink_counts (zero-count slugs), traverse_paths (BFS/depth limit/direction in/link_type filter)
  - 3处 bugfix:
    1. InMemory add_links_batch 重复检测：None link_source 未 normalize 为 "markdown"，导致 duplicate suppression 失败 → 修复 normalize 逻辑
    2. InMemory traverse_paths direction="in"：from_slug/to_slug 被 swap 了 → 修复为始终保留原始边方向
    3. libsql link_source CHECK constraint 只允许 {NULL, markdown, frontmatter, manual, mentions} → 测试改用有效值
  - Postgres 测试推迟到 Phase 7C (pg-embed 并行化)
  - 全量回归: 691 lib + 26 links = 717 全绿
  - Roadmap: 1-2-4 + 1-2 全部子节点 completed; 1-2 仍 in_progress (facts 部分未做)
  - 下一步: 1-2 的 Facts 部分 (新表 migration, FactRow 类型, fence parser, CRUD)

- **1-2-5: Facts DB migration completed**
  - 新建 `crates/zbrain-core/migrations/0013_facts.sql` (PostgreSQL) — 27 列，5 索引含 `expired_at IS NULL` partial index
  - 新建 `crates/zbrain-core/migrations-sqlite/0013_facts.sql` (SQLite) — TEXT 时间戳，REAL embedding (JSON)
  - 两个引擎 migration registry 均注册 v13

- **1-2-6: Facts Rust types completed**
  - `types.rs` 新增 8 类型: FactKind (enum), FactVisibility (enum), FactInsertStatus (enum), FactRow (19 fields), NewFact (17 fields), FactsHealth (7 fields), EntityCount (2 fields), FactListOpts (5 filter fields)
  - `lib.rs` re-export 全部类型

- **1-2-7: Facts fence parser/renderer completed**
  - 新建 `crates/zbrain-core/src/facts_fence.rs` — 36 个单元测试全绿
  - 实现: parse_facts_fence (14 列支持), render_facts_fence (10→14 列自适应), upsert_fact_row (append-only), strip_facts_fence (全量/visibility 过滤双模式)
  - Strikethrough 语义: `~~claim~~` → active=false + superseded_by/forgotten 自动解析
  - 类型: FenceFact (18 fields), FactsParseResult, FenceFactInput, UpsertFactResult, StripFactsFenceOpts
  - FactVisibility 新增 Display impl (types.rs)
  - 全量回归: 691 lib + 36 facts = 727 全绿
  - 下一步: 1-2-8 (BrainEngine trait facts methods) + 1-2-9 (3 backend CRUD) + 1-2-10 (integration tests)

- **1-2-9: Backend facts CRUD completed (libsql + postgres)**
  - libsql: 4 方法 — insert_fact (transaction API, 查重→supersede→INSERT→UPDATE), list_facts_by_entity (动态 SQL + positional params), get_facts_health (5 COUNT + top-10 entities), expire_fact (UPDATE WHERE expired_at IS NULL)
  - postgres: 4 方法 — insert_fact (单条 CTE: dup_check→supersede_target→inserted→updated→SELECT status), list_facts_by_entity (QueryBuilder), get_facts_health (query_scalar), expire_fact (直接 execute)
  - 各一个 row mapping helper: `row_to_fact` (libsql), `pg_row_to_fact` (postgres)
  - extras: InMemoryEngine valid_from 默认 now() 对齐 DB NOT NULL 约束
  - 编译通过，零新警告
  - Roadmap: 1-2-9 marked completed
- **1-2-10: Facts integration tests completed + 1-2 Phase 7B done**
  - 新建 `crates/zbrain-core/tests/facts_crud.rs` — 35 个测试 (17 InMemory + 18 Libsql)
  - 覆盖: insert roundtrip, duplicate detection, supersede (high confidence > 0.9 / low confidence no-op), list filters (active_only/kinds/visibility/limit/offset), health (counts/top entities/empty source), expire (basic/double/wrong source/nonexistent), persistence (disconnect/reconnect, supersede survives)
  - Bugfix: libsql `list_facts_by_entity` 将 `?NNN` 占位符改为 `?` — 原 `?NNN` 在 multi-param 场景下行为不确定（single-kind 正常但 multi-kinds 过滤错误）
  - Bugfix: 测试中低置信度事实被意外 supersede — 原因是 same kind + default conf(1.0)>0.9。修复：非 supersede 测试用不同 kind
  - libsql 并行 segfault 是已知问题，测试用 `--test-threads=1` 稳定
  - 全量回归: 735 lib + 35 facts = 770 全绿
  - Roadmap: 1-2-10 completed; 1-2 (Phase 7B: Backlinks + Facts) 所有 10 子节点全部完成，父节点标记 completed
  - 下一步: 1-3 (Phase 7C: Graph + Salience 收尾 + CLI 接线)

- **1-3-1: traverse_paths libsql + postgres 实现 completed**
  - libsql.rs: 替换 Unsupported stub → fetch pages+links 到内存，BFS (与 InMemory 同构)
  - postgres.rs: 同样模式，sqlx query + BFS
  - 8 个新 libsql traverse 测试 (adjacency_boosts.rs)，全部通过
  - links_crud.rs: `libsql_traverse_paths_returns_unsupported` → `libsql_traverse_paths_basic_bfs`（正向 BFS 测试）
  - libsql_migration_registry_red.rs: 修复 3 个 stale 测试（10→12 migrations, latest v10→v13）— Phase 7B facts 迁移引入的预存问题
  - 全量回归: 735 lib + 各集成测试 全绿。adjacency_boosts 34 测试全跑时有预存 STATUS_ACCESS_VIOLATION，分片运行正常
  - Roadmap: 1-3-1 marked completed
  - 下一步: 1-3-2 (Salience 方法) 或 1-3-3 (CLI 接线)

- **1-3-2: Salience 方法 completed**
  - Grill Q1-Q6 全 A。设计决策:
    1. touch_salience 独立 trait 方法: `async fn touch_salience(&self, slug: &str, source_id: &str) -> Result<bool>`，返回 true=页面存在已 bump
    2. get_recent_salience 新建 SalienceResult struct (9 字段，独立于 Page)
    3. 公式统一在 Rust 侧计算 (libsql 无 ln() SQL 函数)
    4. 不暴露 recency_bias 参数，硬编码 flat 模式 `1.0/(1.0+days_old)`
    5. boundary 在 Rust 算，SQL 用 CASE 替代 GREATEST()
  - types.rs: 新增 SalienceResult struct (derive Serialize, camelCase)
  - engine.rs trait: 新增 touch_salience + get_recent_salience (2 default + 2 InMemory impl)
  - libsql.rs: touch_salience (UPDATE + execute rows_affected) + get_recent_salience (SQL JOIN takes + Rust ln/decay 计算)
  - postgres.rs: touch_salience (sqlx bind) + get_recent_salience (PG native ln/exact + 动态 LIKE prefix)
  - 新建 `tests/salience_methods.rs` — 18 个测试 (9 InMemory + 9 Libsql) 全绿
  - 覆盖: touch bumps/sets/false-missing, get_recent empty/limit/slug_prefix/sort/field-correctness/touch-affects-window
  - 全量回归: 等待中 (cargo test -p zbrain-core 在后台跑)
  - Roadmap: 1-3-2 marked completed
  - 下一步: 1-3-3 (CLI 接线 — facts/links/takes 命令)

- **1-3-2 全量回归确认**: 735 passed, 0 failed — 所有 tests pass (含 18 新 salience_methods tests)

- **1-3-3: CLI 接线 completed**
  - Commands 枚举新增 3 变体: Facts(FactsAction), Links(LinksAction), Takes(TakesAction)
  - 实际交付范围 > 原始决策 (10 命令 > 6):
    - facts: add, list, health, expire (4)
    - links: add, list, backlinks, rm (4)
    - takes: add, list (2)
  - 模式: 所有命令用直接 LibsqlEngine 实例化 (同 Sources)，parse helpers 解析 FactKind/FactVisibility 枚举
  - takes add/list: 通过 get_page 解析 slug→page_id
  - 输出格式: --json 输出 JSON，否则人类可读文本
  - Bugfix: short option 冲突 — claim 移除 short (-c 被全局 --config 占用)，TakesAddArgs source 移除 short (-s 与 slug 冲突)
  - 125 CLI tests pass + 735 core tests pass
  - Roadmap: 1-3-3 marked completed
  - 下一步: 1-3-4 (CLI 查询命令 — salience/orphans/backlinks/graph-query)

- **1-3-4: CLI 查询命令 completed**
  - Grill Q1/Q2/Q4 全 A。设计决策:
    1. backlinks 复用 1-3-3 的 `zbrain links backlinks`，1-3-4 只做 salience/orphans/graph-query 三个
    2. salience 三参数全暴露: `--days 7 --limit 50 --prefix`
    3. graph-query 5 参数: `slug [--depth 1] [--link-type] [--direction out] [--source]`
  - 三个新命令 + 3 个 arg structs + 3 个 dispatch + 3 个 impl 函数
  - 所有命令用直接 LibsqlEngine 实例化模式，--json flag 切换输出格式
  - 125 CLI tests pass。core 回归测试后台运行中。
  - Roadmap: 1-3-4 marked completed
  - Phase 7C 只剩 1-3-5 (Postgres 集成测试补全)
  - 下一步: 1-3-5

- **1-3-5: Postgres 集成测试补全 completed → Phase 7 全部完成**
  - Grill Q1-Q3: 严格 links/facts/takes 三域 (~15 PG 测试)，salience/orphans/graph-query 不测；facts 复用 `&dyn BrainEngine` 合约函数，links/takes 新写测试体；PG 强制 FK 需 seed sources/pages
  - 交付 12 个新 PG 测试：facts 5 (复用合约: insert/supersede/list_active/health/expire) + links 4 (add+get/backlinks/remove/traverse) + takes 3 (roundtrip/resolve/empty)
  - 模式: `PgFixture::start()` 起隔离 pg-embed；side-channel `sqlx::PgPool`(fix.url) seed sources + 解析真实 page_id
  - **PG mirror 捕获 3 个真实 postgres.rs bug (SQLite 无类型/不强制约束掩盖了它们)**:
    1. `insert_fact`/`expire_fact`/`get_facts_health` 把 text bind 到 timestamptz 列 → SQL 层加 `$N::timestamptz` cast；valid_until 空串 `.unwrap_or("")` 改 `.as_deref()` (NULL)
    2. `list_facts_by_entity` 读 timestamptz 列成 String 失败 → SELECT 加 `col::text AS`
    3. `superseded_by`/`consolidated_into` 是 INT4 但 FactRow 读成 i64 → SELECT 加 `col::bigint AS`
  - **schema 分歧登记 KNOWN-GAPS G22**: PG takes 有 `CHECK(row_num>0)` (对齐 TS 1-based `#row_num`)，libsql 无 CHECK + `unwrap_or(0)` 默认值语义错。PG 测试必须用 row_num≥1。takes_crud.rs PG 段加就近注释 + 双向指针
  - 全量回归确认无回归: core 735 lib + facts 40 + links 30 + takes 22 全绿。`libsql_engine_migrations` 多线程报 STATUS_ACCESS_VIOLATION 是 Windows libsql native crash (单线程复跑 6/6 全绿)，与本刀无关
  - Roadmap: 1-3-5 → 1-3 → Phase 7 (节点 1) 级联全部 completed。**Phase 7 (Facts/Takes/Timeline/Salience/Graph) 全部完成**
  - 关键经验: PG mirror 测试的价值 = PG 强类型 + 约束强制能捕获 SQLite 无类型环境掩盖的真实 bug (本刀实证 4 个)

- **Part4 Phase 6 "Search/Retrieval 生产后端复活" (roadmap 1-3, 4 子节点) 全部完成**：
  - 背景（grill Q1-Q5 全 A 确认的"下一刀"）：生产后端 `search_pages` 是 trait default 空实现 (`engine.rs:1067` 返回 `Ok(Vec::new())`)，只有 InMemory 有真实检索。CLI `query` 走 LibsqlEngine → 返回空。生产搜索整体是死的。
  - **1-3-1**：抽后端无关 `fuse_and_boost(engine: &dyn BrainEngine, candidates: &[Page], opts) -> Vec<SearchResult>` helper (engine.rs)，迁 lexical打分+vector cosine+RRF+snippet+salience/recency boost+sort/limit。InMemory `search_pages` 改薄委托（先在 lock 内 clone 候选，drop MutexGuard 后再 await helper，规避非-Send guard）。10 个现有测试证明行为零漂移。
  - **1-3-2**：LibsqlEngine::search_pages 真实实现 (libsql.rs:1187)：30 列投影 SQL 拉候选 (`WHERE deleted_at IS NULL AND (?1 IS NULL OR source_id = ?1)`) → full_row_to_page → 调 fuse_and_boost，与 InMemory 共用融合代码。不引入 FTS5/sqlite-vec（过度工程）。新建 `tests/libsql_engine_search_pages.rs` 7 测试（lexical title/content、no-match、source scoping、soft-delete、limit、vector 优雅降级），`--test-threads=1` 全绿（Windows libsql native crash + FK 约束需 seed source）。
  - **1-3-3**：query embedding 接线，照抄 rerank 先例。`EmbeddingClient::from_env()` (embedding.rs, feature-gated，读 `ZEROENTROPY_API_KEY` 缺则软关闭) + `OperationContext.embedding` 字段 + `with_embedding` builder + QueryOperation embed_query fail-open (失败降级 lexical-only)。CLI 门控 `config.search.hybrid_search` + Cargo.toml 加 `embedding` feature。3 新测试，17 query 测试全绿。
  - **1-3-4**：验证 + KNOWN-GAPS 登记。全量回归全绿 (InMemory 10 + libsql 7 + query 17 + CLI check 干净)。新登记 3 缺口 + 代码锚点双向指针：
    - **G23**：postgres 后端 `search_pages` 仍落 trait default 空实现（`engine.rs:1067` + postgres.rs 无 override）。推荐路径：照 1-3-2 给 PG 实现 SQL 拉候选 + fuse_and_boost。
    - **G24**：libsql `put_page` INSERT 列清单无 embedding (`libsql.rs:863`) → `pages.embedding` 恒 NULL，向量路对 libsql 恒降级 lexical-only。query embedding 接线只对已有 embedding 的 page 生效，但无写入口产生它。
    - **G25**：`import_from_content` doc 承诺"生成 embeddings"但 chunk `embedding: None` 硬编码 (`import.rs:61/83`)，无 embedding_client 参数。与 G24 互补（page 级 vs chunk 级）。
  - Roadmap: 1-3-1~1-3-4 → 1-3 → Part4 Phase 6 (节点 1) 级联全部 completed，stats 10/10 全完成，render 到 `.workbuddy/roadmaps/ZJ_ROADMAP.md`。
  - 关键设计判断：search 拆"后端相关半（拿候选 Page）+ 后端无关半（融合 helper）"，后者只依赖 `&[Page]`+`&dyn BrainEngine`，三后端零重复共用。embedding 只做 query 路 plumbing（索引路是 G24/G25 待补），vector 路优雅降级保生产安全。

- **规划 Phase 8 + Phase 9 roadmap（grill + explore/exploit）**：
  - 现状盘点：5 个 part roadmap（Part1-5）全部 100% 完成，覆盖 PRD Phase 1-7。剩余未规划 = Phase 8（AI Gateway）、9（Jobs/Minions）、10（Evals）、11（Final Cutover/TS 物理退役）。566 个 TS 文件仍在（已迁域靠 `UNMIGRATED_TS_*` 锚点留痕，物理退役是 Phase 11 的活）。
  - 用户选择：同时启动 Phase 8 + Phase 9 规划。
  - **依赖方向（代码证据，非偏好）**：minions → ai gateway 单向依赖无循环。`minions/handlers/subagent.ts:52` import `gateway.toolLoop`、`:823` 调用；`embed-backfill.ts:36` import `withBudgetTracker`。反向 ai/ 对 minions 只有注释无 import。**Phase 8 是被依赖方，必须先迁**。
  - **关键结构判断**：TS `gateway.ts`(3008 行) 无自定义 Provider interface，靠 recipe 数据 + Vercel `ai` SDK 统一调用（配置驱动）。Rust 无 SDK 等价物 → 落成 `ChatProvider` trait 多态；`rerank_client.rs`(RerankClient trait + 真 HTTP + fail-open) 是最成熟先例。jobs = MinionQueue 的 thin CLI wrapper，非独立子系统。think 域是 gateway 消费者，不属 Phase 8（G1 独立）。
  - **组织**：延续每 phase 一个 part 文件。
    - **Part6 = Phase 8**（exploit，`zbrain-ts-to-rust-part6-phase8-ai-gateway.json`）：6 刀 = 1-1 数据层(registry/capabilities/pricing 仿 embedding_pricing.rs static 表) → 1-2 model-resolver+routing → 1-3 ChatProvider trait → 1-4 chat 单调用 → 1-5 toolLoop(最重) → 1-6 embed/rerank/expand 收口(可并行)。焦点设 1-1 in_progress。
    - **Part7 = Phase 9**（explore + blocked，`zbrain-ts-to-rust-part7-phase9-jobs-minions.json`）：8 子域骨架 = MinionQueue / Worker+supervisor / Budget+rate-leases / handlers+tools / Autopilot+fanout / Remote / jobs-agent CLI / G7 收口(webhook 接 MinionQueue)。blocked 于 Phase 8 chat+toolLoop 落地；子域 slice 待开工前再 grill 细拆。
  - 两份都 render 到 `.workbuddy/roadmaps/ZJ_ROADMAP.md`。**坑：`roadmap_cli link` 存相对路径会导致 render 时相对 json 目录解析失败（FileNotFoundError），必须传绝对路径 md-file**。
  - 下一步：开始 Part6 刀1（Phase 8 数据层：model registry + capabilities + pricing static 表）。
- **Part6 Phase 8 刀1（1-1: Model registry 数据层）completed**：grill 4 决策全落盘 →TDD 实施。
  - **4 决策**：Q1 只迁 Recipe 纯数据(hook 留刀3)；Q2 落点 `crates/zbrain-core/src/ai/` 新子模块，吸收孤儿 `embedding_pricing.rs` 不留碎片；Q3 嵌套保真 provider-centric(`Recipe{touchpoints:{chat/embedding/reranker/expansion:Option<..>}}`)；Q4 全迁 17 条(3 native+14 openai-compat)+contract test 兜底。
  - **关键探索发现**：`embedding_pricing.rs` 是**孤儿模块**——grep 全仓除自身测试+lib.rs pub mod 声明外**零外部调用点**，且是新 registry 真子集(仅 model/provider/price/dims)。→零成本吸收取代，改 lookup_pricing 从 REGISTRY embedding touchpoint 派生，单一数据源无漂移。
  - **产出**：`ai/types.rs`(207行 Recipe/Touchpoints/4 Touchpoint 结构 + Implementation/Tier 枚举)、`ai/registry.rs`(706行 REGISTRY 17 条 static)、`ai/mod.rs`(resolve_recipe by-provider + parse_model_id split_once + resolve_alias + lookup_pricing/estimate_cost_usd 派生 + 16 contract/查询/pricing 测试)。删 `embedding_pricing.rs`，lib.rs `pub mod embedding_pricing`→`pub mod ai`。
  - **验证**：`ai::` 16/16 全绿(--test-threads=1)，`cargo build -p zbrain-core` clean 无残留引用 error。Task #543 完成。
  - **TS 保留**：本刀是"Rust 建新+数据迁移"，TS gateway 仍消费 TS recipes，物理退役挂 Phase 11。无需 UNMIGRATED_TS_ 锚点(纯 additive)。
  - **下一刀**：1-2 model-resolver+routing(依赖数据层，已就绪)。
- **Part6 Phase 8 刀2（1-2: model-resolver 纯静态层）completed**：
  - **关键探索**："routing" 一词横跨两文件：`model-resolver.ts`(123行, 4 纯函数 parseModelId/resolveRecipe/assertTouchpoint/knownProviderIds, **零 engine 耦合**, extendedModels 是调用方构造传入的 Set) vs `model-config.ts`(325行 resolveModel/TIER_DEFAULTS/enforceSubagentCapable/enforceSubagentAnthropic, **async + 读 BrainEngine config + capability 门控**)。
  - **grill Q1 决策（用户"同意"）**：刀2 只迁纯静态 resolver 层；tier-routing(model-config.ts)天然属 chat 消费侧(刀4 才真需按 tier 选 model)，async+DB 耦合+capability 分类会污染纯数据层 → 切出 sub-node **1-2-1**(explore, pending, 不级联根节点)跟踪。
  - **产出**：`ai/resolver.rs` — `AiConfigError{message,fix:Option}`+Display+std::error::Error(仿 rerank_client 错误风格)、`ParsedModelId{provider_id(小写),model_id(保留大小写)}`、`parse_model_id_strict`(fail-fast 带 hint)、`resolve_recipe_strict`(unknown provider 列出 known)、`assert_touchpoint(recipe,kind,model_id,extended_models:Option<&BTreeSet>)`(touchpoint 缺失/native 未列出 fail-fast；extended_models 逃生舱；openai-compat/空 models 接受任意 model id)、`known_provider_ids`(sorted)。15 测试(parse 4+resolve 3+assert 6+known 1+display 1)。
  - **验证**：`cargo test -p zbrain-core --lib ai:: -- --test-threads=1` → 31 passed(16 旧 registry+15 新 resolver) 0 failed；`cargo build -p zbrain-core` clean(仅既有 SCHEMA_VERSION/RerankError 未用 warning)。roadmap 1-2 completed(8 节点 2 completed/1 in_progress/5 pending，1-2-1 保持 pending 未级联)。Task #544 完成。
  - **TS 保留**：纯 additive，TS model-resolver.ts 仍在，物理退役挂 Phase 11。
  - **下一刀**：1-3 ChatProvider trait 抽象(扩 llm.rs LlmClient 为含 chat 的 trait，复用 rerank_client fail-open 模式)。sub-node 1-2-1(tier-routing)待 chat+capability 层落地后解 explore。
- **Part6 Phase 8 刀3（1-3: ChatProvider trait + 类型全保真 + OpenAI HTTP）completed**：
  - **grill 3 决策（用户全拍）**：Q1 **新建独立 ChatProvider trait**(core/ai/chat.rs)而非 roadmap 原文的"扩 LlmClient"——探索发现 `llm.rs::LlmClient` 是 Think 专用单发(system+user+context→纯 string)，与 chat 多轮 messages+tool-call+blocks 结构根本不兼容，硬扩违背 deep module；llm.rs 不动。Q2 范围**含真 OpenAI HTTP 实现**(照 llm.rs OpenAiClient 扩 /chat/completions)，即刀3 **吸收原 1-4 的 chat 单调用 HTTP**；1-4 收窄为 anthropic/google native + BudgetTracker 接线。Q3 **完全保真 TS 形状**一次到位铺路刀4/5。
  - **TS 契约精确复刻**(gateway.ts:2151-2206/2261/2397-2449 + errors.ts:44)：ChatRole(4)/ChatBlock(text/tool-call/tool-result，**无 image**)/ChatContent(Text|Blocks)/ChatMessage/ChatToolDef(JSON Schema)/StopReason(6 枚举 from_signals: anthropic refusal 优先→content_filter→tool_calls→length→end→other)/ChatUsage(input/output/cache_read/cache_creation)/ChatResult(text/blocks/stop_reason/usage/model/provider_id/provider_metadata)/ChatOpts。错误 `ChatError{Config{message,fix}|Transient{message}}` + `normalize(status,name,msg,ctx)` 保真 normalizeAIError(4xx≠429→Config+key/model hint；LoadAPIKeyError/InvalidArgumentError→Config；5xx/429/network→Transient)。
  - **产出**：`ai/chat.rs`(~640 行) = 类型 + `ChatProvider` trait(transport seam 仿 rerank_client) + `MockChatProvider`(queue_text/queue_result/queue_error) + 纯函数 `serialize_messages_openai`(text/tool_calls/tool→独立 tool message)/`serialize_tools_openai`/`parse_openai_response`(text+tool_calls blocks，arguments JSON string 回解析) + `OpenAiChatProvider`(#[cfg(feature=openai)] reqwest /chat/completions，build_body 纯函数可测)。mod.rs 注册 + re-export。
  - **验证**：`ai::` **51/51 全绿**(16 registry+15 resolver+**20 chat**)，含/不含 openai feature 均编译+测试通过；`cargo build -p zbrain-core` clean。clippy: 修 unnested_or_patterns(match/matches!)+identical match arms(Display)+map_or_else，剩 9 个 doc_markdown backtick 是 crate 级 ambient(全 crate 148 个，llm.rs 自身 6 个)不 deny，与既有风格一致不改。roadmap 1-3 completed(8 节点 3 completed/1 in_progress/4 pending，1-2-1 保持 pending)。Task #545 完成。
  - **坑复记**：(1) Write 大文件尾部截断(停在 `Vec<ChatM`/`if !response`)——Edit 续写补全；(2) Rust unicode 转义是 `\u{2014}` 非 `\u2014`(Python 脚本写 em-dash 断言时踩)；(3) Python append 脚本路径别传目录(EISDIR)。
  - **下一刀**：1-4 chat 剩余 native provider(anthropic/google) + BudgetTracker 接线；或 1-5 toolLoop(最重，依赖 chat，现在 chat trait 已就绪)。
- **roadmap_cli 写入坑（复记）**：大 static 数组用 Write/Edit 单次写会截断(litellm setup_hint 被砍)；分批 Edit + 尾部校验；heredoc 含反引号/特殊字符在 bash 会 EOF 报错，改用 Python 脚本 append 再删脚本。
