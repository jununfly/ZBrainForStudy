# 2026-07-03 工作日志

- 按 `/zj-tdd 开始执行` 进入 Part2 `1-1-2 Add init parser parity tests`：采用 RED-GREEN 小循环，为 Rust CLI `zbrain init` 添加 TS 历史可见 flag 的 parser parity 测试，并做最小 clap 字段实现。
- `crates/zbrain-cli/src/lib.rs` 新增/覆盖 init parser 测试：`--pglite`、`--supabase`、`--url`、`--migrate-only`、`--mcp-only`、`--json`、`--non-interactive`、OAuth MCP flags、`--embedding-model`、`--embedding-dimensions`、`--no-embedding`，以及 engine selection conflict。
- 最小实现仅扩展 `InitArgs` 的 clap 字段与冲突约束，不实现实际 init 行为；后续行为实现留给 `1-1-3` 起的子节点。
- 验证通过：`cargo test -p zbrain-cli init --lib`（8 passed）与 `cargo test -p zbrain-cli --lib`（73 passed）。`cargo fmt --package zbrain-cli --check` 仍输出仓库既有 `config.rs` 格式化差异，本刀未处理。
- Part2 roadmap 已将 `1-1-2 Add init parser parity tests` 标记为 completed 并重新 render；下一节点为 `1-1-3 Implement engine selection flags for init`。
- 完成 Part2 `1-1-3 Implement engine selection flags for init`：完整修复 `run_init_command()`，默认/`--pglite` 使用 LibsqlEngine 初始化并写 `sqlite://...brain.pglite`；`--url` 使用 PostgresEngine，连接+migration 成功后才写 config；`--supabase` 明确禁用并在写盘前 fail-loud（避免误解为已支持 Supabase 向导）。验证通过：`cargo test -p zbrain-cli init_ --lib`（11 passed）与 `cargo test -p zbrain-cli --lib`（76 passed）。路线图节点已标记 completed 并 render。
- 完成 Part2 `1-1-4 Implement migrate-only init behavior`：新增 `run_init_migrate_only()`，`--migrate-only` 只读取现有 `config.database_url` 并迁移 sqlite/libsql 或 Postgres，不重写 config；无 config fail-loud；禁止与 `--pglite`/`--url`/`--supabase` 混用。新增 3 个行为测试并验证通过：`cargo test -p zbrain-cli init_ --lib`（14 passed）与 `cargo test -p zbrain-cli --lib`（79 passed）。路线图节点已标记 completed 并 render。
- 完成 Part2 `1-1-5 Implement thin-client MCP-only init behavior`：`zbrain init --mcp-only` 写入 `remote_mcp` 与 `database_url = "remote-mcp://thin-client"`，不创建/迁移本地 DB；要求 `--issuer-url`/`--mcp-url`/`--oauth-client-id`，可选 secret；拒绝 DB/migrate/embedding flags 混用且失败不写 config。新增 3 个行为测试，验证通过：`cargo test -p zbrain-cli init_mcp_only --lib`（3 passed）、`cargo test -p zbrain-cli init_ --lib`（17 passed）、`cargo test -p zbrain-cli --lib`（82 passed）。路线图节点已标记 completed 并 render。
- 完成 Part2 `1-1-6 Implement init embedding setup flags`：新增 `embedding.enabled`（默认 true）和 `embedding.dimensions: Option<u32>`；`zbrain init` 支持把 `--embedding-model` 写入 `config.embedding.model`、`--embedding-dimensions` 写入 dimensions、`--no-embedding` 写入 enabled=false。不做模型下载、provider 初始化、向量表维度迁移或模型校验；继续拒绝与 `--mcp-only` 混用。验证通过：`cargo test -p zbrain-cli config_defaults --lib`、`init_embedding_flags_write_config_without_model_setup --lib`、`init_ --lib`（18 passed）、`cargo test -p zbrain-cli --lib`（83 passed）。路线图节点已标记 completed 并 render。
- 完成 Part3 `1-2-1 MCP per-call timeout wiring (--timeout)`（TDD，commit `2d04d78`，+17 tests，118 pass）：
  - `parse_timeout(&str)->Option<u64>` 逐字对齐 TS `parseTimeout`（整数/小数 + ms/s/m，无后缀默认 ms，非正/非法 -> None）。关键坑：Rust `f64::parse` 会接受科学计数法 `1e3`、前导 `+`、裸小数 `.5`/`5.`，而 TS 正则 `^([0-9]+(?:\.[0-9]+)?)(ms|s|m)?$` 不接受——加 `is_ts_timeout_magnitude` 字符类守卫先卡再 parse。
  - clap 顶层全局 `--timeout <DURATION>`（`global=true`，`value_parser=parse_timeout_clap`）。解析失败 -> clap `ValueValidation` -> exit 2（fail-loud，唯一有意偏离 TS 软 fall-through，已记 audit/roadmap Q5）。
  - `resolve_timeout_ms(op, override)`：think 180s / else 30s（对齐 `cli.ts:302`），`--timeout` override 优先。
  - `McpClient::new(config, Duration)` 改签名，用 `Client::builder().timeout(dur)`；`map_reqwest_error(&e)`：`e.is_timeout()` -> `RemoteMcpError::Timeout`，否则 `TransportError`。timeout 值从 `run()` -> 8 个 op 命令 -> `run_operation(…, cli_timeout_ms)` -> `McpClient` 全程线程化。
  - 本地路径 `local_timeout_warning(Some)` -> stderr 诚实警告（非静默；本地 wallclock 超时是独立未迁特性，roadmap 1-2-3 跟踪）。旧 `FUTURE(mcp-timeout)` 注释替换为现状说明。
  - reqwest timeout 集成测试用 `tokio::net::TcpListener` accept 后 `std::mem::forget(stream)` 挂起服务 + 150ms client timeout 验证 -> `Timeout`。binary 冒烟：`--timeout=nonsense` rc=2、`--help` 展示 flag。clippy 新增代码零告警（`needless_pass_by_value` 用 `&reqwest::Error`+闭包规避）。
