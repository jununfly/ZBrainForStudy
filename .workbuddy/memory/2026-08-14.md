# 2026-08-14 工作日志

## 1-1-5-6 忠实 port 偏差（grilling 中发现，重要）

- 用户质问「为什么 roadmap 要有 per-pair findings」→ 经 `git show bcafcafd^:src/core/eval-contradictions/trends.ts` 等恢复 TS 源核实：
- **TS `trends.ts` 的 `report_json: ProbeReport` 存的是完整 ProbeReport blob**（注释原文 "Full ProbeReport blob; consumed by `review` sub-subcommand"）；`ProbeReport` 含 `per_query`→`contradictions`（per-pair findings）。
- **TS `review` 子命令 (`runReview`) 忠实消费完整 report**：`report.per_query.flatMap(q=>q.contradictions)` 取全部 findings；`--severity` 真实过滤 findings（high→medium→low→info 分组打印 `[verdict] a.slug vs b.slug`+axis+resolution_command）；`--since` 设 run 选择窗口（默认 90d）。
- **Rust `ContradictionsResult.findings: Vec<ContradictionFinding>`（contradictions.rs:232）运行时本来就有 per-pair 数据**。
- **偏差根因**：我的 `1-1-5-6` `build_contradictions_run_row`（~721行）只把聚合塞进 `report_json`（含 `findings_count: result.findings.len()` 只计数、丢数组）→ 这是忠实 port 缺口，不是 roadmap 多写。
- 结论：roadmap `1-1-5-7`「配对明细/pairs」+ `severity`/`since` 是**合法忠实 port 要求**，不是 scope creep；我上一条把"report_json 只存聚合"当基线、提议删 severity/since 是**搞反因果**。
- **修正路径（待用户拍板）**：先补 `1-1-5-6`——`build_contradictions_run_row` 的 `report_json` 增 `"findings": result.findings`（ContradictionFinding 已 Serialize，见 build_summary_receipt 847 行）；再忠实 port `1-1-5-7` runReview。`severity`/`since` 保留为合法 flag，不需新 migration（report_json 列已存在）。
- 教训：恢复 TS 源核对 `report_json` 语义，是 grilling 必要动作；已提交节点也可能含忠实 port 偏差，不能把偏差当设计基线来"优化范围"。

## 1-1-5-7 实施收口（决策 A，本会话完成）

- 用户选 A → 先修 1-1-5-6（塞 findings）再忠实 port 1-1-5-7。
- `contradictions.rs`：`build_contradictions_run_row` 的 `report_json` 增 `"findings": result.findings`（完整 ProbeReport blob）；加 `Severity::rank()`(High=3..Info=0) + 纯函数 `render_review_report(report_json, severity_filter)`（提取 findings→按 severity 降序分组→逐条 [verdict] pair_id+axis+resolution_kind，空 findings 友好提示）+ 1 单测。
- `cli/lib.rs` review 臂实装（原 stub "deferred in MVP"→真实实现）：镜像 trend 臂 DB 样板；`--run-id` 选特定 row 否则最新、`--since` 字典序过滤(映射 loadContradictionsTrend(days))、`--json` 打印 report_json 否则 render_review_report；`EvalSuspectedContradictionsReviewArgs` 增 `run_id`/`json`；`SuspectedContradictionsAction` 枚举过时注记清理。
- 测试：12 passed（含新 render_review_report 单测）；离线 `cargo build --offline -p zbrain-core -p zbrain-cli` 0 error；CRLF 核验通过（cli/lib.rs 树 14871 CR / HEAD 14783 CR，行尾未翻转，无 N/N 全文件重写）。
- 提交 `8c26390d`（6 文件，+242/-16）含 1-1-5-6 修正 + 1-1-5-7 实现；推送 `8989ee7a..8c26390d rust-rewrite -> rust-rewrite`。
- 路线图 JSON（SoT）：1-1-5-6 加忠实 port 修正注记、1-1-5-7 pending→completed（加决策 A 注记）；从仓库根 `roadmap_cli.py render` 同步 md（[x][X+]）。
- 1-1-5 系列现况：1-1-5-1/2/3/4/6/7 ✅；剩 1-1-5-5(brainstorm) 与 1-1-5-8(JudgeCache) 未结。
