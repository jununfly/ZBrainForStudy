# 2026-08-05

## 1-6-8 收尾（消费者切换 — 删除遗留 TS）

- 删除 `src/commands/dream.ts`（零 importer；仅其两个测试文件引用 `runDream`）+ `tests/unit/dream.test.ts` + `tests/unit/e2e/dream.test.ts`。commit `a96f721`（rust-rewrite）。`zbrain dream` 命令现完全由 Rust 二进制服务（1-6-8 消费者切换完成）。
- **关键 blocker（已登记 G66）**：`src/commands/calibration.ts` + `src/core/cycle.ts` + `src/core/cycle/*` **不能删除**。`calibration.ts` 导出 `getLatestProfile`/`CalibrationProfileRow`，被活 TS 代码 `src/core/calibration/{cross-brain,nudge,recall-footer}.ts`、`src/core/eval-contradictions/calibration-join.ts`、`src/core/think/index.ts` 引用；这些又被 `src/commands/eval-longmemeval.ts` + `src/eval/longmemeval/extract.ts` 引用。calibration.ts 又 `import { runPhaseCalibrationProfile } from '../core/cycle/calibration-profile.ts'`。删链会断 eval-longmemeval。
  - 按"TS 先不动；不适合删的到时讨论记录决策"原则保留。解缠：relocate `getLatestProfile`/`CalibrationProfileRow` 到 `src/core/calibration/` 改 5 处 import，或随 eval-longmemeval Rust 化一并移除。
- 测试清理（Task #143）同受此 blocker 约束：calibration/cycle 的 ~40 个单测 + e2e 不能删（模块还在）。仅 dream 的两个测试随 dream.ts 删除。
- e2e dream 测试删除后，dream 的 e2e 覆盖暂缺（q-1 "e2e 改测 Rust 二进制" 未做——无 spawn-zbrain helper，需 from-scratch 重写，留待后续），已记录于 commit message + G66。
