# 2026-08-05

## G66 执行 — 删除 src/core/cycle（1-6-8 收尾）

完成 G66 解缠并删除 legacy TS `core/cycle` 引擎。两个 commit 已落地（待 push origin/rust-rewrite）：

- `2822456` — relocate calibration profile read helpers out of core/cycle (G66)
  - 新建 `src/core/calibration/profile-store.ts`（`CalibrationProfileRow` + `getLatestProfile`，无 cycle 依赖）。
  - 删 `src/commands/calibration.ts`（命令已由 Rust 二进制服务；`bin/zbrain-rs.js` 透传）+ `tests/unit/calibration-cli.test.ts`。
  - 改 5 处 src import（cross-brain/nudge/recall-footer/calibration-join/think）+ 4 处 test import。
- `ed964cb` — delete src/core/cycle (G66) — relocate live pure helpers first
  - 先把被活 src 引擎用的 3 个纯函数移出 core/cycle：
    - `cycle/emotional-weight.ts` → `src/core/emotional-weight.ts`（backfill-registry）
    - `cycle/anomaly.ts` → `src/core/anomaly.ts`（pglite-engine/postgres-engine）
    - `cycle/transcript-discovery.ts` → `src/core/transcript-discovery.ts`（transcripts）
    - 修内部 `../types.ts`→`./types.ts`、`../sync.ts`→`./sync.ts`；改 4 处 src import。
  - 删 `src/core/cycle.ts` + `src/core/cycle/`（17 个 phase 模块）。
  - 测试：删 31 个测 cycle phase 的 TS 单测（q-1 删单测）；保留 6 个测已迁纯函数的测试并改 import（anomalies/salience/emotional-weight/brainstorm-lsd/cycle-synthesize-md-discovery/e2e-salience-pglite）；保留 1 个仅注释引用者（markdown-serializer）。
  - CI：drop `cycle/synthesize.ts` from gateway-routed guard（现 Rust phase）；修 4 处 stale doc comment。
  - tsc baseline 95→82（13 inherited error 随删测试消失），gate 绿。

### 关键教训（重要）
- 勘测删除范围时，用 `grep -rn "core/cycle" ... | grep -v "src/core/cycle/"` 会漏掉**相对路径** `./cycle/*.ts` 的引用（如 `src/core/backfill-registry.ts` 的 `import './cycle/emotional-weight.ts'`）。必须先 `grep -rn "cycle/"` 覆盖 `./cycle/` 形态，否则会误删活依赖、崩构建。本次首轮误删 core/cycle 后靠 `git checkout HEAD -- $(git diff --cached --name-only --diff-filter=D)` 回滚（57 文件），再正确重做。

### 当前状态
- #142 (dream 子命令)、#144 (calibration 子命令)、G66 全部 done，push origin。
- #143 **第一刀完成**（本会话末）：e2e 改测 Rust 二进制的基础设施 + smoke。commit `79e4fb2`。
  - `tests/unit/e2e/spawn-zbrain.ts`：`resolveZbrainBin()`（honor `ZBRAIN_BIN` → 默认 `target/` 候选，mirror `bin/zbrain-rs.js`；无 binary 返回 null，不 auto-build 以免监视器锁 wedging）+ `spawnZbrain()`（spawnSync 抓 stdout/stderr）+ `runZbrainOk()`。`binaryAvailable()` 供 `describe.skipIf` 门控。
  - `tests/unit/e2e/binary-smoke.test.ts`：6 case — `--version`、top-level `--help`（含 dream/calibration）、`dream --help`、`calibration --help`、`schema-sql`、真实 `init --pglite --config <tmp>/zbrain.yml`→`list-pages` 往返（JSON 空页）。全部 deterministic，无网络/LLM。本地 6 pass、tsc gate 绿、无 `~/.zbrain/config.json` 污染。
  - 仅经 `scripts/run-e2e.sh` 运行（`tests/unit/e2e/*` 被 unit-parallel runner 排除）。
- #143 **已完成**（见下方"#143 完成"段）：cycle/dream 的 TS e2e 套件已重写驱真实二进制。
- TS 引擎 pglite/postgres 迁移 + anomaly/transcript-discovery 的 TS 删除仍受 1-6 收尾阻塞（但 core/cycle 已不在）。

## #143 完成 — cycle/dream e2e 套件二进制重路由（接 79e4fb2）

把 5 个被删的 cycle/dream TS e2e 套件重写为驱真实 Rust 二进制（#143 收尾，现 fully done）：

新增文件（tests/unit/e2e/）：
- `binary-helpers.ts`：共享 PGLite 脑装配 `makePgliteBrain()`（默认**空 repo**——init 不 sync、dry-run 断言"零写入"才成立）+ `runDreamReport()` / `runDreamOnBrain()` / `runZbrainOkRetry()`（**崩溃重试**：每次尝试新建脑，绕过 Windows libsql FFI 间歇崩溃）+ `binaryE2eGate` / `SKIP_ON_WINDOWS` 门控。
- `dream.test.ts` / `cycle.test.ts` / `dream-cycle-phase-order.test.ts` / `cycle-recompute-emotional-weight.test.ts`：重建 dry-run CycleReport 形状+零写入、`--phase orphans` 单相位派发、相位顺序精确校验（23 phase 顺序常量，mirror Rust `execute_phase`）、recompute 单相位派发。
- `cycle-consolidate.test.ts`：**embed-gated**（`ZBRAIN_E2E_EMBED=1` 才跑；consolidate 需 embeddings，是 #143 显式延期的 LLM 切片）。

关键决策（重要）：
- **Windows libsql FFI 崩溃实测 ~42%**（12 次 dry-run 5 次 SIGSEGV exit 139），且崩溃会污染共享脑文件。故套件**默认在 Windows skip**（`SKIP_ON_WINDOWS`：非 win32 跑，或设 `ZBRAIN_E2E_ALLOW_WIN=1` 强跑——重试兜住崩溃），与项目"CI 跑 Linux 避 libsql flake"方针一致。本地验证用 `ZBRAIN_E2E_ALLOW_WIN=1` 跑通 6 pass；默认 Windows 路径 9 skip 0 fail。
- live `dream`（真写同步页面）的断言 **gated 到非 Windows**（`it.skipIf(process.platform==='win32')`），即使 `ALLOW_WIN` 也不在 Windows 跑——写路径崩溃率更高；Linux CI 跑。
- 二进制 CycleReport 是 **camelCase**（`schemaVersion`/`brainDir`/`phases`/`totals`），不是旧 TS 的 snake_case（`schema_version`/`brain_dir`）。
- 空 repo dry-run 全 phase 状态仅 `ok`/`skipped`；非空 repo 某些 phase 会出 `warn`（故 dry-run 套件用空 repo 才满足严格校验）。
- tsc baseline 绿（82 inherited，无新增）。未动 `helpers.ts`（仍供 postgres e2e 用）；新套件零 `src/` import。

#143 至此 fully complete（spawn-zbrain+binary-smoke 第一刀 + 本次 5 套件重路由）。1-6-8 收尾中"删单测 + e2e 改测 Rust 二进制"已闭环。

## 路线图对账 + 地图远比预期滞后（grill/roadmap-driven）

用户 /zj-grill-me /zj-roadmap-driven 继续。对账发现地图严重滞后于代码：

- Part12 (cycle 大迁移) 实际已全收口：1-6-2（周期锁→`autopilot/cycle_lock.rs`，cycle.rs:409-451 已有 acquire 逻辑）、1-6-8（消费者切换→`zbrain dream` CLI + 删 `src/core/cycle.ts`、`src/core/cycle/`、`src/commands/dream.ts`）。JSON 仍标 pending。已 `update`→completed 级联关 1-6 与 root，render，commit `629950c` push。
- Part11 的 1-12（cycle 主战场）级联关闭。
- **更深发现**：`src/cli.ts`、`src/core/operations.ts`、`src/core/minions/`（整树）已从代码库删除。`bin/zbrain-rs.js` 是**纯 Rust spawn 垫片**（只透传 target/ 二进制，无 TS 命令派发）。grep 确认**产品代码零 `src/` import**（仅 tests + 1 个 eval harness `evals/functional-area-resolver/harness-runner.ts` 引用）。
- 结论：**415 个 `src/*.ts` 实质是死代码**（被 Rust 取代，但未被 bulk 删除），仍被 unit 测试维持（tsc baseline 冻结 82）。路线图 1-1/1-4/1-11 等"节点式删已迁模块"的前提（src 仍部分活着）已不成立。
- cutover (1-13) 实际完成：1-13-1-6（删 cli.ts）、1-13-1-7（删 operations.ts）均 [x]；唯一残留 `1-13-1-3-2` [?]（audit 标记"无 Rust 对应"的断言补 Rust 集成测试）。1-11-2（minions）引用"待 1-13-1-6 删命令后清"——该阻塞已解除，minions 实际已删。

待 grill 决策：src 死代码终局策略（bulk-delete + 迁移依赖测试 vs 节点式 vs 先活性审计）。路线图多 in_progress 节点实际已 done，需整体对账。

## 残留 TS 活性审计结果（q-0=先审计，已执行）

写 import-graph 脚本（`C:/Users/victo/AppData/Local/Temp/zbrain-ts-audit.py`，注意 Windows 路径分隔符须归一化 forward-slash，否则 `r in fset` 因混用 `C:/...` 与 `C:\...` 失配→误报 0 test-covered）跑出真实地面真相：

- **415 src .ts**：0 product-live（确认无活派发）、398 test-covered、17 orphan（可立即删）。
- **555 测试文件**：541 引 ≥1 src（删 src 须迁/删这些）、14 不引（已二进制/Rust 驱动，安全）。
- 17 orphan = commands 12（edges-backfill/eval*/lsd/migrate-engine/pages/skillify*/skillpack*/transcripts）+ core 3（facts/phantom-audit、search/by-image、spend-log）+ eval/code-retrieval/strategies + types/image-decoders.d.ts。
- 按簇：core 331 covered+3 orphan、commands 60 covered+12 orphan、eval 6 covered+1、version/types 各 1。
- **关键**：删 src 的真正成本是 **541 测试面**，不是 415 src。bulk-delete 选项实质＝迁/删 541 测试文件（多测纯函数/二进制未暴露的内部，不能简单改测二进制）。
- heaviest test：writer.test.ts(9)、schema-cathedral(7)、benchmark-search-quality/eval-longmemeval/v0_37_gap_fill/v030_1-integration-pglite(各6)。
- 路线图 1-14 节点记了审计结论 + decide q-0；1-13-1-3-2 [?] 是 cutover 唯一残留（audit 标记"无 Rust 对应"断言补 Rust 集成测试）。

q-1 待 grill：541 测试面怎么处理（低垂果实=删17 orphan+收口1-13-1-3-2 / 先量化 Rust 覆盖重合度 / 节点式按簇 / 宣战式全迁）。

## 关键修正：src 不是"死代码可删"，是"未移植"

读 `bin/zbrain-rs.js` 证实它是**纯 Rust spawn 透传**（只找 target/ 下 zbrain 二进制、spawnSync 透传 argv、propagate exit code），**不派发任何 TS 命令**。所以：
- `src/commands/*`（72 文件）**实质不可达**——产品 CLI 只有 Rust 实现的 dream/calibration/init/list-pages/schema-sql 等。那 ~80 个 TS 命令（search/pages/recall/export/import/eval-*）当前在产品里**根本不可调用**。
- Rust 无 `search`/`facts`/`think`/`commands`/`engine` 模块（仅 345 .rs：autopilot/calibration/ai/DB/operation/skillpack/chunking）。
- **结论：删 src 当前不可行**——会删掉无 Rust 替代的功能（搜索/事实/思考/80命令）。迁移真实剩余＝把这些模块 **PORT 到 Rust**，不是删 TS。
- 路线图 1-7/1-8/1-9（search/facts/think core 标 [x]）是**假的**（同 Part12 滞后病，范围更广）。1-7/1-8/1-9 实际未移植。
- L2 覆盖重合（GREEN46/AMBER133/RED362）印证：362 测试覆盖未移植功能。
- 真正根决策＝**迁移终局 scope**：全量 port（search/facts/think/80命令→Rust）vs 混合/缩范围。q-1 的"删 541 测试"前提因此不成立——先决条件是 scope 决策。

注意：本沙箱 `grep -rn "core/cycle"` 类勘测 + import-graph 脚本都须归一化 Windows 路径分隔符（forward-slash），否则 `r in fset`/匹配失配。

## 域级 port 状态对账（L3 — 推翻"虚假完成"节点）

跑 `C:/Users/victo/AppData/Local/Temp/zbrain-domain-recon.py`（按 `src/core/<dir>` 聚合 src 文件数 + Rust 对应 .rs 覆盖数）。结果（src_files / rust_covered / 状态）：

```
commands                   72        14  PARTIAL
core/skillpack             27         5  PARTIAL
core/ai                    26         2  PARTIAL
core/schema-pack           25         6  PARTIAL
core/search                22         0  MISSING   ← 1-7 假[x]
core/eval-contradictions   15         3  PARTIAL
core/facts                 11         1  MISSING   ← 1-8 假[x]
core/calibration           11         0  MISSING   ← CLI 已迁(1-6-8)但 core 模块未迁
core/output                 9         0  MISSING   ← 1-4 假[~]
core/think                  7         0  MISSING   ← 1-9 假[x]
```

- 修正 roadmap 节点：`1-7`(search core)、`1-8`(facts core)、`1-9`(think core) 由假 `[x]` 改标 `pending` + 注 MISSING；`1-4`(output) 由假 `[~]` 改标 `pending` + 注 MISSING。commit `398d805`。
- `core/calibration` MISSING 但 CLI 已 port（1-6-8）——功能在 Rust 端（`calibration_queries`/`calibration_profile`/`think_ab`），src 模块是滞后计数，优先级低。
- 真正零 Rust 替代的硬骨头＝`search`(22)/`facts`(11)/`think`(7)/`output`(9) + `commands` 剩余 58 个未暴露命令。
- 路线图累计对账 commit：`629950c`(Part12 收口) + `fc929b2`(审计+地图滞后) + `398d805`(节点修正)，均 push origin/rust-rewrite。
- scope 决策（全量 port 终局）已记 `1-14` decide q-0：补 search/facts/think/commands/engine 到 Rust（含 Rust 测试）再删 src。

## 下一步 grill 分支：port 序列

scope=full-port 已定。下一拷问＝**先 port 哪个域**（依赖顺序 + 性价比）。候选：search(22,0) / facts(11,1) / think(7,0) / output(9,0) / commands(剩58) / calibration-core(11,0,低优先)。grill 前先验代码依赖图确定推荐顺序（leaf-first vs 按测试覆盖率）。
