# 2026-08-16

## 1-3 by-mention 实体提及自动链接子系统（G76）— Rust 真实核心 port 完成
- TS 源 `dfa15ba2:src/core/by-mention.ts`（307 行，已删于 bcafcafd）忠实移植到 `crates/zbrain-core/src/mentions.rs`：
  - `build_gazetteer`（按 4 类实体 page_type 走 `list_pages` 枚举 + 忽略表 CK12 + 最小长度 + 桶内 longest-first 排序）。
  - `find_mentioned_entities`（纯 maximal-munch 扫描 + self-link/cross-source/first-mention 守卫；`strip_code_blocks` 长度保留保 offset）。
  - `run_by_mention` 编排：全局 gazetteer + scoped markdown 扫描（`compiled_truth`+`timeline`）→ `add_links_batch` 写 `mentions`/`mentioned_by` 双向边（`link_source='mentions'`，D12），幂等分块 200。
  - D7 禁 FS：全程 `list_pages`（DB 迭代），不触 FS。
- CLI：`LinksAction::ByMention` + `LinksByMentionArgs`（`--source`/`--dry-run`/`--json`/`--extra-ignore`）+ `run_links_by_mention` + 2 解析测试。
- 验证（PowerShell + lld-link）：`cargo test -p zbrain-core --lib mentions` **16/16**；`cargo test -p zbrain-cli --lib links` **8/8**（含 2 新 by-mention）。
- roadmap：g74-g76 JSON `1-3` → `completed`（幂等 close_13.py，numstat 3/3 CRLF 干净）+ render `ZBRAIN_G74_G76_REIMPL.md`（节点 `[x]`）。
- 坑（已写 MEMORY.md 迁移工程规则）：`list_pages` 在 InMemoryEngine 返回软删页 + `PageFilters` 无 `page_kind` 过滤 → Rust 侧过滤 `deleted_at.is_none()` + `page_kind==Markdown`。
- 已 commit+push（feat/chore 分离，rust-rewrite）：feat = mentions.rs 新 + lib.rs×2(M) + cli lib.rs（LinksAction::ByMention 等）→ `157f9d9b`；chore = g74-g76 JSON/MD + 本日志 + MEMORY.md(list_pages 坑) → `11c4009e`。

## 1-2-2 纠错 + 1-6/1-1 父节点收口
- **纠错**：上轮把 1-2-2（`--source fs --dir` 文件系统扫描路径）误列「剩余前沿」——实测证伪：`crates/zbrain-core/src/extract_fs.rs`（484 行，递归 walk + slug 解析 + 链接/时间线抽取 + 缺页自动创建 + 幂等，5/5 测试全绿）早已真实现并接线；CLI `extract links/timeline/all --source fs --dir` 派发到 `extract_fs`（lib.rs:11551/11600/11643）；g74-g76 `1-2-2`（G76a）早已 `completed`。节点不可全信 → 改查实际代码+测试。
- **收口两个父节点**（子树全 completed 已核实）：part13 `1-6`（G77 图维护族，子 1-6-1/2/3 全 completed）→ `completed`；g74-g76 `1-1`（G74 eval 19 命令，子 1-1-1..1-1-5 + 1-1-5-x 共 18 个全 completed）→ `completed`。
- 实现：`close_parents.py` 幂等 surgical 正则（仅改 status + metadata.updated，1-6 补收口 note；不碰行尾/CRLF），JSON 校验通过，numstat 各 3/3、2/2 干净。render 两份 MD（各 2 个 ROADMAP_SECTION marker → 原地替换），父节点树显示 `[x]`。
- 剩余真实开放前沿：命令级迁移在节点层面已实质完成；真正剩下的 (a) G74 1-1 的 19 eval 命令**真 port**（notes 已做完能力审计，但 `eval_candidates` 表等基建 Rust 缺失，需先 grill 选切片）；(b) part13 5 个 `blocked`（lint/lint_fix/integrity_auto/sync_retry_failed/repair_jsonb，已明确 wontfix/待决策）。
- 本轮改动**未提交**（chore = 两份 JSON + 两份 MD），按约定等显式触发 commit/push。

## G74 1-1 收尾：补 3 个 eval 命令 CLI 接线（grill 选 a）
- 用户 grill G74 1-1 后选 (a)：补 3 个 thin CLI wrapper 达成 eval 命令 19/19 接线。
- 回查已删 TS 源（`db3dd7bc^`）确认这 3 个是 `zbrain eval extract-atoms` / `eval synthesize-concepts` / `eval schema-authoring` 子命令，且 TS 本身即 **scaffold**：前两者返回 `status:"not_yet_implemented"`，后者无 fixture 返 `inconclusive`（v0.39.0.0 未接 hermetic engine）。忠实移植 = 复刻命令表面 + 占位输出，不引入 LLM 执行（偏离 TS 且超范围）。
- 改动 `crates/zbrain-cli/src/lib.rs`（纯增量 284 行 / 0 删，numstat 干净 CRLF）：
  - 新增 `EvalExtractAtoms`/`EvalSynthesizeConcepts`/`EvalSchemaAuthoring` 三变体 + 对应 `*Args`（`--parity-baseline`/`--sample`/`--json`；schema-authoring 用 `--fixture` + `--source-id`(alias `--source`)）。
  - dispatch 三臂 → `run_eval_*_command`（scaffold，构造与 TS 一致的 JSON 占位结果；`emit_eval_scaffold` 统一 `--json` 美化 / human 摘要）。
  - 4 个解析测试（含 `--source`/`--source-id` alias 守卫）。
- 验证：`cargo test -p zbrain-cli --lib <4 tests>` **4/4 通过**（lld-link；整 lib 含 3 个 run 函数已编译通过）。默认 target 被 Windows 监视器锁死（`.cargo-build-lock` os error 5）→ 用独立 `CARGO_TARGET_DIR` 重建 bin 冒烟（进行中）。
- 未提交，等显式触发 commit/push。

## g74-g76 根节点收口 + KNOWN-GAPS G76/G77 过期描述修正（对账 SSOT）
- 收口 g74-g76 根 `1`（children 1-1/1-2/1-3 全 completed）→ status 翻 `completed`，整图 26/26 completed。
- 按「节点 notes 不可全信、以 HEAD 为准」纪律，grep 核验 CLI 动词真实存在后与 KNOWN-GAPS 对账，修正两处过期：
  - **G76a 全完**：KNOWN-GAPS 原称「fs-source + by-mention 仍缺」——实测 `ExtractAction::{Links,Timeline,All}` 接 `extract_fs.rs`（fs --dir）+ `LinksAction::ByMention`+`run_links_by_mention`（by-mention）均已接线。状态列改「G76a done; G76b blocked: G35」，内联 ④「仍缺」改「已于 2026-08-16 收口」。仅剩 G76b（extract-conversation-facts，真 LLM，被 G35 阻塞）。
  - **G77 edges-backfill + reconcile-links 已完**：KNOWN-GAPS 原称「无 core op」——实测 `LinksAction::EdgesBackfill`+`run_links_edges_backfill`、`LinksAction::Reconcile`+`run_links_reconcile` 均在（core `backfill_edges`+`reconcile_links`）。状态列改「backlinks + edges-backfill + reconcile-links done; generic backfill remains」。generic `backfill` 仍缺独立 verb。
- 改动均为唯一子串替换（Edit 工具），numstat 干净（KNOWN-GAPS 2/2、JSON 1/1），CRLF 无泄漏。未提交，等显式触发 commit/push。
- 修正后真实剩余前沿：G74b（--rrf-k/--expand 仅写 report 不作用检索，小缺口）/ G75（reindex-code/frontmatter/multimodal）/ G77 generic backfill / G79（15 杂项）/ LLM 门控（G58/G76b）。

## G74b：`zbrain eval` 的 `--rrf-k`/`--expand` 接进真实检索（2026-08-16 收口 resolved）
- 接线：
  - `--rrf-k`：`SearchOpts` 新增 `rrf_k: Option<f64>`；`fuse_and_boost`（engine.rs:639）改 `opts.rrf_k.unwrap_or(RRF_K)`；`HybridSearchOpts.rrf_k` 透传到 `SearchOpts`。三引擎 keyword/vector/hybrid 全链路生效，`RRF_K=60.0` 退默认值。
  - `--expand`：新增生产级 `ChatExpansionProvider`（adapter 包 `Arc<dyn ChatProvider>`，`expand` 调 chat + `parse_expansion_response` 容错，错误/无法解析降级 `Ok([query])`）；`build_eval_expansion_provider`（`resolve_model` fallback "sonnet" + `resolve_recipe_strict` + `instantiate_chat`）无 key/model 返回 `None`→单查询降级 + 单次 warning；`run_one_eval_config` 闭包多查询按 best rank 合并（`HashMap<slug,usize>`）。
  - cli `eval_retrieve_slugs`：三策略均透传 `rrf_k`；`dedup_opts` 形参改为裸 `DedupOpts`（与 `eval_dedup_opts` 返回类型 + 调用点一致），内部构造 `HybridSearchOpts` 包 `Some(...)`。
  - core 新增 `adapter_tests`(6) + `rrf_fuse_tests`(2) 单测；`ai/mod.rs` 导出 `ChatExpansionProvider`。
- 验证（PowerShell + lld-link + 独立 `CARGO_TARGET_DIR`）：
  - 首编 `BUILD_EXIT=101`：`eval_retrieve_slugs` 调用点传裸 `DedupOpts` 但形参是 `Option<DedupOpts>`（2× E0308）→ 改形参为裸类型修复。
  - **后台任务丢失陷阱**：会话空闲后 `TaskOutput` 报 task "not found"，但 PowerShell 实际跑完、日志尾部 `error: could not compile zbrain-cli ... BUILD_EXIT=101`（无 cargo 进程在跑=真失败退出非卡死）。**务必直接读日志文件的 `BUILD_EXIT` 行，别信 `TaskOutput not found`**。
  - 重编绿后跑测试：`adapter_tests` 初 1 失败（`parse_useless_reply_yields_nothing`）——`parse_expansion_response` 第三分支把纯散文当备选返回；加列表标记判定（`- `/`* `/`N.`/`N)`）后纯散文降级空，与「useless reply yields no alternatives」契约一致。修复后 `adapter_tests` 6/6、`rrf_fuse_tests` 2/2、cli `--lib` 350 passed / 0 failed，全 `TEST_EXIT=0`。
- KNOWN-GAPS：G74b 行改 `resolved`；G74 主条目两处「`--rrf-k`/`--expand` 残余见 G74b」回写为「已由 G74b（2026-08-16）收口」。
- **未提交**，等显式 `commit+push`：feat = engine.rs+search/engine.rs+ai/expand.rs+ai/mod.rs+cli/lib.rs；chore = KNOWN-GAPS.md。
