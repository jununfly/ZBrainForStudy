# 2026-08-16

## 1-3 by-mention 实体提及自动链接子系统（G76）— Rust 真实核心 port 完成
- TS 源 `dfa15ba2:src/core/by-mention.ts`（307 行，已删于 bcafcafd）忠实移植到 `crates/zbrain-core/src/mentions.rs`：
  - `build_gazetteer`（按 4 类实体 page_type 走 `list_pages` 枚举 + 忽略表 CK12 + 最小长度 + 桶内 longest-first 排序）。
  - `find_mentioned_entities`（纯 maximal-munch 扫描 + self-link/cross-source/first-mention 守卫；`strip_code_blocks` 长度保留保 offset）。
  - `run_by_mention` 编排：全局 gazetteer + scoped markdown 扫描（`compiled_truth`+`timeline`）→ `add_links_batch` 写 `mentions`/`mentioned_by` 双向边（`link_source='mentions'`，D12），幂等分块 200。
  - D7 禁 FS：全程 `list_pages`（DB 迭代），不触 FS。
- CLI：`LinksAction::ByMention` + `LinksByMentionArgs`（`--source`/`--dry-run`/`--json`/`--extra-ignore`）+ `run_links_by_mention` + 2 解析测试。
- 验证（PowerShell + lld-link）：`cargo test -p zbrain-core --lib mentions` **16/16**；`cargo test -p zbrain-cli --lib links` **8/8**（含 2 新 by-mention）。
- roadmap：g74-g76 JSON `1-3` → `completed`（幂等 close_13.py，numstat 3/3 CRLF 干净）+ render `ZBRAIN_G74_G76_REIMPL.md`（节点 `[x]`）。
- 坑（已写 MEMORY.md 迁移工程规则）：`list_pages` 在 InMemoryEngine 返回软删页 + `PageFilters` 无 `page_kind` 过滤 → Rust 侧过滤 `deleted_at.is_none()` + `page_kind==Markdown`。
- 已 commit+push（feat/chore 分离，rust-rewrite）：feat = mentions.rs 新 + lib.rs×2(M) + cli lib.rs（LinksAction::ByMention 等）→ `157f9d9b`；chore = g74-g76 JSON/MD + 本日志 + MEMORY.md(list_pages 坑) → `11c4009e`。

## 1-2-2 纠错 + 1-6/1-1 父节点收口
- **纠错**：上轮把 1-2-2（`--source fs --dir` 文件系统扫描路径）误列「剩余前沿」——实测证伪：`crates/zbrain-core/src/extract_fs.rs`（484 行，递归 walk + slug 解析 + 链接/时间线抽取 + 缺页自动创建 + 幂等，5/5 测试全绿）早已真实现并接线；CLI `extract links/timeline/all --source fs --dir` 派发到 `extract_fs`（lib.rs:11551/11600/11643）；g74-g76 `1-2-2`（G76a）早已 `completed`。节点不可全信 → 改查实际代码+测试。
- **收口两个父节点**（子树全 completed 已核实）：part13 `1-6`（G77 图维护族，子 1-6-1/2/3 全 completed）→ `completed`；g74-g76 `1-1`（G74 eval 19 命令，子 1-1-1..1-1-5 + 1-1-5-x 共 18 个全 completed）→ `completed`。
- 实现：`close_parents.py` 幂等 surgical 正则（仅改 status + metadata.updated，1-6 补收口 note；不碰行尾/CRLF），JSON 校验通过，numstat 各 3/3、2/2 干净。render 两份 MD（各 2 个 ROADMAP_SECTION marker → 原地替换），父节点树显示 `[x]`。
- 剩余真实开放前沿：命令级迁移在节点层面已实质完成；真正剩下的 (a) G74 1-1 的 19 eval 命令**真 port**（notes 已做完能力审计，但 `eval_candidates` 表等基建 Rust 缺失，需先 grill 选切片）；(b) part13 5 个 `blocked`（lint/lint_fix/integrity_auto/sync_retry_failed/repair_jsonb，已明确 wontfix/待决策）。
- 本轮改动**未提交**（chore = 两份 JSON + 两份 MD），按约定等显式触发 commit/push。

## G74 1-1 收尾：补 3 个 eval 命令 CLI 接线（grill 选 a）
- 用户 grill G74 1-1 后选 (a)：补 3 个 thin CLI wrapper 达成 eval 命令 19/19 接线。
- 回查已删 TS 源（`db3dd7bc^`）确认这 3 个是 `zbrain eval extract-atoms` / `eval synthesize-concepts` / `eval schema-authoring` 子命令，且 TS 本身即 **scaffold**：前两者返回 `status:"not_yet_implemented"`，后者无 fixture 返 `inconclusive`（v0.39.0.0 未接 hermetic engine）。忠实移植 = 复刻命令表面 + 占位输出，不引入 LLM 执行（偏离 TS 且超范围）。
- 改动 `crates/zbrain-cli/src/lib.rs`（纯增量 284 行 / 0 删，numstat 干净 CRLF）：
  - 新增 `EvalExtractAtoms`/`EvalSynthesizeConcepts`/`EvalSchemaAuthoring` 三变体 + 对应 `*Args`（`--parity-baseline`/`--sample`/`--json`；schema-authoring 用 `--fixture` + `--source-id`(alias `--source`)）。
  - dispatch 三臂 → `run_eval_*_command`（scaffold，构造与 TS 一致的 JSON 占位结果；`emit_eval_scaffold` 统一 `--json` 美化 / human 摘要）。
  - 4 个解析测试（含 `--source`/`--source-id` alias 守卫）。
- 验证：`cargo test -p zbrain-cli --lib <4 tests>` **4/4 通过**（lld-link；整 lib 含 3 个 run 函数已编译通过）。默认 target 被 Windows 监视器锁死（`.cargo-build-lock` os error 5）→ 用独立 `CARGO_TARGET_DIR` 重建 bin 冒烟（进行中）。
- 未提交，等显式触发 commit/push。

## g74-g76 根节点收口 + KNOWN-GAPS G76/G77 过期描述修正（对账 SSOT）
- 收口 g74-g76 根 `1`（children 1-1/1-2/1-3 全 completed）→ status 翻 `completed`，整图 26/26 completed。
- 按「节点 notes 不可全信、以 HEAD 为准」纪律，grep 核验 CLI 动词真实存在后与 KNOWN-GAPS 对账，修正两处过期：
  - **G76a 全完**：KNOWN-GAPS 原称「fs-source + by-mention 仍缺」——实测 `ExtractAction::{Links,Timeline,All}` 接 `extract_fs.rs`（fs --dir）+ `LinksAction::ByMention`+`run_links_by_mention`（by-mention）均已接线。状态列改「G76a done; G76b blocked: G35」，内联 ④「仍缺」改「已于 2026-08-16 收口」。仅剩 G76b（extract-conversation-facts，真 LLM，被 G35 阻塞）。
  - **G77 edges-backfill + reconcile-links 已完**：KNOWN-GAPS 原称「无 core op」——实测 `LinksAction::EdgesBackfill`+`run_links_edges_backfill`、`LinksAction::Reconcile`+`run_links_reconcile` 均在（core `backfill_edges`+`reconcile_links`）。状态列改「backlinks + edges-backfill + reconcile-links done; generic backfill remains」。generic `backfill` 仍缺独立 verb。
- 改动均为唯一子串替换（Edit 工具），numstat 干净（KNOWN-GAPS 2/2、JSON 1/1），CRLF 无泄漏。未提交，等显式触发 commit/push。
- 修正后真实剩余前沿：G74b（--rrf-k/--expand 仅写 report 不作用检索，小缺口）/ G75（reindex-code/frontmatter/multimodal）/ G77 generic backfill / G79（15 杂项）/ LLM 门控（G58/G76b）。

## G74b：`zbrain eval` 的 `--rrf-k`/`--expand` 接进真实检索（2026-08-16 收口 resolved）
- 接线：
  - `--rrf-k`：`SearchOpts` 新增 `rrf_k: Option<f64>`；`fuse_and_boost`（engine.rs:639）改 `opts.rrf_k.unwrap_or(RRF_K)`；`HybridSearchOpts.rrf_k` 透传到 `SearchOpts`。三引擎 keyword/vector/hybrid 全链路生效，`RRF_K=60.0` 退默认值。
  - `--expand`：新增生产级 `ChatExpansionProvider`（adapter 包 `Arc<dyn ChatProvider>`，`expand` 调 chat + `parse_expansion_response` 容错，错误/无法解析降级 `Ok([query])`）；`build_eval_expansion_provider`（`resolve_model` fallback "sonnet" + `resolve_recipe_strict` + `instantiate_chat`）无 key/model 返回 `None`→单查询降级 + 单次 warning；`run_one_eval_config` 闭包多查询按 best rank 合并（`HashMap<slug,usize>`）。
  - cli `eval_retrieve_slugs`：三策略均透传 `rrf_k`；`dedup_opts` 形参改为裸 `DedupOpts`（与 `eval_dedup_opts` 返回类型 + 调用点一致），内部构造 `HybridSearchOpts` 包 `Some(...)`。
  - core 新增 `adapter_tests`(6) + `rrf_fuse_tests`(2) 单测；`ai/mod.rs` 导出 `ChatExpansionProvider`。
- 验证（PowerShell + lld-link + 独立 `CARGO_TARGET_DIR`）：
  - 首编 `BUILD_EXIT=101`：`eval_retrieve_slugs` 调用点传裸 `DedupOpts` 但形参是 `Option<DedupOpts>`（2× E0308）→ 改形参为裸类型修复。
  - **后台任务丢失陷阱**：会话空闲后 `TaskOutput` 报 task "not found"，但 PowerShell 实际跑完、日志尾部 `error: could not compile zbrain-cli ... BUILD_EXIT=101`（无 cargo 进程在跑=真失败退出非卡死）。**务必直接读日志文件的 `BUILD_EXIT` 行，别信 `TaskOutput not found`**。
  - 重编绿后跑测试：`adapter_tests` 初 1 失败（`parse_useless_reply_yields_nothing`）——`parse_expansion_response` 第三分支把纯散文当备选返回；加列表标记判定（`- `/`* `/`N.`/`N)`）后纯散文降级空，与「useless reply yields no alternatives」契约一致。修复后 `adapter_tests` 6/6、`rrf_fuse_tests` 2/2、cli `--lib` 350 passed / 0 failed，全 `TEST_EXIT=0`。
- KNOWN-GAPS：G74b 行改 `resolved`；G74 主条目两处「`--rrf-k`/`--expand` 残余见 G74b」回写为「已由 G74b（2026-08-16）收口」。
- **未提交**，等显式 `commit+push`：feat = engine.rs+search/engine.rs+ai/expand.rs+ai/mod.rs+cli/lib.rs；chore = KNOWN-GAPS.md。

## G75：reindex-code / frontmatter / multimodal 全族收口（2026-08-16 续）
- 全族 4 子命令现均已 port（`ReindexAction` 枚举 Frontmatter/Multimodal 由 unit 变体改为带 Args；dispatch 改写；`run_reindex_frontmatter`/`run_reindex_multimodal` 新增）：
  - core：`BrainEngine` trait 加 `put_chunk_multimodal_embedding`（默认 Unsupported）+ libsql/postgres 双引擎实装；迁移 `0036_content_chunks_embedding_multimodal.sql`（sqlite BLOB / pg BYTEA）注册 version 36（**刻意跳过未注册孤儿 0035**）；`embedding.rs` `dimensions==0` 宽松哨兵（接受 provider 原生维度、HTTP 请求不送 dimensions）。
  - cli `config.rs`：`Config` 加 `embedding_multimodal_model: Option<String>`（`#[serde(default)]`）+ `impl Default` 补 `None`。
- **CLI 编译 4 错 → 全修复**（PowerShell + lld-link + 独立 `CARGO_TARGET_DIR=zb_target_g74b`）：
  1. **E0038 ×2**：`dyn serde::Serialize` 不是 dyn-compatible（`Serialize` 有泛型方法和对象不安全）→ `execute_raw` 的参数类型须是 `dyn erased_serde::Serialize`。`zbrain-cli` **不直依赖 erased_serde** → 改用 `format!` 把整数参数（`last_id`/`this_batch`）内联进 SQL，传 `&[]`（与既有 `execute_raw(sql, &[])` 调用一致，靠类型推断）。
  2. **E0609**：`config.embedding.base_url` 不存在——CLI 的 `config::EmbeddingConfig` 无 `base_url` 字段（与 core 的 `EmbeddingConfig` 是两回事）。multimodal client 直接 `.build()` 不传 base_url（provider base_url 由模型前缀/环境变量解析，与 `EmbeddingClient::from_env()` 同机制）。
  3. **E0063**：`impl Default for Config` 的结构体字面量缺新字段 → 补 `embedding_multimodal_model: None,`。
  4. **E0425**：删 `let p_empty` 声明后，尾部 `pending_after` 查询仍引用 `p_empty` → 改 `&[]`。
- 验证：`cargo build -p zbrain-cli` → **EXIT=0**（28s warm）；core `cargo test -p zbrain-core --lib` 前轮已 **2637 passed**；`cargo clippy -p zbrain-cli --lib` 对 `lib.rs`/`config.rs` **零 warning**。
- KNOWN-GAPS.md G75 行改 `resolved`（2026-08-16），描述全族 4 子命令 + core 改动 + 诚实保留（`reindex multimodal` 缺 TS 原 db-lock/checkpoint/unified-flag 崩溃恢复外壳）。
- **未提交**，等显式 `commit+push`：feat = engine.rs/libsql.rs/postgres.rs/embedding.rs/core lib.rs/effective_date.rs/cli lib.rs/config.rs + 两个 0036 迁移文件；chore = KNOWN-GAPS.md G75 + 本日志。

### Pitfall：cli 调 `execute_raw` 带参数
- `execute_raw` 形参类型是 `&[&(dyn erased_serde::Serialize + Sync)]`，而 `serde::Serialize` 不可作 trait object（E0038）。`zbrain-cli` 未直依赖 erased_serde，故**不要在 cli 里构造 `dyn serde::Serialize` 切片**。标量参数优先 `format!` 内联进 SQL 传 `&[]`；确需 trait-object 参数则给 cli 的 Cargo.toml 加 `erased_serde` 依赖并 `use erased_serde::Serialize;`（参考 core 的 consolidate.rs / symbol_edges.rs）。

## G77：generic `backfill` 独立 verb 收口（2026-08-16）
- 对账（TS→Rust 现状）：TS `src/commands/backfill.ts`（199 行，删于 `3c09a69f` Batch B）是 registry 分发器，`backfill list` + `backfill <kind>`，3 类：`effective_date`(implemented) / `emotional_weight`(implemented) / `embedding_voyage`(declared-only, v0.30.2 才落地)。
- Rust 现状：`effective_date` 已由 `reindex frontmatter` 覆盖；`emotional_weight` 已实现为 cycle phase `run_phase_recompute_emotional_weight(engine, &opts)`（仅 cycle 可达，**未直连 CLI**）；`embedding_voyage` Rust 侧根本没实现（与 TS declared-only 一致）。无通用 backfill registry。
- 实现（纯 CLI 增量，无 core 改动）：`Commands::Backfill(BackfillArgs)`（name="backfill"，positional `kind: Option<String>` + `--list`/`--batch-size`/`--concurrency`/`--resume`/`--dry-run`/`--keep-index`/`--max-errors`/`--fresh`/`--json`，镜像 TS flags）；`run_backfill_command` 分发：`list`/`--list`→`print_backfill_list`（✓/⊘ 枚举）；`effective_date`→`run_reindex_frontmatter(force,no-prompt)`；`emotional_weight`→自建 `LibsqlEngine` + `run_phase_recompute_emotional_weight`（JSON/human 双输出）；`embedding_voyage`/`unknown`→`anyhow::bail!`。4 个解析测试（list 位置/--list 旗/effective_date+dry-run/embedding_voyage）。
- 诚实保留：TS 的 `--concurrency/--keep-index/--resume/--fresh/--max-errors` 依赖连接池+checkpoint 机制，Rust 单趟幂等 runner 无对应列，这些 flag 接受但为 advisory/no-op（仅 `--dry-run`/`--json` 真作用）；未重建 TS 的 backfill-registry/backfill-base 框架（Rust 已有等价单命令 runner）。
- **未提交**，等显式 `commit+push`：feat = cli `lib.rs`（Backfill 枚举+Args+run_backfill_command+emotional_weight runner+list+4 测试）；chore = KNOWN-GAPS.md G77→resolved + 本日志。

### Pitfall：PowerShell 工具命令分隔符
- PowerShell 工具的 `command` 参数**不接受 `\n` 字面换行**当语句分隔（会 ParserError "UnexpectedToken"）。多语句用 `;`（或真实换行）。cargo 编译/`*>` 重定向仍按 PowerShell 语义工作（`$env:VAR=...; cargo build ... *> log`）。

## /zj-grilling + /zj-roadmap-driven 整体完成度审查（2026-08-16 晚）
- 快照：15 个 part JSON 共 454 节点，14/15 全 completed；仅 part13 剩 5 个 blocked（lint/lint_fix/integrity_auto/sync_retry_failed/repair_jsonb handler），其 decisions[] 早已裁定 wontfix（对应 KNOWN-GAPS G80-G83 + G78 repair-jsonb）。
- 用户对 5 个 Round-1 推荐**全部同意**：
  - Q1=b：完成判定 = TS 可退役、Rust 服务全部生产路径（非全命令 parity）。
  - Q2：part13 5 节点翻 wontfix 闭环路线图 → 工具无 wontfix 状态，落地为翻 `completed`（保留 notes wontfix 说明）；roadmap 现 **15/15 全 completed**（part13 28/28，无重复 id，父 1-3/根 1 级联）。改动未提交。
  - Q3：G55（remote MCP `takes_holders` 泄露）若 HTTP MCP 仅本地/受信则降级为已知缺口（非硬闸门）；**部署面未知，待用户确认是否对不受信外部客户端暴露**。
  - Q4：facts 高级 UX（G54）作独立产品路线图；G53 实体解析若影响现有 `recall` verb 可用性则优先小修。
  - Q5：G78（摄入/迁移 9 文件）/G79（杂项 16 文件）**triage 而非盲目 port**——先列生产调用方/实际用量，只 port 被生产流或用户依赖的，其余 wontfix（同 G80-G83 先例）。
- Round 2 前沿：Q1b 还需「生产路径覆盖矩阵 + 退役验证门槛」才能宣布 TS 可退役；G55 部署面是 Q3 唯一未决条件。

## Round 2 决策（用户回答，2026-08-16 晚）
- Q1 G55 部署面：用户答「暂不确定，但当下首选本地/受信、未来会考虑外部暴露」→ G55 维持已知缺口（非硬闸门），在 KNOWN-GAPS 标注「若 `serve --http` MCP 未来对不受信外部客户端暴露则升级为 must-fix 安全闸门」。
- Q2 退役门槛：选「轻量 = 绿编 + core 测试 + 手动冒烟」即可宣布 TS 退役（**不建 TS↔Rust 对等回放基建**）。
- Q3 G78/G79：选「启动 triage」→ 先 grep 代码/历史确认哪些被生产流或用户实际依赖，列 port/wontfix 清单（不盲目全 port）。

## G78/G79 triage 结果（2026-08-16 晚，启动 triage）
- 方法：查被删 TS 快照（`3c09a69f^`）命令间 import + 当前仓库（CLAUDE.md/docs/scripts）外部调用 + Rust CLI verb 覆盖。
- **Tier A 已解决（KNOWN-GAPS G79 过期，须翻 resolved/更新）**：`brainstorm`+`lsd` Rust 早已 port（cli lib.rs:358-751 `BrainstormArgs`+`run_brainstorm_command`，注释 "Faithful port of src/commands/{brainstorm,eval-brainstorm}.ts"；二者共享 orchestrator，eval-brainstorm 也接了）。G79 行仍写"brainstorm 等均 TS-only"是**过期 notes**（又一条"节点 notes 不可全信"）。`recall` 已在 G79 行标 resolved。`upgrade` 链：Rust 有 `ApplyMigrations`（CLAUDE.md 教用户直接跑 `apply-migrations`），故 `upgrade`/`post-upgrade` 仅是薄 wrapper（Rust cli 无 post-upgrade verb）。
- **Tier B wontfix（死技术/被替代/仅 dev/小众）**：`reinit-pglite`/`repair-jsonb`(pglite/JSONB 死技术)；`import`(audit 标 RUST_OWNED→Capture)；`embed`(Capture/API 替代)；`migrate-engine`(schema-pack/migrations 替代)；`migrations/index`+`migrations/types`(Rust MigrationRegistry 替代的内部 lib)；`files`/`integrations`(列表工具，1-4 test refs，小众)；`friction`(dev 工具)；`notability-eval`(小众 eval)；`ze-switch`(手动杆，小众)；`founder-scorecard`/`bench-publish`/`init-mode-picker`(小众/interactive)。
- **Tier C 真缺口（无 Rust 等价，可能生产用，待用户拍板 port/wontfix）**：`export`(通用 KB export，Rust 仅 EvalExport)、`frontmatter`(+`frontmatter-install-hook`，文件 frontmatter 工具，无 Rust 等价)、`auth`(auth 流/key 管理，Rust 无 verb)、`providers`(provider 配置管理，Rust 仅 config.providers map)、`upgrade`+`post-upgrade`(薄 wrapper，apply-migrations 已覆盖真实动作)。→ **用户裁定（2026-08-16）：Tier C 五个全部 port**（非 wontfix）。

## Tier C 实现进度（2026-08-16 晚）
- **export 已 port 完**（首个切片）：`Commands::Export(ExportArgs)` + `run_export_command`。复用 `list_pages(PageFilters{page_type/slug_prefix/source_id})`+`get_tags`+`serialize_markdown`+`get_raw_data`（`.raw/<slug>.json` sidecar）。`--restore-only` 依赖 storage-tier 配置 Rust 尚未 port → 清晰 `anyhow::bail!` 而非静默全库导出。验证：PowerShell+lld-link `cargo build -p zbrain-cli` EXIT=0；`cargo test -p zbrain-cli --lib export` **4/4**（3 新解析测试+1 eval_export）；`cargo clippy -p zbrain-cli --lib` 零 lib.rs 警告；numstat 184/0 纯增量。KNOWN-GAPS G79 行 export→resolved。其余 frontmatter/auth/providers/upgrade 待 port（task #52/#53/#54/#56）。

## Tier C 其余四命令（frontmatter/auth/providers/upgrade）port 完成（2026-08-16 深夜）
- 四个命令全部实现于 `crates/zbrain-cli/src/lib.rs`（纯增量，numstat 1059/0 无 CRLF 泄漏）；`Cargo.toml` 加 `sha2`/`hex`(workspace) + `rand="0.8"`。KNOWN-GAPS G78/G79 两行改 resolved (2026-08-16)，详述三 verb 落点。
- 关键发现（再证"节点 notes 不可全信"）：G78/G79 原称 frontmatter/auth/providers "无 Rust 等价"，实测 Rust 已有完整能力 → 全部可忠实 port，不必 wontfix：
  - `upgrade`(+`post-upgrade`)：Rust `apply_migrations::run_apply_migrations_command` 已覆盖真实动作；`run_upgrade_command` 仅打印 cargo 二进制 self-reinstall N/A 提示 + 委托 `post-upgrade`（=apply-migrations）。cargo 二进制无法 self-reinstall，故真实迁移动作被薄 wrapper 覆盖。
  - `providers`：Rust `zbrain_core::ai::registry::REGISTRY`（17 recipe 静态数据）+ `Recipe::has_touchpoint`；List/Env/Explain 全只读可行，Test 实时探针未 port（仅 env 就绪检查 + `--model` 缺则 bail）。
  - `frontmatter`：`parse_markdown`/`serialize_markdown`/`parse_frontmatter_from_body` 已在 core；Validate（空 frontmatter=错，`--fix` 暂未 port→bail）、Generate（推断 type/title/tags → serde_yaml 重建 frontmatter 块，`--fix` 写盘否则 preview）。
  - `auth`：`LibsqlEngine`+`execute_raw`（sha2 算 token hash + rand 生成 40 位 token，复刻"打印一次不再显示"）+ `AdminQueries`(list/revoke) + `OAuthQueries`(register/revoke client) + `reqwest` test。`--takes-holders`/`permissions`（无 permissions 列）诚实 bail。
- 5 个解析测试新增：`parses_upgrade_default`/`parses_post_upgrade_yes`/`parses_providers_list_and_env`/`parses_frontmatter_validate_and_generate`/`parses_auth_create_and_register_client`。
- 验证（PowerShell + lld-link + 独立 CARGO_TARGET_DIR=zb_target_g74b）：`cargo build -p zbrain-cli` EXIT=0；`cargo test -p zbrain-cli --lib parses_` 107 passed / 0 failed（含 5 新）；`cargo clippy -p zbrain-cli --lib` 新增代码区域零 warning（仅余 crate 全局 574 pre-existing 旧 warn）；build 6 warning 全在既有区域。
- 坑（已记 MEMORY 迁移规则）：`run_providers_command`/`run_frontmatter_command` 内部无 `.await` 被 clippy `unused_async` 标记 → 去掉 `async` + dispatch 去 `.await`。CLI runner 无 await 不得声明 `async`。
- 未提交，等显式 commit+push：feat = `crates/zbrain-cli/{src/lib.rs, Cargo.toml}`；chore = `docs/plans/MIGRATION.md`(G78/G79) + 本日志 + 此前未提交 export/part13 JSON/MEMORY。Tier C 五个命令（export+frontmatter+auth+providers+upgrade）至此全部 port 完成。

## /zj-grilling + /zj-roadmap-driven 收尾（2026-08-16 深夜）
- 背景盘查：450/450 roadmap 节点全 completed 但系统性滞后（节点只作索引）；真实剩余在 KNOWN-GAPS（54 条 open/blocked：48 open + 6 blocked）。TS 运行时已死（`bin/zbrain-rs.js` 直跑 Rust 二进制；`src/cli.ts`/`src/mcp/server.ts` 已死；`src/commands/` 空；operations.ts 4 个 schema 操作 grep 零活调用方）。
- 用户「全部同意推荐」三项决策：
  - Q1（迁移完成口径）→ (b) **命令级完成即终点**：Rust 已服务全部产品命令，未结项作 documented limitations 留 KNOWN-GAPS，迁移宣告完成。
  - Q3（G55 安全闸门）→ (b) 本地/受信部署维持已知缺口，非硬闸门；`serve --http` 对外暴露则升级 must-fix。
  - Q4（roadmap 脱节）→ (a) 新建 part14 路线图把 KNOWN-GAPS open/blocked 落成节点。
- 第二轮 frontier（依赖 Q1）：**不删 src/ 树** —— Q1=(b) 下 TS 死代码保留为 documented limitation，G38 维持「保留+子gap」。
- 交付：`docs/plans/roadmap-remaining.json`（63 节点 = 1 root + 8 簇 + 54 叶；簇：1-1 progress reporter / 1-2 doctor 收尾 / 1-3 facts 子系统 / 1-4 search 增强(blocked×5) / 1-5 engine 保真 / 1-6 命令作业misc / 1-7 安全MCP脱敏 / 1-8 TS死代码清理）。root decisions 记录 Q1/Q3/Q4 + G38 裁定。KNOWN-GAPS 加「迁移完成状态」横幅 + G38 加 Q1b 注。
- 注：part14 JSON 与既有 part13 同用 CRLF（git 仓库 JSON 约定），无需改写。本轮改动未提交，等显式 commit+push。

## docs/plans 收尾清理 + 死链最终清零（2026-08-16 最终轮）
- 全仓死链最终扫描：除已合并删除的 57 文件（trash 可恢复）及 MIGRATION.md §6 有意 provenance 列举外，repo 内无残留死链。
  - HEAD=true（曾提交、内容已沉淀 MIGRATION.md）的 28 处引用 → 重指 `docs/plans/MIGRATION.md`。
  - 从未提交（沙箱丢过的临时文件，内容不在 MIGRATION.md）的 32 处引用 → 标 `（已删除）` 保留文件名溯源。
  - 漏网代码注释 `bin/zbrain-rs.js:25`（global-flag-gap-audit）→ 重指 MIGRATION.md。
  - LIVE 引用（MIGRATION.md / roadmap-remaining.json 自身，未跟踪但存在）→ 保留。
- **CRLF 陷阱实测修正**（已写 MEMORY.md 行尾符陷阱节）：本仓实际 `core.autocrlf=true` + `.gitattributes`（`* text=auto` 入库归一 LF；`*/lib.rs text eol=crlf` checkout 强制 CRLF），并非旧记的 `false`。早轮 Pass1/Pass2 写操作使 64 个改动文件工作树呈 CRLF（HEAD 为 LF）；已 `python data.replace(b'\r\n',b'\n')` 清掉，`git diff --numstat` 全部仅真实行变更（最大 9/9），无 N/N 全文件重写。`.git/COMMIT_EDITMSG` 误报忽略。
- 状态：docs/plans/ 仅剩 `MIGRATION.md` + `roadmap-remaining.json`；57 旧文件 `git rm --cached` 已暂存删除、物理在 `C:/Users/victo/AppData/Local/Temp/zbrain_docs_trash/` 可恢复。全部改动未提交，等显式 `commit+push`。
