# 2026-07-19

## check-resolvable 全量子系统迁移轨道 (roadmap 1-6-5)

续前序会话。本批完成切片 1-6-5-1~1-6-5-4：

- **核心栈 (zbrain-core/src/skill_resolver/)** 全部端口完成：
  - `resolver_filenames.rs` / `skill_frontmatter.rs` / `skill_manifest.rs` / `trigger_index.rs` / `check_resolvable.rs`(checks 1-4) / `repo_root.rs`(auto-detect + install-path 兜底)。
  - `check_resolvable.rs` 给 `ResolvableReport`/`ResolvableIssue`/`IssueType` 等加 `serde::Serialize` + rename，供 CLI 发 JSON envelope；`IssueType` 加 `as_str()`。
  - `repo_root.rs` install-path 兜底从 `file!()`（编译期相对路径）改为对 `current_dir()` join 求绝对路径，并抽出 `install_path_fallback(module_dir)` 可测函数（避 `file!()` 在 `cargo test` 下 cwd=包目录导致解析失败）。
- **CLI 外壳 (zbrain-cli/src/check_resolvable.rs)** 新建：clap `CheckResolvableArgs`(--json/--fix/--dry-run/--verbose/--strict/--skills-dir) + `run_check_resolvable_command` + Envelope + resolve_skills_dir + render_human + `resolve_exit_code`(纯函数, 锁 D-CX-3 退出码契约)。`--fix/--dry-run` 暂拒(roadmap 1-6-5-8)。`lib.rs` 加 `pub mod check_resolvable` + `Commands::CheckResolvable` + dispatch。
- **TS 清理 (slice 1-6-5-5 部分)**：从 `src/cli.ts` 删除 `check-resolvable` 的 dispatch 分支、`CLI_ONLY`/`CLI_ONLY_SELF_HELP` 集合条目、help 文本行。`src/commands/check-resolvable.ts` 暂留(roadmap 1-6-5-9 重接 doctor/skillify-check 后删)。
- **验证**：`cargo test` 全绿（skill_resolver 共 53 单测；CLI 2 单测）。`zbrain check-resolvable` 对仓库 skills 树端到端验证：JSON/human 输出、exit 1(有 error)、`--strict` exit 1(有 warning)、`--fix` 拒、clean 树 exit 0 全部符合。

待办（未在本批完成）：1-6-5-5 收尾的 TS typecheck baseline 门、1-6-5-6/7/8(routing-eval/filing-audit/dry-fix)、1-6-5-9(重接 doctor+skillify-check)、提交。

## 本批收尾 (slice 1-6-5-5 完成)

- **TS typecheck baseline 门通过**：`bash scripts/typecheck-baseline.sh` → OK，64 inherited，0 new。
- **roadmap 状态更新**：`roadmap_cli.py update` 标记 1-6-5-1~1-6-5-4 = completed、1-6-5 / 1-6-5-5 = in_progress，并 `render` 刷新 md。
- **提交**：`cc5a567` — feat(cli): migrate `zbrain check-resolvable` skill_resolver core + CLI (1-6-5-1..4)，15 files +2764/−29。
- 决策复核：`--fix/--dry-run` 在本次 Rust CLI 显式拒绝(roadmap 1-6-5-8 后续)，不引入假 no-op；`src/commands/check-resolvable.ts` 保留为残留 TS(待 1-6-5-8/9 重接后删)，与"保留 TS core 供 doctor/skillify-check 消费"一致。
- 构建警告均为 zbrain-core 既有债务(autopilot/embedding 模块)，本次新增代码零警告。
- 余下轨道切片：1-6-5-6(routing-eval, Check5) / -7(filing-audit, Check6) / -8(dry-fix, --fix 写回) / -9(重接 Rust core 进 TS doctor+skillify-check 并删 TS 栈)。

## 开始 1-6-5-6 (routing-eval / Check 5) — grill-me + roadmap-driven 锁设计

用 `/zj-grill-me` + `/zj-roadmap-driven` 先锁设计再动手。探查发现 routing-eval 有**两个消费者**：
1. `src/core/check-resolvable.ts`(539–585) 内部调用为 **warnings**(仅当树含 routing-eval.jsonl)。
2. 独立 CLI verb `zbrain routing-eval`(`src/commands/routing-eval.ts`)：flags `--json/--llm/--skills-dir/--help`；退出码 0=clean / 1=任意 failure / 2=setup 错(no_skills_dir|no_resolver)；用统一索引 `loadSkillTriggerIndex`+`findPrimaryResolverPath`+`entriesToResolverContent`(v0.41.11 修复路径)；`--llm` 是占位。

**4 个 grill 决策 (均已 decide 落盘 roadmap 1-6-5-6 / 1-6-5-6-6)：**
- Q1 范围：核心 + 独立命令**一起迁**(q-0)。
- Q2 粒度：细粒度 **7 子节点**(已 add 到 roadmap：1-6-5-6-1~7)。
- Q3 `--llm`：独立命令**拒绝**(exit 1)，与 check-resolvable `--fix` 拒绝原则一致(诚实、不接収假 no-op)。
- Q4 fixture：**inline 字符串**(镜像现有 53 个 skill_resolver 测试，无外部文件)。

**实现进度 (1-6-5-6-1)：**
- `trigger_index.rs` 的 `parse_resolver_entries` 提为 `pub`(供 routing_eval 复用，对齐 TS `indexResolverTriggers` import)。
- 新建 `crates/zbrain-core/src/skill_resolver/routing_eval.rs`(Section A：normalize_text/extract_trigger_phrases/skill_slug_from_path/index_resolver_triggers + 9 单测)；`mod.rs` 加 `pub mod routing_eval`。
- 正在跑 `cargo test -p zbrain-core skill_resolver::routing_eval` 验证(全 crate 重编译中)。
- 后续 B/C/D 分节已留注释位：1-6-5-6-2 lintRoutingFixtures / -3 loadRoutingFixtures(JSONL) / -4 runRoutingEval+structuralRouteMatch / -5 接 check_resolvable 警告 / -6 独立 CLI 命令 / -7 门+提交。
