# 2026-07-19

## check-resolvable 全量子系统迁移轨道 (roadmap 1-6-5)

续前序会话。本批完成切片 1-6-5-1~1-6-5-4：

- **核心栈 (zbrain-core/src/skill_resolver/)** 全部端口完成：
  - `resolver_filenames.rs` / `skill_frontmatter.rs` / `skill_manifest.rs` / `trigger_index.rs` / `check_resolvable.rs`(checks 1-4) / `repo_root.rs`(auto-detect + install-path 兜底)。
  - `check_resolvable.rs` 给 `ResolvableReport`/`ResolvableIssue`/`IssueType` 等加 `serde::Serialize` + rename，供 CLI 发 JSON envelope；`IssueType` 加 `as_str()`。
  - `repo_root.rs` install-path 兜底从 `file!()`（编译期相对路径）改为对 `current_dir()` join 求绝对路径，并抽出 `install_path_fallback(module_dir)` 可测函数（避 `file!()` 在 `cargo test` 下 cwd=包目录导致解析失败）。
- **CLI 外壳 (zbrain-cli/src/check_resolvable.rs)** 新建：clap `CheckResolvableArgs`(--json/--fix/--dry-run/--verbose/--strict/--skills-dir) + `run_check_resolvable_command` + Envelope + resolve_skills_dir + render_human + `resolve_exit_code`(纯函数, 锁 D-CX-3 退出码契约)。`--fix/--dry-run` 暂拒(roadmap 1-6-5-8)。`lib.rs` 加 `pub mod check_resolvable` + `Commands::CheckResolvable` + dispatch。
- **TS 清理 (slice 1-6-5-5 部分)**：从 `src/cli.ts` 删除 `check-resolvable` 的 dispatch 分支、`CLI_ONLY`/`CLI_ONLY_SELF_HELP` 集合条目、help 文本行。`src/commands/check-resolvable.ts` 暂留(roadmap 1-6-5-9 重接 doctor/skillify-check 后删)。
- **验证**：`cargo test` 全绿（skill_resolver 共 53 单测；CLI 2 单测）。`zbrain check-resolvable` 对仓库 skills 树端到端验证：JSON/human 输出、exit 1(有 error)、`--strict` exit 1(有 warning)、`--fix` 拒、clean 树 exit 0 全部符合。

待办（未在本批完成）：1-6-5-5 收尾的 TS typecheck baseline 门、1-6-5-6/7/8(routing-eval/filing-audit/dry-fix)、1-6-5-9(重接 doctor+skillify-check)、提交。
