# 2026-08-13 工作日志

## /zj-grilling × /zj-roadmap-driven — #62 收口 grill（用户全程 A+A+A）

路线图节点 `docs/plans/zbrain-g74-g76-reimpl.json`（render → `ZBRAIN_G74_G76_REIMPL.md`）。

### Round 1（#62 收口）
- `1-1-5-4` (#62 `eval-suspected-contradictions`) → **completed**（run + retrieval 为终态）。
- Q1 收口标准：trend/review 拆为 `1-1-5` 下平级节点 `1-1-5-6`(trend) / `1-1-5-7`(review)，**不挂为 1-1-5-4 子节点**（路线图父子状态自动同步会把父强制降级 in_progress，#62 无法真收口）。
- Q2 judge 架构：one-call-one-pair judge 终态。TS 仅 1 个 judge 函数 `judgeContradiction`；标题 "judge×153" = 调用次数(pairs×runs)，非 153 个 judge。
- Q3 JudgeCache：不纳入 #62，单独节点 `1-1-5-8`（持久化 judge 缓存，30d TTL，正交性能优化）。

### Round 2（延伸实现策略）
- Q4 (`1-1-5-6` trend)：忠实 port TS `trends.ts`(118 行薄封装)。三后端各加 `writeContradictionsRun`/`loadContradictionsTrend` 引擎方法 + `contradictions_runs` 表 migration(存 TrendRow + report_json) + 移植 `renderTrendChart` ASCII 图表。
- Q5 (`1-1-5-7` review)：trend store 的 `report_json` viewer（加载最近一次 run 美化打印 verdict_breakdown/配对明细），薄封装，不膨胀为交互式标注队列。
- Q6 (`1-1-5` 优先级)：先 trend(6)→review(7)，再 #63 brainstorm(5)。

## ⚠️ 事故：本地 `.git/refs` 丢失 + 恢复（重要）

**现象**：`git rebase origin/rust-rewrite` 因沙箱缺少交互式编辑器报错（`could not mark as interactive`），随后 `git reset` 报 "not a git repository"。诊断发现 `.git/refs` 目录整个被删（疑似沙箱 safe-delete 在失败 rebase/reset 期间触发），`.git/objects`/`packed-refs`/`HEAD` 完好。`packed-refs` 含全部 ref 值（含 `refs/heads/rust-rewrite = 4ad5291c` 旧快照）。

**双重陷阱（本会话新发现）**：
1. 沙箱对**写文件命令会双重执行**（"⚠️ Sandbox bypassed (escalation-approved)"）——`decide`/`add`/`commit` 类命令跑两次，曾造成重复节点/决策，必须幂等写入 + 事后去重。
2. 目录改名 `zbrain→ZBrain`（NTFS 大小写不敏感）后，重命名后的 cwd 路径串失效，原生 git 报 "not a git repository"；但 `cd /c/workspace/github/jununfly/ZBrain && git ...`（MSYS 翻译路径）可用。

**恢复步骤（已验证）**：
1. `mkdir -p .git/refs/{heads,remotes/origin,tags}` 重建 refs 目录。
2. `git fetch origin rust-rewrite` 刷新远端引用（远端 tip = `df89e23f`，含全部历史 + 3 个 CRLF 治理提交 `d6c395d7/11bac542/df89e23f`）。
3. `git reset --hard df89e23f` —— 本地已损坏，远端才是权威，直接对齐到远端干净态（丢失的仅是被拒的、未合入远端的在途提交）。
4. 在途提交 `d2dcbfcb`（含 R1+R2 决策）已因 refs 丢失变 dangling 并被 GC 回收 → **无法恢复**，按对话历史用 Python 脚本确定性重建 json 全部决策/节点（幂等、保 CRLF），再 render。
5. `git push origin rust-rewrite` → 快进 `df89e23f..1b349350` 成功。

**教训**：
- 沙箱里尽量**避免 `git rebase`**（交互式依赖编辑器，易失败并触发 safe-delete 删 refs）。改用 `git reset --soft` + `git pull --ff-only` 或 `reset --hard <remote-tip-SHA>`。
- 写文件命令（含 roadmap CLI、commit）一律**幂等化**（按 id/q 缺失才补），并准备"从远端 tip 重建"的兜底脚本，因为沙箱可能丢失未推送的本地提交。
- `packed-refs` 在 `refs/` 缺失时会返回陈旧值，诊断时用 `git rev-parse <SHA>` 直接核对 commit 而非依赖 ref 名。

## 当前进度
- `1-1-5-4`(#62) completed；`1-1-5-6`(trend)/`1-1-5-7`(review)/`1-1-5-8`(JudgeCache) pending，待后续实现。
- 下一动作（用户决定）：实现 trend(1-1-5-6) → review(1-1-5-7)，或跳 #63 brainstorm(1-1-5-5)。均未开工。

## #298 brainstorm CLI 接线 + 路线图 1-1-5-5 收口（本会话）

**代码层面（前序会话已完成，本会话未改 Rust）**：
- `crates/zbrain-core/src/eval/brainstorm/`（orchestrator + judges + domain-bank + checkpoint 占位 + error-classify + `BrainstormResult`/`BrainstormIdea`/`JudgeAxisScores`/`JudgeIdeaResult` 等加 `serde::Serialize` 供 `--json`）。
- `crates/zbrain-cli/src/lib.rs` 三分发臂（`Brainstorm`/`Lsd`/`EvalBrainstorm`）+ `LSD_PROFILE` import；命令/参数/测试函数前序已落。
- `cargo check -p zbrain-cli --offline` 0 error；`cargo test -p zbrain-cli brainstorm_cli_tests` **4 passed / 0 failed**（PowerShell + lld-link）。CRLF 核验（lib.rs/engine.rs/eval/mod.rs/libsql.rs/postgres.rs 删除列全 0）通过。

**路线图 1-1-5-5 收口**：
- JSON 节点 `status`：`pending`→`completed`（`mode` explore→exploit），追加 `decisions`(Q1–Q5) + 收口 notes。
- **事故修复**：本会话初 `docs/plans/zbrain-g74-g76-reimpl.json` 的 `1-1-5-5.notes` 因前序用 Edit 插入多行被裸换行 + 未转义双引号 + 丢失闭合引号损坏（strict 解析失败）。用 Python 二进制读 + 字符级转义 + 二进制写回保 CRLF 修复，并归一早前翻倍的 `\\\\n`→`\\n`。修复后 `json.load` 通过、`status=completed`、无双反斜杠残留。diff 严格限定在 `1-1-5-5` 节点（hunk `@@ -321,13 +321,38 @@`，无全文重写）。
- 配套 md `ZBRAIN_G74_G76_REIMPL.md`「当前施工」→「已收口」段已更新（markdown 合法）。

**待用户显式触发**：commit + push（分支 `rust-rewrite`），feat/chore 分离。待提交范围：新增 `crates/zbrain-core/src/eval/brainstorm/` + 0033 双方言 migration；修改 lib.rs/engine.rs/eval/mod.rs/libsql.rs/postgres.rs + docs/plans md/json + .workbuddy/memory。本会话未自动 commit/push（遵守"等显式触发"铁律）。
