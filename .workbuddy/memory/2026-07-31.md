# 2026-07-31

## 1-6 orchestration 探索
- 完成 TS 侧事实摸底：runCycle 843行编排 + base-phase/budget-meter/drift/phantom-redirect/consolidate ~1460行 + dream.ts 306行。
- 关键事实：Rust cycle.rs 1660行、22 phase 中仅 6 stub (Sync/Lint/Backlinks/Extract/Embed/Consolidate)；drift.ts TS 无调用者；phantom-redirect 仅 extract-facts 消费；budget-meter 跨 3 phase 共享；Sync/Backlinks/Embed 在 Rust 已有子系统可直包臂。
- 决策 q-0 一并迁移 drift；q-1 phantom 纳入 1-6-6。
- 8 子节点已落 roadmap JSON (1-6-1..1-6-8)，1-6 与 1-6-1 置 in_progress。
- 待开始 1-6-1 编码：CycleOpts 补 signal/yield/synth 透传、no_database 守卫、pack 门控、resolveSourceForDir、extractTotals、错误信封、deriveStatus、last_full_cycle_at。

## 1-6-1 收口（编排骨架强化）
- CycleOpts +6 字段：yield_between_phases / yield_during_phase (Arc<dyn Fn() -> Pin<Box<dyn Future + Send>>>) / synth_input_file / synth_date / synth_from / synth_to / synth_bypass_dream_guard。
- derive(Default) 改 manual impl（Arc<dyn Fn> 不 Debug），CycleOpts 去掉 Debug 派生。
- run_cycle: extract_totals 回填 + write_last_full_cycle_at 调 get_source → 改 config JSONB + update_source。
- derive_cycle_status 改双参数 (phases, totals)，空列表→Failed；Ok/Clean 用 CycleTotals::any_nonzero 收敛 19 字段。
- Synthesize 臂透传 synth opts 到 SynthesizePhaseOpts (corpus_dir/date/from/to/bypass_dream_guard)。
- UpdateSourceInput 加 Default (原 Deserialize-only)。
- 错误信封 hint/docs_url 与 pack 门控延后到 1-6-3（BudgetMeter 阶段）；no_database 守卫因 Rust 强制 &dyn BrainEngine 不可 null 不适用，记为设计差异。
- 测试：cycle 14/14 绿、zbrain-core 2072/2072 绿。

## 1-6-1 后续
- 1-6-2 周期锁（per-source cycle lock）：busy→skipped/cycle_already_running、失败→failed/lock_acquisition_error。复用 crates/zbrain-core/src/sync/lock.rs 基建。

## 1-6-2 收口（周期锁）
- 新增 crates/zbrain-core/src/autopilot/cycle_lock.rs（文件式 advisory lock + per-source 命名 + 30min TTL stale auto-break + Busy 错误）。
- run_cycle 入口：needs_lock 阶段存在 + brain_dir 非空时 acquire；Busy→Skipped/cycle_already_running；Io→Failed/lock_acquisition_error（class=FilesystemError, code=CYCLE_LOCK_IO）。
- DB-lock 半边（zbrain_cycle_locks 表）随 1-6-4 stub 接线时落地，1-6-2 收敛文件锁。
- 改动：autopilot/mod.rs 增 pub mod cycle_lock；autopilot/cycle.rs run_cycle 增锁；cycle.rs 7 个测试 brain_dir 改 unique_brain_dir（消除并行撞锁）；runner.rs 8 个测试加 tmp_brain helper + 改 unique path。
- Test：cycle_lock 5 测 + cycle 集成 2 测（busy→skipped、per-source 独立）+ autopilot 316/316 绿 + zbrain-core 2080/2080 绿。

## 1-6 烤问 + 1-6-3 重拆
- zj-grill-me 拷问 BudgetMeter 范围：用户三选项全选"范围更大"（独立薄装器 / BaseCyclePhase 抽象 / drift 提前实装）。
- 1-6-3 重拆为 1-6-3-1/2/3 三子节点；1-6-7 合并到 1-6-3-3 标 completed。
- zj-roadmap-driven 校准：当前 focus 1-6-3-1（budget_meter.rs 薄装器）。
- 踩坑：roadmap_cli.py 的 --key=value 不被正确解析（_parse_args 用 -- 前缀切，a[2:] 切出 status=in_progress 整段作为 key；后续 argv[i] 不以 -- 开头但被当 value 取走）。后续 update --status 改用空格分隔 --status in_progress。

## skill 修复
- zj-roadmap-driven/roadmap_cli.py _parse_args 修：原版 --key=value 整段被当 key 解析（_parse_args 切 a[2:] 取 status=in_progress 为 key，args.get("status") 拿 None）。新增 partition("=") 分支支持 --key=value。

## 1-6-3-1 收口（budget_meter.rs 薄装器）
- 新建 crates/zbrain-core/src/autopilot/budget_meter.rs（port budget-meter.ts 188行 → 1-6-3-1 范围=纯模块，消费者属父 1-6-3）。
- **关键设计**：BudgetMeter 是 **pre-call 最大成本门**——check() 累加每次计划提交的 projected 最大成本、超 cycle cap 即拒绝下一次（拒绝不累加）；与 BudgetTracker（post-call 实际用量累加器：reserve 只投影、record 才加真实花费 TX1/TX2）语义根本不同，故独立成模块。
- 复用 budget.rs `cost_for_usage`（chat kind，REGISTRY 单一来源；TS 原 ANTHROPIC_PRICING 在 Rust 无对应表）；写独立 `dream-budget-YYYY-Www.jsonl` ledger（与 BudgetTracker 的 `budget-*.jsonl` 分离，对齐 TS 两文件分离）。
- 公开 API：BudgetMeter / SubmitEstimate / BudgetCheckResult / BudgetMeterOpts / estimate_max_cost_usd / dream_budget_audit_filename / reset_budget_meter_warnings_for_test。unpriced 模型按 model 维度 warn-once（BUDGET_METER_NO_PRICING）放行；budget<=0 禁用门。
- budget.rs：cost_for_usage 改 pub(crate) 供复用；autopilot/mod.rs 增 pub mod budget_meter。
- 测试：budget_meter 10/10 绿（priced/unpriced/disable/under-cap/exhaust-deny/accumulate/iso-week/ledger-row/denied-row）；zbrain-core 整 crate 编译通过（其他 test binary 0 failed）。
- 决策：auto_think 仍直用 BudgetTracker::reserve，是否改接 BudgetMeter 待 1-6-3-3 drift 接入时一并评估（消费者不在 1-6-3-1）。
- roadmap：1-6-3-1 标 completed + 加 decisions（关系说明）；删 md 重 render 单一段（⚠️ ROADMAP_SECTION_START/END）。**未 commit**（等显式 commit+push）。
