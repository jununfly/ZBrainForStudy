# 2026-08-09

## WSL 全 workspace cargo test 验证（nightly_probe 清理后）
- 命令：`wsl -u zb bash -lc '... cargo test --workspace --no-fail-fast'`（CARGO_TARGET_DIR=/home/zb/.cache/zbrain-target）。耗时 ~4m12s。
- 结果：**100 个 test-result 块，3694 passed / 0 failed / 0 ignored / 0 编译错误**（FAILED=0, error[=0）。
- 覆盖全部 7 crate（zbrain-core/cli/web/mcp/svg/chunking/worker）单测+集成测试+doc-tests。
- 含义：nightly_probe.rs / runner.rs 死代码清理（NIGHTLY_FIXTURE_REL_PATH 等）经全 workspace 真实验证安全，无 dead_code 警告。

## WSL 调用坑（已踩）
- `wsl -u zb 'script'` 会把整串当命令名 exec → 报 "No such file"；必须 `wsl -u zb bash -c '...'`。
- 外层 Git Bash 会吞 `$VAR` → 脚本内避免用变量，用字面路径。

## commit+push（用户指令）
- 提交 5 文件：nightly_probe.rs / runner.rs / 2026-08-08.md / MEMORY.md（含验证记录）/ 本 2026-08-09.md。HEAD 由 a162c526 推进。

## G74/G76 线开工：grill 定向 → G76 前提被源码对账推翻
- grill 四叉决策：主线=ZBrain 收尾线 → 攻 G74/G76 命令重实现（非 G58 原生 port，因通用 LLM seam ChatProvider+tool_loop 早已 port 完，G58 只是夜检探针占位）→ 第一刀 G76 extract 垂直切片。
- **动手前对账推翻前提**：`git show 3c09a69f^:src/commands/extract.ts` 取回 TS 源（1287 行），grep `chat|llm|openai|anthropic|ChatProvider` **零命中**。extract.ts 实为"抽 links + timeline"的**纯解析**（extractPageLinks/parseTimelineEntries + engine.addLinksBatch/addTimelineEntriesBatch），与 facts、与 LLM 全无关。KNOWN-GAPS 把它标成"依赖 LLM，被 G35/G60 阻塞"是**误判**。
- 且 Rust 侧**已实现大半**：`auto_fix::extract_links`(auto_fix.rs:179，已由 CLI `links rebuild-md-links` 暴露) + `auto_fix::extract_timeline`(:319，算法在但只被 run_auto_fix 内部调用)，core 内联测试 13 个（幂等/单 slug 范围/dangling/self-link 全覆盖）。
- 教训固化：**KNOWN-GAPS 的 blocked 标注不可全信，动手前必做「TS 源 vs Rust 现状」逐能力对账**。G74 同样标 blocked by G58，动手前需同样核实（core 侧 search/eval.rs 的 G73 已 resolved，19 命令里真需 LLM 的应是少数）。

## G76a 收口：新增顶层 `zbrain extract` verb（1-2-1 completed）
- 决策：不只补 timeline verb，做完整顶层 `extract` 带 `links|timeline|all` 三子命令，一次对齐 TS 命令形状；未来 `--source fs` 直接加 flag，不用再改命令树。clap 形状照抄 LinksAction/LinksRebuildMdLinksArgs。
- 改动：`crates/zbrain-cli/src/lib.rs` +298 行（5 个 hunk：454 Commands::Extract / 1083 ExtractAction+3 Args / 2037 dispatch / 6535 run_extract_command / 10735 五个 clap 解析测试）。
- 验证：`cargo test -p zbrain-cli --lib` = **297 passed / 0 failed**；`cargo check --workspace --all-targets` = EXIT 0；clippy 告警对改动区间 **0 命中**（workspace clippy 门禁本身既有红：zbrain-chunking 29 个 pedantic error + zbrain-cli 364 warning，非本次引入）。
- **e2e 真库冒烟**（隔离 config，`-c $WORK/zbrain.yml` + `init --pglite --force`）：links 2 页扫出 2 条（wikilink 与 markdown link 已去重）、复跑幂等 0 新增、`--slug` 单页范围生效、`links list` 可查落库；timeline 4 条（2 bullet + 2 header）、复跑幂等 0、不存在 slug 优雅返 0/0 exit 0 不崩。
- **保真待办**：Rust timeline 解析器只认 `- **YYYY-MM-DD** | Source — text` 与 `### YYYY-MM-DD — text` 两种（core 既有行为）；`- 2026-01-15: xxx` 这类不认。与 TS parseTimelineEntries 的格式覆盖面尚未逐条对账，已写入 KNOWN-GAPS 保真提醒。
- SSOT 同步修正：KNOWN-GAPS.md G76 行（拆 G76a 非阻塞 / G76b blocked by G35，标注 db-source 三子命令 done）、COMMANDS_TEAR_DOWN.md:45、`minions/handlers/extract.rs` 的 doc 注释（"has no Rust verb" 已 stale）。
- **刻意不做**：minion `extract` job type 仍返 unsupported_job — 入队会跑全脑抽取并写库，payload 契约（按 slug？全脑？dry-run？）未定，登记为 roadmap 节点 1-2-4 待决策，doc 注释已指向它。
- roadmap：`docs/plans/MIGRATION.md` 1-2-1 → completed，新增 1-2-4；md 已删后重 render（单一 ROADMAP_SECTION 校验通过）。
- 状态：**已 commit+push**（三笔分离）：`e0bee791` feat(cli) extract verb（lib.rs +298）→ `3ad7ae2e` docs(gaps) G76 误判修正（KNOWN-GAPS + COMMANDS_TEAR_DOWN + handler doc）→ `eddb4861` chore(roadmap) roadmap+memory。push origin/rust-rewrite，ahead/behind = 0 0。

## G74 对账（19 个 eval 命令）：blocked 标注同样失准，第二次印证
- 审计法：`git show 3c09a69f^` 批量取回 20 个 `eval*.ts` 到 Temp/zb_eval_src（`notability-eval.ts` 归 G79 不计入 19），Python 正则扫 `isAvailable('chat')|ChatProvider|anthropic|openai|.chat(|messages.create|gateway` + 提取 docstring + 提取内部 import，一次出全表。
- **结论：真依赖 LLM 的只有 4/19（21%）**，KNOWN-GAPS 说"多数依赖 LLM/Anthropic SDK"+ 整体 `blocked: LLM seam G58` 是错的。
- **三个疑似命中经逐行核实为假阳性**：`eval-replay.ts:132` 的 OpenAI 只在注释里且指 embedding 非 chat；`eval-schema-authoring.ts:12` 注释明说走 stubbed gateway test seam；`eval-suspected-contradictions.ts:253` 的 anthropic 是 `resolveModel` fallback 模型 ID 字符串（但该文件确调 `runContradictionProbe`+judge，仍计真 LLM）。→ **grep 命中必须逐行看上下文，注释/字符串/stub 都会假阳性**。
- 四分类：**A 空壳 3 个**（extract-atoms 39L / synthesize-concepts 36L / markdown-greenfield 40L，TS 自身即 `return {status:'not_yet_implemented'}`，迁移=迁 stub，建议 wontfix）／**B 底座已在只缺 verb 2 个**（`eval.ts` 依赖的 `search/eval.rs` 619L 已 port 完但**零调用者**，仅 `search/mod.rs:27` re-export；`eval-trajectory` 的 `find_trajectory`+CLI verb 已有）／**C 需新基建 10 个**（先决条件：`eval_candidates` 表在 Rust migrations 中**完全不存在**，是 export/prune/replay 的前提）／**D 真 LLM 4 个**（cross-modal+longmemeval 已由 G58 单列；takes-quality+suspected-contradictions 未覆盖）。
- **共同模式（G76 与 G74 连续两次）：算法层早已在 Rust，缺的只是 CLI 出口。** 后续 G75/G77/G78/G79 动手前一律先对账。
- SSOT 已修：KNOWN-GAPS.md G74 行（四分类全写入 + 状态 open→partial）、COMMANDS_TEAR_DOWN.md:43。roadmap 1-1 加对账决策 + 拆 1-1-1..1-1-5 五子节点，md 删后重 render（marker 唯一）。
- 状态：**未 commit**（等用户指令）。改动 = KNOWN-GAPS.md / COMMANDS_TEAR_DOWN.md / zbrain-g74-g76-reimpl.json / ZBRAIN_G74_G76_REIMPL.md，numstat 干净无行尾符污染，纯 docs 无代码改动故未跑 cargo。

## 环境修正
- WSL cargo 实际在 `$HOME/.cargo/bin`，**不是**记忆里写的 `$HOME/.rust/bin`（已修 MEMORY.md）。
- WSL 侧 `git status` 因 CRLF 差异会把整树报成 modified —— **判断改动范围必须以 Windows 侧 git 为准**。

## G74 B类 `eval.ts` verb 收口（1-1-2 completed）
- 决策：照 TS `eval.ts` 421 行 CLI 表面做保真 port。与 1-2-1(extract verb) 同构——算法已在 Rust(`search/eval.rs` G73 resolved)、零调用者、只缺 CLI 出口（第三次印证"算法层已在、缺 CLI 出口"模式）。
- 改动 `crates/zbrain-cli/src/lib.rs`：`Commands::Eval(EvalArgs)` 顶层 verb；`EvalStrategyArg`(Keyword/Vector/Hybrid, ValueEnum)+`From<...> for EvalStrategy`；`EvalArgs`(`qrels`/`config_a`/`config_b`/`strategy`/`rrf_k`/`expand`+`no_expand`(conflicts)/`dedup_*`/`limit`/`k` default 5/`json`)。
- 三策略分派（异步 query 闭包注入 `run_eval`）：keyword→`keyword_search_slugs`；vector/hybrid→`#[cfg(feature="embedding")]` 门控 `EmbeddingClient::from_env()`(+ `search_pages{query_embedding}` / `hybrid_search`)。hybrid 无 key 软降级 lexical-only；`--strategy vector` 无 key 报错。
- 未 port 子命令拒绝：`UNPORTED_EVAL_SUBCOMMANDS`(12 个 TS 子命令)+`reject_eval_subcommand` 指向 KNOWN-GAPS G74；负向 clap 测试守住缺口。人类可读表格 + `--json` 双输出；`print_eval_single_table` 保留 TS label 怪癖(`label = config.name ?? config.strategy ?? 'hybrid'`，默认打印 `strategy: Config A`)。
- **limit 保真 bug 修复**：`search/eval.rs` 抽 `resolve_eval_limit(config, k)` 单一真相源，原 `config.limit.unwrap_or(k*2).max(10)` 会把显式 `limit:3` 错 clamp 到 10（TS `??` 语义是 floor 只作用于派生默认）→ 显式 limit 原样透传。加 2 回归测试(`resolve_eval_limit_honors_explicit_limit_below_the_default_floor` / `run_eval_truncates_hits_to_an_explicit_small_limit`)。
- **诚实缺口 G74b**：`--rrf-k`/`--expand` 当前仅透传进 `EvalConfig` + 打印 `warning:`，未作用于 `run_eval`/`hybrid_search`（`engine.rs:367` 的 `RRF_K: f64 = 60.0` 硬编码 const，`--expand` 在 Rust 检索链无对应物）。已登记为 G74b，不假装实现。
- 验证：`cargo test -p zbrain-core --lib search::eval` = 15 passed；`cargo test -p zbrain-cli --lib eval` = 10 passed；`cargo check --workspace --all-targets` EXIT 0。
- **e2e 真库冒烟**（隔离 config + `init --pglite --force` + seed 3 页）：keyword 表 NovaMind P@3=0.67/R@3=1.00/MRR=1.00/nDCG@3=1.00；inline qrels + `--limit 1` 仅回 1 条(R@3=0.5)→ **直接证明 limit 保真修复生效**；A/B 模式、未 port 子命令负向、缺 --qrels、vector 无 key、--rrf-k warning 均按预期。
- SSOT 回写：KNOWN-GAPS.md G73(加 resolve_eval_limit 注) / G74 B类(eval verb 标记 ship) / 新增 G74b 行；roadmap `zbrain-g74-g76-reimpl.json` 1-1-2 → completed（md 同步翻 `[x]` + 时间戳）。
- 状态：**已 commit+push**：`8c991c56` feat(cli) eval verb + SSOT 回写（9 文件，+944/−28）；push origin/rust-rewrite → b99b8455..8c991c56，ahead/behind = 0 0。

## 1-1-3 判废空壳：对账推翻"3 空壳"前提（**关键修正**）
- 动手前 `git show 3c09a69f^:src/commands/eval-{extract-atoms,synthesize-concepts,markdown-greenfield}.ts` 取回三文件，并 grep Rust 现状，发现**前序会话 A 类"3 空壳、零产品价值、建议 wontfix"前提失准（命名假阳性）**：
  - `eval-extract-atoms` / `eval-synthesize-concepts` 虽 TS 侧 `return not_yet_implemented`，但其 bench 的 `extract_atoms`/`synthesize_concepts` cycle phase **已在 Rust 完整 port**（Part12 `1-1-2`/`1-3-1`：`autopilot/phases/{extract_atoms,synthesize_concepts}.rs` 接 `cycle.rs`、`postgres.rs:639`/`libsql.rs:734` 有 discovery SQL、`tests/extract_atoms.rs` 有集成测试）。→ **非空壳、不可 wontfix**，重分类为「可 port 的 eval 子命令」（跑 phase + 比 OpenClaw baseline 算 precision/recall/tier-agreement）。
  - `eval-markdown-greenfield` 才是**真空壳**：bench 的 OpenClaw→zbrain 迁移导入器在 Rust 仅是 `imported_from: 'markdown-greenfield'` frontmatter 排除标记（`libsql.rs:749`/`postgres.rs:654` discovery SQL 过滤），无命令/phase/feature 可 bench → 判废 wontfix。
- 结论：**3 个初判空壳中 1 真废（markdown-greenfield）、2 重分类（extract-atoms/synthesize-concepts）**。改动范围因此与前序计划不同——未"全 wontfix"，而是修正分类。
- SSOT 修正：KNOWN-GAPS G74 A 类拆 `A 真空壳(1, wontfix: eval-markdown-greenfield)` + `A′ 非空壳重分类(2)`；状态 tail "3 个空壳可判废"→"1 真空壳 + 2 重分类"；COMMANDS_TEAR_DOWN:43 同步；roadmap JSON `1-1-3` label 改判废+重分类、status→completed，父 `1-1-1` 也翻 completed（失准描述修正收口），md 渲染同步(`[x]` + 当前施工指针移到 1-1-4 + 时间戳 10:49:00)。
- 验证：JSON 解析 OK、无残留"3 个空壳"、4 个 docs 文件行尾符与 HEAD 一致（LF，无污染）。
- 状态：**已 commit+push**：`694a7278` docs(g74) 1-1-3 SSOT 修正（5 文件，+23/−14）→ b8652646..694a7278，ahead/behind = 0 0。这是一次 SSOT 修正，无 Rust 代码改动，未跑 cargo。

## 1-1-4 阶段0/1：eval_candidates migration + eval-export/prune verbs（**已完成，未 commit**）
- 决策（续 1-1-3 侦察）：C 类原"10 个非 LLM 需新基建"失准，eval-brainstorm 复核为真 LLM 已移 D 类 → C 类实 9 个。先落最薄、共享的 foundation：0030 `eval_candidates` 表 + 2 个 verb。
- 改动：
  - `crates/zbrain-core/migrations/0030_eval_candidates.sql` + `migrations-sqlite/0030_eval_candidates.sql`（26 列；sqlite 数组列 `retrieved_slugs`/`retrieved_chunk_ids`/`source_ids` 降级 JSON TEXT，引擎层 `parse_json_array` 解析）。**注册三件事**：include_str! const + `LIBQL_MIGRATIONS`/`POSTGRES_MIGRATIONS` registry.add(version 30) + `libsql_engine_migrations.rs` EXPECTED_VERSION 29→30。
  - `engine.rs`：`EvalCandidate`/`EvalCandidateFilter` 结构体 + trait 默认方法 `list_eval_candidates`/`delete_eval_candidates_before`（InMemory 默认空 impl，立即可编译）。
  - `libsql.rs`/`postgres.rs`：两 engine 实现 + 三 helper（`eval_candidate_from_row`/`parse_json_array`/`pg_row_to_eval_candidate` 必须放 **inherent impl 块**，非 trait impl 块）。
  - `zbrain-cli/src/lib.rs`：`Commands::EvalExport`/`Commands::EvalPrune` + `EvalExportArgs`(`--tool`/`--since`/`--limit`)/`EvalPruneArgs`(`--older-than` required/`--dry-run`) + dispatch + `run_eval_export_command`(流 NDJSON 每行 `{schema_version:1,candidate}`)/`run_eval_prune_command`(`--dry-run` 计数不删)；`UNPORTED_EVAL_SUBCOMMANDS` 移除 export/prune；+2 clap 测试。
- **编译踩坑（已修）**：① 误把 `#[async_trait]` 插到结构体前 → trait 丢属性引发 591×`E0038 not dyn compatible` 级联 1132 错误；必须回到 `pub trait BrainEngine`。② helper 方法误置 trait impl 块 → `E0407 not a member of trait`；移到 inherent impl。③ postgres `Vec<T>` 列 `.ok().flatten().unwrap_or_default()` 非法（`Option<Vec<T>>` 无 flatten）→ `Result<Vec<T>>::unwrap_or_default()`。④ CLI `write_all` 需 `use std::io::Write;`。
- **验证**：`cargo check -p zbrain-core --all-features` + `-p zbrain-cli --all-targets` 全绿；`zbrain-cli` eval 单测 12 passed（含 2 新）；`libsql_engine_migrations` 10 passed（含 v30 版本+幂等）；**新增 libsql round-trip 测试** seed→list 顺序(JSON 数组解析正确)/`--tool` 过滤/delete 全绿。
- **e2e 假阴性提醒**：用 python `sqlite3` 直连 libsql 生成的 `brain.db` 报 `no such table` —— 因 libsql 文件格式与 python sqlite3 不兼容，是**假阴性**，不是表缺失（单测已证 0030 通过 `init_schema` 真实建表）。验证读路径必须用 libsql 自身连接（round-trip 测试或 binary 自身），不可用外部 sqlite3。
- **诚实缺口**：捕获侧写入未接线（eval_candidates 表初为空），export/prune 真实数据需等捕获层（阶段4 replay 依赖）。阶段2-7（schema-authoring/gate/replay/whoknows/run-all/compare/code-retrieval）待排期。
- SSOT：roadmap JSON `1-1-4` notes 阶段0/1 翻 `[x]` + 阶段2-7 待办，`metadata.updated`→13:22:00；md 渲染同步（时间戳 + 当前施工段记阶段0/1 已 ship）。
- 状态：**未 commit**（等用户 commit+push 指令）。改动 = 2 migration SQL + engine.rs + libsql.rs + postgres.rs + cli/lib.rs + libsql_engine_migrations.rs(EXPECTED_VERSION) + roadmap JSON/md。

## 1-1-4 阶段2：eval-schema-authoring `aggregateVerdict` 纯函数 port（**已完成，未 commit**）
- 范围（roadmap 既定）：C 类 `eval-schema-authoring` TS 侧即 stub（`Full CLI harness lands in v0.39.1`），**只需移植 `aggregateVerdict` 纯函数 + 单测**，不建 CLI verb（与 TS 未完成态一致）。
- 取源：`git show 3c09a69f^:src/commands/eval-schema-authoring.ts`（137 行）。`aggregateVerdict(baseline, post_suggest, suggestion_count, low_confidence_count)` → `{verdict, delta, reasoning}`，5 决策分支：① `suggestion_count==0 && baseline>=0.9`→pass；② `suggestion_count==0`→inconclusive；③ `delta>=0.1`→pass；④ `delta>=0.0`→inconclusive；⑤ else→fail。
- 改动：新增 `crates/zbrain-core/src/eval/{mod.rs,schema_authoring.rs}` + `lib.rs` 加 `pub mod eval;`（插在 `embedding_context` 与 `eval_drift` 间，无 feature gate）；`SchemaAuthoringVerdict(Pass/Fail/Inconclusive)` enum + `AggregateVerdict` struct（derive Serialize，rename_all lowercase 对齐 TS 字符串）+ `aggregate_verdict` 纯函数；reasoning 用 `format!("{:.2}"/"{:.1}pp")` 忠实复刻 TS `toFixed`。
- **浮点边界坑（真·与 TS 一致）**：初版测试 `aggregate_verdict(0.60,0.70,...)` 断言 pass，实际返回 inconclusive —— 因 f64 `0.70-0.60=0.0999…<0.1`，`delta>=0.1` 为假。TS 同款算术结果一致，故**不是 bug 是忠实**。修正测试用 `aggregate_verdict(0.0,0.1,...)`（delta 恰为 0.1）→ pass，并加注释说明 0.6/0.7 会 artifact 到 inconclusive。
- **验证**：WSL `cargo check -p zbrain-core` 绿（120 个既有 dead-code warning，无任何来自新 eval 模块的新 warning）；`cargo test -p zbrain-core --lib schema_authoring` = **6 passed / 0 failed**（5 分支 + 0.1 阈值边界）。
- **诚实缺口**：仅 port 纯聚合器；`eval-schema-authoring` hermetic harness 主体（runDetect+runSuggest 重放、per-page 准确率对比）仍是 pending（TS 原即 stub），不假装实现。
- SSOT：roadmap JSON `1-1-4` 阶段2 翻 `[x]` + `metadata.updated`→13:29:00；md 当前施工段 "阶段0/1/2 已 ship" + 下一步指阶段3 gate qrels；KNOWN-GAPS.md G74 C 类补 aggregateVerdict port 注记。
- 状态：**未 commit**（与阶段0/1 同工作树，等用户 commit+push 指令）。改动 = eval/{mod,schema_authoring}.rs + lib.rs(1 行 pub mod) + 3 docs 文件。

## commit+push 后抓到行尾符事故（已救回）
- 用户 `commit+push` 后 `git show --stat` 发现 feat 提交 `92630e49` = 13 文件 31702+/30925-，**异常巨大**。诊断：`engine.rs`/`libsql.rs`/`postgres.rs`/`libsql_engine_migrations.rs` 四个 LF 文件被 Edit 工具写成 CRLF（orig_CR=0 head_CR=1），整文件重写；`cli/lib.rs` 本就 CRLF 故无碍（170 真实改动）。docs 文件 numstat 干净（1/1、7/5、5/5）未中招。
- 救回：python `data.replace(b'\r\n',b'\n')` 把 4 文件改回 LF；**提交前用 `git hash-object` 验证内容无损**（HEAD 去 CR 后 hash == 工作树 hash，4 文件全 ✓）。修正提交 `83320030`（4 文件 31117+/31117-，CRLF→LF 一次性代价）已 push（`5ce2a27b..83320030`），工作树干净。
- **教训固化**：push 前必须对改动【每个 Rust 文件】单独 `git diff --numstat` 查全文件重写；CRLF 泄漏用 python 改回 + hash-object 验证。已写进 MEMORY.md 行尾符陷阱规则 + 事故档案。

## 1-1-4 阶段3：eval-gate qrels 半边（2026-08-09）
- 范围（roadmap 既定）：移植 TS `eval-gate` 的 qrels 正确性门禁（faithful port `core/bench/correctness-gate.ts` 的 `runCorrectnessGate` + `qrels-file.ts` 的 `parseQrelsFile`）。TS 源从 `bcafcafd^` 取回（`src/core/` 更早被删）。
- 改动：
  - `crates/zbrain-core/src/eval/gate.rs`（新增，~590 行）：`parse_qrels_file`（federated `{relevant,expected_top1}` + legacy `relevant_slugs/first_relevant_slug`→default source 双 shape）、`run_correctness_gate<F,Fut>`（泛型 query_fn 注入检索，per-query error 记 `errored` breach）、`evaluate_correctness_gate`、`assemble_gate_result`、`GateResult` 信封（regression_gate.ran=false 诚实占位）。
  - `eval/mod.rs` 加 `pub mod gate;`（LF）。
  - `crates/zbrain-cli/src/lib.rs`：`Commands::EvalGate(EvalGateArgs)` + `run_eval_gate_command`（连接 LibsqlEngine→对每个 q 跑 `search_pages`→映射 `${source_id}::${slug}` 键→gate→打印信封→verdict fail 时 `anyhow::bail` 触发 exit 1）；`UNPORTED_EVAL_SUBCOMMANDS` 移除 `gate`；2 clap 测试（必填 qrels + 阈值解析）。
- 验证（WSL2）：`cargo check -p zbrain-cli --all-targets` 绿；`eval::gate` **11 passed**（7 parse + 4 gate 逻辑含 per-query error/expected_top1 分母）；CLI `eval_gate` **2 passed**；**空 brain e2e**（`-c` 隔离 config + `eval-gate --qrels json:... --json`）产出合法 JSON 信封（verdict fail、3 breaches、thresholds 0.7/0.6/0.5）且退出码 1；缺 `--qrels` 走 clap 退出码 2。
- 踩坑（已修）：① `QrelsRef` 缺 Serialize/Deserialize derive；② `Option<&str>.unwrap_or_else(|| format!())` 类型错→改 `.map(String::to_string)`；③ `async move -> Type` 非法语法（async 块不能注返回类型）→去掉注解靠推断；④ gate 测试 `.await` 需 `#[tokio::test] async fn`（5 处）。
- 诚实缺口：仅 qrels 半边；**baseline 半边（--baseline replay）pending**，等阶段4 replay 捕获写入。行尾符校验通过（lib.rs numstat 182/1 真实编辑；gate.rs/mod.rs 均 LF）。
- SSOT 回写：roadmap JSON 1-1-4 阶段2+3 翻 `[x]`（前序阶段2 标记静默失败，本次补标）+ metadata.updated→15:06；md「当前施工」段记阶段0/1/2/3 已 ship + 下一步阶段4；KNOWN-GAPS G74 ③' 补 stage3 gate port 注记。
- 状态：**未 commit**（与阶段0/1/2 同工作树，等用户 commit+push 指令）。

- **阶段3 commit+push（2026-08-09，`b480366a` feat + `987f89c9` chore memory）**：行尾符预检通过（lib.rs numstat 182/1 真实编辑；gate.rs 783 LF 行；docs 1/1、2/2 小改动），无泄漏。feat 提交 970+/5-（6 文件），干净。推至 origin/rust-rewrite `9ce314d9..987f89c9`。工作树干净。1-1-4 阶段0/1/2/3 已全部入库，下一步阶段4 eval-replay。

## 1-1-4 阶段4：eval-replay（2026-08-09）
- 范围（roadmap 既定）：重放 export 的 eval_candidates 快照 + Jaccard@k + top1 稳定性 + 延迟差。TS 源 `eval-replay.ts`(489L) 从 `3c09a69f^` 取回。
- 改动：
  - `crates/zbrain-core/src/eval/replay.rs`（新增）：`CapturedRow`(serde 容错，`_kind:baseline_metadata` 跳过 + schema_version==1 校验) + `parse_ndjson` + `jaccard_slugs`(纯集合 Jaccard，双空=1.0) + `replay_row` + `summarize`(含 rows_over_2x_latency 报警) + `replay_core`。检索用**泛型 query_fn 注入**（同 gate.rs 模式，`Fn(&str, ReplayTool, usize) -> Fut`），search→keyword / query→hybrid 双路径由调用方决定。
  - `eval/mod.rs` 加 `pub mod replay;`。
  - `crates/zbrain-cli/src/lib.rs`：`Commands::EvalReplay(EvalReplayArgs)`(--against 必填/--limit/--compare-limit/--json/--verbose/--top-regressions) + `run_eval_replay_command`（连 LibsqlEngine→读 NDJSON→parse_ndjson 预校验→query_fn 分发 keyword/hybrid→replay_core→JSON 信封或 human 表）+ `print_replay_human`；`UNPORTED_EVAL_SUBCOMMANDS` 移除 `replay`；2 clap 测试。
- 验证（WSL2，全绿）：`cargo check -p zbrain-cli --all-targets` 绿；core `eval::` **75 passed**（22 replay + 11 gate + 6 schema_authoring + 36 其它）；CLI `eval` **16 passed**；migration `libsql_engine_migrations` 10 passed（EXPECTED_VERSION=30 无回归）；**空 brain e2e**（`-c` 隔离 config + NDJSON 2 行 query+search）产出 JSON 信封（rows_replayed 2 / mean_jaccard 0.0 / top1 0.0 / 延迟 Δ-38ms）exit 0 + human 表 + 缺 `--against` clap exit 2。
- 踩坑（已修）：① 测试 NDJSON 误用 `json!([...])` 数组而非逐行对象 → 加 `ndjson()` helper 逐行 join；② CLI query_fn 初版返回 async block 非 boxed future → `Box::pin`；③ 关键：`replay_core` 初用 `&ReplayQueryFn` trait object 触发 E0271（async block 非 'static 无法 coerce）→ 改**泛型** `replay_core<F,Fut>`（同 gate.rs `run_correctness_gate`），消除 boxed-dyn 生命周期问题；④ `print_replay_human` 的 `'+'`(char) 与 `""`(&str) 类型不兼容 → 都改 `&str`。
- 诚实缺口：**捕获侧写入仍未接线**（阶段0 诚实缺口，`eval_candidates` 表初为空）；replay 消费 export 的 NDJSON、**不直接依赖表数据**，故不阻塞。gate baseline 半边（`--baseline` 进程内调 replay）的依赖已就绪，但该 CLI 接线本身仍 pending。
- SSOT 回写：roadmap JSON 1-1-4 阶段4 翻 `[x]` + metadata.updated→16:42；md「当前施工」段记阶段0/1/2/3/4 已 ship + 下一步阶段5；KNOWN-GAPS G74 新增 ④' replay port 注记（baseline 半边依赖已就绪）。
- 状态：**未 commit**（与阶段0/1/2/3 同工作树，等用户 commit+push 指令）。改动 = eval/replay.rs + eval/mod.rs + cli/lib.rs + 3 docs 文件 + memory。

- **阶段4 commit+push（2026-08-09，`aa3d8854` feat + `456f2bce` chore memory）**：行尾符**提交前**预检抓到 lib.rs 被 Edit 改写为 LF（numstat 12183/11945）→ python LF→CRLF 改回 → 塌缩到 239/1 纯真实编辑（上次 commit+push 事故教训应用成功）。replay.rs(585 LF)/mod.rs(LF)/lib.rs(CRLF 12183) 均正确。feat 提交 829+/5-（6 文件），干净。推至 origin/rust-rewrite `987f89c9..456f2bce`。工作树干净。1-1-4 阶段0/1/2/3/4 已全部入库，下一步阶段5 eval-whoknows。

## 1-1-4 阶段5：eval-whoknows（2026-08-09）
- 范围（roadmap 既定）：v0.33 双层 eval gate（ENG-D2）。L1 quality（hand-labeled fixture，top-3 命中率>=0.8）；L2 regression（eval_candidates query 行 re-run findExperts，Jaccard@3>=0.4，稀疏<20 行自动 skip）。TS 源 `eval-whoknows.ts`(451L) 从 `3c09a69f^` 取回。
- 改动：
  - `crates/zbrain-core/src/eval/whoknows.rs`（新增）：`read_fixture`(JSONL 跳过 `//`/`#` 注释 + 校验必填 query/expected_top_3_slugs) + `jaccard_at_k`(纯集合，取前 k，双空=1.0) + `top_k_hit` + `run_quality_gate`(泛型 whoknows_fn 注入, 阈值 0.8) + `run_regression_gate`(阈值 0.4, 稀疏<20 行自动 skip) + `assemble_report` 信封。检索用**泛型 whoknows_fn 注入**（同 gate/replay 模式）。
  - `eval/mod.rs` 加 `pub mod whoknows;`。
  - `crates/zbrain-cli/src/lib.rs`：`Commands::EvalWhoknows(EvalWhoknowsArgs)`(<fixture.jsonl> 必填位置参数/--json/--skip-replay/--limit default 5) + `run_eval_whoknows_command`（连 LibsqlEngine→read_fixture 预校验→whoknows_fn 调 `whoknows::find_experts`→L1 quality→L2 regression(用 `list_eval_candidates(tool_name='query' LIMIT 200)` 加载捕获行, skip_replay 则直接 skip)→assemble_report→JSON 或 human 表→overall fail 时 bail exit 1）+ `print_whoknows_human`；`UNPORTED_EVAL_SUBCOMMANDS` 移除 `whoknows`；2 clap 测试。
- 验证（WSL2，全绿）：`cargo check -p zbrain-cli --all-targets` 绿；core `eval::` **84 passed**（22 replay + 11 gate + 9 whoknows + 6 schema_authoring + 36 其它）；CLI `eval` **18 passed**；migration `libsql_engine_migrations` 10 passed（无回归）；**空 brain e2e**（隔离 config + 2 行 fixture）产出 JSON 信封（quality.hit_rate 0.0→FAIL、regression.status "skipped" 因无 eval_candidates 行<20、overall_passed false、exit_code 1）exit 1 + human 表(LAYER 1/2/VERDICT FAIL) + --skip-replay 同 exit 1。
- 关键点：L1 的 find_experts 已在 Rust（whoknows.rs），L1 无新基建；L2 复用阶段0 的 `list_eval_candidates`。捕获写入仍未接线（阶段0 诚实缺口），表初空→回归门实际走 skip（TS 的稀疏 fallback 正是为此设计）。
- SSOT 回写：roadmap JSON 1-1-4 阶段5 翻 `[x]` + metadata.updated→17:30；md「当前施工」段记阶段0/1/2/3/4/5 已 ship + 下一步阶段6；KNOWN-GAPS G74 新增 ⑤' whoknows port 注记。
- 状态：**未 commit**（与阶段0/1/2/3/4 同工作树，等用户 commit+push 指令）。改动 = eval/whoknows.rs + eval/mod.rs + cli/lib.rs + 3 docs 文件 + memory。

- **阶段5 commit+push（2026-08-09，`8bdf94b6` feat + `bc15d2f2` chore memory）**：行尾符提交前预检通过（lib.rs CRLF 216/1 真实编辑；whoknows.rs LF 433；mod.rs LF；docs 小改动），无泄漏。feat 提交 654+/5-（6 文件），干净。推至 origin/rust-rewrite `456f2bce..bc15d2f2`。工作树干净。1-1-4 阶段0/1/2/3/4/5 已全部入库，下一步阶段6 eval-run-all + eval-compare。

## 1-1-4 阶段6：eval-run-all/eval-compare（重设计成真编排，2026-08-09）
- 范围（roadmap 既定，经用户 AskUserQuestion 确认方向 q-0=重设计成真编排）：C 类 9 命令最后 2 个。TS 侧 `eval-run-all.ts`(356L)/`eval-compare.ts`(256L) 即 orchestrator stub / 落地即空表，且 `SearchMode`/`SEARCH_MODES`（conservative/balanced/tokenmax）系 TS 专有概念（Rust `search/` 无 `mode.rs`），是**不能直译**的根因——直译=搬未完成态。
- 决策：**重设计成真编排**。run_all 真调已 built 的 gate/replay/whoknows 三 verdict gate，聚合各自 verdict 成单次运行报告 `RunAllReport`（overall_passed = 各 check 全 Passed|Skipped，Errored/Failed 失败，Skipped 容忍）；compare 比对两次 run-all 报告看 per-check verdict 变化 `CompareReport`（regression = baseline Passed 且 current Failed|Errored；drop 成 Skipped 算 changed 非 quality regression）。
- 改动：
  - `crates/zbrain-core/src/eval/run_all.rs`（新增，~145 行，LF）：`RunAllStatus`(Passed/Failed/Errored/Skipped, serde lowercase) + `RunAllCheck` + `RunAllReport`(schema_version/run_id/ran_at/commit/overall_passed/checks/duration_ms) + `assemble_run_all_report` 纯函数；4 单测（all_passed/one_failed/errored_fails/skipped_tolerated/json_roundtrip）。
  - `crates/zbrain-core/src/eval/compare.rs`（新增，~150 行，LF）：`CheckDiff`(name/baseline/current/changed/regression) + `CompareReport`(schema_version/baseline_run_id/current_run_id/checks/any_regression) + `compare_reports` 纯函数（按两报告 name 并集匹配）；4 单测（identical_no_change/passed_to_failed_is_regression/skipped_to_failed_changed_not_regression/new_check_appearing_changed_not_regression）。
  - `crates/zbrain-core/src/eval/mod.rs` 加 `pub mod run_all;` + `pub mod compare;`（插在 whoknows 后，LF）。
  - `crates/zbrain-cli/src/lib.rs`（CRLF）：`Commands::EvalRunAll(EvalRunAllArgs)`/`EvalCompare(EvalCompareArgs)`（各带 G74 1-1-4 stage6 doc 注释）；`EvalRunAllArgs`(#derive Debug Parser: checks/qrels/against/fixture/k default 10/limit default 5/output/json/skip_replay) + `EvalCompareArgs`(#derive Debug Parser: baseline/current required/json)；dispatch 加两 arm；`UNPORTED_EVAL_SUBCOMMANDS` 移除 run-all+compare（剩 cross-modal/code-retrieval/brainstorm/suspected-contradictions/trajectory）。新增辅助：`parse_check_list`(None/空→全三；非法值 bail "not a valid gate")、`read_eval_input`(json: 前缀内联否则读文件)、`current_commit`(git rev-parse --short HEAD，失败回退 "unknown")、`run_eval_run_all_command`(连 LibsqlEngine→对 selected check 复用现有 gate/replay/whoknows 闭包模式→组装 RunAllCheck→assemble_run_all_report→默认写 `.zbrain-evals/run-all-<run_id>.json`→overall fail 时 bail exit1)、`print_run_all_human`、`run_eval_compare_command`(读两份报告→compare_reports→human 表→any_regression 时 bail)、`print_compare_human`；5 clap 测试（run_all defaults/parses_checks_and_flags/rejects_unknown_check / compare requires_both / compares_both）。
  - **replay check 判定**：空 brain 下 Jaccard=0.0 但 `rows_errored==0`→Passed；`rows_errored>0`→Failed；`rows_over_2x_latency` 仅作 metric。
- 验证（WSL2，全绿）：`cargo check -p zbrain-cli --all-targets` 绿（仅 pre-existing warnings）；core `eval::` **92 passed**（较阶段5 84 +8：run_all 4 + compare 4）；CLI `eval` **23 passed**（较 18 +5）；migration `libsql_engine_migrations` 10 passed（EXPECTED_VERSION=30 无回归）；**空 brain e2e**（隔离 config + `init --pglite --force`）：gate→FAIL(exit1, breaches=2, 报告落盘 `.zbrain-evals/run-all-<id>.json`) / replay(修正 fixture 数值 id)→PASS(exit0, status passed) / missing --qrels→清晰报错(exit1) / compare→正确比对表(exit0, any_regression=false)。
- 踩坑（已修，与阶段3/4/5 同型）：① WSL 默认 PATH 含空格导致 `export: not a valid identifier`→改用干净 `export PATH="$HOME/.cargo/bin:..."`；② 新结构体漏 `#[derive(Debug, Parser)]`→E0277（clap::Args 不满足）→补 derive；③ unused import（`parse_ndjson`/`CompareReport`）→精简导入；④ CLI 单测 `eval_run_all_rejects_unknown_check` 初版断言 try_parse_from 失败，但 `--checks` 在 parse 阶段接受任意字符串（校验在运行时 `parse_check_list`）→改调 `parse_check_list(&Some(vec!["bogus"])).unwrap_err()` 断言含 "not a valid gate"；⑤ e2e 命令名 `eval run-all`(空格)应为 `eval-run-all`(dash)；⑥ replay e2e ERRORED 因 fixture `"id":"r1"` 字符串应为 i64 `1`，修正后走 PASS 路径（编排层已优雅捕获为 errored 状态）。
- 诚实缺口（延续阶段0）：捕获侧写入仍未接线（eval_candidates 表初为空）；export/prune 真实数据待捕获层；replay 消费 export NDJSON 不依赖表数据；whoknows L2 因表空走 skip；schema-authoring hermetic harness 与 gate baseline 半边均 pending。
- SSOT 回写：roadmap JSON 1-1-4 阶段6 翻 `[x]` + 阶段7 留 `[ ]` + metadata.updated→18:29:00（md 渲染同步「当前施工」段记阶段0/1/2/3/4/5/6 已 ship + 下一步阶段7）；KNOWN-GAPS G74 新增 ⑥' run-all/compare 重设计 port 注记。
- 状态：**未 commit**（与阶段0/1/2/3/4/5 同工作树，等用户 commit+push 指令）。改动 = eval/{run_all,compare,mod}.rs + cli/lib.rs + 3 docs 文件 + memory。

## 1-1-4 阶段7：eval-code-retrieval（2026-08-09）
- 范围（roadmap 既定，经用户 AskUserQuestion 确认方向 q-0=接真实 code-intel ops）：C 类 9 命令最后一个。TS `WithCodeIntelStrategy` 即空 stub（仅 try/catch 返回空），但 Rust 已有真实 code-intel ops（`engine.find_code_def/refs` + CLI `run_operation`：code_def/code_refs/code_blast/code_flow）→ 严格优于 TS，接真实 ops。
- 改动：
  - `crates/zbrain-core/src/eval/code_retrieval.rs`（新增，~700 行，LF）：Rust port of TS `harness.ts`+`strategies.ts` 纯函数 + 单测。`CodeQuestionKind`(snake→code_intel_op：callers/blast_radius→code_blast、callees/execution_flow→code_flow、definition→code_def、references→code_refs、cluster_membership→None) + `CodeQuestion`/`QuestionResult`/`EvalRunReport`/`GateOpts`/`DEFAULT_GATE`(required_precision_delta_pp=10、required_top_1_stability_delta=0.15、min_questions_cleared=15)。纯指标 precision_at_k/recall_at_k/top1_stability_rate/normalize_retrieved/load_questions/load_default_questions(`include_str!` questions.json)。`run_code_retrieval_eval<F,Fut>` 泛型 `retrieve: &F` 注入（E0562 修：具名泛型 future，不 impl Trait 在 Fn 返回位）。27 单测全绿（含 `run_eval_baseline_like_fake` tokio::test）。
  - `crates/zbrain-core/src/eval/code_retrieval_questions.json`（新增，9337B，LF，30 问）：`git show bcafcafd^:src/eval/code-retrieval/questions.json` 逐字 bundle（含 .ts 路径，字符串匹配检索，对 Rust brain 产出诚实空 baseline；集合固定不可事后调）。
  - `crates/zbrain-core/src/eval/mod.rs` 加 `pub mod code_retrieval;`（插在 compare 后，LF）。
  - `crates/zbrain-core/Cargo.toml` 加 `anyhow = { workspace = true }`（core 不依赖 anyhow，本阶段需要）。
  - `crates/zbrain-cli/src/lib.rs`（CRLF）：`Commands::EvalCodeRetrieval(EvalCodeRetrievalArgs)`（baseline/with_code_intel/compare<2 reports>/questions/source/k default 5/save/json，doc 指向 G74 1-1-4 stage7）；dispatch 加 arm；`UNPORTED_EVAL_SUBCOMMANDS` 移除 code-retrieval（剩 cross-modal/brainstorm/suspected-contradictions/trajectory）；`run_eval_code_retrieval_command`（compare→`run_eval_code_retrieval_compare`；否则校验 baseline/with_code_intel 必选一→`load_default_questions` 或指定文件→连 LibsqlEngine→构造 `retrieve` 闭包（baseline: `HybridSearchOpts{limit, source_id}` + `hybrid_search` keyword-only；with-code-intel: `with_code_intel_files` 按 `question.kind` 分发到真实 ops，cluster_membership 及 op 报错诚实降级空）→`run_code_retrieval_eval`→`--save` 写 JSON / `--json` 或 `print_code_retrieval_report`）；`with_code_intel_files`/`extract_code_retrieval_paths`/`collect_paths`/`looks_like_file_path`；`run_eval_code_retrieval_compare`（`evaluate_gate`→`--json` 或 human 表→`!passed` bail exit 1）；`read_code_retrieval_report`/`print_code_retrieval_report`；4 clap 测试。
- 验证（WSL2，全绿）：`cargo check -p zbrain-cli --all-targets` 绿（pre-existing warnings 无新增）；core `eval::` **119 passed**（较阶段6 92 +27 code_retrieval）；CLI `eval` **27 passed**（较 23 +4）；**空 brain e2e**（隔离 config）：`--help` exit0 / `--baseline`&`--with-code-intel` exit0 诚实空报告+`--save` 写 JSON / `--compare A B` gate FAIL exit1 / 缺 mode exit1 / `--compare` 单报告 clap exit2。
- 踩坑（已修）：① async 闭包不能写 `-> Type` 返回类型→去掉；② E0433 anyhow 未链接→core Cargo.toml 加 anyhow dep；③ **E0562 impl Trait 不能用于 Fn 返回位**→`run_code_retrieval_eval<F,Fut>(retrieve: &F)` 具名泛型（照 replay_core 模式），test 传 `&retrieve`；④ E0373/E0728 闭包 async block 借 `q`/`k` 致 future 生命周期无 bound→闭包内 `let q = q.to_string()`（owned）+ `async move`；test `run_eval_baseline_like_fake` `#[test]` 调 `.await`→改 `#[tokio::test]`；⑤ WSL `tr -d '\000'` 去 NUL。
- **行尾符救回（铁律）**：提交前 `git diff --numstat` 发现 `Cargo.toml 119/118` 全文件重写签名 → 经查证 `git ls-files --eol` 显示 `i/lf w/crlf`（index 旧 LF、工作树被 Edit 改写成 CRLF），HEAD blob 实为 LF（118 行，0 CRLF）→ **Edit 把 LF 文件写成 CRLF 的泄漏**（`cat -A` 确认 `^M$`）。经 python `data.replace(b'\r\n', b'\n')` 改回，numstat 塌缩到 `1/0`，内容无损（CR-stripped diff 仅多 `anyhow = { workspace = true }` 一行）。其余 Rust 文件：lib.rs 维持 CRLF（13482 CR，与 HEAD 同 convention，450/1 真实编辑无泄漏）、mod.rs/code_retrieval.rs/questions.json 均 LF 0 CR。**教训落地：提交前必查 numstat + `git ls-files --eol` 双保险**，LF 文件被 Edit 改写成 CRLF 的泄漏仍可发生。
- SSOT 回写：roadmap JSON 1-1-4 status→completed + 阶段7 翻 `[x]`（C 类 9 命令全 ship）+ metadata.updated→22:50:38；md「当前施工」段 1-1-4 标 [x] + 头改指下一焦点 1-1-5 + 补 阶段7 已 ship 注记；KNOWN-GAPS G74 补 ⑦' code-retrieval port 注记。
- 状态：**未 commit**（与阶段0/1/2/3/4/5/6 同工作树，等用户 commit+push 指令）。改动 = eval/{code_retrieval.rs,code_retrieval_questions.json,mod.rs} + cli/lib.rs + core/Cargo.toml + 3 docs 文件 + memory。

## 1-1-5 侦察对账（2026-08-09 晚，mode=explore，待用户定范围）
- 范围：roadmap JSON 1-1-5 label = 真 LLM 的 4 命令（cross-modal/longmemeval/takes-quality/suspected-contradictions）+ md 把 brainstorm 也归 1-1-5 → **共 5 个 `eval` 子命令**。与 1-1-4（非 LLM，只缺 CLI 出口）本质不同：本批**算法层 Rust 侧基本空白 + 全 LLM 重依赖 + 无 LLM key 不可测**。
- TS 源（删除前导出 Temp 扫描）：`bcafcafd^` 取 37 个 core 文件 + `3c09a69f^` 取 eval-longmemeval.ts + `b583a075^` 取 nightly-probe-adapters/nightly-quality-probe.ts = **38 相关文件 ~8000 行**。LLM 信号密度（grep）：suspected-contradictions `judge×153`（15 文件/2613 行/13 LLM 文件）、brainstorm `judge×82`+`gateway.chat×3`（5 文件/2288 行/4 LLM）、takes-quality `judge×4`+openai/anthropic/gpt/claude（10 文件/1139 行/4 LLM）、longmemeval `LLM×11`（3 文件/1243 行/1 LLM）、cross-modal anthropic/claude/openai/gpt（5 文件/743 行/2 LLM）。
- Rust 现状对账：① **CLI UNPORTED_EVAL_SUBCOMMANDS = [cross-modal, brainstorm, suspected-contradictions, trajectory]**（注意 takes-quality/longmemeval 不在——longmemeval 走 G58 nightly_probe 通道非 `eval` verb；takes-quality 是 `eval-takes-quality.ts` 但 Rust 未登记 guard，目前 `eval takes-quality` 会落入 eval dispatch 未知分支）；② **引擎方法全缺**：brainstorm domain-bank 依赖的 `listPrefixSampledPages`/`enumeratePrefixes`/`fetchFar` Rust 无；③ **各命令族 Rust 模块全无**（cross-modal 仅 G58 占位、takes-quality/severity_classify/矛盾 judge 均零；contradiction/brainstorm/judge 的 grep 命中皆 autopilot 自身逻辑误报）；④ **通用 LLM seam 已 port**（ai/chat.rs:331 `ChatProvider` trait + ai/tool_loop.rs:263 `tool_loop`）→ 不阻塞；⑤ **G58 占位已存在**：nightly_probe.rs `run_long_mem_eval`/`run_cross_modal_batch` trait 方法无条件返 `Err("fixture not found"/"batch eval crashed")`、flag off（诚实 stub）。
- 关键判断：本批不能照搬 1-1-4「只补 CLI 出口」模式——judge/引擎方法需真 port。规模显著（~8000 行），且 LLM 命令无 key 不可单测 → 须先定 L1 范围（最小闭环 vs 全保真）再动手。导出物在 `C:\Users\jununfly\AppData\Local\Temp\st75\`（ts/ 38 文件 + recon.json 扫描结果），可复用。
- 待办：向用户问范围（L1 最小闭环 / G58 式诚实占位 / 全保真 port），再拆阶段。

## 1-1-5 #58 roadmap SSOT 回写（2026-08-09 16:54）
- 完成 #59 cross-modal 收尾后的 SSOT 同步。JSON（zbrain-g74-g76-reimpl.json）为 SSOT：
  - `1-1-5`：`status` pending→in_progress；label「4 个」→「5 个」含 brainstorm；`children` 展开 5 子节点。
  - 新增 `1-1-5-1` cross-modal（completed，记 #59 全量单测 21 + CLI 325 + core lib 2439 + clippy regex-in-loop=0 + e2e 无 key 诚实降级 EXIT1 + repair_json 真移植 bug 修复）；`1-1-5-2` longmemeval(pending,#60) / `1-1-5-3` takes-quality(pending,#61) / `1-1-5-4` suspected-contradictions(pending,#62) / `1-1-5-5` brainstorm(pending,#63)。
  - `metadata.updated`→16:54:25。roadmap_cli.py validate = Valid；tree 1-1-5 渲染正确。
- md（ZBRAIN_G74_G76_REIMPL.md）**手工同步**（未跑 render，避免丢弃 1-1-4 运行史）：1-1-5 翻 `[~]` + 加 5 子 bullet；「当前施工」标题改指 1-1-5 + 末尾补「阶段8 / 1-1-5 启动」段落（#59 cross-modal 完成 + 剩余 4 族待启）；头部时间戳对齐 16:54:25。
- **行尾符预检通过**：lib.rs(CRLF 保真) / mod.rs+md+json(LF 保真) / cross_modal.rs(新文件 LF, 0 CR)；无泄漏。
- 状态：**未 commit**（等用户 commit+push 指令）。改动 = zbrain-g74-g76-reimpl.json + ZBRAIN_G74_G76_REIMPL.md（纯 docs，无 Rust 代码改动，未跑 cargo）。
- 下一步：用户 `开始 1-1-5` 已确认 q-0 全保真 port；#59 已完成，待启动 #60 longmemeval（替换 G58 占位）等。
