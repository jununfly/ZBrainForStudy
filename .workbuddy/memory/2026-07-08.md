# 2026-07-08

- 收尾 Part3 `1-1-3 Rust 交叉编译 + 发布管线`（上个 session 已 grill Q1-Q5 全落盘 + 实现 + 本机验证全绿，但未提交、未写 memory；本 session 补 commit + memory）。**scope=a**：本机可验证部分现在做，darwin/linux 真实交叉编译与 clawhub publish 写审计移交。四项改动：(1) **`.cargo/config.toml` 删全局 `[build] target` 硬编码**——原 `x86_64-pc-windows-msvc` pin 使每次 cargo 双应用、阻断 `--target` 交叉编译、且把 binary 挪到 `target/<triple>/` 而非自然 `target/{debug,release}/`（Part2 rc=127 footgun 根因）；删后靠 host 默认（已验 rustc host=msvc / rustup default msvc / 无 rust-toolchain 文件，保持 msvc 安全）。(2) **`bin/zbrain-rs.js` wrapper 加 debug 路径覆盖**——抽纯函数 `getBinaryCandidates(platform,arch,root)` 导出可测；候选顺序 triple-specific 先于裸 `target/`，每处 release 先于 debug（优先 shipped 二进制，裸 `cargo build` debug 兜底）；关掉 rc=127 footgun。(3) **`.github/workflows/release.yml` 重写为 Rust 4-triple 矩阵**——`aarch64/x86_64-apple-darwin`+`x86_64-unknown-linux-gnu`+`x86_64-pc-windows-msvc`，各在原生 runner 跑 `cargo build --release --target <triple> -p zbrain-cli`，artifact 按 triple 命名去掉 `gbrain-*`；退役 `bun --compile` TS 编译。(4) **`package.json` `build:all` → `bun run build:rust`**（=`cargo build --release -p zbrain-cli`），本地 `prepublish:clawhub` 产 host Rust binary。
- **TDD（wrapper 部分）**：`bin/zbrain-rs.test.mjs` +3 用例（release-before-debug 顺序、裸 `target/debug` 覆盖、unknown platform 回退双 profile），RED（原 wrapper 无 `getBinaryCandidates` 导出）→ GREEN 7/7。**本机验证**：`cargo build -p zbrain-cli` 成功落 `target/debug/zbrain.exe`、`node bin/zbrain-rs.js --version`→`zbrain 0.0.1`、release.yml/package.json 合法。
- **验证盲区（审计移交，写 `docs/plans/MIGRATION.md`）**：本机 Windows 仅装 x86_64-pc-windows-{msvc,gnu}，无法真验 (1) darwin/linux 交叉编译（首个 `v*` tag push 才真跑，各 triple 用原生 runner 免 cross-linker）、(2) `clawhub package publish`、(3) **DELIVERY GAP**——wrapper 只在 `target/` 找二进制，但 release.yml 上传的是 GitHub release **artifact** 不落 `target/`，`target/` 又 gitignore 不入 bundle；当前唯一 delivery 靠 wrapper 运行时 `cargo build` fallback（需用户有 Rust 工具链），follow-up 需把 release artifact/postinstall 下载接进 wrapper 搜索路径。(4) version drift（`--version` 0.0.1 vs package.json 0.41.x）pre-existing 非本刀范畴。commit `<pending>`。
- **至此 `1-1` 发布基础设施迁移全树 completed**（1-1-1✅/1-1-2✅/1-1-3✅），父 `1-1` 冒泡 completed。Part3 剩 `1-2`（1-2-2 blocked 压父 in_progress）、`1-3` TS 入口退役（硬依赖 1-1 已满足）、`1-4` search rerank。下一候选：`1-3` 或 `1-4`。
- **`1-4` 转向：单拆 boost 子树 + `1-4-4-2` salience_boost 首刀 completed（TDD GREEN）**。grill 探查发现 1-4 唯一剩余叶子 `1-4-3`（--explain 展示层）blocked 于 boost 归因缺失；用户定方向"先转向做 boost 迁移"，新建 `1-4-4`（boost metadata-axis 子系统，in_progress）挂 `1-4-4-2`(salience，首个端到端 tracer)/`1-4-4-3`(recency，pending)。grill 落盘 4 决策：Q2=a（1-4-4 下按数据就绪度分层拆）、Q3=a（数据未迁 boost 登 KNOWN-GAPS 不建 pending 子节点避免冒泡卡 part3 收尾）、Q4=a（骨架+salience 合并为首刀）、Q5=(1)A+(2)A（boost 接 **engine 层** `InMemoryEngine::search_pages` 紧接 RRF 融合后 + salience strength 硬编码 'on' k=0.15）。**关键 TS 真相**：`hybridSearch` pipeline=keyword+vector→RRF→normalize→**boost**→cosine re-score→dedup；`runPostFusionStages`(hybrid.ts:282) 先 `computeFloorThreshold`(top*ratio 一次算) 再各 boost gated；`applySalienceBoost` factor=`1+k*ln(1+score)` k=0.15'on'/0.30'strong'；每 boost stamp `*_boost` 字段供 --explain；`base_score` 入口捕获一次（boost 前）。**Rust 病根**：`SearchResult` 无任何 `boost_*` 字段，打分硬编码关键字加权融入 score 无独立可归因阶段。**实现**(engine.rs)：`SearchResult`+=`salience_boost:Option<f64>`（None=未施加供 --explain 诚实）、`SearchOpts`+=`floor_ratio:Option<f64>`、`const SALIENCE_BOOST_COEF_ON=0.15`、`fn compute_floor_threshold`(None/越界/无正分→NEG_INFINITY 否则 top*ratio)、`search_pages` 重构把持锁融合段包进 scoped block 让 MutexGuard 在 async `get_salience_scores().await` 前 drop（否则 future 非 Send）、post-fusion salience 阶段(gate 跳过 score<floor + factor 乘 + stamp)、测试 mutator `set_emotional_weight_for_tests`。2 tracer 测试(reorders_and_stamps + respects_floor_gate)。**修了 summary 遗留的重复 dangling 测试块**。验证：603 lib tests + cargo build --workspace 全绿。缺口 **G13**（salience-strength-by-mode 硬编码 'on' + sibling boosts backlink/recency/graph/source/exact-match 各阻塞数据层）已登记 KNOWN-GAPS.md，engine.rs 两处 FUTURE 锚点双向指针。commit `<pending>`。
- **`1-4-2-3` completed（调查+对齐落盘刀，无生产接线代码）**。grill 探查推翻节点前提：TS Think 检索（`gather.ts:110`→`hybridSearch`）本就内建 rerank（由 search mode `reranker_enabled` 门控，非 Think 层产品判断）；Rust 上一刀（1-4-2-2）把 rerank 接在 operation 层 `QueryOperation::execute`，而 `ThinkOperation` 走 `engine.search_pages`（operation.rs~1580，limit5/向量关）**绕过** operation 层后处理 → Think 意外丢 rerank。**病根=接入点分层错位**，不是"要不要"。**新项目约定（Q4，已写 MEMORY.md）**：目标外已知缺口集中登记到 `docs/plans/MIGRATION.md`（无日期前缀活文档，"毒草长在阳光下"），散落 FUTURE 注释瘦身为指路牌、UNMIGRATED 常量+锚点测试原样保留、双向指针。交付：(1) 建 KNOWN-GAPS.md 登记 G1-G12（G1 Think-rerank 含详情 + 推荐路径=抽 operation 层共享 `retrieve_and_rerank` helper，否决 engine 下沉/operation 复制）；(2) operation.rs 加 `FUTURE(think-rerank)` 自解释注释+G1 指针；(3) 3 FUTURE 瘦身（search-attribution/progress-reporter/schema-pack）+ 2 UNMIGRATED 常量（doctor/schema-pack）加 registered 指针。验证：cargo build 3 crate 通过、zbrain-cli 125 tests 全绿（含锚点测试）。commit `<pending>`。
- **`1-4-4-3` recency_boost completed（TDD GREEN，boost 子系统收官）**。grill 落盘 3 决策：Q1=C（全套配置解析链都迁：DEFAULT_RECENCY_DECAY 11 prefix + DEFAULT_FALLBACK + env parser + yaml parser + caller override + resolve merge later-wins）、Q2=A（`SearchOpts` 携已解析 map，engine 只 apply 不读 env——决定性区分**数据 vs 配置**：salience 走 engine 因 `get_salience_scores` 是 engine 自己的数据；recency decay map 是策略/配置属 caller 关注点，engine=纯打分机）、Q3=A（抽纯函数 `apply_recency_boost` 注入 `now_ms` 断言精确 factor，反 engine 内联 `SystemTime::now` 只能宽容断言测不出公式对错）。**TS 真相**：`applyRecencyBoost`(hybrid.ts:185) dates keyed `{source_id}::{slug}`=`COALESCE(effective_date,updated_at)`；per-prefix 最长匹配；`daysOld=max(0,(now-date)/86_400_000)`；evergreen(hl=0||coef=0)跳过；`factor=1+strengthMul*coef*hl/(hl+daysOld)`('on'=1.0/'strong'=1.5)；floor-gated。**Rust 数据就绪度探查推翻记忆悲观判断**："半齐"实指 per-prefix 半衰期**配置**未迁而非日期数据未迁——`get_effective_dates` trait 方法**已存在**（InMemory 返 ISO8601 keyed `{source_id}::{slug}`）、`serde_yaml`/`chrono`(clock) **已是 core 依赖**。分层张力：Rust config 基础设施在 zbrain-cli 而非 core，engine 不能依赖 cli → Q2=A 解决（parser 是纯 transform 吃 `serde_yaml::Value` 不依赖 cli 的 Config）。**实现**：新建纯模块 `crates/zbrain-core/src/recency_decay.rs`（RecencyDecayConfig/Map/Strength/DEFAULT_FALLBACK/default_recency_decay 11prefix/parse_env fail-loud rfind 从右两 `:` 保护 slug 内 `/`(源 M-CX-3)/parse_yaml/resolve_map/RecencyRow engine-agnostic adapter 可跨 InMemory/libsql/postgres/apply_recency_boost 纯函数注入 now_ms + 17 单测)；engine.rs `SearchResult`+=`recency_boost:Option<f64>`、`SearchOpts`+=`recency_decay`+`recency_fallback`、`iso8601_to_unix_ms`(chrono, 不可解析→None 跳过)、search_pages salience 后加 recency 阶段、`set_effective_date_for_tests` mutator、集成测 `search_pages_recency_boost_reorders_and_stamps`(daily/fresh vs daily/stale)；修 salience 测改用 `concepts/` evergreen 前缀隔离(recency no-op 保 salience 唯一轴)；10 engine+2 operation SearchOpts 构造点补 None。验证：**621 lib tests 全绿** + cargo build workspace clean；clippy 修廉价风格(doc backticks/overindent、sort_by_key、map_or)，i64→f64 cast(crate 27 处)+implicit_hasher(4 处)保持 crate 既有 pedantic 容忍。**G13 更新**：recency 从 sibling 未迁列表移出（已迁 salience+recency），剩 backlink/graph/source/exact-match + strength-by-mode(salience k=0.15 + recency 硬编码 `RecencyStrength::On`)；engine.rs recency 阶段加自解释 G13 双向指针。**至此 `1-4-4` boost 子系统全 completed**（1-4-4-2 salience✅ + 1-4-4-3 recency✅），冒泡 completed；解除 `1-4-3`(--explain 展示层)阻塞→pending（归因字段现齐 base_score/salience_boost/recency_boost/reranker_delta）。下一候选：`1-4-3`（--explain 展示层）。commit `<pending>`。
- **`1-4-3` --explain 分阶段归因输出 completed（TDD GREEN，`1-4` search rerank 全树收官，父 1-4 冒泡 completed）**。grill 落盘 3 决策全 A：Q1=A（归因数据流+--explain 人读渲染**一刀端到端**最小垂直切片，反 B 半迁态/反 C 悬空 None 死代码）、Q2=A（formatter 放 **zbrain-core** 吃强类型 `&[QueryResultItem]` 返 String 可 core 单测断言精确文本，CLI 层 `--explain` 时 `serde_json::from_value::<QueryOutput>` 往返再调——薄；反 B CLI 弱类型 Value 索引/反 C formatter 困 CLI 难单测）、Q3=A（**逐字节忠实**复刻 explain-formatter.ts 但只实现 3 个已迁分支 salience/recency/reranker，未迁 5 分支不写代码不留占位）。**病根探查（比节点描述更根本）**：不只"加渲染函数"——Rust query 的 JSON 在 operation.rs paginated map 就丢弃全部归因字段（只留 page/score/snippet），故须先让归因数据流出 operation 层。**实现**：(1) `QueryResultItem`(operation.rs:1763)+=`base_score/salience_boost/recency_boost/reranker_delta`(camelCase, optional 加 `#[serde(default, skip_serializing_if=Option::is_none)]`)，paginated map(:1911) 从 SearchResult 复制；`QueryOutput`/`QueryResultItem` 补 `Deserialize`；连带 **`engine.rs` `Page` 补 `Deserialize`**（与 Serialize 对称，嵌套 enum PageKind/CRMode/EffectiveDateSource 已有 Deserialize）。(2) 新建 `crates/zbrain-core/src/explain_formatter.rs`：`format_result_explain`/`format_results_explain`（空列表`"No results.\n"`，否则 `join("\n\n")+"\n"`）/`fmt`（复刻 `toFixed(4)` 去尾零：`{:.4}` trim `0` 再 trim `.`；非有限→`NaN`/`Infinity`/`-Infinity`）；仅 salience(`!=1.0`)/recency(`!=1.0`,**保双空格 `+ recency  ×`**)/reranker(`!=0`,`↑`/`↓`)三分支，空路径 `no boosts applied`；8 单测断言精确多行字符串全绿。(3) CLI `QueryArgs`+=`--explain` bool(lib.rs:553，删旧 `FUTURE(search-attribution)` 注释)，`run_query_command`(:869) 依 flag 分支：explain 时 `from_value::<QueryOutput>` 调 formatter `print!`，否则 JSON pretty-print。**KNOWN-GAPS G2 删除**（--explain 已接线；残留未迁 boost 轴归 G13），代码注释改指 G13。验证：workspace build clean + core 629 + cli 125 lib tests 全绿 + 新代码 clippy 无警告。commit `cd411c0`。
- **`1-2-2` progress reporter completed（TDD GREEN，恢复因 summarization 中断的施工 → clippy 修→提交）**。原 conversation 已 grill Q1-Q3 全落盘+代码写完但未提交（阻塞前提过时失效——sync perform_full_sync 已有 per-path 循环提供真实 .tick() 落点）。恢复后：修 clippy 警告 6 处（must_use/new, unused self/default_min_items+render_human_line, let...else×3, map_or→is_some_and, cast allows×3 fn）+ KNOWN-GAPS G3→resolved+G14-G20 登记 + roadmap 1-2-2 标 completed。验证：workspace build clean + core 654 + cli 125 全绿。commit `95c0c03`。
