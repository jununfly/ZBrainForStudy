# 2026-07-15

## Phase11 1-3 第三轮完成：删 graph-query / capture / agent 三子系统（commit 见下）

- 删除清单：`src/commands/{graph-query,capture,agent,agent-logs}.ts`（agent-logs 仅被 agent.ts + agent-cli.test 引用，随 agent 删）+ 5 测试（graph-query.test / capture-build-content / capture-runcapture / commands/capture / agent-cli）。净删 2477 行增 2 行。
- **关键认定（承接上轮 autopilot e2e 同逻辑，务必记住）**：这 5 个测试虽 import 保留 impl（PGLiteEngine / MinionQueue / computeContentHash / protected-names），但那些**只搭 fixture 台子**，真正的**被测对象**（runGraphQuery / runCapture / agent）随命令文件删除而消失，因此整个测试随删——**不算需要"改写"的混合测试**。判据 = "被测对象是否随本轮删除消失"，而非"是否 import 了保留代码"。保留 impl 都是 KEEP 核心，有大量其它 importer，删测试不 dangle。
- **cli.ts 改动清单（capture 比其它命令多俩特殊结构，务必留意同类命令）**：① 3 处 dispatch case（graph-query / agent / capture）；② capture 特有的 `--help` **pre-engine 早退分支**（`if (command === 'capture' && ...) { runCapture(null, args) }`，在 engine bind 之前，为了让 capture --help 不需配置 brain）；③ `CLI_ONLY_SELF_HELP` set 里的 `'capture'` 条目（带 v0.39.3.0 WARN-5 注释块）；④ `CLI_ONLY` set 移除三名；⑤ 3 处 help 文本（graph-query 2 行；capture 的 BRAIN section 头改名去掉 "capture /" + 删 3 行）。
- **全 gate 绿**：tsc 75→75 零新增；ts_dep_closure 四文件 importer 全空零 dangle；eval questions.json 无残留 expected_files；cargo build 0.50s 绿；`zbrain.exe {graph-query,capture,agent} --help` Rust 侧全部存活。
- **下一步 = 1-3 后续轮**：jobs（4 混合测试）、sources（9 混合测试）——这两个混合更重，需逐个核实测试的被测对象是否真随命令删除消失（同判据）；随后扩到 160 全集剩余部分。之后 1-4 收尾（typecheck 脚本 + tsconfig 处置 + CHANGELOG + 删 scripts/_phase11_cutover_assert.sh）。

## Phase11 1-3 第四轮完成：删 jobs 子系统 + 回收 cli-path（净删 2564 行）

- 删除：`src/commands/jobs.ts`（1575 行，本 Phase 最大单文件，已迁 Rust：crates 有 jobs 命令 + `register_builtin_handlers@registry.rs`）+ **回收 cli-path.ts**（上轮为切 jobs→autopilot 边而临时提取的中性文件；jobs 删后它只剩自身测试引用，存在理由消失，连测试一起删；Rust 有自己的 CLI path 解析）。
- **混合测试两类精准区分（本轮核心方法论，务必记住）**：
  - **(A) 被测对象随 jobs 删消失 → 整删**：`autopilot-cycle-handler` / `handlers`（测 registerBuiltinHandlers）+ `minions-shell-pglite`（e2e 测经 registerBuiltinHandlers 编排的 shell 路径；shellHandler 另有 `minions-shell.test` 独立覆盖不丢）。
  - **(B) 主体测保留代码、仅局部触 jobs → 改写摘除 jobs 块**：`minions.test`（摘 2 个 describe 块 resolveWorkerConcurrency/parseMaxWaitingFlag，保留 backpressure/db 等）、`cycle-abort`（摘 1 个 test，保留 worker/cycle 两 test）、`exit-classification`（SITES 摘 jobs 站点 + 删 jobs 专项 it，保留 classifyWorkerExit + doctor/supervisor 站点）、`doctor.test`（摘 jobs supervisor status 源码 test，保留 doctor test）。
- **运行时源码读取陷阱（务必记住，dep_closure 抓不到）**：有 4 个测试用 `Bun.file(...).text()` / `readFileSync(...jobs.ts)` **在运行时读取命令源码文件做字符串断言**（不是 import！）。ts_dep_closure 的 importer 校验抓不到这类依赖，删文件后是**运行时崩溃而非编译期 dangle**。删命令前必须 `grep 'commands/<name>\.ts'` 全仓扫描注释+运行时读取，逐个摘除/改注释。
- cli.ts：删 jobs dispatch + CLI_ONLY 移除 + 整个 JOBS help section（**保留 L1599 的 warning 文本**，因 jobs 在 Rust 侧仍活）。清理 questions.json 3 处 expected_files + 4 处保留代码注释（supervisor/shell-validate/shell/worker，改 "ported to Rust"）。
- 全 gate 绿：tsc 75→75 零新增；importers(jobs)+importers(cli-path) 双空零 dangle；cargo build 0.44s 绿；`zbrain jobs --help` Rust 侧存活。
- **下一步 = sources（9 混合测试，1-3 最后一个重头）**；之后扩到 160 全集剩余 + 1-4 收尾。

## Phase11 1-3 第五轮完成：删 sources 子系统（commit eb7fa11，净删 1742 行）

- 删除清单：`src/commands/sources.ts`（命令层）+ 3 整删测试（`sources.test`/`sources-set-cr-mode.test`/`repos-alias.test`，被测对象=`runSources` 本身，属 (A) 类）。
- 改写 6 个 (B) 类测试（fixture 换底层 API）：
  - **4 个 fixture-only**：`multi-source-drift`/`sync-sole-non-default-routing`/`source-id-tx-regression`/`performfullsync-source-id`——把 `runSources(engine,['add','X','--no-federated'])` 换成 `addSource(engine,{id:'X',federated:false})`，`--path P` 换 `localPath:P`。
  - **2 个复杂混合**：`multi-source-integration` + `e2e/multi-source`——**删除 federate/unfederate/rename 测试块**（这些操作只在命令层实现，`sources-ops.ts` 无等价函数），add/remove fixture 换 `addSource`/`removeSource`。e2e 里用 `Parameters<typeof addSource>[0]` 保持类型 cast 形式。
- **关键判据深化**：`src/core/sources-ops.ts` 是保留的底层 API（`addSource`/`removeSource`/`listSources`/`getSourceStatus`/`resolveDefaultSource`/`recloneIfMissing`），从 sources.ts 抽出供 CLI+MCP 共用 → add/remove/list/status 类 fixture 可无损替换；但 **federate/unfederate/rename/set-cr-mode 是命令层 `runSources` 独有逻辑**（sources-ops 没有），测它们的块必须整删。`addSource` 语义：`federated:false`→写 `{federated:false}`，omit→写 `{}`（isolated-by-default），与 e2e "new sources default to isolated" 断言吻合。
- cli.ts 清理点（比前几轮多）：删 3 处 dispatch（主 switch `case 'sources'` / `case 'repos'` alias case 整删 / `dispatchReadOnlyCommand` 的 sources case）+ CLI_ONLY 移除 `'sources'`&`'repos'` + THIN_CLIENT_REFUSED_COMMANDS 移除 sources + THIN_CLIENT_REFUSE_HINTS 移除 sources hint + `readOnlyDefaultTimeoutMs` 的 sources list 10s special-case + help SOURCES section 精简（删 list/add/remove/repos 行保留 sync）。
- 死链清理：3 处保留代码注释（`content-sanity.ts`/`sources-load.ts`/`sources-ops.ts` 指向已删 sources.ts，改 "ported to Rust" 自解释）+ `scripts/check-source-config-leak.sh` 白名单 2 处（注释 L29 + grep filter）。本轮 questions.json 无死链。
- 全 gate 绿：tsc 75→75 零新增；importers(sources.ts)+3 删测试全空零 dangle；cargo 2.24s 绿；`zbrain sources --help` Rust 侧存活（add/list/remove/status 4 子命令）。
- **1-3 五轮累计净删约 8934 行**（首刀 think/remote/salience + autopilot 2151 + graph-query/capture/agent 2477 + jobs 2564 + sources 1742）。下一步：扩到 160 全集剩余部分 + 1-4 final 收尾。

## Phase11 1-3 第六轮完成：D 尾巴清理，安全删除全集消化归零（commit b583a07，净删 1218 行）

- **里程碑：重算安全删除集 D 从 9→0**。原 ~160 文件的"安全删除全集"至此完全消化，1-3 逐子系统删除工作实质完成。
- 删 7 文件：`src/core/audit-quality-probe.ts`(122) + `src/core/cycle/nightly-probe-adapters.ts`(136) + `src/core/cycle/nightly-quality-probe.ts`(220) + `src/core/index.ts`(4行死 barrel) + `src/core/minions/handlers/contextual-reindex-per-chunk.ts`(247) + 2 测试(`nightly-probe-adapters.test`/`nightly-quality-probe.test`)。
- Rust 覆盖核实（Explore agent）：nightly probe 簇已迁（`nightly_probe.rs` 头注 "Ported from"）；reindex handler 已按名注册（`registry.rs:156`，仍 not_implemented 占位但注册在案，属已认领）。
- **plugin-loader 有意保留（G36，遵守 AGENTS.md 铁律"未迁移能力不盲删"）**：`src/core/minions/plugin-loader.ts` + `plugin-loader.test.ts` 保留——Rust 无 `ZBRAIN_PLUGIN_PATH` subagent 插件发现等价物。经用户确认"暂缓,登记 KNOWN-GAPS"→ 登记 **G36** + 加 `ts_keep_seeds.txt` KEEP seed（152→153）+ 头部双向指针注释 `UNMIGRATED (TS→Rust): registered in docs/plans/KNOWN-GAPS.md (G36)`。
- 死链清理：`contextual-retrieval-service.ts` L12 注释改指 Rust `crates/zbrain-core/src/minions/handlers/contextual_reindex.rs`；`test-weights.json` 删 2 条已删测试权重（nightly-probe-adapters:6 / nightly-quality-probe:50）。
- 全 gate 绿：tsc 基线 **75→64**（消失的 11 error 全来自被删的 `nightly-quality-probe.test.ts` 既有错误，`comm -13` 零新增 / `comm -23` 确认来源）；5 删除源文件 importers 全空零 dangle；**D 复算=0**；cargo build 0.85s 绿；`zbrain --help` 存活。
- **1-3 六轮累计净删约 10152 行 TS**。因 D=0，1-3 节点安全删除实质完成，下一步转 1-4 final 收尾（typecheck 脚本 + tsconfig 处置 + CHANGELOG + 残留 TS + 删 `scripts/_phase11_cutover_assert.sh`）。

## Phase11 1-4 final 收尾完成 → **Phase11 (Part9) 全节点 completed**

- **1-3 标记 completed（D=0）；1-4 标记 completed；父节点 1 标记 completed**。Phase11 package 入口 cutover + src/core 删除整体收官。
- **核心决策：typecheck 保留为硬 gate + 建冻结基线（用户拍板）**。关键认知修正：1-1 原倾向"1-4 删 typecheck"是基于"cutover=彻底移除 JS 库身份"语境，但**现实残留 TS 仍 1300+ 文件（src 529 + tests/unit 783），TS 单测仍经 `bun test` 实际运行，整个 TS→Rust 迁移远未完成** → 不能删 typecheck，否则 1300 文件失去类型防护。
- **新增 baseline gate（与 1-3 baseline diff 手法一脉相承）**：
  - `scripts/typecheck-baseline.sh`：归一化 tsc 输出（`sed` 去 `(line,col)` 避免行号漂移假阳性，保留重复行以抓同类新增）→ `comm -13` 只阻新增 / `comm -23` 提示已修复可收缩。支持 `--update` 冻结当前为新基线。
  - `scripts/tsc-baseline.txt`：64 行冻结基线（44 个 TS2307 = 保留态 TS 悬空 import 已删的 `init.ts`/`doctor.ts`/`config.ts`；20 个 TS7006 隐式 any，全在 doctor 测试簇），全是既有债非本次引入。
  - `package.json`：`typecheck` → `bash scripts/typecheck-baseline.sh`；新增 `typecheck:raw`(tsc --noEmit) 和 `typecheck:update-baseline`。**verify（run-verify-parallel.sh CHECKS 含 typecheck）+ ci-local.sh 经 `bun run typecheck` 自动继承新 gate**，消除 verify 长期红（此前 tsc 硬 gate 有 64 error 一直失败）。
  - gate 双向自测通过：注入 TS7006 → 红（精准报新增行）；清理 → 绿（64 inherited）。
- **tsconfig 保留**：`include:["src","tests/unit"]` 正好匹配 gate 范围，TS 迁移中间态仍需，无需改。
- **CHANGELOG 无需动**：已是 ZBrain reset 后状态（"Historical GBrain release notes removed... Start recording from first ZBrain release"）。
- **删一次性脚手架** `scripts/_phase11_cutover_assert.sh`（第 11 行自注 "delete in 1-4 finalization"，无 build/ci 引用）。
- 回归：cargo build 0.50s 绿 + `zbrain --help` exit=0。
- **教训沉淀**：删除阶段结束 ≠ TS 迁移结束。Phase11 只消化了"已迁移且安全可删"的约 160 文件；残留 1300+ TS 是活的迁移中间态，需 baseline gate 长期守护（64 既有 error 应随后续迁移只减不增，可随时 `typecheck:update-baseline` 收缩）。

## 残留 TS 构成盘点完成 → `docs/plans/RESIDUAL-TS-INVENTORY.md`

- 用户选"分析残留 TS 构成"（为下一轮实际迁移选目标）。用 Explore agent 做 Rust↔TS 覆盖交叉盘点 + `find` 量化 + `UNMIGRATED_TS_*` 硬锚点核对 + KNOWN-GAPS 交叉。
- **总量 ~1314 TS**：src 非测试 529（core 412 + commands 105 + eval 7）+ tests/unit 765 test + 18 helper + heavy 2。src 下已无 .test.ts（测试全在 tests/unit）。
- **Rust CLI 27 顶层命令**（`crates/zbrain-cli/src/lib.rs` `enum Commands` L344）：init/doctor/config/schema-sql/get-page/think/query/put-page/delete-page/restore-page/purge-deleted-pages/list-pages/serve-mcp/serve/sync/sources/capture/facts/links/takes/salience/orphans/graph-query/autopilot/remote/jobs/agent。
- **残留 TS 四分类**：
  - **A「只是还没删」(Rust 已覆盖=数据面/流水线)**：minions/ai/ingestion/cycle + takes/search/facts/sync/orphans/embed/extract/lint/integrity/backlinks/reindex*/jobs-watch 等命令。可继续 Phase11 手法删。
  - **B「真正待迁移」(Rust 完全没有=评估治理层)**：schema-pack(26,G4+UNMIGRATED_TS_SCHEMA_PACK_VERBS@lib.rs:2990)、skillpack(27)/skillify、eval 一族(~20命令+src/eval+core/eval)、eval-contradictions(15)、takes-quality-eval(10)、calibration(10,仅DB层)、core/output(9)、doctor 11检查(G5+UNMIGRATED_TS_DOCTOR_CHECKS@lib.rs:86)、外加 whoknows/routing-eval/notability-eval/frontmatter*/publish/brainstorm/dream/models/providers/mounts/integrations/auth/upgrade/book-mirror/founder-scorecard/friction/anomalies/code-callees·callers·def·refs/resolvers/transcripts/pages/files/backfill/apply-migrations/reconcile-links/repair-jsonb/features/lsd/ze-switch/claw-test 等。**这些是 ts_keep_seeds 钉住的 KEEP 核心,删前必须先在 Rust 补齐能力(区别于 Phase11"已迁移才删")**。
  - **C「部分覆盖」**：search(23,散在 engine/libsql/rerank)、facts(13,仅 facts_fence)、think(7,仅CLI入口)、calibration(仅DB)。迁移需补 core 逻辑。
  - **D「有意保留」**：G36 plugin-loader。
- **下一轮迁移选目标建议(优先级)**：1) schema-pack(G4,体量大+硬锚点+schema-sql已腾名) 2) skillpack/skillify 3) eval 一族(整体作独立Phase) 4) doctor 11检查(G5,reranker_health已示范迁出模式) 5) search/facts/think 补齐。
- 盘点与 KNOWN-GAPS(G4/G5/G36)+ts_keep_seeds 完全吻合,证明既有锚点体系准确。crates: cli/core/web/worker/mcp/chunking/svg 七个。

## Part10 Phase12 Schema-Pack 迁移 roadmap 建成

- 用户选"从 schema-pack(G4) 起做迁移调研 + 建 roadmap 节点"。
- **调研发现**：TS schema-pack = `src/core/schema-pack/` 26 源文件(~4929行) + ~20 测试文件；命令层(schema.ts,1166行)已在 Phase11 删。Rust **完全没有** schema_pack 模块(zbrain-core/src 下无 schema_pack/ 目录)；engine.rs 的 PageType 只是 String alias；CLI 只有 `schema-sql`(DDL dumper)，`schema` 名已腾位。32 verb 全部在 `UNMIGRATED_TS_SCHEMA_PACK_VERBS`(lib.rs:2990) 常量里未实现。
- **TS 消费者**：operations.ts(4243行,12+动态import,最重) + cycle/(3文件) + commands/(4文件) + calibration/(1,仅类型) + artifact/(1,仅类型+校验)。
- **数据模型**：SchemaPackManifest 含 page_types(primitive+path_prefixes+aliases+extractable+expert_routing) / link_types(inverse+inference) / frontmatter_links / takes_kinds / enrichable_types / filing_rules / phases / calibration_domains。Pack 身份 = name@version+sha8。7 层解析链 + stat-TTL 缓存 + extends-chain BFS(depth warn 4, hard cap 8)。
- **roadmap 结构(Part10, 6 节点)**：1-1 数据模型基础(manifest+loader+primitives+closure) → 1-2 Registry+resolution engine(7层链+缓存+extendsBFS+lock+trustGate) → 1-3 Inspection verbs(9只读verb,tracer bullet端到端) → 1-4 Activation+Authoring verbs(3+15=18变更verb) → 1-5 Discovery+repair verbs(5verb,清空UNMIGRATED常量) → 1-6 Consumer rewiring+TS删除(解决G4,gate=operations.ts消费状态)。
- 文件：`docs/plans/zbrain-ts-to-rust-part10-phase12-schema-pack.json` + `ZBRAIN_TS_TO_RUST_PART10_PHASE12_SCHEMA_PACK.md`。

## Part10 Phase12 1-1 完成：schema_pack 数据模型基础 (37 tests green)

- 4 个新模块文件 + lib.rs 注册 `pub mod schema_pack`
  - `manifest.rs` (12 tests): SchemaPackManifest v1 struct + serde, PackPrimitive enum (5 variants), AggregatorKind, parse_schema_pack_manifest, pack_identity (`name@version+sha8`), compute_manifest_sha8 (SHA-256 前 4 bytes hex)
  - `primitives.rs` (6 tests): FillDefaults struct + get_primitive_defaults 返回 (manifest, fill_defaults) for all 5 primitives
  - `closure.rs` (10 tests): AliasGraph (HashMap<String, HashSet<String>>), build_alias_graph (对称 per-declaration), DfsColor enum (module-level, white/gray/black), dfs_detect (iterative DFS cycle detection), expand_closure (BFS depth cap 4 + sorted output), ExpandClosureOpts (FnMut callback)
  - `loader.rs` (9 tests): load_pack_from_file, load_pack_from_string (JSON/YAML extension sniffing via serde_yaml), LoaderError/LoaderErrorCode
- **编译修复**：DfsColor 从 detect_cycles 内部提到模块级别（dfs_detect 需引用）；depth_cap 测试用 Rc<Cell<bool>> 满足 `Box<dyn FnMut + 'static>` 生命周期约束
- commit: `5a58965` (code) + `75a5609` (roadmap)
- **roadmap md_file 路径修复**：CLI render 把 md_file 相对 JSON 所在目录解析，md_file 应只写文件名（不含 `docs/plans/` 前缀），否则双重拼接


## Part10 Phase12 1-2 完成：Registry + resolution engine (62 new tests, 99 total)

- 6 个新模块 + 2 个已有模块更新：
  - `registry.rs` (23 tests): resolve_active_pack_name 7-tier chain, PackRegistry (stat-TTL cache + cascade invalidation + identity fast-path), resolve_pack (extends-chain BFS + cycle detection + depth caps), try_cached_pack (TTL-gated), invalidate (cascade)
  - `per_source.rs` (12 tests): build_per_source_bindings, build_source_closure_cte (SQL UNION ALL), escape_sql_literal
  - `pack_lock.rs` (12 tests): acquire_pack_lock (atomic create_new + stale detection), with_pack_lock, is_lock_stale (TTL/PID), LockFileRecord JSON
  - `trust_gate.rs` (9 tests): validate_schema_pack_trust_gate (fail-closed), check_federated_divergence (multi-source pack name divergence)
  - `load_active.rs` (4 tests): load_active_pack (resolve→cache→disk→resolve_pack), load_active_pack_best_effort (None on failure), resolve_active_pack_name_only
  - `closure.rs` +2 tests: compute_alias_closure_hash (SHA-256 前 8 bytes = 16 hex, 与 manifest_sha8 的 4 bytes = 8 hex 不同)
  - `manifest.rs`: extends String→Option<String> (null = 无父 pack)
- **设计决策**：load_by_path 在 load_active_pack 中跳过（ResolvePackOpts 需要 'static 闭包，初始移植可接受）；Windows is_pid_alive 默认 true（保守，防止偷活锁）；PackRegistry 用 struct 而非 TS 的模块级单例（更 Rust-idiomatic + 可测试）
- commit: `04af50c`

## Phase12 1-3 完成：Inspection verbs + lint rules（commit f18dab3）

- **1-3 Inspection verbs**：9 个只读 CLI verb 全部接线，tracer bullet 端到端跑通。
- `lint_rules.rs` (13 tests)：9 条文件面 lint 规则（aliasShadowsType / aliasDeclaredByTwoTypes / aliasReferencesUndeclaredType / enrichableTypesUndeclared / linkTypesUndeclared / frontmatterLinksUndeclared / expertRoutingWithoutPrefix / prefixCollision / prefixStrictSubsetOverlap）+ run_file_plane_lint_rules + run_all_lint_rules
- `schema_cmd.rs`：SchemaSubcommand enum (9 variants) + run_schema_pack_command + 9 verb handlers + load_pack helper。6 个内置 pack YAML 通过 include_str! 嵌入 `src/schema_pack_base/`。
- `lib.rs`：Schema(SchemaSubcommand) 注册到 Commands enum + match 分发。UNMIGRATED_TS_SCHEMA_PACK_VERBS 从 32 降到 23（删 9 个 inspection verbs）。Anchor test 更新 32→23。
- stats/usage 为 stub（需 engine 连接，后续节点补）。
- **include_str! 路径修复**：文件在 `src/schema_pack_base/`，include_str! 相对源文件目录解析，不需要 `../` 前缀。
- 测试：112 schema_pack core + 13 lint_rules = 125 total。CLI anchor test green。冒烟：schema --help/active/list/show/lint 全通过。

## Phase12 1-4 完成：Activation + Authoring verbs（commit 3e886f3 + a575c4e）

- **1-4 Activation + authoring verbs**：18 个 mutating verb 全部接线 + 3 个新核心模块。
- `mutate_audit.rs` (17 tests)：append-only audit log，env 控制路径/时钟，summary 聚合。
- `mutate.rs` (41 tests)：with_mutation flow (load→modify→sha8→write→audit)，15 个 mutator（add/remove/update type, add/remove alias, add/remove prefix, add/link-type, set extractable/expert-routing）。
- `activate.rs` (6 tests)：set/clear active pack 写 `~/.zbrain/config.json`（Tier 6），get_active_pack_from_config，reload_pack_cache stale-lock flush。
- `schema_cmd.rs`：18 个新 verb（use/downgrade/reload + init/fork/edit/diff/add-type/remove-type/update-type/add-alias/remove-alias/add-prefix/remove-prefix/add-link-type/remove-link-type/set-extractable/set-expert-routing）。
- **关键修复**：
  1. `load_pack` 先查 user pack 目录 `schema-packs/<name>/pack.yaml` 再 fallback 内置 pack。
  2. `run_active` 读 home-config Tier 6（via `activate::get_active_pack_from_config`），否则 `use` 后 `active` 看不到。
  3. clap flag：`UpdateType` 的 `--extractable/--expert-routing` 用 `SetTrue` 开关（Option<bool> 默认要值会解析失败 exit 2）；`Set*` 的 `value: bool` 位置参数用 `Set` action（默认 SetTrue 不接受值 exit 101 panic）。
- `lib.rs`：UNMIGRATED_TS_SCHEMA_PACK_VERBS 23→5（删 18 个），anchor test 23→5；常量加 `#[allow(dead_code)]`（测试专属引用，非测试构建静默）。
- 测试：159 schema_pack core (112+17+41+6) green；CLI anchor green。
- **冒烟坑（测试脚手架）**：Git Bash 下 `export HOME=/tmp/...` 后，cargo 子进程收到 MSYS 翻译的 Windows 路径（`C:\Users\victo\AppData\Local\Temp\...`），但命令行 `$HOME/...` 被 shell 展开成 Unix 路径 `/tmp/...`，导致 `schema validate <unix-path>` 报 file not found（代码正确，是路径翻译问题）。同调用内 `lint`/`show` 走 `user_pack_dir()` 读 Windows 路径正常。佐证：smoke3 内 validate 打印 OK。
- commit: `f18dab3`（代码）+ `d8071fc`（roadmap update + render）

## Phase12 1-5 完成：Discovery + repair verbs（5 verb + 4 核心模块，G4 resolved）

- **1-5 Discovery/repair verbs**：5 个引擎动词全部接线，内存式策略（用户 grill 选"内存式 (推荐)"，不引入 `execute_raw`）。
- 4 个新核心模块（crates/zbrain-core/src/schema_pack/）：
  - `type_accessors.rs`：expert/extractable/enrichable/types 访问器 + `infer_link_type_from_pack`（Pass1 page_type → Pass2 字典序 regex + PageRegexBudget，Exhausted 返回 None）。
  - `redos_guard.rs`：PageRegexBudget（协作式 CPU 预算守卫，clock: fn()->u128；crossing call 返回 Exhausted）。**关键修正**：compile_and_match 用 `caps.get(0)`(full match) + `caps.get(1..)`(groups)；test_budget clock 返回 `u128`（v as u128 * 10）。
  - `candidate_audit.rs`：logCandidate/readRecentCandidates/sha8(8hex)/computeIsoWeekName(YYYY-Www) JSONL 轮转，best-effort 脱敏。
  - `discovery.rs` (~770 行，最大)：纯逻辑（slug_prefix/cluster_pages/build_candidate/heuristic_suggestions/detect_from_views）+ 引擎动词（fetch_all_pages 分页 + run_detect/run_suggest/run_review_candidates/run_review_orphans/run_sync_core）。sync 默认 dry-run，apply 用 put_page patch 保留 title/compiled_truth 只改 page_type。
- `schema_cmd.rs`：`run_schema_pack_command` 改 async（lib.rs 调用加 `.await` + config_path）；新增 5 verb（detect/suggest/review-candidates/review-orphans/sync）+ connect_engine（复用 `resolve_database_path` + LibsqlEngine，已 `pub(crate)`）+ resolve_active_manifest（7-tier 链）。
- `lib.rs`：UNMIGRATED_TS_SCHEMA_PACK_VERBS 5→**空**（G4 resolved）；常量注释改"closed-out tracking point"；锚点测试改断言 `len()==0`。`FUTURE(schema-pack)` 注释更新：32 verb 全迁（1-1..1-5）。
- **KNOWN-GAPS G4 → resolved**（表行改 resolved + 现载体指 schema_cmd.rs 全 32 verb）。
- **188 schema_pack core tests 全绿**（159 + 29 新：type_accessors/redos_guard/candidate_audit/discovery）；CLI anchor test green；5 个 verb 端到端冒烟（空 source + 真实 libsql 引擎）全通过，sync 正确解析 active zbrain-base 的 path prefixes。
- **发现 pre-existing 坑（out of scope，未改）**：`run_operation`（put-page/get-page/query/think/list-pages 经此）传 `database_url` 但 LibsqlEngine::connect 要求 `database_path`，本地模式直连失败；`run_sync_command` 正确调 `resolve_database_path`。本地模式 CLI 可能依赖 thin-client。未动（8 命令行为，scope creep）。可在 KNOWN-GAPS 登记 G26 跟踪。
- roadmap：`1-5` → completed + 加决策"内存式"；子节点 1-1..1-5 全 completed，父节点 `1` 仍 pending（待 1-6 Consumer rewiring）。
- commit: `837de4b`（代码 + roadmap + KNOWN-GAPS，1-5 全部落地）。

## Part12 1-6 完成：Consumer rewiring + TS 删除（决策=保留+子gap，G4 收口）

- **1-6 决策（用户 grill 拍板：保留+子gap）**：26 个 TS schema-pack core 文件**不删**，维持 `ts_keep_seeds.txt` KEEP 种子；在 KNOWN-GAPS 登记 **G38**（TS schema-pack core 删除 pending operations.ts 移植）。G4 功能迁移已在 1-5 完成（Rust 全 32 verb CLI，188 tests 绿），1-6 安全收口。
- **闸门根因（ts_dep_closure.py 闭包证据）**：26 文件全在 R（保留）。8 个非测试消费者（operations.ts 4243行共享枢纽 + cycle.ts/cycle/calibration-profile.ts/cycle/schema-suggest.ts + import.ts/sync.ts/whoknows.ts + eval-schema-authoring.ts + artifact/index.ts + calibration/domain-aggregators.ts + import-file.ts/markdown.ts）仍 import 它们；operations.ts 被未迁移 KEEP 种子（calibration/dream/takes/ai-gateway/minions）经传递链拉住。
- **关键事实（决定闸门走向）**：
  - `bin.zbrain` → `bin/zbrain-rs.js`（Rust 二进制透传包装）→ **TS CLI 已死**。
  - `src/mcp/server.ts` 在 D（可删）且无任何引用者 → **TS MCP server 已死**（Rust `zbrain serve-mcp` 取代）。
  - 故 operations.ts 的 4 个 schema 操作（get_active_schema_pack/list_schema_packs/schema_apply_mutations/reload_schema_pack）+ put_page threading 实际无活调用方（仅被死的 TS CLI/MCP 触发），但 operations.ts 枢纽不能动（波及未迁移子系统保留测试）。
  - `src/commands/schema.ts`（TS 32-verb CLI）**已不存在**（早于本轮；TS schema 命令表面早已清除）。
- **改动文件**：`docs/plans/KNOWN-GAPS.md`（G4 行加 G38 交叉引用；新增 G38 行，含闭包证据+解决路径）、`scripts/ts_keep_seeds.txt`（schema-pack 行注释从"porting reference"改为"operations.ts 枢纽封锁，待 G38"）、roadmap JSON（1-6→completed + 决策记录）、本 memory。
- **未做（按决策）**：未动 operations.ts/cycle/import/sync/whoknows/artifact；未删除 26 core + ~25 tests + base/*.yaml；typecheck 基线不变（64 冻结 error，未触碰 schema-pack 文件故不收缩）。
- roadmap：父节点 `1` 现可标记 completed（1-1..1-6 全 done）→ Part10 Phase12 整体完结。
