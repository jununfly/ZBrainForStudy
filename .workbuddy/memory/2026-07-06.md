# 2026-07-06

- 完成 Part2 `1-1-7 Validate init existing-config and JSON output behavior`（`1-1` 子树最后一节点，完成后 `1-1` 整树 completed）。TDD 三颗 tracer bullet：(1) existing-config 无 `--force` 幂等不改写 config 且退出 0；(2) `--force` 覆盖；(3) `--json` 成功路径产出可 parse 的结构化 envelope。实现：`--json` 生效并抑制所有人类文本（`Setting up...`/成功/exists），stdout 仅单个 JSON。JSON 形状统一 envelope 含 `status` 判别字段——成功 `{status:initialized, config_path, database_url, mode:local|url|mcp-only}`，existing 无 force `{status:exists, config_path, hint}`。JSON 构造抽成纯函数 `init_initialized_json`/`init_exists_json` 直接单测（不捕获 stdout），成功输出统一走 `emit_init_success` helper。顺带清理 config.rs 遗留 unused import `tempfile::tempdir`。验证：`cargo test -p zbrain-cli init --lib`（21 passed）、`--lib`（86 passed），无回归。roadmap 节点 completed 并 render。
- 决策要点：existing-config 拒绝保持退出码 0（audit 基线 exits successfully unless --force，本刀 validate 不改语义，幂等，脚本靠 status 区分而非退出码）；TS init 已整体路由到 Rust，无历史 JSON 契约，形状自定义参照仓库 serde_json+to_string_pretty 惯例。已提交 commit `3a2c1dc`。
- 开始 Part2 `1-2 config command strict TS flag parity`（grill 5 问定策略）。子 agent 调查 TS config.ts（从 `5d5b404~1` 恢复，已删）。关键 parity 决策：(Q2) TS 用双存储平面——show 读文件平面、get/set/unset 走 DB 平面（engine.getConfig/setConfig）；Rust 统一到**文件平面**，接受为**故意偏离**并在 audit 记录，embedding_model/dimensions 硬拒绝、search_embedding_column 覆盖率门槛等 DB-plane 专属校验列为 TS-only 不迁移。(Q3) set 未知/拼错键用 **Config schema round-trip 当白名单**拒绝（不复刻 TS 手写 KNOWN_CONFIG_KEYS），保留 `--force` 逃生口，堵住现状裸 dot-path 静默写野字段的 bug。(Q4) get not-found 对齐 TS 退出非 0（现状 Ok 是 bug）；get **不脱敏**对齐 TS（脚本读回 secret），show 仍脱敏。拆 3 子节点：1-2-1 audit（已完成，写 `docs/plans/MIGRATION.md`）、1-2-2 set schema 门控、1-2-3 get 退出码+去脱敏。audit 已提交 commit `0cc95d2`。
- 完成 Part2 `1-2-2 Enforce config set schema validation`（TDD 三 tracer bullet）。新增 `is_known_config_key(key)`：把默认 `Config` 序列化成 YAML 当权威白名单，walk dot-path 校验；`providers.*` 作为自由 provider map 放行。`ConfigAction::Set` 加 `--force` flag：未知/拼错键（如 `embeding.model`）拒绝且不写文件，`--force` 绕过门控。堵住裸 dot-path 静默写野字段的 bug。注意 schema 里 `Config` 和 `ProviderConfig` 都有 `#[serde(flatten)] extra: BTreeMap`，所以不能靠 round-trip 报错检测未知键（会被 extra 吸收），必须用序列化后的 schema 树逐段 walk。4 新测试。验证：`cargo test -p zbrain-cli config_set --lib`（4 passed）、`--lib`（90 passed）。已提交 commit `50f167d`。
- 完成 Part2 `1-2-3 Align config get semantics`（TDD 两 tracer bullet），`1-2` 整树 completed。Gap1：`config get` 未找到 key 改为 `anyhow::bail!`（非 0 退出），此前是 `eprintln`+Ok（bug）。Gap2：`config get` 返回**原始值不脱敏**（显式单值读，用于脚本读回 secret）；`config show` 仍走 `redact_value` 脱敏（防 scrollback 泄漏）。2 新测试（missing key fails loud；get 返回原值且断言该 key 确实是 redaction 敏感的以证明 get 有意跳过）。`redact_value` 仍被 show 与测试使用，无 dead code。验证：`config_ --lib`（21 passed）、`--lib`（92 passed）。至此 `1-1`+`1-2` 两个子树全部完成；下一刀 `1-3 doctor command strict TS flag parity`。
- `1-3 doctor` grill 完成（Q1–Q5 全落盘），写 audit `docs/plans/MIGRATION.md`。**对齐 TS 调查改变整刀重心**：TS doctor（`src/commands/doctor.ts@5d5b404~1`）**从来没有 `--offline`**——那是 Rust 自己发明的死 flag（声明但被 `_args` 忽略）；TS 网络检查始终跑、失败降 warn、非致命（CI/offline 保持绿）。Rust 真正缺的是 **`--json` 契约**：TS `computeDoctorReport`/`outputResults` 产 `{schema_version:2, status(healthy/warnings/unhealthy), health_score(100−fail*20−warn*5 clamp0), checks[]}`，Rust 现只打 Pass/Warn/Fail 计数、无 JSON。决策：(Q1) 行为契约 parity 不复刻 70+checks；(Q2) 新增 `not-implemented` 态 + 代码常量 `UNMIGRATED_TS_DOCTOR_CHECKS` + 测试锚点给后续 agent 硬留痕，不影响退出码/health_score；(Q3) 按子系统聚合 ~8-12 条粗粒度，全明细留 audit；(Q4) **移除 `--offline`**；(Q5) `--json` 信封逐字段对齐 TS，`checks[]` 用 `{name,status,message}` 核心子集，未迁移条目以 not-implemented 列入。实现顺序 7 步、测试矩阵 6 条见 audit。下一步 `/zj-tdd` 实现。
- 完成 Part2 `1-3 doctor command strict TS flag parity`（TDD 6 tracer bullet）。实现要点：(1) **移除死 flag `--offline`**（TS 从无此 flag），改加 `--json`；(2) `CheckStatus` 新增 `NotImplemented` 态 + `CheckStatus::as_str()`（wire string: ok/warn/fail/not-implemented）；(3) 常量 `UNMIGRATED_TS_DOCTOR_CHECKS`（12 条子系统聚合条目 `(name, covers)`）给后续 agent 硬留痕；(4) 纯函数 `doctor_health_score`（`100−fail*20−warn*5` clamp0，Ok/NotImplemented 不扣分）、`doctor_status`（fail→unhealthy/warn→warnings/else healthy）、`doctor_json_report`（信封 `{schema_version:2,status,health_score,checks[]}`，每条 `{name,status,message}`）；(5) `run_doctor_command` 注入 12 条 not-implemented + `--json` 抑制人类文本打单行 JSON、退出码仍仅 fail→1。手验 `doctor --json`：schema_version:2/status:healthy/health_score:100/16 checks（4 真实+12 留痕）。6 新测试。验证 `--lib` 99 passed（+6）无回归。roadmap `1-3` completed 并 render。下一刀 `1-4 schema command`。
- 完成 Part2 `1-4`（原名 `schema command strict TS flag parity`，grill 后重命名为 `rename schema DDL dumper to schema-sql and trace unmigrated schema-pack`）。**核心发现（用户直觉触发）**：Rust `schema` 与 TS `schema` **语义零重叠**——2026-06-25 audit 看错 TS `schema.ts`（记成 ~150 行 DDL dumper），实际 TS `schema` 从 `3c1cc8a~1` 起就是 schema-pack 管理器（`3c1cc8a` Cathedral v3 达 1166 行、32 verb 分派）。Rust 实现的是 audit 想象出来的命令；真实 TS DDL 能力散在 apply-migrations/migrate/pglite-schema。故"strict TS flag parity"是伪命题。决策（Q1-Q5）：(Q1) Rust DDL dumper `schema`→`schema-sql`，腾出 `schema` 名给未来 schema-pack 迁移，不复刻 32 verbs；(Q2) 不保留 alias，破坏性改名（AGENTS.md 第一阶段无兼容层立场）；(Q3) 加 `UNMIGRATED_TS_SCHEMA_PACK_VERBS`(32 verb) 常量+测试锚点；(Q4) DDL dumper flag 现状不动（不加 --json，YAGNI，纯 SQL 无消费者）；(Q5) 不新建 sub-node（旧目标被证伪非推迟），schema-pack 迁移用**代码 `FUTURE(schema-pack)` 注释锚点**+常量作未来 grep 找回点。TDD：clap `#[command(name="schema-sql")]`，`Schema`→`SchemaSql`。3 tracer bullet：schema-sql 解析（default+postgres）、bare `schema` 被拒（clap 提示 similar `schema-sql`，exit 2）、常量锚点（恰 32 verb+含代表性 verb 防 typo）。手验 `schema-sql --backend postgres` 输出 SQL 正常。audit `docs/plans/MIGRATION.md`。README 的 `zbrain schema use/detect/...` 是 TS pack 管理器文档、`schema` 名预留给它，故意不动。验证 `--lib` 101 passed（+2）。`1-4` completed。下一刀 `1-5 package/bin entrypoint`。踩坑：`grep -c "case '...'"` 把 `--help`/`-h` 也算进去误报 34，实际 verb 32。
- 完成 Part2 `1-5`（原名 `package/bin entrypoint strict TS flag parity`，grill 后重命名为 `bin wrapper transparent pass-through correctness (argv + exit-code/signal)`）。**又是被误框的节点**：TS 时代 `src/cli.ts` 由 bun 直跑、自己解析全局 flag（`parseGlobalFlags` in `src/core/cli-options.ts`：`--quiet/--progress-json/--progress-interval/--timeout/--explain`），**从无 node wrapper 做 flag 解析**。`bin/zbrain-rs.js` 是 cutover 后（`38752ec`）的纯透传壳，让它复刻 flag 解析会与 Rust clap 双重解析冲突（反模式）。故真实 scope 是"透传正确性"，改 label。决策（Q1-Q5）：(Q1) A 重新框定+改 label；(Q2) C 全局 flag 缺口双留痕——新建 sibling 节点 **`1-8`**（Rust CLI global flag parity，pending）+ wrapper 头部 `FUTURE(global-flag-parity)` 注释；(Q3) A 去掉 exec `spawnSync` 的 `shell:true`（Windows cmd.exe 二次解析会 mangle 含空格/引号/`^&|` 的 argv），build 的 cargo spawn 保留 shell（无用户 argv、需 PATH 解析）；(Q4) A 抽纯函数 `resolveExitCode({status,signal,error})` 修 `status||0` 把 signal-kill(status=null) 报成 0 的核心 bug——status→透传/signal→128+n(未知→1)/error→1/else→0；(Q5) A 用零依赖 `node:test`（`bin/zbrain-rs.test.mjs`，4 分支），`main()` 用 `import.meta.url===pathToFileURL(argv[1])` 入口守卫，import 测试时不 spawn。加 `test:wrapper` npm script（bun runner 只 glob `tests/unit/*.test.ts`，捞不到 mjs）。手验：`--version` 透传 exit0、无效子命令透传 clap exit2、4 测试全绿。audit `docs/plans/MIGRATION.md`。**故意不动**：Rust clap 全局 flag（→1-8）、cargo build 的 shell、`scripts/postinstall.ts` fallback（→1-6）。`1-5` completed。下一刀 `1-6 migration cleanup` 或 `1-8 全局 flag parity`。
- **收尾撞到发布级 bug（Q6）**：`.gitignore` 第2行是裸 `bin/`，导致 `package.json` `bin.zbrain` 指向的入口脚本 `bin/zbrain-rs.js` **从未进版控**（cutover 后 fresh clone/npm install 无 CLI 入口）。修复：把裸 `bin/` 换成精确忽略 bun `--compile` 产物（`bin/zbrain`、`bin/zbrain.exe`、`bin/zbrain-*-x64/-arm64`），放行源 wrapper + `.test.mjs` 进版控，`git check-ignore` 验证通过。教训：cutover 类改动务必查入口文件是否真在 git 里，别只看工作区存在。
- 完成 Part2 `1-8`（原名 `Rust CLI global flag parity(...)`，grill 后重命名为 `global flag gap audit & subsystem hand-off(progress reporter / MCP timeout / search attribution)`）。**连续第三个被误框节点**（1-3 死 flag、1-5 误框、1-8 假 parity）。Explore 调查结论：TS 5 个全局 flag（`--quiet/--progress-json/--progress-interval/--timeout/--explain`，来自 `src/core/cli-options.ts` parseGlobalFlags）在 Rust **全无消费者**——(1)progress 三个 flag 依赖未迁的 progress reporter（Rust 只有 `CliOpts` struct 挂 operation.rs，从不被填充/读取）；(2)`--timeout` 有 thin-client 路由骨架（`McpClient::call_tool`+`RemoteMcpError::Timeout` 变体）但 `Client::new()` 无 timeout、无消费机制；(3)`--explain` 依赖 rerank/per-stage 归因，但 Rust query 评分是硬编码关键字加权（title+0.4/content+0.4/fm+0.2）、无归因数据（doctor 已标 reranker_health=UNMIGRATED_TS）。决策：(Q1) A 不加占位 flag（=新 --offline 谎言），改做调查+留痕；(Q2→被 Q3 撤销) 原计划按子系统拆 3 子节点；(Q3) **B 不在 part2 建 pending 子节点**——父子状态同步会让 pending 子节点卡死 1-8→1→part2 收尾，子系统实现留给未来 part3/feature roadmap，与 1-4 一致；(Q4) A 就近散落 3 条 Rust `FUTURE` 锚点（operation.rs CliOpts→progress-reporter / mcp_client.rs call_tool→mcp-timeout / lib.rs QueryArgs→search-attribution）+ 更新 bin/zbrain-rs.js 顶层 `global-flag-parity` 锚点指向新 audit；(Q5) **C 不测注释**（纯注释测存在性太脆，区别于 1-3/1-4 测 UNMIGRATED_* 数组常量），验证=cargo build 不破坏编译 + grep 4 条 FUTURE tag + audit 权威对照表（flag→依赖子系统→落点文件:锚点 tag）。**本刀无 TDD 红绿、无运行时改动**，纯 audit 文档 `docs/plans/MIGRATION.md` + 4 条 FUTURE 注释锚点。验证：grep 命中 4 锚点、`cargo build -p zbrain-cli` 11.6s 通过（仅既有无关 warning）。`1-8` completed。part2 剩 `1-6 cleanup`、`1-7 final validation`。
- 完成 Part2 `1-6 migration cleanup for TS remnants and documentation links`（grill Q1-Q5 全落盘，无 TDD/无运行时改动，纯清理+文档）。**本刀明确排除品牌 gbrain→zbrain 大规模改名（独立 PRD）**。三项清理：(1) 删 `package.json` 死 `build` script（`bun build --compile --outfile bin/zbrain src/cli.ts`，无人引用 + 与 Rust wrapper 入口冲突；全局 grep 确认仅剩 `cd admin && bun run build` 与 spec 文档提及，均不牵连根 build）；(2) `scripts/check-no-legacy-getconnection.sh` 删 **3** 个死 allowlist 项——`init.ts`/`doctor.ts`/`serve-http.ts` 均已不存在（grill 只预估前两个，实测多一个 `serve-http.ts`，逐文件 `-f` 核实后删），守卫脚本重跑 ok；(3) 修 2 处失效 build 命令文档链接 `CONTRIBUTING.md:169`+`CLAUDE.md:1246`（旧 `bun build --compile ... bin/gbrain` → `bun run build:rust`）。**核实纠正 grill 预估**：ENGINES.md:200/ZBRAIN_V0.md:127 指向仍存在的 `src/cli.ts`（TS 退役属 part3），未失效，不改。**发布链移交**（Q4）：`build:all`/`prepublish:clawhub`/`publish:clawhub` 交叉编译 mac/linux + openclaw 清单 serve/serve-mcp 语义 + 二进制命名有本机验证盲区，不提交无法验证的改动。
- **新建 part3 roadmap 骨架** `.workbuddy/roadmaps/zbrain-ts-to-rust-part3-release-and-ts-retirement.json`（Q5-d 选 A：独立 roadmap 承载移交，不污染 part2 收尾）。三个 pending explore 锚点节点：`1-1` 发布基础设施迁移、`1-2` 子系统补齐（1-8 审计移交的 progress/timeout/rerank）、`1-3` TS 入口整体退役（硬依赖 1-1 完成）。根节点记起源决策。完整范围与切片待开 part3 时 grill。`1-6` completed；part2 仅剩 `1-7 final validation`（parent `1` 正确保持 in_progress）。
- **完成 Part2 `1-7` 并收口整个 Part2**（grill Q1-Q4 全落盘，无 TDD/无运行时改动，纯聚合验证+报告）。**用户问"Part2 不能完成完整迁移是否该把 1-6/1-7 挪 part3"——纠正前提**：Part2 从建立起 charter 就不含"完整迁移"（根节点决策三次声明其他未完成 TS→Rust 归 part3），"完整迁移"跨 Part1/2/3 是设计如此；1-6 已完成无可挪、1-7 验证 part2 交付物属收口，都不挪。真问题是 `1-7` label "final validation" 过度承诺，故 Q1 收窄边界改 label 为 "Part2 slice deliverable validation"。**验证三类料全绿**：`cargo test -p zbrain-cli --lib` 101/101、`node --test bin/zbrain-rs.test.mjs` 4/4、`check-no-legacy-getconnection.sh` ok。**+顶层 binary 冒烟**（Q2-B，防 1-5 踩过的"单元全绿但装配坏"）：`--version`/`--help`/init|config|doctor|schema-sql `--help` 全 rc=0、无效子命令 rc=2、bare `schema` 已移除 rc=2（1-4 成果保持）。产出收官报告 `docs/plans/MIGRATION.md`（三类料命令+结果 + 1-1~1-6 六刀勾验表 + part3 移交边界表）。**移交边界**（Q4，记录不修）：progress/timeout/rerank 三 flag 无消费者、发布链交叉编译盲区、**--version 显示 0.0.1(Cargo.toml) vs package.json 0.41.14.0 未同步**(版本同步范畴非 TS parity)、TS 入口退役——全归 part3。无 part2 内回归、无需修复。**踩坑**：`.cargo/config.toml` 设了 `target = "x86_64-pc-windows-msvc"`，binary 落在 `target/x86_64-pc-windows-msvc/debug/zbrain.exe` 而非 `target/debug/`，首次冒烟 rc=127 找不到。`1-7` completed → 父节点 `1` 自动冒泡 completed，**Part2 全树 8 节点全部 completed，Part2 关闭**。下一阶段：开 part3（发布链/子系统/TS 退役），待 grill。

- 完成 Part3 `1-2-2 progress reporter` grill（Q1-Q3 全落盘，explore 结论=本轮不写代码，commit `30ddbfa`）。**Rust 侧 progress 真实消费者=0**（explore 坐实）：`operation.rs:339-354` 已有预留 `CliOpts{quiet,progress_json,progress_interval}` 但注释自陈无消费者/加 flag=死flag；已迁 sync/capture/sources/doctor 均无 per-item tick 循环（仅阶段 eprintln+汇总 println）；TS 20+消费者（sync逐条/embed/extract/reindex...）绝大多数未迁 Rust。TS `progress.ts` 490 行（auto/human/json/quiet 四态+interval/items 双闸节流+NDJSON schema+进程级信号协调+child+heartbeat+EPIPE 防御）全部价值只在 `.tick()/.heartbeat()` 被长跑循环调用时兑现。决策：(Q1-A) 不独立迁 progress reporter，重框为阻塞节点；现在迁=造 3 死 flag+490 行无人调用渲染码，正是 1-3(--offline)/1-8(timeout) 已踩坑 + zj-tdd 禁止的 horizontal slicing（只能测渲染 shape 非真实行为）；等首个带 per-item 循环命令（sync per-item导入/embed/extract 择一先落地）时 progress reporter 同批垂直切片迁入，才有真实 tick 调用点驱动 TDD。(Q2-A) 落盘方式：1-2-2 标 `blocked` + label 追加 BLOCKED 标注 + notes 写清依赖，**不建投机消费者节点**（那些迁移工作 roadmap 里还没节点，凭空建 addBlockedBy=explore 空壳污染树，违反就近原则）；等消费者真要迁时再建其节点并 addBlockedBy 回连 1-2-2；focus 从此跳过 1-2-2。(Q3-A) `operation.rs` `FUTURE(progress-reporter)` 注释引用由 `roadmap 1-8`（已关闭审计节点）改指 `roadmap 1-2-2`——纯文档一致性，零行为/零测试影响，`cargo build -p zbrain-core` 通过。1-2-2 现 `[!]` blocked，1-2 保持 in_progress（正确，无误冒泡）。下一刀 `1-2-3 local read-only wallclock timeout`（explore）。

- 完成 Part3 `1-2-3 local read-only wallclock timeout`（grill Q1-Q6 + Q2-修正 全落盘 → exploit TDD 实现，commit 实现 `07bfd3c` + roadmap `9b0b7f7`）。**核心 explore 坐实推翻 Q2 前提**：`git` 确认 `cli.ts:1136 'search ? 30_000'` 是 **TS 死代码**——它在 `handleCliOnly`(808) 函数体内，唯一调用点(122)被 `if(CLI_ONLY.has(command))` 守卫；但 `search`/`query` 是 shared op（`operations.ts:1259` 注册进 `cliOps`）**不在 `CLI_ONLY`**，走 `cli.ts:127` shared 路径永远进不了 `handleCliOnly`。search 真实本地路径(176-264)只有 drain+disconnect force-exit 兜底（10s disconnect / 5s drain），**无 connect/dispatch wallclock、无 exit124**，且 force-exit 计时器在 `connectEngine()` 成功后才 arm → hang-in-connect 的 search 在 TS 里无限挂起。**只有 `sources`（在 CLI_ONLY）的 `sources list` 10s 真实可达生效**。→ Q2-修正 A：撤销「query 默认 30s」，只迁 sources-list 10s（对齐 TS 运行时而非不可达意图，不发明 TS 从未有过的行为）。**实现**：新建 `crates/zbrain-cli/src/timeout.rs` 单一真相源三件套——`ReadOnlyTimeout{label,ms,user_supplied}` + `with_read_only_timeout(fut,ms,label,user_supplied)`（`tokio::time::timeout` 包裹，超时返类型化错误不碰进程）+ `format_timeout_message`（纯函数：default 带 `(default Nms; pass --timeout=Ns to override)` hint / user-supplied 不带）+ `report_timeout_and_exit`（stderr + exit124，GNU 约定）。`lib.rs`：`SOURCES_LIST_DEFAULT_TIMEOUT_MS=10_000` + `resolve_sources_list_timeout(Option<u64>)->(ms,user_supplied)`；`run_sources_list` 两段独立包裹（connect label `zbrain sources list: connect` + body[init_schema+list_sources] label `zbrain sources list`，Q5 保留 TS 诊断粒度），`cli_timeout_ms` 经 `run_sources_command` 穿参。**决策要点**：Q3-A helper 放 zbrain-cli 内（YAGNI 不放 core）；Q4-A helper 纯 Result + exit 决策在命令层（对齐 TS 分层，全程可测）；Q6 简化——query 本地仍无 wallclock，`local_timeout_warning` 对其保持真实，仅更新 doc 注释 + `mcp_client.rs` 注释同步。**测试全绿**：`timeout.rs` 4 单测（format×2 + with_timeout ok/err）+ lib `resolve_sources_list`×2，`cargo test -p zbrain-cli --lib` **124 passed**，clippy 新代码零警告。**后续**：TS search 潜伏 wallclock bug（是否让 Rust query 主动补上=修不可达意图 vs 纯移植）新建 **sub-node `1-2-4`**（pending/explore，待与用户对齐），未吸收进 1-2-3。1-2-3 completed。

- 开始 Part3 `1-1` 发布基础设施迁移，Explore 调查后**拆 3 子节点**（split-vs-absorb：三个各自独立且各有反直觉运行时事实）：`1-1-1` openclaw manifest serve 语义+bin 路径修复、`1-1-2` 二进制命名对齐、`1-1-3` Rust 交叉编译+发布管线。**关键运行时发现**：(1) TS 时代 `serve`=stdio MCP / `serve --http`=HTTP，但 Rust 语义**相反**——`serve`=HTTP API server、`serve-mcp`=stdio MCP（clap 两个独立变体，lib.rs:757/765）；(2) `openclaw.plugin.json` mcpServers 是**双重错误**：`command:"./bin/zbrain"` 文件不存在 + `args:["serve"]` 在 Rust 会启 HTTP 而非 MCP client 期望的 stdio；(3) `.cargo/config.toml` 硬编码 `target=x86_64-pc-windows-msvc` 阻断交叉编译；(4) `release.yml`+`build:all` 仍用 `bun --compile` 编译 TS、artifacts 仍叫 `gbrain-*`，Rust 二进制根本不在发布管线里。
- 完成 Part3 `1-1-2` 二进制命名对齐（explore+2 决策落盘，无 TDD）。Q1-A：openclaw `command` 指向已存在的 wrapper `bin/zbrain-rs.js`（经 `node` 显式调用，复用其平台探测+cargo fallback，不新建文件；不依赖 shebang 因 Windows 无 .cmd 兜底会失败）。Q2-A：`package.json` 的 `zbrain-ts` bin 条目**不动**，推迟到 `1-3` TS 入口退役统一拆（与 src/cli.ts+build:all TS 编译+dev 脚本一起，避免半成品悬空）。1-1-2 completed，解锁 1-1-1。
- 完成 Part3 `1-1-1` openclaw manifest serve 语义+bin 路径修复（TDD 3 tracer bullet，先做 1-1-2 定名再消费其决策，故 1-1-1 一度 blockedBy 1-1-2）。**今日无任何测试覆盖真实 manifest 的 mcpServers**（gap）→ 新建 `tests/unit/openclaw-manifest-mcp.test.ts`：读真实 `openclaw.plugin.json` 断言 (1) args 含 `serve-mcp` 且不含 `serve`、(2) `command==="node"`、(3) 引用的 wrapper 文件 `existsSync`。RED 3/3 fail（command 是 `./bin/zbrain`、无 serve-mcp）→ 改 manifest mcpServers.zbrain 为 `command:"node"` + `args:["./bin/zbrain-rs.js","serve-mcp"]` → GREEN 3/3。1-1-1 completed。`1-1` 剩 `1-1-3` Rust 交叉编译+发布管线（本机验证盲区，待 grill）。
- 完成 Part3 `1-4-1` hybrid+vector 检索地基第一刀（TDD 1 tracer bullet，commit `a3e5a10`，roadmap 状态 `f53d1d4`）。**1-4 先被重新框定为聚合节点**（`1a6d88a`）：侦察推翻 label 前提——Rust query 不是"硬编码关键字加权"那么简单，而是**整个向量地基缺失**（pg/libsql 引擎连 `search_pages` 都没实现，只有 InMemoryEngine 有；`Page.embedding` 字节列存在但从不写/查；无 cosine/RRF/tsvector）；TS `src/core/search/` ~7249 行、rerank 依赖外部 cross-encoder。地基无融合分数则 rerank 无对象、attribution 无数据，强合并=horizontal slicing。故拆 3 有序子节点：`1-4-1` 地基（融合分数 base_score）/ `1-4-2` rerank 服务层（blocked）/ `1-4-3` --explain 归因（blocked）。**1-4-1 实现**：`InMemoryEngine::search_pages` 从纯词法 substring 改为**双路 RRF 融合**——词法路（title 0.4/content 0.4/frontmatter 0.2 加权 substring）+ 向量路（对 `query_embedding` 与各页 f32-LE `embedding` 算 cosine）→ `rrf_fuse` (`1/(K+rank)`, K=60, 归一化 0..1) → 产出 `base_score`。`SearchResult` 加 `base_score`（pipeline 入口捕获一次的 pre-boost 融合分，供后续 rerank/--explain 重构乘子分解，镜像 `hybrid.ts:289` attribution stamp）；`SearchOpts` 加 `query_embedding: Option<Vec<f32>>`（可注入 f32 query 向量，`None` 则关闭向量路退化为纯词法——把管道与真实 embedding provider **解耦**，provider 接线是**后续 sub-node**；所有现有 caller 传 None）。辅助函数全部镜像 TS 并用**行号锚点**注释（`RRF_K=60`↔hybrid.ts:34、`cosine_similarity`↔hybrid.ts:1344、`rrf_fuse`↔hybrid.ts:1251、`decode_embedding_le` f32-LE↔gateway.ts:864），snippet 提取加 UTF-8 char-boundary clamp（向量-only 命中无关键字可锚）。**作用域**：只 InMemoryEngine；pg/libsql `search_pages` + 真实 embedding provider 是**推迟的 tracer bullet**。TDD：新增 `search_pages_finds_vector_match_without_keyword`（一页零词法重叠但 embedding 与 query 向量共线，必须经向量路浮现且 `base_score>0`）。RED 6/7 → GREEN 7/7；full lib **570/570** 无回归。踩坑：roadmap CLI 合法 status 是 `completed` 不是 `done`；`render` 需先 `link` md_file（本次跳过，JSON 为唯一真相源）。1-4-1 completed，解锁 1-4-2（现有真实融合分数可 rerank）。
- 完成 Part3 `1-2-4`（grill Q1-Q2 落盘 → 闭环不施工 + 附带注释清理，commit `8162374`）。**性质判断**：`1-2-4` 是 1-2-3 explore 衍生的「TS search 本地 wallclock 潜伏 bug」（cli.ts:1136 `search->30s` 死代码，search 是 shared op 永不进 handleCliOnly，TS 本地 search 从未真有 wallclock）。Q1-A 决定：**Rust 已与 TS 运行时对齐（都无 wallclock），补 query wallclock=修 TS 从未真有过的行为=产品增强非迁移，超出 charter，不做**；节点 status=completed（调查闭环，结论=不施工），helper 三件套（timeout.rs）已就位，将来若作为 Rust 主动增强再接。**用户新增长期约定（已记 MEMORY.md）**：`.workbuddy/roadmaps/*.json` 是**临时文件、做完清理**，因此**源码注释禁止引用 roadmap 编号**（清理后成悬空死链），必要信息必须自解释写进注释（TS 行号锚点/行为原因/技术路径），可引用持久的 docs/plans/。Q2-A 据此：在 query 本地分支（`run_operation`）加 **anti-fix 锚点注释**（说明无 wallclock 是刻意对齐 TS + 死代码原因 + 将来用 timeout.rs 现成三件套怎么接），防未来误补。**附带清理全仓 14 处 roadmap 引用**（lib.rs 8 + mcp_client.rs 1 + timeout.rs 2 + operation.rs 1 + 其余），改自解释或指向 docs/plans/。**顺带修 timeout.rs 模块 doc 一处事实错误**（原写 query+sources list 都有 timeout，实际只 sources list）。零行为零测试影响：cli 124 + core 569 全绿。**至此 `1-2` 子系统补齐：1-2-1✅/1-2-3✅/1-2-4✅ + 1-2-2 blocked**（正确压住父节点，等首个 per-item 消费者命令）。Part3 下一候选：`1-1` 发布基础设施 / `1-3` TS 入口退役（硬依赖 1-1）/ `1-4` search rerank 子系统。

- 完成 Part3 `1-4-2-1` rerank 可观测契约（grill Q1-Q5 落盘 → TDD 实现，commit `e9bf0f1` core + `9d87b58` cli/roadmap）。**对齐 TS 权威**：TS `checkRerankerHealth` 实际不在预想位置——`src/commands/doctor.ts` 已被 `5d5b404`（删除 TS bootstrap 命令面）整文件删除、测试仍悬空导入，源码从 `aa24d84`(=5d5b404^) 恢复读取；`reranker_health` check 在 doctor.ts:991（buildChecks:606/5064 调用），读 `search.reranker.enabled` DB 键 + `readRecentRerankFailures(7)`。**新建 `crates/zbrain-core/src/rerank_audit.rs`**：`RerankFailureEvent`/`RerankFailureReason`（snake_case，与 TS writer wire-兼容以便迁移期共读 `~/.zbrain/audit/`）/ `rerank_audit_filename`（chrono `IsoWeek` `%G-W%V` ISO 周轮转，处理年边界 2027-01-01→2026-W53）/ `log_rerank_failure`（best-effort JSONL append，dir 作参数传入=对齐 sync/failures.rs 惯例，ts 用现成 `time::current_utc_iso8601`）/ `read_recent_rerank_failures`（走当前周+上一周两文件，再按 now-days cutoff 过滤，复刻 TS audit-writer 的跨周一午夜窗口）/ `classify_reranker_health`（纯函数四分支，短路顺序 auth≥1→payload≥1→transient(network+timeout+rate_limit)≥5→else ok，文案逐字节对齐 TS）。**chrono 提升**为 workspace + zbrain-core 直接依赖（原仅 sqlx feature 传递引入，零新增编译成本；ISO 周年边界易手写出 bug）。**cli 接线**：`SearchConfig` 加 `reranker_enabled: bool`（`#[serde(default)]` false，config 文件平面镜像 TS 的 `search.reranker.enabled` DB 键，DB 平面键故意不迁）；`run_doctor_command` 加 engine-free reranker_health check（读 config + 读 `~/.zbrain/audit` 尊重 `ZBRAIN_AUDIT_DIR` override + classify，纯文件 I/O 无 DB/网络）；从 `UNMIGRATED_TS_DOCTOR_CHECKS` 移除 reranker_health（12→11，anchor 范围 8..=12 仍成立）+ 加 `reranker_health_is_no_longer_unmigrated` 迁移硬痕迹测试；更新 `--explain` FUTURE 注释（reranker_health 已真实，但 --explain 需的评分 attribution stages 仍缺）。**TDD**：core 14 单测（filename 边界/writer→reader 往返/窗口+corrupt 跳过/四分支+auth 优先级），cli 相应测试全绿；clippy 对新代码零警告（修了 let-else + 5 处 doc backticks）。**踩坑**：Windows UAC 把含 "update" 的集成测试二进制（`page_methods_update_cr_state.exe`）当安装程序要求提权（os error 740），用 `--lib` 只跑单元测试规避。**writer 本刀无生产调用点是预期的**（唯一未来 producer=1-4-2-2 rerank HTTP client fail-open 分支），但立即被 doctor reader + 测试消费，非死代码。1-4-2-1 completed，1-4-2 保持 in_progress（1-4-2-2 未做）。下一刀 `1-4-2-2` rerank HTTP client。
- 完成 Part3 `1-4-2-2` rerank HTTP client + call-site 接线（grill Q1-Q5 早已落盘 → TDD 实现，commit `6276c70`，roadmap `completed`）。**核心产物 `crates/zbrain-core/src/rerank_client.rs`**（762 行）：合并 TS `gateway.rerank()`(HTTP+分类) + `applyReranker()`(head/tail reorder+fail-open+audit)。`RerankClient` trait 作 transport seam（对齐 embedding.rs Mock/Http 双实现先例）；`apply_reranker<T>` 泛型 over row + document_of/stamp 闭包，`split_off(top_n=30)` 拆 head/tail，`Vec<Option<T>>` slots 做 reorder 且省略行按原序 re-append 保 recall，**任何失败 fail-open 回 RRF 序 + 写一行 audit（此即 1-4-2-1 audit writer 的唯一生产 producer）**，成功不写；`ZeroEntropyRerankClient`（`#[cfg(feature="rerank")]` reqwest 实现）预检 payload(5MB)→bearer→timeout(5s)→HTTP status 分类(auth/rate_limit/network/timeout/payload_too_large/unknown 逐字节对齐 gateway.ts:2960)→parse `{results:[{index,relevance_score}]}`；`from_env` 读 `ZEROENTROPY_API_KEY` 空/无返 None（secret 不落 config）。**engine.rs**：`SearchResult` 加 `rerank_score`+`reranker_delta`（均 Option，不覆盖 base_score）。**operation.rs**：`OperationContext` 加 `#[serde(skip)] rerank: Option<RerankSettings>`+`with_rerank()` builder，`QueryOperation::execute` 在融合后/分页前跑 rerank 阶段（None=off 默认门控）；Think/evidence 路径**故意不接**（→ 已存在的 sub-node `1-4-2-3` 承载，非新建）。**cli/lib.rs**：唯一生产构造点，gated on `reranker_enabled`+env key；抽 `resolve_audit_dir()` 供 writer(run_operation) 与 doctor reader 共用永不分叉；`zbrain-cli` 打开 `rerank` feature。**关键契约**：reranker 返回的 outcomes **已按 relevance 降序**，`apply_reranker` 按列表顺序应用**不再排序**（对齐真实 ZE API）。**TDD 踩坑**：初版接线测试 `ReversingRerank` mock 违反此契约（按 index 序发 outcome），且断言依赖 off-run 的 RRF 基线序——但 tied 页的 RRF 融合序**非确定**（HashMap 迭代），两次 run 序不同致 flaky。修复=mock 改为**按 document 文本降序**发已排序 outcome + 断言固定期望序（Beta>Alpha by compiled_truth），彻底摆脱 RRF tie-break 依赖。**验证全绿**：`--lib` 601 passed、`--lib --features rerank` 604（含 3 ze_wire_tests：request body 契约/response parse/from_env None）、clippy 对 rerank 代码零警告、`cargo check -p zbrain-cli`(feature on) 通过。1-4-2-2 completed，1-4-2 focus 落到剩余 pending 子节点 `1-4-2-3`（Think/evidence 是否 rerank 的产品判断，explore）。
