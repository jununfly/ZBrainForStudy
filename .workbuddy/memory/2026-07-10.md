# 2026-07-10

- **Part6 Phase 8 刀5（1-5 toolLoop）completed**：provider-agnostic tool loop，Phase 8 最重的一刀。产出 `crates/zbrain-core/src/ai/tool_loop.rs`（912 行，含测试）。
  - **Grill 3 决策（全 A）**：① `tool_loop` 注入 `&dyn ChatProvider`（不内部从 model 串解析；provider-resolution 工厂留给刀4；loop 保持纯 deep module，`MockChatProvider` 可无 DB 测试）② 5 个持久化回调 → `ToolLoopHooks` trait + `NoopHooks` 默认 impl（async_trait；`on_assistant_turn/on_tool_call_start(->ZbrainToolUseId)/on_tool_call_complete/on_tool_call_failed/on_heartbeat(sync)`；保持 write-BEFORE-side-effect 不变量：loop await `on_tool_call_start` 拿 id 再 execute；`NoopHooks` fallback id 照抄 TS `inline-{turn}-{ordinal}`；拒 async 闭包/事件流反转）③ crash-replay + ToolHandler 单刀切（3 层：loop 控制 / ToolHandler+dispatch / crash-replay reconciliation；replay 仅 ~40 行且与 dispatch 循环物理编织，短路检查在 `for callIdx` 内 execute 前，拆出会 double dispatch 代码；拒拆 1-5-1）。
  - **实现结构**：类型层（`ZbrainToolUseId`/`PriorToolStatus`/`ToolLoopReplayState`/`ToolHandler` trait/`ToolLoopStopReason`/`ToolLoopResult`/`ToolLoopOpts`）+ hooks 层 + 循环体。crash-replay 四分支：`Complete`→喂回 prior output 不 re-exec；`Failed`→喂回 error result；`Pending`+非 idempotent→`Unrecoverable`（TS 是 throw，Rust 改为 typed stop reason 返回 Ok）；`Pending`+idempotent→安全 re-exec。abort 用 `&dyn Fn()->bool` 闭包（Rust 版 AbortSignal），loop-top + per-call dispatch 两处 poll。
  - **验证**：14 tool_loop 测试全绿（loop 控制 6 / dispatch 2 / abort 2 / replay 4），ai:: 65 全绿（含 openai feature，无回归）。clippy：`too_many_lines` 加 justified `#[allow]`（忠实 port 单一 loop，拆 helper 会割裂 write-ordering 不变量），`implicit_hasher`（HashMap 参数）保持 crate ambient 一致（5 处未抑制无先例）。
  - **消费方边界确认**：`src/core/minions/handlers/subagent.ts:740 runSubagentViaGateway` 是 Phase 9 消费方，用 5 个回调把 DB 写（subagent_tool_executions ON CONFLICT + RETURNING zbrain_tool_use_id）接进 toolLoop。刀5 只做 loop control，DB 持久化留 Phase 9 impl `ToolLoopHooks`。
  - **写文件坑**：测试模块体量大，Write 一次性写多次被输出长度截断。解法 = 拆成 3 个 `.part` 小文件（loop 控制 / dispatch+abort / crash-replay+闭合 mod）分别 Write，再 `cat >>` 拼接、删临时文件。heredoc `cat >> <<'EOF'` 遇 Rust 代码里的单引号（`'tool_calls'` 等）会 "unexpected EOF"，改用 Write 写 `.part` 再 cat 更稳。
- **Part6 Phase 8 刀6（1-6 embed/rerank/expand 收口）completed**：横向收口一刀，3 个 grill 决策全落地。
  - **Q1 rerank 出范围**：`rerank_client.rs` 生产已完成（刀 1-4-2），gateway provider 层无遗留；唯一剩项是 operation 层 retrieval-sharing 重构（ThinkOperation 绕过 apply_reranker），已登记为 **G1** 保持 gap，不在本刀。
  - **Q2 embed feature-split new()**：修掉 `EmbeddingClient::new()` 硬编码 `Arc::new(MockEmbeddingProvider)` 返全零向量的陷阱（会污染向量索引）。`EmbeddingConfigError` 加 `FeatureDisabled` 变体；`new()` feature-split —— 有 `embedding` feature → 委托 `http_provider::create_http_client`（真 HTTP）；无 → `Err(Config(FeatureDisabled))` **绝不静默零向量**。`MockEmbeddingProvider` 降 `#[cfg(test)]`（注释"must never reach production"）。核查确认 new()/with_provider/Mock 生产侧零调用（全在测试）→ feature-split 零破坏。4 处测试改用 `with_provider(config, Arc::new(MockEmbeddingProvider))`；新增 2 个 new() 行为测试（无 feature 返 FeatureDisabled / 有 feature 建 HTTP client）。验证：default 17 / feature 22 / 全 embedding 30 全绿。`unwrap_err()` 要求 Ok 类型实现 Debug（EmbeddingClient 未实现）→ 用 match 避开。
  - **Q3 expand 纯层 + 新建 ai/expand.rs**：3a=A（纯/安全层现做 + `ExpansionProvider` trait seam + mock，真 structured-output HTTP deferred）+ 3b=A（新建单文件 `crates/zbrain-core/src/ai/expand.rs` ~360 行含测试）。精确对照 TS `src/core/search/expansion.ts` 移植两层契约：`sanitize_query_for_prompt`（char-safe 截断 MAX_QUERY_CHARS=500 → strip fence/HTML tag/injection 前缀 → WS collapse）+ `sanitize_expansion_output`（strip 控制字符 `!c.is_control()` 匹配 TS `[\x00-\x1f\x7f]` / trim / dedup 大小写不敏感 / cap MAX_ALTERNATIVES=2）+ `expand_query`（CJK 门控复用 `crate::cjk::count_cjk_aware_words` / None 短路 / provider let-else 软失败 fallback / prepend original / dedup+cap MAX_QUERIES=3）。4 个 `LazyLock<Regex>`（先例 takes_fence.rs）。`ExpansionProvider` trait 返 `[echoed_query, ...alternatives]`。15 测试全绿。
  - **G26 登记**：真 structured-output HTTP `ExpansionProvider` 缺失（仅 trait seam + 纯层）。阻塞点 = 刀3 `ChatProvider` 是 free-text chat seam，无 `generateObject` 等价物（structured-object seam）；TS `gateway.expand` 用 `generateObject` + `ExpansionSchema`。当前 query 检索不做多查询扩展（等价 `[query]` 降级，功能安全）。双向指针：expand.rs 模块 doc 指向 KNOWN-GAPS.md G26。
  - **写文件坑复现**：Windows Write 一次性 >~250 行被截断；heredoc `cat >> <<'EOF'` 遇 Rust 单引号 unexpected EOF。expand.rs 用 Write 建头段 + 两次 `cat >>` 追加尾段/测试。
  - **验证命令**：`cargo test -p zbrain-core --lib ai::expand -- --test-threads=1`（15 绿）/ `... embedding`（17）/ `--features embedding ... embedding`（22/30）。
- **Part6 Phase 8 刀 1-4 拆 3 子节点 + 1-4-1（Anthropic native chat provider）completed**：grill 5 决策全落地。
  - **拆分决策**：1-4 拆 1-4-1（Anthropic provider 层）/1-4-2（Google/Gemini，无 TS 逐字样本照官方 REST）/1-4-3（BudgetTracker 接线 + provider 工厂分派）。
  - **关键 TS 架构发现**：TS `gateway.chat`（gateway.ts:2284）走 **Vercel AI SDK**（`generateText()`+`@ai-sdk/*`），provider 差异被 SDK 吸收，**无手写 wire format**；唯一手写 Anthropic 原生 body 的样本是 `subagent.ts:478` legacy 直连（`@anthropic-ai/sdk`）。Gemini 完全无逐字样本（1-4-2 只能照官方 REST）。**provider 工厂**（TS `instantiateChat` switch）Rust 等价物完全不存在，OpenAI provider 也未被生产实例化——决策：工厂分派不进 1-4-1，并入 1-4-3。
  - **Q3=A StopReason::from_signals 扩展（顺带修刀3 遗留隐性 bug）**：原 `anthropic_stop` 分支只认 `refusal`，Anthropic 原生值 `end_turn`/`tool_use`/`max_tokens`/`stop_sequence` 全落 `Other` → `tool_use` 落 Other 会**断 tool_loop 的 ToolCalls-驱动循环**（OpenAI 不走第二参故刀3 未暴露）。扩为 match 全值：refusal→Refusal / tool_use→ToolCalls / max_tokens→Length / end_turn|stop_sequence→End。
  - **Q4 序列化逐消息直译不合并**：tool_loop.rs:486-491 已把一 turn 全部 tool_result 塞单条 `{role:User,Blocks([ToolResult...])}`（与 subagent.ts N→1 一致）→ serialize 无需跨消息合并。
  - **Q5=A cache_control 全做**：`ChatOpts::cache_system` 已铺全链路（tool_loop.rs:303 透传 / anthropic recipe `supports_prompt_cache:Some(true)` / tool_loop.rs:148 注释"system + last tool def"）；不做则 `cache_system` 成死字段。实现：system 数组形式带 `cache_control:{ephemeral}` + 最后一个 tool def 挂 cache_control。
  - **实现结构（chat.rs 1252→1503 行）**：5 个 Anthropic 段——`anthropic_role`（Assistant→assistant，其余→user）/`anthropic_tool_result_content`（对齐 asStringIfNotObject：String 取内值其余 to_string）/`serialize_messages_anthropic`（逐消息直译，跳 System，Text→字符串/Blocks→数组，tool_use.input 直传不 stringify，tool_result is_error 仅 true 带）/`serialize_tools_anthropic`（无 `{type:function}` wrapper，cache_last&&末尾挂 cache_control）/`parse_anthropic_response`（content 数组缺失→Transient，读 input_tokens/output_tokens/cache_read/cache_creation，stop_reason 走 from_signals(None,raw)）+ `AnthropicChatProvider`（`#[cfg(feature="anthropic")]`，x-api-key + anthropic-version=2023-06-01 header，POST `/v1/messages`，非 2xx→ChatError::normalize，build_body cache_system 时 system 数组带 cache_control）。
  - **Cargo/mod**：Cargo.toml 加 `anthropic=["dep:reqwest"]`；mod.rs re-export 3 纯函数（`AnthropicChatProvider` struct **不** re-export，严格镜像 OpenAiChatProvider 也未导出的先例）。
  - **验证**：`cargo test -p zbrain-core --lib ai::chat`（default 34 绿）/`--features anthropic ... ai::chat`（36 绿）/`--features anthropic ... ai::`（全模块 96 绿，tool_loop 未受 from_signals 改动回归）。clippy 零新告警类；workspace build 通过。15 新测试（4 stop_reason + 5 serialize_messages + 2 serialize_tools + 3 parse + 2 build_body）。
  - **写文件坑复现 + 新教训**：Write 一次性 >~250 行截断，用 `.part` 拆写拼接。**新坑**：Write 的 `file_path` 参数偶发丢失/被截断成 `...jununfly\zbr`，内容误写到裸文件 `zbr` → 用 `mv "C:/.../zbr" crates/zbrain-core/src/ai/_tests.part` 移回正确位置再拼接。
- **Part6 Phase 8 刀 1-4-2（Google/Gemini native chat provider）completed**：grill 5 决策全落地。chat.rs +246 行（1864→2110）。
  - **关键前提**：TS Gemini 走 Vercel AI SDK（`createGoogleGenerativeAI().languageModel()` + `generateText()`，gateway.ts:28/1094/1987/2232），**完全无逐字 wire 样本** → 照 Gemini 官方 REST 实现。
  - **Gemini wire 与 Anthropic 的结构性差异（决定实现）**：endpoint `/v1beta/models/{model}:generateContent`（model 嵌 path）；role 只有 `user`/`model`（Assistant→model，其余→user）；system 走顶层 `systemInstruction:{parts:[{text}]}`；tool call 是 `functionCall:{name,args}` part（**无 id**）；tool result 是 `functionResponse:{name,response}` part（**用 name 关联，response 必须是 JSON object**）；tools 用单个 `{functionDeclarations:[{name,description,parameters}]}` 包装（input_schema→parameters）；**无 tool stop 信号**——有 functionCall 时 finishReason 仍是 `STOP`；无 prompt cache（`supports_prompt_cache:false`）→ `cache_system` 忽略；auth 用 `x-goog-api-key` header。
  - **5 决策**：① StopReason 走 `parse_gemini_response` **独立映射**（不碰 `from_signals`；Gemini finishReason 值域 `STOP`/`MAX_TOKENS`/`SAFETY`/`RECITATION`/`PROHIBITED_CONTENT`/`BLOCKLIST`/`OTHER` 与 OpenAI/Anthropic 完全不同）② 一刀做完 Gemini provider（工厂分派留 1-4-3）③ tool_call_id 合成 `{name}-{index}`（Gemini 无 call id，index 消歧同 turn 同名并行调用）④ 非 object output 包 `{"result":output}`（Gemini functionResponse.response 必须是 object，与 Anthropic asStringIfNotObject 反方向对称）⑤ auth `x-goog-api-key` header。
  - **tool_loop 判定确认**：`tool_loop` 靠 `blocks` 里有没有 `ToolCall`（tool_calls_of，tool_loop.rs:354）判定继续，**不依赖** `stop_reason==ToolCalls` → Gemini 缺 tool 信号不断循环；但 `parse_gemini_response` 单独消费时仍需语义正确 stop_reason，故用 `has_tool_call` 标志覆盖为 ToolCalls。
  - **实现结构（chat.rs 插 Anthropic impl 后 line 994）**：`gemini_role` / `gemini_function_response`（object 直传否则包 result）/ `serialize_messages_gemini`（逐消息直译跳 System，Text→`{role,parts:[{text}]}`，Blocks→parts，ToolCall→`{functionCall:{name,args}}` 无 id，ToolResult→`{functionResponse:{name,response}}` 用 name，is_error 不发）/ `serialize_tools_gemini`（单个 functionDeclarations 包装）/ `gemini_finish_reason`（MAX_TOKENS→Length / SAFETY|RECITATION|PROHIBITED_CONTENT|BLOCKLIST→ContentFilter / STOP→End / _→Other，**不返 ToolCalls**）/ `parse_gemini_response`（candidates[0]/content.parts 缺失→Transient；遍历 parts；has_tool_call 覆盖 stop_reason；usage 读 `usageMetadata.{promptTokenCount,candidatesTokenCount,cachedContentTokenCount}`）+ `GeminiChatProvider`（`#[cfg(feature="google")]`，`x-goog-api-key`+`Content-Type` header，POST `/v1beta/models/{model}:generateContent`，build_body: contents + `generationConfig.maxOutputTokens`(默认4096) + systemInstruction + tools，**cache_system 忽略**）。
  - **Cargo/mod**：Cargo.toml 加 `google=["dep:reqwest"]`（插 anthropic 与 embedding 间）；mod.rs re-export 3 纯函数（`GeminiChatProvider` struct **不** re-export，镜像 OpenAi/Anthropic 先例）。
  - **验证**：`cargo test -p zbrain-core --lib ai::chat`（default 46 绿）/`--features google ... ai::chat`（48 绿）/`--features google ... ai::`（全模块 108 绿，tool_loop 无回归）。clippy chat.rs 零告警（581 全 crate ambient）；`cargo build --features google` 通过。14 新测试（5 serialize_messages + 2 serialize_tools + 5 parse + 2 build_body）。
  - **写文件坑复现**：`.part` 拆写拼接（head-994 插 provider、head-1865 替换末尾 `}` 插测试），本轮未复现 file_path 丢失坑。
- **Part6 Phase 8 刀 1-4-3（BudgetTracker 接线 + provider 工厂分派）completed**：Phase 8 最后一刀，1-4 三子节点全绿、父节点 [x]。grill 6 决策全落地（2 轮 AskUserQuestion）：注入 `Option<&BudgetTracker>` 显式参数（None no-op）/ 一刀两块都做 / 忠实复刻延迟抛出 / 工厂返 `Box<dyn ChatProvider>` / env 查找器注入 key / 独立 `BudgetExhausted` 错误型。
  - **块1 新建 `crates/zbrain-core/src/budget.rs`（922 行含测试）**：`BudgetTracker`（reserve/record/snapshot/total_spent/assert_runtime/fire_exhausted 一次性 latch，`Mutex<TrackerState>` 内部可变让 `&BudgetTracker` 跨调用链共享而 record 仍能改 cumulative）+ `BudgetExhausted`（`{message,reason,spent,cap,model_id}` + Display + Error，reason=Cost/Runtime/NoPricing）。**注入模型**：TS 用 `AsyncLocalStorage` 环境单例（scope 外 null→no-op），Rust 无等价惯用法改用显式参数 `Option<&BudgetTracker>`，None=预算逻辑 no-op。
  - **契约忠实复刻**：reserve=调用前乐观预检（input+maxOutput 投影）；record=唯一累加 `cumulative_usd` 点；**TX1**=record 累加后 `cumulative>cap` 返 Err（真天花板）；**TX2**=有 cap 无 pricing → reserve 硬失败（reason:NoPricing）；cap 未设+无 pricing 走 warn-once（`static UNPRICED_WARNINGS: Mutex<Option<HashSet>>` + `reset_..._for_test` seam）；**延迟抛出**=record 超 cap 返 Err 但让原始 chat 结果优先，超预算在下次 reserve 才浮现。cumulative 是进程内单实例生命周期累加，非按周聚合。
  - **pricing 复用 registry 内联价（无新建独立 pricing 表）**：`lookup_chat_pricing`（扫 registry chat 触点读 `cost_per_1m_{input,output}_usd`）/ `lookup_embed_pricing`（复用 `ai::lookup_pricing` + free-local 零价）/ `lookup_rerank_pricing`（扫 reranker 触点 + free-local）/ `lookup_pricing_for_kind` 分派 / `cost_for_usage`。free-local 零价常量 `FREE_LOCAL_{RERANK,EMBED}_PROVIDERS`（本地推理免 --max-cost 硬失败）。pricing 表无 cache token 单价（cache_read/creation 仅透传不计费）。
  - **审计**：`budget_audit_filename(now)`（`budget-YYYY-Www.jsonl`，`%G-W%V`）+ `append_audit_line`（best-effort，create_dir_all+append+eprintln 兜底）——直接照抄 `rerank_audit.rs` ISO-week 先例（injectable clock + 失败不阻断 + round-trip 测试）。audit_dir 由调用者注入（纯函数可测试）。
  - `extract_usage_from_error(err, fallback)`：top-level 或 nested `response.usage`，snake/camel key，缺失回退悲观 ceiling。**20 测试全绿**（pricing 5 + reserve 5 + record 4 + audit 2 + extract_usage 4）。
  - **块2 chat.rs 加 `instantiate_chat` 工厂（2120→2346 行）**：`instantiate_chat(recipe, model_id, env_lookup) -> Result<Box<dyn ChatProvider>, AiConfigError>`，match `recipe.implementation`（**非 provider id**，忠实 TS gateway.ts:2222 switch）四分支 native-openai/native-google/native-anthropic/openai-compatible，native 三家 `#[cfg(feature)]` 门控未开返 `feature_disabled` AiConfigError，openai-compatible 也走 `OpenAiChatProvider`（同 wire，base_url 取 recipe.base_url_default）。`require_api_key` 从 `auth_env.required[0]` 取 key（env_lookup 闭包纯函数可测试，镜像 TS cfg.env），缺失附 setup_hint 报错。补 `OpenAiChatProvider::DEFAULT_BASE_URL="https://api.openai.com/v1"` / `AnthropicChatProvider::DEFAULT_BASE_URL="https://api.anthropic.com"`（native recipe base_url_default:None，TS 靠 SDK 内建，Rust 硬编码常量）。`tool_loop` 已吃 `&dyn ChatProvider` 天然衔接 Box。8 工厂测试（3 native 建 provider + openai-compat + missing key setup hint + 3 feature_disabled）。
  - **lib.rs 挂 `pub mod budget;` / mod.rs re-export `instantiate_chat`**。chat.rs 模块 doc scope 更新（工厂从 Out 移 In，budget 移独立模块）。
  - **验证**：default `ai::` 109 / `budget::` 20 / 全 feature `ai::` 115 全绿（tool_loop 等无回归）；clippy budget.rs+chat.rs 零告警；workspace build default + 全 provider feature 均通过。多 filter 参数（`ai:: budget::`）被 cargo test 当模块路径导致空输出+exit1 → 拆逐模块单跑。
  - **写文件坑复现**：`.part` 拆写拼接（`_bt.part`+`_bt2.part` head-n-1 去半行拼接、`_bttest.part` 拼测试、`_factory.part` head-1367 插入、`_ftest.part` head-2260 插 test mod）。**Write file_path 参数偶发丢失**（"Parameter file_path expected string, but received undefined"）复现 2 次 → 显式带 file_path 重试即成功。
  - **seam 边界**：BudgetTracker 与 instantiate_chat 尚未接入生产 chat 调用链（本刀只做 seam + 单测）；实际 gateway.chat 等价物的 reserve→工厂建 provider→调用→record 编织留给 **Phase 9 消费方**（`onExhausted` checkpoint 持久化亦是 Phase 9 职责）。
- **Part6 Phase 8 sub-node 1-2-1（tier-routing 层）completed**：Phase 8 最后一个节点，收官刀。grill 6 决策全 A（一事一议逐个落）。产出两新文件 `crates/zbrain-core/src/ai/capabilities.rs`（~250 行含测试）+ `crates/zbrain-core/src/ai/model_config.rs`（~500 行含测试）。
  - **核心岔口 —— config 读取（Q0/Q1）**：TS `resolveModel` 靠 `engine.getConfig(key)` **异步读 DB `config` 表**（`zbrain config set` 写入项）；但 Rust `BrainEngine` trait（~90 方法）**完全无 config kv 方法**，CLI 的 `get_config_value(key,&yaml)` 读的是 `zbrain.yml` **YAML 启动配置**（非运行时 DB，语义不等价）。决策：**不碰 BrainEngine trait**，`resolve_model` 纯同步收注入的 `&dyn ConfigLookup { fn get(&self,key)->Option<String> }`（None/缺失等价），DB/YAML 读取由调用方在边界预取后注入。async DB 读留 Phase 9 消费方（届时注入真读 DB config 表的 impl，本纯逻辑层零改动）。`impl ConfigLookup for HashMap<String,String>` 便利测试。
  - **capabilities.rs（Q2 新建全量）**：忠实复刻 TS `src/core/ai/capabilities.ts`。`ProviderCapabilities{4 bool + max_context}`（`#[allow(struct_excessive_bools)]` justified 1:1）；`get_provider_capabilities(model)->Result<_,AiConfigError>`（`resolve_recipe_strict`→无 chat 触点 Err；`supports_prompt_caching = chat.supports_prompt_cache==Some(true)`；**`supports_parallel_tools = chat.supports_tools`** 忠实 TS 无独立字段；**`supports_thinking = false` 硬编码** 忠实 TS，注释明说不读 supports_subagent_loop）；`CapabilityVerdict{Ok/DegradedNoCaching/DegradedNoParallel/UnusableNoTools/Unknown}`；`classify_capabilities` 5 档裁决。**registry 现无任何 `supports_tools:false` recipe → UnusableNoTools/DegradedNoParallel 当前不可达**（测试注释诚实标注）。9 测试。真实数据点：`anthropic:claude-sonnet-4-6`→Ok / `openai:gpt-5.2`→DegradedNoCaching / `voyage:*`&`nope:model`→Unknown。
  - **model_config.rs（Q5 新建单文件）**：`ModelTier{Utility/Reasoning/Deep/Subagent}`——**用 ModelTier 避开 `ai/types.rs` 已存在的 `Tier{Native,OpenaiCompat}`（recipe SDK 分层，与语义路由分层撞名）**。`tier_default`（utility→haiku-4-5/reasoning→sonnet-4-6/deep→opus-4-7/subagent→sonnet-4-6，均 anthropic: 前缀；`#[allow(match_same_arms)]` justified Reasoning/Subagent 概念独立不合并）；`default_alias`（5 内建：opus/sonnet/haiku/gemini→google:gemini-3-pro/gpt→openai:gpt-5）；`resolve_model_alias`（depth>2 断环：用户 alias→内建→透传）；`resolve_model(lookup,opts)->String` **7 步精度链**（①CLI flag ②config_key ③models.default ④tier override ⑤env ⑥tier_default ⑦caller fallback；③④⑤经 enforce_subagent_capable，全经 alias）——**footgun 已测：tier override（⑤ TS 原序）在 default 之后**，模块 doc 警告；`enforce_subagent_capable`（非 subagent no-op；UnusableNoTools|Unknown→回退 tier_default+warn / DegradedNoCaching→返原值+成本 warn / DegradedNoParallel|Ok→原样；照搬 budget.rs warn-once：`static SUBAGENT_TIER_WARNINGS: Mutex<Option<HashSet>>` + reset_for_test seam，Q3 决策）。19 测试。
  - **Q4 砍 legacy**：deprecatedConfigKey 键链 / isAnthropicProvider / enforceSubagentAnthropic 三处 TS legacy **有意不移植**（模块 doc 记录理由），不登 KNOWN-GAPS（无线上用户，破坏性改名阶段）。
  - **接线**：mod.rs 挂 `pub mod capabilities;`+`pub mod model_config;` + re-export；resolver.rs `AiConfigError::new` 改 `pub(crate)`（capabilities.rs 需构造 error，修 E0624 associated fn private），头部 doc 从"sliced out to 1-2-1"更新为"now lives in super::model_config"。
  - **验证**：`cargo test -p zbrain-core --lib ai::model_config -- --test-threads=1` / `... ai::capabilities`；default `ai::` 137 全绿（+28，无回归）；clippy 两文件零告警（4 justified allow：struct_excessive_bools/match_same_arms/implicit_hasher + doc list 缩进重写）；workspace build 通过；resolver 15 测试无回归（pub(crate) 改动安全）。**多 filter 参数陷阱**：全程单模块 filter 各一条避空输出。
  - **seam 边界**：`resolve_model` 尚未接生产 chat 调用链；Phase 9 消费方需注入真读 DB config 表的 `ConfigLookup` impl（本纯逻辑层完全不改）。与 1-4-3 的 BudgetTracker + instantiate_chat 同属待 Phase 9 接线的备料。
- **Phase 8 整体回归 PASS（收官后验证）**：`cargo test -p zbrain-core --lib -- --test-threads=1` **default 全量 889 passed / 0 failed**；`--features anthropic,google,openai,embedding ai::` **143 passed / 0 failed**（tool_loop/capabilities/model_config/chat 三家 provider 全绿）；`cargo clippy --workspace --all-targets --features zbrain-core/{anthropic,google,openai,embedding}` build 通过、**zbrain-core 零告警**，唯一告警在 `zbrain-cli` lib **test**（117 warns/103 dup，`uninlined_format_args`+`map_unwrap_or`，**预存非 Phase 8 引入**，可 `cargo clippy --fix` 批修，留作独立清理）。**全 Part 盘点**：Part1-6 roadmap 根节点全 `[x]`，唯一剩 **Part7 Phase 9**（8 节点全 pending：1-1 MinionQueue/1-2 Worker+supervisor/1-3 Budget+rate leases/1-4 handlers+tools/1-5 Autopilot+fanout/1-6 Remote/1-7 jobs/agent CLI/1-8 G7 webhook 收口）。下一步推荐：开 Part7 Phase 9，从 1-1 MinionQueue+job 持久化起（消费方骨架，后续 1-2 Worker 接 toolLoop+BudgetTracker+instantiate_chat+tier-routing）。
- **Phase 8 收官（彻底清零）**：1-1/1-2（含 sub-node **1-2-1**）/1-3/1-4（1-4-1/1-4-2/1-4-3）/1-5/1-6 **全 completed**。Phase 8（AI Gateway）整段完成。剩余大阶段：**Phase 9（Jobs/Minions）**——toolLoop(1-5)+BudgetTracker+instantiate_chat(1-4-3)+tier-routing(1-2-1) 四块料已全部备齐，可解此前 blocked 的消费方接线（reserve→工厂建 provider→toolLoop→record 编织 + DB 持久化 hooks + 注入真 ConfigLookup）；之后 Phase 10（Evals）/11（Final Cutover）。
- **Part7 Phase 9 刀 1-1-1（MinionQueue A+B: 地基+并发核心）completed（2026-07-10）**：Phase 9 首刀，最小可用队列。三后端 8 job 方法（enqueue/get_job/get_jobs/claim/complete/fail/renew/retry）全实现 + backend-agnostic contract 测试 28 全绿。
  - **交付**：migration 0014（PG + SQLite 各 40 列全建，一次到位免后续 ALTER）+ minions/types.rs（MinionJob 40 字段 / MinionJobStatus 9 值 / Input / JobFilters / FailOutcome）+ minions/queue.rs 薄 facade + engine.rs InMemoryEngine 8 方法（Mutex 持锁=行锁类比）+ libsql.rs 8 方法（BEGIN IMMEDIATE claim + claim_job_locked free fn + MINION_JOB_COLUMNS/last_insert_rowid/libsql_row_to_job helper）+ postgres.rs 8 方法（本轮完成）+ tests/minion_queue.rs（10 contract fn × 三后端 + libsql reconnect 持久化）。
  - **PostgresEngine 落地要点（本轮）**：① claim 用 WITH claimed AS (SELECT id AS cid ... FOR UPDATE SKIP LOCKED) UPDATE minion_jobs m ... FROM claimed WHERE m.id=claimed.cid RETURNING —— CTE 列必须别名 cid，否则 claimed 与 m 都有 id，RETURNING 裸 id 报"字段关联 id 不明确"。② 时间双类型：记录列 SELECT col::text AS col → try_get String；调度列 SELECT (EXTRACT(EPOCH FROM col)*1000)::bigint AS col → try_get Option<i64> epoch-ms；写调度列用 now() + ($N::double precision * interval '1 millisecond')。③ PG SERIAL/INTEGER 是 int4，MinionJob.id/tokens 是 i64 → i64::from(get i32)；timeout_ms 列 int4 但 input 是 i64 → INSERT bind cast $14::int。④ 幂等走 SELECT idempotency_key + INSERT RETURNING；complete/fail/retry 全用 UPDATE ... RETURNING + token-fence WHERE status='active' AND lock_token=$N。⑤ JSONB（data/result/progress/stacktrace/quiet_hours）直接 try_get serde_json::Value。⑥ pg_row_to_job by-name try_get + get! macro。
  - **关键类型决策 —— backoff_jitter 必须 DOUBLE PRECISION 不能 REAL/float4**：Rust backoff_jitter 是 f64，PG 若用 REAL(float4) 存 0.2 round-trip 后为 0.20000000298，破坏 (x-0.2).abs()<1e-9 精确断言（SQLite REAL 是 8 字节 IEEE=f64 无此问题）。改 PG migration 0014 列为 DOUBLE PRECISION，头注释锁 "do not narrow back"；SQLite migration 头注释同步更新类型映射说明。
  - **收尾修的遗留 bug**：tests/libsql_engine_migrations.rs 的 EXPECTED_VERSION 上一轮加 0014 时漏同步（13→14），导致 fresh_db/idempotent 两测试 fail。已改 14 + 注释。全量 cargo test -p zbrain-core 现全绿。
  - **待后续切片**：C（后台 sweep promoteDelayed/handleStalled/handleTimeouts）→ 1-1-2；D（父子依赖图/inbox/attachments/运维）→ 1-1-3；add 的 parent/backpressure 延后。1-2 Worker 接线（toolLoop + BudgetTracker + instantiate_chat + tier-routing 四块料已备齐）是下一步大头。
- **Part7 Phase 9 刀 1-1-2（MinionQueue C: 后台 sweep）completed（2026-07-10）**：4 时间驱动 sweep 方法（promote_delayed/handle_stalled/handle_timeouts/handle_wall_clock_timeouts）忠实 port TS `MinionQueue` 到三后端。纯 C 层状态机推进，inbox 插消息+父解锁归 1-1-3 D 层。全量 `cargo test -p zbrain-core --test minion_queue` **40 passed / 0 failed**（含真实 PG pg-embed）。
  - **交付**：types.rs 新增 `StalledSweep{requeued:Vec<MinionJob>,dead:Vec<MinionJob>}`（双桶区分锁过期重试 vs 停滞耗尽死信）；BrainEngine trait 新增 4 sweep（3 返 Vec<MinionJob> / handle_stalled 返 StalledSweep / wall_clock 带 lock_duration_ms:i64 参）+ 2 test-only helper（set_started_at_for_test / set_timeout_at_for_test，默认 Unsupported）；三后端全实现；queue.rs 4 薄封装 facade；tests/minion_queue.rs 4 backend-agnostic contract fn × 三后端 = 12 新测试。
  - **三后端实现差异**：① InMemory 持 minion_jobs_store mutex 遍历 Vec，3 纯 sweep 对 Option<i64> epoch-ms 整数比较，wall_clock 用 chrono::DateTime::parse_from_rfc3339(started_at) 算 elapsed。② Libsql（无 UPDATE RETURNING）：promote/timeouts/wall_clock 走 SELECT ids → UPDATE → libsql_reselect_jobs（新 helper，按 id 列表 SELECT+libsql_row_to_job）；handle_stalled 复刻 1-1-1 claim 先例 BEGIN IMMEDIATE 包读候选（disjoint WHERE：`stalled_counter+1<max_stalled` requeue / `>=` dead）+双 UPDATE+COMMIT+reselect（含 ROLLBACK 错误路径）；wall_clock 阈值 `(julianday(?now)-julianday(started_at))*86400000` 在 SQL 内算。③ Postgres 全 UPDATE...RETURNING 单语句，handle_stalled 两条 disjoint-WHERE UPDATE 复现分组，wall_clock 用 `EXTRACT(EPOCH FROM now()-started_at)*1000`。wall_clock CASE 阈值双后端一致：timeout_ms 有值→`*2`，无值→`lock_duration_ms*2*GREATEST(max_stalled,1)`。
  - **决策 6 实现期暴露的真实 bug**：原设想「enqueue 负 timeout_ms → claim 时 timeout_at=now+负<now」构造过期，但 SQLite/PG 都有 `chk_minion_timeout_positive CHECK(timeout_ms IS NULL OR timeout_ms>0)`，负值被约束拒绝——InMemory 无约束掩盖了这点（先绿后红暴露）。**修法**：新增 `set_timeout_at_for_test(id,epoch_ms)` trait helper，测试用正 timeout_ms 正常 claim 后直接改写 timeout_at 列。**经验复现**：PG/SQLite 强约束能揪出 InMemory 无约束环境掩盖的构造错误，contract 测试跨三后端一把校验对齐有独立价值。
  - **决策 4 降级退路未触发**：libsql `julianday()` 对 RFC-3339 带 `Z`/`+00:00` 后缀解析验证正确（libsql_handle_wall_clock_timeouts 绿），无需降级为 SELECT+Rust parse。
  - **无 sleep 时间注入策略（决策 6 落地）**：promote_delayed 用 `fail_job(Delayed,backoff_ms=0)` 令 delay_until=now；handle_stalled 用 `claim_job` 传负 lock_duration_ms 令 lock_until<now；handle_timeouts 用 set_timeout_at_for_test 塞过去 + 正常锁保证 lock_until>now（区分 stalled）；wall_clock 用 set_started_at_for_test 塞 10 分钟前。
  - **下一步**：1-1-3（D 层：父子依赖 resolveParent/cancelJob 递归 CTE + inbox sendMessage/readInbox + 附件 CRUD + pause/resume/prune/getStats；sweep 的 notify/解锁 waiting-children 归此层）；然后 1-2 Worker 接线。
- **Part7 Phase 9 刀 1-1-3-2（附件 CRUD）completed（2026-07-13）**：D 层第二子刀，minion_attachments 独立表 + add/list/get/delete。忠实 port TS `src/core/minions/{attachments.ts, queue.ts}`。全量 `cargo test -p zbrain-core --test minion_queue` **70 passed / 0 failed**（含真实 PG pg-embed）+ attachments 纯函数单测 **10/10 全绿**。
  - **交付**：migration 0016 双方言（PG BYTEA + SQLite BLOB，SERIAL→INTEGER PK AUTOINCREMENT / TIMESTAMPTZ now()→TEXT CURRENT_TIMESTAMP / SET STORAGE EXTERNAL 省略，UNIQUE(job_id,filename)+2 CHECK+idx 照搬）+ version 16 注册到 POSTGRES_MIGRATIONS/LIBQL_MIGRATIONS 两 registry（InMemory 无 migration）；types.rs 加 `AttachmentInput{filename,content_type,content_base64}` / `NormalizedAttachment{filename,content_type,bytes:Vec<u8>,size_bytes,sha256}`（不 Serialize，含 bytes）/ `Attachment{id,job_id,filename,content_type,storage_uri:Option<String>,size_bytes,sha256,created_at:String}`；新建 `minions/attachments.rs`（~290 行）`validate_attachment(&AttachmentInput,&AttachmentValidationOpts)->Result<NormalizedAttachment,String>` 纯函数（base64 0.22 STANDARD + regex LazyLock + sha2/hex）；queue.rs facade 加 add/list/get/delete_attachment（add 编排 list existing filenames→validate→engine.insert_attachment 收已解码 bytes）+ `max_attachment_bytes` 字段 + `with_max_attachment_bytes` builder；engine.rs trait 加 5 默认 Unsupported 方法（insert/list_filenames/list/get/delete）+ InMemoryEngine 加 `minion_attachments_store: Mutex<Vec<InternalAttachment{meta,bytes}>>`+`next_attachment_id` 真实存 bytes；libsql.rs/postgres.rs 各实现 5 方法 + row_to_attachment helper。
  - **决策落地（roadmap 4 决策全 A）**：① validation 纯函数解耦（facade 编排，三后端只写查 job 存在+INSERT bytes 纯 SQL，零重复）；② insert_attachment 收 `&NormalizedAttachment` 结构体引用（避 6 参错位），job 不存在显式 SELECT 早退返 NotFound 'job N not found'（不暴露 FK 违约原始错误）；③ get_attachment 返 `Option<(Attachment,Vec<u8>)>` 元组，InMemory 真实存 bytes（否则测不到二进制 round-trip+sha256 核心）；④ created_at=String（record 列不算术，同 InboxMessage.sent_at 范式）。
  - **base64 严格 RE 边界洞察**：严格 `^[A-Za-z0-9+/]*={0,2}$` 下"decode 成空字节"分支对非空输入不可达（非空过 RE 必解出非空 bytes；`"===="` 因 >2 个 `=` 被 RE 先拦成 "invalid characters"）。测试 `rejects_excess_padding` 诚实断言真实拦截路径，`bytes.is_empty()` 保留为防御性 backstop（忠实 TS port）。
  - **DB UNIQUE 权威 + 内存早退双层**：`UNIQUE(job_id,filename)` 是兜底，facade existing_filenames 是友好早退；InMemory 无真实 UNIQUE 故 insert_attachment 自加 Conflict 检查模拟 DB 兜底（应对 TOCTOU）。
  - **storage_uri 恒 NULL**：忠实 TS 只走 inline content bytes；外部存储是 schema 预留未实现，代码注释标 reserved（待登记 KNOWN-GAPS）。空串读回归 None（TS `|| null` → Rust `.filter(|s|!s.is_empty())`）。
  - **PG/libsql 读写惯例复用**：PG INT4 size_bytes/SERIAL id 需 `::bigint AS` cast、TIMESTAMPTZ 需 `::text AS`、BYTEA 读 `Option<Vec<u8>>`、insert `.bind(&att.bytes)`+`RETURNING id::bigint,created_at::text`；libsql BLOB 绑 `Value::Blob(Vec<u8>)`、读 `get_value` 匹配 Blob/Null/Text、last_insert_rowid+反查 created_at。
  - **依赖**：workspace Cargo.toml + core dep/dev-dep 加 `base64 = "0.22"`（sha2/hex 已在）。
  - **下一步**：1-1-3-3（运维 pause_job/resume_job/prune DELETE RETURNING count/get_stats 三段纯读）；之后 1-2 Worker 接线（toolLoop+BudgetTracker+instantiate_chat+tier-routing 四料已备齐）。
