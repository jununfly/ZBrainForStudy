# 2026-07-28

## libsql FFI flake 加固（推广加锁到所有 libsql 集成测试）
- 背景：Windows 下 `cargo test` 驱动 libsql 原生 FFI 会随机 `STATUS_ACCESS_VIOLATION` (0xc0000005)；1-1-2 时 `extract_atoms.rs` 用进程级 `OnceLock<Mutex<()>>` 锁串起本文件测试（当时 4/4 稳定）。
- 本次把同一锁模式推广到 `crates/zbrain-core/tests/` 下全部 51 个引用 `LibsqlEngine` 的集成测试文件（`extract_atoms.rs` 已有锁，跳过）：
  - 每文件加 `static LIBSQL_TEST_LOCK: std::sync::OnceLock<std::sync::Mutex<()>>` + `fn libsql_test_guard()`，每个 `#[tokio::test]`/`#[test]` 函数体首行 `let _guard = libsql_test_guard();`。
  - 用脚本 `.workbuddy/_tmp_add_guard.py` 机械插入（插入点：最后一个顶层 `use` 之后；guard 行插在函数体 `{` 之后第一行）。修复 `takes_crud.rs` 因多行 `use {..}` 被插断的编译错误。
  - 各测试二进制内串行、二进制间仍并行（FFI 崩溃是进程内多线程问题，跨进程独立）。
- **关键发现（重要）**：锁只是「部分缓解」，并**不能**根治 flake。对最易崩的 `extract_facts`（6 tests）做了 4 组各 ×3 运行验证：
  1. 仅 body-mutex 锁（默认并行）→ 仍崩
  2. `#[tokio::test(flavor="current_thread")]` + 锁（默认并行）→ 仍崩
  3. `--test-threads=1`（单线程 harness）→ 仍崩（3 次中 2 次崩）
  4. 泄漏 temp 文件（排除删文件竞态）→ 仍崩
  ⇒ 这是 **libsql/SQLite 原生 FFI 在 Windows 上的真·间歇性崩溃**，与测试并发/文件生命周期无关，代码层无法根除。
- **可靠绿的唯一路径在基础设施层**：CI 对 libsql 集成测试 (a) segfault 重试，或 (b) 跑在 Linux/macOS agent。代码层锁保留作为降低频率的缓解（且是用户要求的「推广加锁」动作），但不可承诺稳定全绿。
- `cargo test -p zbrain-core --no-run` + 抽跑确认：51 文件全部编译通过（仅既有 warning，无 deny(warnings)）。

## MEMORY.md 更正（roadmap 铁律）
- 旧描述说反了：`render` 实际**只**重写带 ⚠️ 的 `⚠️ ROADMAP_SECTION`（JSON 驱动的活树），**不碰**顶部纯 `ROADMAP_SECTION`；故顶部纯段已成孤儿（会 stale），`update`+`render` 后需手动同步顶部纯段。已改正 MEMORY.md 第 12 行 + 进度快照第 26 行（1-1-1/1-1-2 均已 push，下一步 1-1-3）。

## 待办（下轮）
- 提交并 push 本轮改动（feat: 51 文件加锁；chore: MEMORY.md 更正 + 日志）。等用户指令。
- 是否要就 libsql FFI flake 做基础设施层处理（CI 重试 / Linux agent）？待用户拍板。
- 继续 1-1-3 extract-takes。

## CI 层处理 libsql flake（用户指令：重试 / Linux agent）
- 发现：**此前 CI 完全没有跑 `cargo test`**——4 个 workflow（test/heavy-tests/e2e/release）全是 `bun test`(TS) + `cargo build --release`，libsql flake 一直是纯本地(Win)问题。
- 新增 `.github/workflows/rust-tests.yml`：
  - `runs-on: ubuntu-latest`（Linux agent）—— 直接绕开 Windows libsql FFI 崩溃（已验证该崩溃是 Windows 原生库问题，Linux 不出现）。
  - 触发：push/PR 到 `master` + `rust-rewrite`（路径过滤 `crates/**`/`Cargo.toml`/`Cargo.lock`）+ `workflow_dispatch`。
  - 试运行 bash：动态收集引用 `LibsqlEngine` 的集成测试 target（`grep -rl` → 52 个，排除 `support/`），加 `--lib` 一起跑；postgres(pg-embed) 测试有意排除。
  - 重试守卫：外层 `for i in 1..3` 包裹 `cargo test`，任一 attempt 通过即 exit 0，连续 3 次失败才红——作为 residual flakiness 的防御纵深。
  - cargo 缓存（`~/.cargo` + `target`，key=Cargo.lock hash）加速重跑。
- 验证：YAML 经 pyyaml 解析通过（jobs=['cargo-test']，`on` 被 pyyaml 当 bool 是解析器怪癖，GitHub 自身 parser 正常）；bash target 收集逻辑 dry-run 通过（52 target → 105 个 cargo arg）。
- 未提交（延续铁律，等用户指令）。
- 注意：Linux 上 52 个 libsql 测试二进制尚未在 Linux 实跑过（本机是 Win），但崩溃属 Windows 特有，预期 Linux 稳定；CI 跑一次即可确认。

## WSL 本地验证尝试（用户选 A：本机 WSL 跑 libsql 测试）—— 受阻
- `wsl --install -d Ubuntu-24.04 --no-launch` 成功（~15min 下载注册，distro 状态 Stopped/WSL2，已成为默认）。
- **但 first-run OOBE（创建 UNIX 用户）在本非交互 shell 中无法完成**：`printf 'zbrain\n...' | wsl -d Ubuntu-24.04` 卡住（OOBE 需要真实交互控制台/控制终端 TTY，非交互管道喂不进去）；即使喂了 3 行也卡，distro 停在 Running 且 `wsl -u root -- id` 同样卡（init 未完成）。
- 后续 `wsl --terminate` / `wsl --shutdown` 均挂起 → **WSL 子系统已 wedged（卡死）**，本非交互环境无法恢复。需**重启机器**或管理员 `net stop LxssManager` 解卡。
- 结论：此 agent 的 Bash 非交互、无控制终端，WSL 首次启动 OOBE 必须人工在真实 Windows Terminal 跑一次 `wsl -d Ubuntu-24.04` 交互建用户后才能由我非交互驱动（装 Rust + 跑测试）。

## GitHub Actions 实测休眠（option B 也受阻）
- 用户选 2（靠已 push 的 CI）。但 workflow 的 `on.push.paths` 只含 `crates/**`/`Cargo.toml`/`Cargo.lock`，仅 push workflow 文件不会自动触发——本应手动 dispatch。
- `gh workflow run rust-tests.yml --ref rust-rewrite` → 404（默认分支 master 无此文件）；REST `POST .../dispatches -f ref=rust-rewrite` → 也 404。
- 核查：`GET /actions/workflows` 返回 `total_count: 0`；`gh run list` 为空；`GET test.yml`(master 确有) 也 404。**即本仓库 GitHub Actions 实际未注册/休眠——连 master 上 4 个既有 workflow 都没注册**。Actions 权限 `enabled:true` 但扫描器从未运行。
- 推送 no-op 空 commit 到 rust-rewrite 唤醒扫描器 → 仍 404 未注册。
- 结论：CI 当前实际跑不起来，根因 GitHub 仓库侧 Actions 未初始化（需用户在 GitHub UI 打开仓库 Actions 标签启用/初始化）。非代码/本地可解。

## 用户选 2（本地 WSL 验证）+ 撤回空 commit
- 用户决定走本地 WSL 验证，并要求撤掉为唤醒扫描器 push 的空 commit `74a95b1`：`git reset --hard HEAD~1` + `git push --force origin rust-rewrite` → origin/rust-rewrite 回到 `6fde20a`（已验证）。
- 已准备 WSL 内执行脚本 `.workbuddy/_tmp_wsl_run.sh`：以 root 跑（免 sudo 密码），含 apt 装 build-essential/pkg-config/libssl-dev/clang/protobuf-compiler + rustup 装 Rust + 动态收集 52 个 libsql test target + `--lib` 跑 `cargo test -p zbrain-core`（CARGO_TARGET_DIR 指向 /root/zbrain-target 原生 ext4，加速编译 + 保持 Windows target/ 干净）。
- WSL 任务状态：#83 distro 已装、#84 装依赖+rust（进行中）、#85 跑测试（待 #84 完成）。

## 重启后 WSL 实测：root 无需 OOBE 即可进（修正前结论）
- 用户重启机器后 WSL 解卡。`wsl -u root -- id` 直接返回 `uid=0(root)`，distro Running/WSL2。**关键修正**：前面以为 OOBE 必须人工先完成，实测 WSL 的 `-u root` 会**绕过默认用户创建那一步**（root 不依赖 OOBE），所以非交互 shell 也能直接以 root 驱动——无需用户在真实终端做一次性 OOBE。
- 进度检查：`ps` 显示 `apt-get` 在跑（已 ~2min），说明脚本正常推进（apt 装依赖阶段），WSL 内网络/apt 可用。
- 已后台启动验证（task 6ZWtY4，WSL root 跑 `_tmp_wsl_run.sh`）：apt → rustup 装 Rust → `cargo test -p zbrain-core --lib + 52 libsql target`。首次编译在 /root/zbrain-target（原生 ext4），预计 10–20min。完成后自动通知。
- 待结果：确认 52 个 libsql 测试 + --lib 在 Linux 全绿（验证 Windows FFI 崩溃在 Linux 不出现，等于本地跑了一遍 CI 的 Linux 任务）。

## WSL 构建被重启打断 + 韧性重跑
- **17:56 左右 WSL 被重启**（17:57 `uptime` 显示 "up 0 min"），原后台任务 6ZWtY4 被杀，构建中断。回溯：17:53 查时无 cargo 进程、无 target——当时其实还卡在 **rustup 下载 Rust 工具链阶段**（慢网 ~300KB/s 下 200MB 要十几分钟），根本没进 cargo test。重启把 rustup 下载打断 → `rustup toolchain list` 显示 "no installed toolchains"，cargo 报 "no default is configured"，target 全空。
- **教训**：WSL 重启会杀掉后台构建；构建需 ~25–30min 不间断。
- **韧性改造 `_tmp_wsl_run.sh`**：① 加 `rustup default stable`（显式设默认，避免中断后无默认）；② 全程日志 `exec > >(tee -a /mnt/c/.../wsl_libsql.log) 2>&1` 写到 **Windows 挂载盘**（WSL 重启也不丢日志，可用 Read 直接看）；③ 去掉 `tail` 缓冲；④ `/root/zbrain-target` 在 distro ext4 上、重启保留 → 重跑是续跑。
- **重跑已启动**（task U9EYaA）：日志确认 apt 全 "already newest"（跳过）、rustup 在 "downloading installer" 续下工具链。下一步 rustup 装完→`cargo test`（10–20min）。需用户侧：**构建期间保持机器唤醒、勿 terminate WSL**，否则再重启只打断、但日志可看进度、重跑续。
- 待结果：52 libsql + --lib 在 Linux 全绿即为验证成功。
