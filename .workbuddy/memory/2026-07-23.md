## 2026-07-23 工作记录

### takes_calibration 提交完成

- 清理临时文件：temp-libsql-top.rs / tmp.md / stderr
- git add + commit 成功：`962bfbe feat(calibration): add aggregate_domain_scorecards + takes_calibration operation`
- 所有 56 calibration 测试 passed (0 failed)
- 依赖 `erased-serde = "0.4"` 已添加

### doctor 检查项迁移：embedding_health (G41) 完成

- 实现 `embedding_health` doctor 检查：ZeroEntropy API key 配置验证 + 确认 embedding 列持久化
- 从 `UNMIGRATED_TS_DOCTOR_CHECKS` 移除
- 新增锚点测试 `embedding_health_is_no_longer_unmigrated` ✅ passed
- 更新 KNOWN-GAPS.md G41 标记为 resolved
- 所有 7 个已迁移 doctor 检查锚点测试全绿：reranker_health / eval_drift / takes_weight_grid / skill_conformance / brain_score / sync_freshness / embedding_health
- 编译成功，无错误

### grill-me + roadmap-driven: "接下来应该做什么" 三记决策落盘

- 用 zj-grill-me + zj-roadmap-driven 烤 Part11/Part12 下一步方向，决策全部写入路线图 JSON。
- **Q1 主攻方向**: 推 operations.ts grind(1-6-7→1-6-7-9 删 operations.ts + 1-3-3 calibration 收尾)到终局，并**并行起草 cycle 独立 Part12 路线图**备用；grind 不暂停。落盘 Part11 根节点 decide + 1-12 下挂 1-12-1(cycle 路线图起草, explore/pending)。
- **Q2 cycle 切法**: 按能力簇切 ~6 slice(facts-extraction / emotional-calibration / synthesis / anomaly-transcript / auto-think / orchestration-main-loop)。
- **Q3 簇执行序**: 消费者解锁优先: emotional-calibration(1-2)→facts-extraction(1-1)→anomaly-transcript(1-4)→synthesis(1-3)→auto-think(1-5)→orchestration-main-loop(1-6) 最后(主循环留最后降 big-bang 风险)。
- 新建 Part12: `docs/plans/zbrain-ts-to-rust-part12-cycle-migration.json` + `ZBRAIN_TS_TO_RUST_PART12_CYCLE_MIGRATION.md`，6 簇切片就位，执行仍延后(预排)。
- cycle(1-12) 现状: runCycle 2057行 + 20 phase 全 stub(0/20)，B类真迁移主战场；6 硬消费者 dream→runCycle / calibration→calibration-profile / v0_28_0→extract-takes / backfill-registry→emotional-weight / transcripts→transcript-discovery / pglite→anomaly。
- grind 仍在飞: operations.ts 3855 行(最大 TS 单体)，近期已落 sync_brain/search_by_image/code-intel×7/takes_scorecard/calibration op，焦点 1-6-7-16-2-1。
- 本轮未烤分支(可再开 grill): grind 内部硬骨头顺序(file_upload 信任边界 / get_brain_identity / run_doctor / takes_calibration 受 1-3-3 阻塞)。

### operations.ts cutover 重大架构发现 + 阶段 A 批1 完成

- **重大修正**: `operations.ts` 被 27 处 TS 文件引用(cli.ts / mcp/dispatch.ts / mcp/tool-defs.ts / commands/book-mirror.ts / tools-json.ts 运行时 + 9 处 `OperationContext` 类型导入)。删 operations.ts ≠ 删几个 const,而是 **TS legacy 层整体退役**,与 Part12 cycle 迁移耦合。
- 生产 CLI 入口 = Rust `zbrain` binary(`package.json` bin.zbrain → bin/zbrain-rs.js transparent pass-through,零 TS fallback)。TS `cli.ts` 仅被 check-resolvable/TS 测试引用,但仍在实时 dispatch 非 CLI_ONLY 命令(query/pages 等)。mcp/dispatch.ts 注释明写 "Delete in 1-6"(legacy 死代码)。
- 写 `docs/plans/CLI_CUTOVER_MANIFEST.md`(cutover 权威清单),落盘 Part11 `1-13 cutover 执行层` 节点(指向文档)。
- **Q-cutover 决策**: 先补 Rust CLI clap 层(覆盖 cli.ts 全部命令)再退役 cli.ts + 删 operations.ts。
- **阶段划分**: A=Rust registry 100% 完整(零TS风险,当前起点) → B=Rust CLI clap 补全~18缺失命令 → C=退役cli.ts+mcp legacy+删operations.ts → D=CLI_ONLY命令迁Rust(独立,Part12+)。
- **阶段 A 批1 完成**: code_callers/code_callees/code_def/code_refs 4 个 op 的 Rust TypedOperation 包装 + 注册已写入 `crates/zbrain-core/src/operation.rs`(插入 GetRecentSalience 后 + register_all 注册),`cargo check -p zbrain-core` 通过(仅 warning)。引擎方法 get_callers_of/get_callees_of/find_code_def/find_code_refs 三后端(libsql/postgres)已 override;InMemory 返回 Unsupported。TS 侧未动(阶段 A 不删 TS const)。
- 剩余阶段 A: 批2 code_flow/code_blast/code_traversal_cache_clear、批3 find_orphans/run_doctor、批4 recall/find_trajectory/whoami。find_trajectory Rust 零引擎方法需补或决策。

### 阶段 A 续: find_trajectory 卡点 + 批2/批4 收口

- **find_trajectory 卡点完成**（Rust 零引擎方法 → 已补）。新增: engine trait `find_trajectory` 默认 Unsupported；libsql/postgres 实装三后端；`crates/zbrain-core/src/trajectory_stats.rs` 忠实移植 `src/core/trajectory.ts` 数学(detect_regressions / compute_drift_score / cosine_sim,含 embedding 优雅降级)；`types.rs` 加 `TrajectoryOpts/Point/Regression/Stats`；`operation.rs` 加 `FindTrajectoryOperation` + 注册。`cargo test -p zbrain-core --lib trajectory` 4 单测 + `facts_crud libsql_find_trajectory` 集成测试通过。
- **真缺口修复**: `insert_fact` 原 INSERT 漏写 `claim_metric/claim_value/claim_unit/claim_period/event_type` 5 列 → find_trajectory 读到 claim 全 NULL。已在 libsql + postgres 的 insert_fact 补 5 列 + bind。
- **阶段 A 批2 + 批4 收口**(`operation.rs` 新增 4 op 并注册): `whoami`(纯 ctx 信任边界,fail-closed remote 无 auth 抛 unknown_transport)、`code_blast`/`code_flow`(包 `recursive_walk` 引擎方法,libsql/postgres 已实装;Rust 无 traversal cache 故直调、drop TS 缓存层)、`code_traversal_cache_clear`(Rust recursive_walk 无状态 → deleted:0,localOnly+admin)。`cargo check -p zbrain-core` 通过;新增 8 单测 + 全 operation 模块 227 测全绿。
- **阶段 A 剩余唯一真引擎缺口 = `recall`**: 依赖 5 个事实引擎方法(libsql/postgres 均无) — `list_facts_by_session` / `list_facts_since` / `list_supersessions` / `count_unconsolidated_facts` / `resolve_entity_slug`。引擎就绪的 `list_facts_by_entity` 已存在可复用。`grep` 客户端过滤 + `include_pending` 计数可迁。
- 已确认非缺口项: `find_orphans` 早已是 `find_orphan_pages` op(operation.rs:9812 已注册);`run_doctor` 走既有 `zbrain doctor` Rust CLI 命令(非 registry op)。阶段 A 实质只剩 recall 一块。
- `CliHints` struct 无 `hidden` 字段(Rust 侧无法表达 TS cliHints.hidden),code_blast/code_flow/code_traversal_cache_clear 的 hidden 状态待阶段 B clap 补全时处理,不影响 registry 完整性。

### 阶段 A 最终收口: recall 完整迁移完成

- **决策**: `resolveEntitySlug` 不移植（依赖 pages + pg_trgm，Rust libsql 无等价，且 recall 和 find_trajectory 一致仅需要 slug 即可使用），登记为 **G53** 已知缺口 `docs/plans/KNOWN-GAPS.md`（G26 是 query expansion，别混淆）。
- **新增**:
  - `crates/zbrain-core/src/types.rs`: `SupersessionOpts` (since/limit 选项)
  - `crates/zbrain-core/src/engine.rs`:
    -  trait 新增 4 默认方法（`list_facts_since/list_facts_by_session/list_supersessions/count_unconsolidated_facts`）默认 Unsupported
    -  InMemoryEngine 4 方法实装 + 新增 3 模块级公共 helper (`fact_passes_list_filters/sort_facts_newest_first/apply_fact_paging`) 复用减少重复
  - `crates/zbrain-core/src/libsql.rs`: 4 方法实装；修复 E0407: `append_fact_list_filters` 从 trait impl 块移到 inherent `impl LibsqlEngine`
  - `crates/zbrain-core/src/postgres.rs`: 4 方法实装（sqlx QueryBuilder）
  - `crates/zbrain-core/src/operation.rs`: `parse_since_param` regex+chrono 相对时间解析；`RecallOperation` 完整 impl + `RecallParams/RecallFact/RecallOutput` 结构 + 8 个单测（覆盖全分支）；已注册进 `registry.register_all()`
  - `docs/plans/KNOWN-GAPS.md`: G53 登记
- **代码状态**: 三后端（InMemory/libsql/postgres）5 引擎方法全部就位，operation 包装完整，测试覆盖全分支，语法类型正确。
- **锁问题已解 + 编译验证通过（2026-07-23 晚）**: 
  - 之前 Windows `.cargo-build-lock` 瞬态权限拒绝是假死锁（进程已退出但文件残留）；等进程清空后 `cargo check -p zbrain-core` 直接过（仅 45 warnings，0 errors）。
  - **修复 1 个真编译错误 E0599**: `chrono::DateTime::<Utc>::parse_from_rfc3339` 被 chrono `clock` feature 门控、本 workspace 未启用 → 改用无条件可用的 `DateTime::<FixedOffset>::parse_from_str(s, "%+")` 再 `.with_timezone(&Utc)`。`operation.rs:5725` 附近。
  - `cargo test -p zbrain-core --lib` 全绿:**1919 passed, 0 failed**（含新增 12 个 recall 测试，覆盖 registry/parse_since_param/8 分支）。
  - `cargo check -p zbrain-cli` 仍有 ~20 报错，但**全部在 `skillpack/*.rs`（Part10 Phase12 进行中迁移的预存断点），无任何真错误涉及 recall 改动文件**（operation/engine/libsql/postgres/types）。recall 迁移本身与 zbrain-cli 报错无关。
- **阶段 A 最终状态**: 73/73 完整，100% ops 迁移完成，已编译+测试双验证。下一步进阶段 B（Rust CLI clap 补全 18 命令）。

### 续轮收尾: skillpack 编译错误修复 + 任务对账

- **skillpack 编译错误修复已独立验证通过**（上一轮由 subagent 执行 core 138 + cli 43 错误修复）。本轮重新 `cargo check` 三处均 EXIT=0（仅 warnings, 0 errors）：
  - `zbrain-cli`: CLI_EXIT=0 (6 warnings)
  - `zbrain-core` 无 feature: CORE_EXIT=0 (59 warnings)
  - `zbrain-core --features skillpack`: SP_EXIT=0 (126 warnings)
- 任务对账（grep 确认 recall/whoami 已注册并带测试）：#18/#19(skillpack 子任务) + #6/#11/#12/#13/#14(recall+find_trajectory+whoami cutover 批4) 全部标记 completed。
- 仍 pending 任务：#2/#3/#4（operations.ts cutover 批2/批3/终局删 operations.ts）——属阶段 C 退役 cli.ts 前的 TS 残留处理，不在本阶段 A 范围。
- **subagent 提出 2 个待确认语义点**（非阻塞）：(1) cli skillpack scaffold 的 `find_repo_root(&target_workspace)` 若 workspace 不在 zbrain 树内可能应改用 `&cwd`；(2) harvest 的 `zbrain_root` 用 `find_repo_root(&cwd)` 作写入目的地是否符合 editorial 工作流。
- 阶段 A 闭坑完成，等待用户决定是否进阶段 B（Rust CLI clap 补全 ~18 命令）或先处理上述语义点/阶段 C 残留。

### 选B: skillpack 两语义点 rigor 修复（用户选 B）

- 重新勘测发现 subagent 的两处不仅是「从哪个目录推导」问题，更本质是用错了找根函数：
  - `run_scaffold`/`run_harvest` 的 `zbrain_root` 实际加载/写入 `zbrain_root/openclaw.plugin.json`（bundle 清单），需要 **`find_zbrain_root`**（marker=`openclaw.plugin.json`+`src/cli.ts`，见 `crates/zbrain-core/src/skillpack/bundle.rs:113`）。
  - subagent 用的是 `find_repo_root`（marker=`skills/RESOLVER.md`，是「skills 部署工作区」marker）。zbrain 仓库根两者巧合重合（同时有这两个 marker），默认场景能跑过，但语义不对。
- 修复 `crates/zbrain-cli/src/skillpack.rs`：
  - 导入 `find_repo_root` → `find_zbrain_root`（经 `zbrain_core::skillpack::bundle::find_zbrain_root`）。
  - scaffold: `zbrain_root = find_zbrain_root(Some(&cwd))`（原 `find_repo_root(&target_workspace)`，错从 `--workspace` 目的地推导 bundle 根）；并修 move 错误：`target_workspace = opts.workspace.unwrap_or_else(|| cwd.clone())`（原 `unwrap_or(cwd)` 把 cwd 所有权移走导致 `&cwd` 借用失败 E0382）。
  - harvest: `zbrain_root = find_zbrain_root(Some(&cwd))`（原 `find_repo_root(&cwd)`）。editorial 工作流=站在 zbrain 仓库内运行 `zbrain skillpack harvest --from <host>`，`zbrain_root` 是写入目的地=bundle 根，正确。
  - 两处补注释说明语义。
- `cargo check -p zbrain-cli` EXIT=0（6 预存 warnings，无 error）。core 层未动。
- **结论**：scaffold 的 `zbrain_root` 应永远从当前 zbrain checkout（cwd）推导，与 `--workspace` 目的地无关；harvest 的 `zbrain_root=cwd` 符合 editorial 工作流，无需改向。两语义点已闭坑。

### 提交

- `git commit` → `b6327c3 fix(skillpack): make skillpack module compile + correct zbrain_root detection`（52 files, +11548/-146，hook 通过）。
- 涵盖：skillpack 编译修复(core 138+cli 43)、zbrain_root 语义修正、recall/find_trajectory/whoami cutover 完成(阶段 A 100%)、缺失的 calibration 迁移 SQL(0023)、CLI_CUTOVER_MANIFEST + Part12 cycle roadmap 文档、memory。
- 显式排除未提交垃圾：`STATUS` / `cargo-check-output.txt` / `full-check.txt` / `crates/zbrain-cli/src/skillpack.rs.bak`（仍 untracked，未提交）。
- 未推送（用户仅要求提交，未要求 push）。

### grill-me + roadmap-driven: 选 C → 翻转 B-first（Phase C 计划锁定）

- 用户用 `/zj-grill-me /zj-roadmap-driven` 处理「选 C（直接推进阶段 C：删 operations.ts + 退役 cli.ts）」。grill 第一个问题即暴露 manifest §7 硬规则冲突：**B（Rust CLI clap 补全 ~18 命令）必须在 C 前 100% 完成**，因为 `bin/zbrain-rs.js` 对 Rust 零 fallback。
- **实测确认**：Rust `Commands` 枚举（crates/zbrain-cli/src/lib.rs:347）**缺失全部 18 命令**（code_callers/callees/def/refs/flow/blast/traversal_cache_clear/find-trajectory/whoami/timeline/timeline-add/tag/tags/transcripts/revert/search-by-image/history/find-contradictions），且无通用 `op <name>` dispatcher。直接删 cli.ts+operations.ts 会让这 18 命令从产品消失。
- **用户决策：翻转「选 C」为「先做 B」**（安全路径）。5 项决策全部落盘 `docs/plans/zbrain-ts-to-rust-part11-residual-ts-endgame.json` 节点 **1-13-1**（含 7 个执行子节点）：
  1. B 先于 C：先补 18 clap 包装(零回归)再删 operations.ts。
  2. B 范围：全部 18 统一 thin clap 子命令 → run_operation，执行时剔重名异命令。
  3. OperationContext/Operation/AuthInfo 类型：新建 `src/core/operation-types.ts` types-only 模块承载；`sourceScopeOpts` 运行时 helper 迁 core/scope 或 core/cycle。不依赖 Rust binding。（实测 25 处导入中仅 sourceScopeOpts 为运行时值，其余 type-only）
  4. cli.ts 命运：**直接删 cli.ts**（C 中）。前置：npm run check:resolver + dev 脚本(bun src/cli.ts ...)重指 Rust(zbrain check-resolvable/doctor/jobs work 已存在)。CLI_ONLY 命令仅 sync 在 Rust clap 树，其余(dream/extract/import/export/eval)未进；生产入口已是 Rust，删 cli.ts 仅移除 TS dev 调用路径，完全复活归阶段 D。
  5. 删除安全闸门：两提交 + PARITY_GATE 强化。提交1=删消费方(mcp/*、book-mirror/tools-json 改写、OperationContext 迁 types-only、脚本重指 Rust)，保留 operations.ts/cli.ts；提交2=grep 零 import 后删两文件 + 重算 typecheck 基线(新冻结值只阻新增)。每提交跑 `bun test`(剩余 TS 测试) + `cargo check -p zbrain-cli` 绿。
- 执行子节点：1-13-1-1(B 补18) / 1-13-1-2(operation-types.ts+sourceScopeOpts) / 1-13-1-3(删 mcp/*) / 1-13-1-4(book-mirror/tools-json 改写) / 1-13-1-5(脚本重指 Rust) / 1-13-1-6(删 cli.ts) / 1-13-1-7(删 operations.ts+重算基线)。
- **遗留 pending 任务对应**：#3(批2 wire code_flow/code_blast/code_traversal_cache_clear)、#2(批3 wire find_orphans+run_doctor)、#4(终局删 operations.ts) —— 现归并入「先做 B」框架：批2/批3 的 op 已在 Phase A registry 完成，剩的是 B 的 clap 包装 + C 删除。
- 路线图 md 已 render 更新（`docs/plans/ZBRAIN_TS_TO_RUST_PART11_RESIDUAL_TS_ENDGAME.md` §1-13-1）。
