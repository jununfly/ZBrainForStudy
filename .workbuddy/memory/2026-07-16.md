## 1-6-2 收口：剩余活 dispatch PARITY_GATE 判定（roadmap decision，无代码改动）

- **1-6-2 标 completed**：RUST_OWNED 纯死壳已清完 8 个（A类4 stub + B/C不可达dispatch 4）。
- **PARITY_GATE 判定：剩余 RUST_OWNED 命令均不能删**（本轮探查坐实）：
  - `sync`/`import` — 有活 e2e 测试直接 spawn `src/cli.ts`：`sync-lock-recovery.test.ts` 的 `runCli(['sync',...])`（CLI 定义 L32 = `bun run src/cli.ts`）测 break-lock/SIGTERM 恢复；`mechanical.test.ts` spawn `import/init/search/stats/health/doctor`。两者 `describe.skip` 仅当 DATABASE_URL 未设，有 DB 时真跑。且 `runSync`/`runImport`/`performSync` 有真实 src 调用者：`reinit-pglite.ts:168`、`cycle.ts:761`、`operations.ts:2017`、`sync.ts` 内部调 import。
  - `query`/`get_page`/`list_pages`/`think` — cli.ts 里**无专属 dispatch case**，走通用 cliOps 循环（L121）+ `formatResult`（611/620/628 是格式化分支非 dispatch）。删它们 = 删 `operations.ts` 整个共享层 = C 类核心迁移，不属壳清理。
  - `skillpack-check` — Rust 二进制无此 subcommand，真正待迁。
- **关键认知**：`src/cli.ts` 非 shipped 入口（package.json bin=`bin/zbrain-rs.js` 纯透传 Rust，无回退 cli.ts）。cli.ts dispatch 唯一"活"来源 = ①其他 src 模块 import runX ②测试 spawn src/cli.ts。PARITY_GATE 三条件必须逐个查这两处。
- 下一步候选：1-6-3 TRIVIAL_DELETE（27 纯 dev/诊断工具整删）或 1-6-4 REAL_MIGRATE（33 真实功能按域拆）。

## 1-6-2 Batch 2 B/C类死代码清理 + 测试重定向 (commit 2554f8f)

- **关键发现**：`handleCliOnly` switch 里 4 个 case 是不可达死代码——`reconcile-links`/`orphans` 不在 CLI_ONLY 集合永不被 dispatch；`search`/`pages` 真实路由走 `dispatchReadOnlyCommand`（L1394），handleCliOnly 分支从不命中。审计手法：提取 CLI_ONLY 集合 items，与 handleCliOnly switch cases 交叉比对，找出 in-switch-but-not-in-CLI_ONLY 的死 case。
- 清理：4 个死 switch case + 僵尸 THIN_CLIENT_REFUSE_HINTS (orphans/takes) + 帮助文本行 (orphans/reconcile-links)。
- 测试重定向：cli.test.ts (sync/doctor/init --help)、doctor.test.ts 的 spawn 从 `src/cli.ts` 改到 `bin/zbrain-rs.js`（Rust 二进制透传）。附带修正 cli.test 里 `'init'` toContain 假阳性（原本是匹配到 `'reinit-pglite'` 子串）。
- 验证 bun --check OK。净 32 insert / 52 del。
- 顺手清 roadmap json 根级脏 md_file key（记忆铁律，CLI 只读 metadata 内的）。
- 剩余 1-6-2 真实活 dispatch 待 Rust 对等后删：sync/import/skillpack-check（CLI_ONLY 活 handler）+ cliOps 共享层（query/get_page/list_pages/think）。

## 1-6-2 A类纯死壳清理 (commit c5d07ed)

- 清理 cli.ts 中 4 个 Rust stub 死壳：init stub (L806-814)、doctor stub (L954-960)、config case in switch (L1166-1171)、sync --help short-circuit (L1075-1085)
- 净删 -35 行

# 2026-07-16

## Part11 1-4-1 完成：output page validators port（grill 选定的下一刀）

- **grill 选刀（/zj-grill-me + /zj-roadmap-driven）**：Part11 剩余节点中，blocked 项（1-3-3 calibration Phase2、1-5-2/3/6 doctor）全卡未迁基建；干净候选里 Explore agent 探查坐实 **1-4 output 纠缠最低（全仓仅 1 个外部 importer integrity.ts）**。选 1-4，粒度选 B = 4 validators 全切。
  - **更正一处历史误判**：1-6 孤儿命令节点**非过时**——whoknows/brainstorm/dream/publish/models/providers 6 个命令 cli.ts **全有 live dispatch**（cli.ts:853~1324），Rust Commands enum **全无**，是真实重迁移工作（需完整命令实现，非低成本镜像），不是可删的死节点。
- **实装**：新建 `crates/zbrain-core/src/output_validators.rs`（~1050 行含测试）+ lib.rs 注册 `pub mod output_validators`。4 validator 忠实 port TS `src/core/output/validators/`：
  - **纯字符串（无 engine）**：`validate_citation`（等价 CITATION_RE `[Source:非空]`|`](http(s)://…)` + looksFactual 启发式 + splitParagraphs 去 fence/inline-code/html-comment）、`validate_triple_hr`（compiled_truth 裸 `---` 行 + timeline 误置 heading，均 warning 单发）。
  - **engine-read（复用 1-3-2 打法）**：`validate_link`（&dyn BrainEngine 只读 get_page，扫 md 链接分类 external/non-brain/internal，dangling→error）、`validate_back_link`（只读 get_links 双向，缺反链→warning）。**零 trait 扩展**——get_page/get_links 是既有只读方法。
  - **关键手法**：不引入 regex 引擎，手写字符扫描等价 TS 正则（has_source_marker/inline_url_link_body/normalize_to_slug/is_slug_shaped/iterate_links 等）。engine-read validator 参数用 `engine: &dyn BrainEngine` 而非把 engine 塞进 ctx（TS PageValidationContext.engine），使纯 validator 保持 engine-free、ctx 可 Default。
- **测试**：31 单测全绿（citation 10 + triple-hr 4 + helper 3 + link 5 + back-link 3 + splitParagraphs/truncate 等），engine-read 用真实 `InMemoryEngine`（put_page + add_links_batch fixture）。核对 TS `tests/unit/writer.test.ts` 断言逐条对齐（含 inline-code 仍需引文、空 `[Source:]` 不满足、blockquote/html-comment 跳过、splitParagraphs 行号）。全 lib **1496 passed**，core+cli build 干净。
- **诚实性（本切片纯加法，TS 未删）**：TS `validators/` 暂不删——`writer.ts`（编排器，330 行）+ `integrity.ts`（唯一外部消费者）仍 import 它们。遵守 Part11「每切片自带解缠」铁律：删除待 writer/scaffold/slug-registry infra port 切片自然解缠。已建 **1-4-2 pending** 子节点承接「infra port + TS validators/ 删除（解缠 integrity.ts）」。
- **经验复用**：output 与 calibration 同构——都可拆「纯子集 + engine-read 子集 + infra Phase2」。engine-read 子集是最便宜的第二刀（trait 只读方法已就绪，risk 已被 1-3-2 证明可控）。infra（需 path/mount 解析或写路径）永远留最后。
- commit：见下（代码 + roadmap json/md + 本记忆）。

## Part11 1-4-2 探查判定为 BLOCKED（output infra 撞逃生舱禁令 + 未迁消费者死锁）

- **续接 1-4-2（output writer/scaffold/slug-registry infra port + 删 TS）探查结论：不成立**。读完 writer.ts/scaffold.ts/slug-registry.ts/post-write.ts + 核实 Rust engine trait，判定**两个硬阻塞**：
  1. **`BrainWriter` 撞逃生舱禁令**：其核心价值 = `engine.transaction`（事务包裹）+ `engine.executeRaw`（pg_advisory_xact_lock）。Rust engine **刻意消除**这两者（engine.rs:1896 逃生舱禁令 + 2119 注释「CAS-style UPDATE 保证并发正确性 without explicit transactions」= 内部自管理事务 + 幂等 SQL）。BrainWriter 这个抽象在 Rust 架构里**没有对应物**，不是简单 port。
  2. **唯一消费者全未迁**：writer/scaffold/slug-registry 唯一非测试消费者 = `integrity.ts`（773 行命令，Rust 仅 `IntegrityHandler` stub 返回 not_implemented）；post-write 唯一消费者 = `operations.ts:873`（4243 行核心，未迁）。删 TS 被上游未迁大消费者**死锁**。
- scaffold.ts(236) 是纯函数（零 engine，唯一消费者 integrity.ts:606 tweetCitation）可加法 port，但**用户选 C = 不做加法、重新 grill 选刀**。1-4-2 状态改 **blocked** + label 标注阻塞原因 + decide 落盘。1-4 父节点保持 in_progress（1-4-1 已完成有独立价值，1-4-2 阻塞）。
- **教训**：engine-read validators（1-4-1）能干净迁是因为只读 trait 方法就绪；但 **infra 层（writer/事务/advisory-lock）是 TS 架构特有物，Rust 用不同并发模型消除了它** → 这类"抽象在目标架构不存在"的迁移是伪命题，只能等消费者（integrity/operations）整体迁移时顺带重写，不能独立 port。

## Part11 1-11-1 完成：ingestion A 类闭合簇纯删除（重新 grill 选定的干净刀）

- **重新 grill 选刀（用户选 C）**：Explore agent 量化 3 候选干净度——(1) A 类已迁 TS 删除：ingestion 外部活 import **0**、cycle 2、minions 2、ai 7；(2) 孤儿命令：whoknows/dream/publish 无 LLM 但依赖 hybridSearch/engine，models/providers/brainstorm 重度 LLM；(3) search core：Rust 覆盖就绪（search_pages 39/fuse_and_boost 11/rerank 329）但 TS search 仍被 10+ 活文件消费**删不动**。→ **最干净 = `src/core/ingestion/` 纯删除**。
- **深挖验证（避免重蹈 1-4-2 假干净）**：ingestion/types.ts 有 2 外部引用 = `minions/handlers/ingest-capture.ts`（import IngestionEvent + validateIngestionEvent）；test-harness 1 引用 = 自身测试。ingest-capture.ts **无 src 生产调用者**（`makeIngestCaptureHandler` 仅被测试引用，未被任何 registry 装配 = 死代码），Rust `ingest_capture.rs` 已注册覆盖。operations.ts（handler registry）不引用 ingest-capture（仅动态 import shell-*）。→ ingestion + ingest-capture 构成**闭合 A 类簇**，Rust 全覆盖、无未迁上游，可整簇删（用户选 B）。
- **实装（`git rm` 24 文件）**：src 10（ingestion/ 6 + sources/ 4）+ ingest-capture.ts 1 + 测试 13（tests/unit/ingestion/* 11 含 sources/2 + tests/unit/e2e/{ingestion-roundtrip,capture-generation-regression}.test.ts 2）。src 528→518，tests 784→770。
- **验证**：断链 grep 空（无活文件 import 已删模块）；`bash scripts/typecheck-baseline.sh` **OK — no new errors above baseline (64 inherited)**；baseline 无关已删文件无需收缩。
- **判定区别（1-11-1 vs 1-4-2）**：1-11-1 是 Phase11「已迁才删」纯删除（Rust 全覆盖 + 无未迁上游）零风险；1-4-2 是「抽象在目标架构不存在 + 消费者未迁」的死锁。**选刀第一性判据 = 外部活消费者数 + Rust 覆盖度，两者同时满足才是真干净切片**。

## Part11 1-11-2 / src/core/cycle 探查：A 类纯删除已耗尽（重大方法论修正）

- **用户要开 `src/core/cycle/`（原记"2 外部消费者"）** → 探查坐实 **cycle 是 B 类真迁移（非 A 类误判）**：`cycle.ts` 主文件 **2057 行**（runCycle 主循环 + phase dispatch）+ `cycle/` 20 个 phase 文件；Rust `autopilot/cycle.rs`(745) 仅 dispatch 骨架，20 phase 全 `not_implemented`/`not_migrated` stub（extract_facts/patterns/consolidate/recompute_emotional_weight/synthesize handler 均 49 行 skeleton），**0/20 真正迁移**，runCycle 主循环未等价实现。6 个活消费者硬依赖未迁 phase（dream→runCycle、calibration→calibration-profile、v0_28_0→extract-takes、backfill-registry→emotional-weight、transcripts→transcript-discovery、pglite/postgres-engine→anomaly）。**RESIDUAL-TS-INVENTORY 原把 cycle 列 A 类是盘点错误，已修正为 B 类**。建 roadmap **1-12（blocked）** 登记 cycle 大迁移真相。
- **换干净刀（用户选 A=修正误判+换干净 A 簇）**：探针定位 `src/core/minions/` 子集作为候选，但**踩了更深的陷阱**：
  1. 初探（只扫 `src/`）误报 `worker/backoff/quiet-hours/shell/subagent-aggregator` 为零外部引用 → 实际 `git rm` 5 文件后 typecheck gate 爆 3 个 test 的 "Cannot find module"，并发现 `cycle-abort.test.ts`（`new URL('worker.ts')` 读源码断言）、`bench-vs-openclaw/*.bench.ts`（import MinionWorker）也引用 worker.ts → **探针漏扫 `tests/`**。
  2. 补扫 `tests/` 后发现 **minions 每个文件都被测试引用**（queue 29 / worker 14 / types 14 / shell 3 / embed-backfill 2 …），**无一零引用叶子**。删任一文件需连带删/重写整套 minions 测试套件（失去对仍 TS 的 queue/types 覆盖率），违背「干净可完成切片」。
  3. 另：自写 Python import 正则 `(?:from|export)\s+[*\s\w{}]*\s+from` **有 bug**，漏匹配 `import { X } from '...'`（自测返回 `[]`）——不可靠，改用直接 grep。
  4. `embed_backfill.rs` 虽是 Rust 名称孪生但**实测是 v1 skeleton**（注释"BudgetTracker not yet ported"，handler 返回 `not_implemented`）→ TS `embed-backfill.ts` 才是真实现，删 TS 会丢功能（cycle 教训再中）。
- **综合结论（本回合关键认知）**：A 类纯删除对 `minions`/`ai`/`commands` **已全部失效**——cycle=B 类假 A、minions 100% 测试耦合、ai recipes 无 Rust 孪生、命令全被 wiring（sync 28/extract 19/embed 10 引用，仅 `facts` 命令 0 引用但单命令太小）。**ingestion(1-11-1) 是最后一个真干净切片，A 类纯删除已耗尽**。剩余工作 = **B 类真迁移**（schema-pack G4 / doctor G5 / cycle 1-12）。
- **选刀方法论升级（铁律）**：A 类「可纯删」三条件必须**同时满足** = ① 零 `src` 引用 ② 零 `tests` 引用 ③ 真 Rust 覆盖（名称孪生 ≠ 覆盖，必须查 stub 标记）。import 探针**必须扫 src+tests 且用可靠 grep**，不能用漏匹配的正则。
- **动作**：`git restore` 回退误删 5 文件（无净代码变更）；1-11-2 改 **blocked** + label/decide 记录 minions 不干净结论；提交 a4df87a（roadmap + inventory 诚实记录，无代码改动）。
- **下一步方向（待用户拍板，非本回合执行）**：转向 B 类结构化切片——推荐 **doctor G5**（11 检查，各有 `UNMIGRATED_TS_DOCTOR_CHECKS` 锚点 + `reranker_health` 已示范迁出模式，每检查是独立小切片）或 **schema-pack G4**（32 verb，边界清晰体量大）或 **cycle 1-12**（数周级大 Phase，需先拆 runCycle 主循环+20 phase 逐刀）。

## Part11 1-5-2-1 完成 + doctor 收口（grill 选定「收口 doctor + 转 1-6」）

- **sync_freshness 切片完成（1-5-2-1）**：纯分类模块 `crates/zbrain-core/src/sync_freshness.rs`（`SyncFreshnessStatus` enum + `resolve_freshness_hours` env 解析 + `classify_sync_freshness(sources, now_ms, warn_hours, fail_hours)` 纯函数 + `parse_iso_ms` chrono）+ lib.rs 注册 `pub mod sync_freshness`。doctor 连接域 3c 后插 3d 接线（`engine.list_sources(false)` → `classify_sync_freshness` → match push `DoctorCheck`）。从 `UNMIGRATED_TS_DOCTOR_CHECKS` 移除 sync_freshness；锚点测试下界 `(7..=12)`→`(6..=12)`；加 `sync_freshness_is_no_longer_unmigrated` 守卫。**14 模块测试 + 170 CLI lib 测试全绿**。
- **grill 决策（/zj-grill-me + /zj-roadmap-driven 续接）**：探查证实 **doctor 干净期结束**——sync_freshness 是最后一个零新依赖切片。剩余 6 锚点检查全需新基建或巨型：
  - `federation_health` 不干净：依赖 `computeAllSourceMetrics`（212 行 source-health.ts）+ Rust `get_health` 还是 stub → 推翻"中等可迁"初判。
  - `schema_packs` 需 DB 读取（Rust 有 schema_pack 模块但无 doctor 用 typed 读取）；`embedding_health` 需 provider getter(G41)；`frontmatter_integrity` 高复杂；`search_mode` 缺 getConfig/listConfigKeys trait(G40)；`resolver_health` 350+行+5模块(排除)。
  - 与 G41-G43 封顶决策「不藏基建进 doctor」一致 → 收口 doctor，转 1-6。
- **锚点处理决策（grill 第二岔）**：用户选「保留锚点+登记缺口」——`UNMIGRATED_TS_DOCTOR_CHECKS` 保留作 CI 防漂移守卫（仍 push not_implemented 诚实报告 6 项），不清空；6 项登记进 KNOWN-GAPS（已是 G39-G45，非新增）。
- **收口落盘**：KNOWN-GAPS 中 **G42(sync_freshness) 标 resolved**（证伪 execute_raw 前提，list_sources 提供 typed 替代，零 trait 扩展）；**G5 缩为「剩余 6 项」**；roadmap 1-5-2/1-5-3/1-5-6 重 scoped 为 completed（remainder 指向 G39-G45），**1-5 自动冒泡 completed**。decide 落盘 1-5 + 1-5-6。
- **转 1-6 孤儿命令**：1-6 置 in_progress，加 **1-6-1 孤儿命令审计**（explore 模式）。初步审计：TS `cli.ts` 有 **~50 活 dispatch 命令**，Rust `Commands` enum 已覆盖 config/doctor/init/query/sync/schema/takes/orphans/recall/forget 等；真实孤儿混合体 = trivial 工具（`lint/upgrade/reinit-pglite` 等可删）vs 真实迁移（`whoknows/brainstorm/dream/models/providers/publish`）。注：1-6 原注释「~20」低估，需审计分类后才能正确切片。
- **关键认知**：Part11 已无「小而干净」切片——doctor 剩余全阻塞/巨型，pending 节点(1-1/1-2/1-6/1-7/1-8/1-9/1-10)全是大体量。下一步要么解阻塞、要么啃大体量 C 类核心。

## Part11 1-6-1 完成：孤儿命令审计 + 建 1-6 子结构

- **架构探查（决定分类准确性）**：TS `cli.ts` 分发三路 = ① `CLI_ONLY` 集合(`handleCliOnly` 纯 TS 实现) ② `cliOps` Map 引擎 ops(`query/search/get_page/...`连引擎) ③ `schema`/`init` 已变"请用Rust"错误桩(prove cutover 进行中)。`handleCliOnly` 对 CLI_ONLY 是**完整 TS 实现**(非 delegate Rust)，仅 schema/init 是桩。
- **可复跑审计脚本** `docs/plans/scripts/audit-orphan-commands.py`：内嵌 Rust `Commands` enum 集合 + TS 活命令并集(cliOps switch + CLI_ONLY + special)，分类输出 markdown。跑出 **83 唯一活命令**：
  - **RUST_OWNED 17**：Rust 已有等价命令，TS 是重复壳 → 删 TS 副本(过对等闸门)。config/query/search/get-page/list-pages/think/sync/takes/orphans/import/reconcile-links/skillpack/schema/init/doctor。
  - **TRIVIAL_DELETE 27**：纯 dev/诊断工具，无 port 价值 → 整删。lint/upgrade/post-upgrade/check-update/reinit-pglite/apply-migrations/repair-jsonb/report/smoke-test/claw-test/ze-switch/check-backlinks/cache/friction/lsd/founder/files/parse/network/discovery/anomalies/transcripts/book-mirror/call/integrations/frontmatter/mounts。
  - **REAL_MIGRATE 33**：真实功能无 Rust 等价 → 待 port(按域拆 sub-slice)。whoknows/brainstorm/dream/models/providers/publish/resolvers/integrity/auth/eval/recall/forget/extract/extract-conversation-facts/routing-eval/check-resolvable/skillify/embed/export/migrate/features/storage/code-*/reindex-*/backfill/edges-backfill/calibration/notability-eval。
  - **PARITY_REVIEW 6**：可能 RUST_OWNED 但需确认覆盖。ask(alias→query)/get_stats/get_health/get_timeline/get_versions/get_tags。
- **模糊映射验证**：`recall/forget/eval` Rust 无(Part11 1-2 eval blocked; recall/forget 记忆命令无 Rust)；`salience/graph-query` Rust 有但 TS 无活命令(非孤儿)；`whoknows/brainstorm/dream/models/providers/publish` Rust 无(真孤儿)。
- **roadmap 子结构建好**：1-6-1 收口 completed；1-6 修正陈旧"~20命令"→准确计数；建 **1-6-2 RUST_OWNED壳清理** / **1-6-3 TRIVIAL_DELETE批** / **1-6-4 REAL_MIGRATE批(按域拆:memory/model-provider/content/resolver/code-intel/misc)** / **1-6-5 PARITY_GATE(删TS前确认零src+零test引用+真Rust覆盖非stub)**。
- **commit 5698b64**：审计 doc + 脚本 + roadmap json/md。
- **下一刀建议**：1-6-2 RUST_OWNED 壳清理是最高杠杆低风险(17个重复壳，过 1-6-5 闸门逐个删)；1-6-3 TRIVIAL 紧随。REAL_MIGRATE(1-6-4)体量最大需逐命令开刀。

## Part11 1-6-2 Batch 1：RUST_OWNED 壳清理（schema/takes/orphans/skillpack）

- **PARITY_GATE 探查（决定可删子集）**：`bin/zbrain-rs.js` 是 Rust 二进制透传包装（活动 CLI=Rust，cli.ts=遗留分发）。但 `tests/heavy/*.sh` 仍 `bun run src/cli.ts` 调 init/doctor/config/sync（盲删会挂 heavy 测试）；`tests/unit/cli.test.ts` 结构性断言命令存在 + spawn `bun run src/cli.ts` 实测（init/import/sync/doctor/skillpack-check）；`cli-options.test.ts:147` spawn skillpack-check。逐命令画"测试闸门"状态：
  - **零 cli.ts-dispatch 测试引用、可立即删**：`schema`(纯桩)、`takes`/`orphans`/`skillpack`(CLI_ONLY，无 spawn 测试)。
  - **被测试挡住**：`init`/`doctor`/`sync`(cli.test.ts+heavy)、`import`(cli.test.ts 断言)、`skillpack-check`(cli-options.test.ts spawn)、`config`(heavy)。
  - **缠 operations 共享层**：`query`/`search`/`get_page`/`list_pages`/`think`/`reconcile-links`（brain 工具层+allowlist 测试依赖）→ 本批跳过。
- **Batch 1 执行**（4 命令，全部零 spawn 测试引用 + Rust 真覆盖非 stub）：
  - `schema`：移除 `schema||init` 桩分支 → 改为仅 `init` 桩（schema 纯删，Rust SchemaSql 命令已就绪）。
  - `takes`：从 CLI_ONLY 集合 + THIN_CLIENT_REFUSED_COMMANDS + cliOps switch case 移除；**整删 `src/commands/takes.ts`**（无任何外部 importer，真·TS 副本）。Rust `Takes` 命令已就绪。
  - `orphans`：从 CLI_ONLY 集合 + THIN_CLIENT_REFUSED_COMMANDS 移除。`orphans.ts` 模块**保留**（被 cycle.ts/operations.ts import + 5 测试文件直接 import 模块）。cliOps switch `case 'orphans'`(1227) 暂留（需 engine 的冗余壳，留后续精细处理）。
  - `skillpack`：从 CLI_ONLY 集合 + CLI_ONLY_SELF_HELP + handleCliOnly case 移除。`skillpack.ts` 模块保留（被 skillpack/endorse.ts import）。Rust 无 skillpack 命令但属 1-10 schema-pack 路线，本批仅删 cli 分发。
- **验证**：`bun --check src/cli.ts` SYNTAX_OK（语法通过）；`cli-options.test.ts` 28 测试全过（含 skillpack-check spawn，证明 skillpack-check 仍在）；`cli.test.ts` 4 失败是**预存 bug**（`new URL('../src/cli.ts')` 路径解析错误，tests/unit→tests/src/cli.ts 不存在，与本次编辑无关）。orphans 模块测试直接 import 模块不受 dispatch 移除影响。
- **关键认知**：1-6-2 不能整批删——17 个 RUST_OWNED 命令按"测试闸门"分批，零 spawn 引用的先删、被测试挡的需先迁测试依赖到 Rust 二进制。Batch 1 = 4 个最安全的。cliOps switch 内 `case 'orphans'`/`case 'reconcile-links'` 等需 engine 的壳保留（模块被共享层依赖，删 case 涉及 op 表逻辑，留后续）。

## Part11 1-6-4 启动：REAL_MIGRATE 去重 + features 纯推荐引擎 port（commit 150d536）

- **1-6-4 去重（grill 拷问 Q1）**：原 label 名下 33 命令混入已被专门节点覆盖者，`decide` 记录去重映射并改 label：skillify->1-1 / eval·notability-eval·routing-eval->1-2 / calibration->1-3 / dream->1-12（dream.ts:32 硬 import ../core/cycle.ts runCycle，cycle 2057 行主循环未迁）/ extract·export·integrity->撞 1-4 output（BrainWriter 逃生舱禁令）。真孤儿=code-intel(code-*·reindex*·edges-backfill·backfill) + memory(recall·forget) + models·providers + whoknows·brainstorm(无cycle依赖)·auth·features·storage·migrate·publish·extract-conversation-facts·resolvers·check-resolvable + 20 个 1-6-3 归入带 test 命令。
- **第一刀选 features（grill Q2/Q3）**：features.ts(305行)天然三层——纯推荐 scanFeatures+shouldPitch / 持久化 feature-offers.json / auto-fix(调 embed·extract)。Rust 就绪度探查：`engine.getHealth()`->BrainHealth **已就绪**（含 missing_embeddings/dead_links/embed_coverage/brain_score）、getConfig 就绪；但 `engine.getStats()`->**BrainStats** Rust **无**（现有两个 get_stats 是 admin Stats / minions QueueStats，均非 BrainStats）；auto-fix 依赖的 embed/extract CLI 命令 Rust 也无。选 A=只切纯推荐子集（同 1-5 doctor / 1-3 calibration 手法）。
- **1-6-4 建 3 子节点**：1-6-4-1 纯推荐引擎 port(completed) / 1-6-4-2 前置补 Rust BrainStats getStats 三后端(blocked) / 1-6-4-3 CLI 接线+auto-fix(pending，待前置)。
- **1-6-4-1 实现**（crates/zbrain-core/src/features.rs ~330 行 + 330 测）：`recommend_features(&FeatureScanInputs)->FeatureScanResult` + `should_pitch` + `pitchable` 纯函数。关键设计：`secret_present: fn(&str)->bool` 指针注入（测试传 fn，避免读真实 env 保持纯）；DI 结构体 FeatureScanInputs(HealthSnapshot+BrainStatsSnapshot+secret_present+sync_repo+version)。RECIPE_META 7 条 verbatim。FeaturePriority serde as u8(1/2) 对齐 TS wire。**15 单测钉死**：推荐顺序严格(missing-embeddings->dead-links->zero-links->zero-timeline->low-coverage->no-integrations->no-sync)、边界(page_count>=3 进 P2 块、zero-links/timeline 需 >5 页、low-coverage 严格 0<x<0.9)、shouldPitch(P1 永 pitch、P2 按 major.minor 前缀 decline 抑制)。15 测全绿，core 编译干净。
- **经验**：孤儿命令迁移前必先探 Rust engine 依赖就绪度——TS `getStats()`(BrainStats) 与 Rust 的 admin/minions `get_stats` 同名不同物，是真实缺口，须显式建 blocked 前置节点承接而非硬迁。features 这类"扫描+推荐"命令天然可拆纯函数子集，与 doctor/calibration 同构，DI fn 指针喂 env 是保持纯的关键技巧。

## Part11 1-6-4-2 完成：补 Rust engine BrainStats getStats 三后端（commit 86d2d4a / roadmap 424caac）

- **BrainStats 契约**：TS `src/core/types.ts:1055` 定义 7 字段 page_count/chunk_count/embedded_count/link_count/tag_count/timeline_entry_count/pages_by_type，**snake_case wire**（与 camelCase 的 admin Stats/FullStats 不同）→ Rust struct **不加 `#[serde(rename_all)]`** 保 snake_case。
- **名字冲突解法**：`BrainEngine` trait 上 `get_stats(since)` 已被 minions QueueStats 占用，admin `get_stats()` 返回 dashboard Stats，均非 BrainStats → 新增 **`get_brain_stats()`** trait 方法（default `Unsupported`）。
- **后端存储模型差异 = G46 核心**：Rust 生产后端（libsql/postgres）**无 content_chunks / timeline_entries 表**；embedding 是 pages 单列 BLOB（G24 一页一向量）；tags 在 page_tags 表；timeline 是 pages 上 JSON-array 字符串。故 chunk_count/embedded_count 用 **page-level 代理**（同既有 get_full_stats 先例），**InMemoryEngine 有真 chunk_store 逐 chunk 精确计数**（双保真度策略）。已登记 **KNOWN-GAPS G46**。
- **实现落点**：BrainStats struct 放 admin_queries.rs（与 Stats/FullStats 同源，含 doc caveats 指向 G46）；engine.rs trait default + InMemory override（真 chunk_store 计数 + frontmatter tags + timeline JSON 长度求和 + pages_by_type 覆盖全部页 BTreeMap + page_count 排除 deleted_at）；libsql.rs（内部 `scalar_count()` helper + JSON timeline Rust 侧解析 + GROUP BY 多行）；postgres.rs（`sqlx::query_scalar` + `query` GROUP BY `row.try_get`，Row trait 已 @38 导入）。
- **踩坑修复**：(1) **E0407** — libsql get_brain_stats 误插入 `impl AdminQueries` 块（get_full_stats 所在），但方法属 `BrainEngine` trait → 移到 `impl BrainEngine for LibsqlEngine` 内 QueueStats get_stats 之后。(2) **E0308** — 测试 helper embedding 参数误用 `Vec<f32>`，PageInput.embedding 实为 `Vec<u8>`（f32-LE blob）→ 改 `Option<Vec<u8>>`、值 `vec![1u8,2,3,4]`。(3) **`no such table: pages`** — libsql 集成测试 connect 后须 `engine.init_schema().await`。(4) **软删断言失败** — libsql `delete_page` 是**硬删**（`DELETE FROM pages`），`soft_delete_page` 才软删（`SET deleted_at`）；InMemory `delete_page` 是软删。测试软删语义须用 `soft_delete_page`。**pages_by_type 无 soft-delete filter**（镜像 TS：只 page_count 排除 deleted，pages_by_type 覆盖全部页）。
- **测试**：5 InMemory 单测（engine.rs rate_lease_tests 末尾）+ 6 libsql 临时库集成测试（新建 tests/libsql_brain_stats.rs，import `zbrain_core::PageKind` 是 crate 根 re-export 非 types）全绿；全 core lib **1530 tests 全绿**；build 34 warnings 0 error。

## roadmap md 叠层污染修复（commit 5cdf906）

- **症状**：part11 md 存在**两套 ROADMAP_SECTION marker**——纯 marker `<!-- ROADMAP_SECTION_START/END -->`（陈旧，仍显示当前施工 1-3）+ 带 `⚠️` 的 `<!-- ⚠️ ROADMAP_SECTION_START/END -->`（render 实际更新的那对）。两段状态不一致，且 1-6-4 显示 pending 而非应有的 in_progress。
- **根因铁律（更正 MEMORY.md 旧记）**：`roadmap.py write_markdown_section()` **只认带 `⚠️` 的 marker 对**（`file_start = "<!-- ⚠️ ROADMAP_SECTION_START -->"`）；纯 marker 段是死地层，render 永不更新。**且 CLI 读取的是 JSON 根级 `md_file` key（`link` 命令写的绝对路径），NOT `metadata.md_file`**——我一度误删根级 key 当脏 dupe，导致 render 报 "No md_file linked"，重新 `link` 后才恢复。metadata.md_file 只是信息性副本。
- **修法**：Edit 删掉纯 marker 陈旧段（1-50 行）只留 `⚠️` 段 → `update 1-6-4 --status in_progress`（诚实聚合：1-6-4-1/1-6-4-2 done、1-6-4-3 pending）→ `link` 重挂 md → `render` 从项目根 cwd 跑 → 单一权威 section，focus 正确指向 1-6-4，1-6-4 显示 `[~]`。

## Part11 1-6-4-3 完成：features CLI 接线（zbrain features [--json]）

- **交付**：`crates/zbrain-cli/src/lib.rs` 加 `FeaturesArgs{json}` + `Commands::Features` clap 变体 + `run_features_command`（自建 LibsqlEngine → `get_health`+`get_brain_stats` → 组 `FeatureScanInputs` → `recommend_features`+`load_offers`+`pitchable` → `--json`(FeatureScanReport pretty)/human(render_human)/空(All features adopted) → 写回 last_version/last_scan + save_offers）。`crates/zbrain-core/src/features.rs` 加持久化层（`offers_path`/`load_offers`/`save_offers` best-effort，照 schema_pack/activate.rs 用 `paths::zbrain_home()`）+ `FeatureScanReport<'a>`（Serialize-only 借用 recs，scan_ts 由 CLI 层 chrono 盖章）+ `render_human`（Brain score header + P1 DATA QUALITY(Fix:) / P2 UNUSED FEATURES(Try:) 分组）。
- **wire parity 单测钉死**：feature-offers.json camelCase(lastVersion/lastScan/OfferStamp{at,version})、FeatureScanReport{version,scan_ts,brain_score,recommendations} 形状、priority as u8。core **22 lib 测全绿**（15 原 + 7 新：load_offers 默认/round_trip/camelCase/corrupt容错/render_human 分组/空块省略/report json shape）。
- **踩坑**：(1) `FeatureScanReport<'a>` 含 `Vec<&'a FeatureRecommendation>` 借用无法派生 `Deserialize`（E0277）→ 去掉只留 Serialize（报告是纯输出）。(2) `cargo test -p zbrain-core features::` 报 rlib format error（libsql_admin_watch/token_queries_contract 集成 crate 既有 intermittent）→ 绕过用 `--lib features::`。(3) 插入编辑吞掉 schema-pack const doc 首行 → 补回。
- **端到端 smoke 揭示真实缺口 → 决策拆 1-6-4-5**：`zbrain features --json` 报 `get_health not yet implemented for this engine`。查证 **`get_health` 仅 InMemoryEngine 实现（engine.rs:5879），生产后端 libsql/postgres 走 trait 默认 unsupported**——同 1-6-4-2 前"生产搜索是死的"同构。旁证：doctor handler(lib.rs:2953)对 `get_health` Err 软降级成 warn "Could not compute brain_score"，故生产 doctor 的 brain_score 检查一直没真正算过；autopilot targeted-submit 同受影响（横切三消费者）。用户决策 **A=拆独立节点 1-6-4-5「get_health 生产后端(libsql+postgres)」当场紧接做**（get_health 本就该独立节点非 features 接线范围，但让 features 死命令+优先级高）。登记 **KNOWN-GAPS G48（tracked，指向 1-6-4-5）**。1-6-4-3 收口为"CLI 接线完成"（代码层落盘：core 22 测绿 + CLI 编译+help+--json smoke 过）。
- **经验固化**：孤儿命令 CLI 接线**端到端 smoke 是必做步骤**——编译过+单测绿≠命令活。生产后端的 trait 默认 `unsupported` 实现是隐藏死路（InMemory 全绿会掩盖），必须真跑一次 `zbrain <cmd>` 打真实 engine 才暴露。边界探查即便读了代码也可能漏（前序只确认 get_brain_stats 就绪，漏了 get_health），smoke 是最后一道防线。

