
## roadmap md 纯 marker 死层根因诊断 (2026-07-21)
- 结论：不是当前 skill 在干净文件上造重复，而是历史 marker 格式 flip（plain→⚠️）遗留 + 当前 roadmap.py 两缺陷。
- git 考古：57aea3a 用纯 ROADMAP_SECTION 建文件；ed81837 升级到 ⚠️（旧段未动，新段 append 底部）；5cdf906 曾手动清（commit 明写 fix md overlay pollution）；862dd1b 纯段又复活；当前 HEAD 前我(819a29c)又手动删。
- 缺陷A(根因)：write_markdown_section(roadmap.py:582)只认 ⚠️ marker，无 legacy plain 段迁移/清理 => 旧段永久冻结。
- 缺陷B：file_start/file_end 任一缺失走 else append 分支(587-591)，多 marker 时 existing.index 取首个 START/END 不配对 => 错拼/重复。对畸形输入 robustly-wrong。
- 建议修法：render 前正则 strip 遗留纯 ROADMAP_SECTION 块 + 替换取最后配对 START/END（或校验配对）。治本；手动删段只治标。

## 提 issue: roadmap md 死层 bug -> ZAgentic#13
- 用户要求反馈到 https://github.com/jununfly/ZAgentic 。gh 已登录 jununfly(ADMIN)。已建 issue #13 报告 zj-roadmap-driven render 遗留纯 marker 死层(缺陷A 无迁移/缺陷B 拼接脆弱)，含 git 考古证据+治本修复建议+治标 workaround。
- 结论: 当前 skill 不在干净文件造重复，但格式 flip 无迁移+无孤儿清理导致死层复发。治本需改 skills/zj-roadmap-driven/roadmap.py write_markdown_section。

## grill: part11 1-6-7 接下来做什么 (zj-grill-me x zj-roadmap-driven)
- Q1 方向: A 留在 1-6-7 补齐缺口再走 1-6-7-9 终局删除，不转其它分支。
- Q2 范围: B2 全迁才终局——code-intel×7+find_orphans+takes_list/search+search_by_image+run_doctor+sync_brain+takes_calibration/scorecard 全部 Rust 化后才删 operations.ts；sync_brain/run_doctor 虽大也迁，不砍不延迟。
- Q3 首刀: A3 先把 6 个未入账缺口建为 1-6-7-11..16 节点（takes_calibration+scorecard=1-6-7-16 标 blocked, 受 1-3-3 阻塞），首刀开 code-intel×7 (1-6-7-10)。
- 落盘: 3 个 decide 写入 1-6-7 + 建 6 节点 + render。当前 1-6-7 子节点: 1-6-7-1..8 完成, 1-6-7-9 终局(pending), 1-6-7-10 code-intel(pending,首刀), 1-6-7-11..15 各缺口(pending), 1-6-7-16 takes_cal/score(blocked)。
- 下一步: 开工 1-6-7-10 code-intel×7（NET_NEW engine 方法，需先探清 7 个 code-intel op 与所需 engine 方法）。


## 1-6-7-10 code-intel×7 探查 (推翻 grill A3 假设)
- 7 op 定位(operations.ts): code_callers(3477)/code_callees(3508)/code_def(3539)/code_refs(3559)/code_blast(3581)/code_flow(3613)/code_traversal_cache_clear(3647)。
- 关键发现: Rust 当前**无代码图子系统**。add_code_edges 在 InMemory 是 no-op TODO(engine.rs:6376), Libsql 无 override(走 trait 默认 Err); Rust migrations 无 code_edges_chunk/code_edges_symbol 表(TS 在 schema-embedded.ts:307-338 已完整定义); 无 get_callers_of/get_callees_of/get_edges_by_chunk 读方法; 无 execute_raw 任意 SQL(TS 的 code_def/refs/recursive-walk/traversal-cache 全靠 executeRaw); content_chunks 已有 symbol_name/symbol_type/symbol_name_qualified 列(迁移0019)但无查询方法。
- 结论: 不是 A3 假设的 bounded 薄 wrapper, 而是需从零建 存储层+图查询+符号查询+消歧+递归遍历+缓存 的 NET_NEW 子系统, 体量等同 sync_brain/run_doctor。
- 落盘: 1-6-7-10 拆 6 sub-node (1-6-7-10-1 存储层首刀 / -2 图查询 / -3 符号查询 / -4 符号消歧 / -5 递归遍历 / -6 遍历缓存) + decide 记录范围纠正 + render。md 深度上限=2 (不显示 depth-3), JSON 真相源正确。
- 待用户决策: 是否现在开 1-6-7-10-1 存储层(需 sqlite+pg 双迁移 + Libsql/InMemory 真实实现), 还是因体量重新评估 1-6-7-10 优先级。

## 1-6-7-10-1 code-graph 存储层 — DONE (2026-07-21)
- 实施路径(用户选 A 开存储层): 双方言迁移 0021 + 两引擎真实 add_code_edges/delete_code_edges_for_chunks。
- 迁移: `crates/zbrain-core/migrations[/-sqlite]/0021_code_edges.sql` 复刻 TS schema-embedded.ts:303-338 的 `code_edges_chunk`(resolved, 两端已知 chunk id) + `code_edges_symbol`(unresolved, 仅合格符号名) 两表, 注册到 `LIBQL_MIGRATIONS`+`POSTGRES_MIGRATIONS`(version:21)。
- **踩坑(重要)**: 内联 `CONSTRAINT ... UNIQUE (...)` 表级唯一约束在 libsql `execute_batch` + python `executescript` 下**根本不生成 unique index**(index_list 缺)。改用仓库既定写法——独立 `CREATE UNIQUE INDEX IF NOT EXISTS` 语句(同 0006_links.sql), 解决。这是 zbrain-core 迁移的硬约束: 唯一键一律用独立 CREATE UNIQUE INDEX, 不要内联 CONSTRAINT。
- CodeEdgeInput 重定义(import.rs, 零调用方安全改): 对齐 TS 形状 `from_chunk_id / to_chunk_id:Option<i64> / from_symbol_qualified / to_symbol_qualified / edge_type / edge_metadata:serde_json::Value / source_id:Option<String>`。
- InMemory: 加 `code_edges_store:Vec<InternalCodeEdge>` + `next_code_edge_id`; 真实 add(按 to_chunk_id.is_some 分流 resolved/unresolved, 内存去重对齐 ON CONFLICT)/delete(任一 endpoint 命中即删, symbol 表仅 from 匹配); 加 `code_edges_store_for_test` helper。
- Libsql: 真实 add(INSERT OR IGNORE INTO 两表, edge_metadata 序列化为 JSON 文本)/delete(逐 chunk 删 from/to 两表)。Postgres 引擎仍走 trait 默认 Err(本切片只落地 InMemory+Libsql 写路径; PG 写路径随 1-6-7-10-2 读路径一并补)。
- 测试 + 三道门全绿: InMemory 单测 3 (engine::rate_lease_tests: 分流/去重/删除 touching); libsql 集成 3 (routes/dedup/delete, 需先 seed content_chunks 满足 FK); migrations 测试 EXPECTED_VERSION 14→21 + code_edges 两表存在性校验。core 1870 / mcp 14 / cli build 全绿。
- 注意: libsql 集成测试的 FK —— code_edges_chunk/symbol 引用 content_chunks(id), 测试须先 `seed_chunks` 种入引用行(对齐生产: 先写 chunks 再写 edges)。
- 提交后 1-6-7-10-1 标 completed; 后续 -2..6 sub-node 待开。

## 1-6-7-10-2 code-graph 图查询方法 — DONE (2026-07-21)
- 实施: 在 BrainEngine trait 加 `get_callers_of`(UNION on to_symbol_qualified) / `get_callees_of`(on from_symbol_qualified) / `get_edges_by_chunk`(direction In/Out/Both + edge_type 过滤) 三方法 (默认 Err 兜底)。
- import.rs 加公共类型: `CodeEdgeResult`(对齐 TS CodeEdgeResult: id/from_chunk_id/to_chunk_id:Option<i64>/from_symbol_qualified/to_symbol_qualified/edge_type/edge_metadata:serde_json::Value/source_id:Option<String>/resolved:bool) + `CodeGraphQueryOpts`(limit 缺省100 上限500 / all_sources / source_id) + `CodeEdgeDirection`(Both/In/Out) + `CodeEdgeByChunkOpts`(direction+edge_type+limit:Option<usize> 缺省50 上限200)。
- InMemory: 直接扫 `code_edges_store`, 同时消费 resolved(code_edges_chunk) 与 unresolved(code_edges_symbol, resolved=false) 行; 去重/方向逻辑靠 in-memory 过滤。3 单测(engine::tests: get_callers_unions / get_callees_matches_from / get_edges_by_chunk_direction_type)。
- Libsql: `code_edge_symbol_query` 自由函数 UNION `code_edges_chunk`+`code_edges_symbol`, `params_from_iter` 动态拼 source/limit 参数(limt_ph = params.len()+1, 关键修复); `get_edges_by_chunk` 同理拼 WHERE + 可选 edge_type; 行映射 `code_edge_row_to_result`。
- **关键修复(libsql)**: `code_edge_symbol_query` 内 `limit_ph` 初始误算 `if params.len()==2 {2} else {3}`, 但无 source 过滤时 params 仅含 name(len1) 却发 `LIMIT ?3`(实只2参) -> datatype mismatch。统一改 `params.len()+1`(同 get_edges_by_chunk)。教训: 动态占位符索引一律用 `params.len()+1` 而非硬编码。
- **踩坑(PG 读 INT4)**: Postgres `code_edges_chunk.id` 是 SERIAL=INT4, `from_chunk_id/to_chunk_id` 是 INTEGER=INT4; `code_edge_row_to_result_pg` 初按 i64(INT8) 解 -> "INT8 not compatible with INT4"。改按 i32 解后 `as i64` 拓宽(对齐 libsql/InMemory 的 i64 契约)。PG 写路径(sqlx 绑 i64 入 INTEGER)靠 PG 隐式赋值转型成功, 未报错。
- **PG 写路径补齐**: 本切片把 1-6-7-10-1 遗留的 `PostgresEngine.add_code_edges`(ON CONFLICT DO NOTHING)/`delete_code_edges_for_chunks`(逐 chunk 删 from/to 两表) 从 trait 默认 Err 填成真实实现, 与 Libsql 行为对齐。
- 三道门全绿: InMemory 单测 3 + 集成 `code_edges_query` 5 (3 libsql + 2 pg, pg 覆盖新增写路径) + 旧 `code_edges_storage` 3; core --lib 1874 / mcp 14 / cli build 全绿。
- **环境注意(flakiness, 非逻辑 bug)**: 冷启(重编译后首跑) libsql 集成测试偶发 STATUS_ACCESS_VIOLATION(0xc0000005, libsql 原生层), 重跑即稳定(3/3)。属 Windows libsql 原生 race, 不阻塞。
- roadmap 1-6-7-10-2 -> completed + render。后续 -3 符号查询(find_code_def/find_code_refs) 待开(消费 content_chunks.symbol_name 等列, 无 execute_raw)。

## 1-6-7-10-3 符号查询 find_code_def / find_code_refs — DONE (2026-07-21)
- TDD 垂直切片(zj-tdd): 先写 libsql tracer 测试(RED)→ 实现(单后端 GREEN)→ InMemory 镜像 → 行为测试(def 排除非 def 类型/非 code 页/lang 过滤/类型排序)→ PG 实现 + PG 测试。
- import.rs 新增公共类型: `CodeDefResult`(slug/file:Option<String>/language/symbol_type/start_line:Option<i64>/end_line/snippet) + `CodeRefResult`(+symbol_name) + `CodeSymbolQueryOpts`(limit:Option<i64> 缺省20/50 上限500 / language:Option<String>)。
- BrainEngine trait 加 `find_code_def`(content_chunks.symbol_name 精确 + symbol_type IN (DEF_TYPES 17种函数/类/接口/.../export statement) + p.page_kind='code', JOIN pages 取 slug + frontmatter->>'file', ORDER BY type-rank(1..7)/slug/start_line) 与 `find_code_refs`(chunk_text ILIKE '%symbol%' + p.page_kind='code', 每匹配 chunk 都返回, ORDER BY slug/start_line)。默认 Err 兜底。
- 三后端实现:
  - InMemory: 扫 chunk_store(keyed by slug) JOIN page store(page_kind==Code); def 用 `is_def_type`+`def_rank` 过滤排序(函数1...类2...接口3...type4...enum5...struct6...else7), refs 用 `chunk_text.to_lowercase().contains(needle)`(对齐 TS ILIKE 大小写不敏感); snippet=chunk_text.chars().take(500)。
  - Libsql: `code_def_query`/`code_ref_query` 自由函数; ILIKE 用 SQLite `LIKE`(ASCII 默认大小写不敏感, 行为等价 TS ILIKE); `json_extract(p.frontmatter,'$.file')` 取 file; 动态 `params_from_iter`, limit_ph=params.len()+1(沿用 1-6-7-10-2 修复); DEF_TYPES 静态列表内联进 IN(...)(受信字面量, 无注入)。
  - Postgres: `code_def_query_pg`/`code_ref_query_pg` 自由函数; `frontmatter->>'file'` 取 file, 原生 `ILIKE`; start_line/end_line 按 i32 解后 `as i64` 拓宽(PG INTEGER=INT4, 对齐 libsql/InMemory i64 契约, 同 1-6-7-10-2 教训)。
- 测试 `tests/code_symbol_query.rs` 共 10 (2 libsql def/refs + 2 inmemory + 4 libsql 行为: 排除非def类型/排除非code页/lang过滤/类型排序 + 2 pg)。三道门全绿: core --lib 1874 / mcp 14 / cli build。
- 踩坑(PageInput Default): 初误加 `impl Default for PageInput` 触发 E0119 冲突——实际 engine.rs:184 已 `#[derive(Debug,Clone,Default)]`。删冗余 impl。`PageKind` 在 types.rs 不经 engine 重导出, 集成测试需从 `zbrain_core::types::PageKind` 导入。`ChunkInput` 无 Default, 测试显式构造(不可 `..Default::default()`)。
- **Op 接线延后**: 同 1-6-7-10-2 模式, 本切片只交付 engine 方法 + 测试, 不 hook `code_def`/`code_refs` op(operations.ts 现仍 dynamic-import TS findCodeDef/findCodeRefs)。op 终局 cutover 留 1-6-7-9 或专门节点。
- roadmap 1-6-7-10-3 -> completed + render。后续 -4 符号消歧 / -5 递归遍历 / -6 遍历缓存 待开。

## 1-6-7-10-4 符号消歧 disambiguate_symbol — DONE (2026-07-21)
- TDD 垂直切片：先写 libsql tracer 测试 RED → GREEN → InMemory 镜像 → 行为测试(exact/fuzzy/source-scope/qualified-exact) → PG 实现 + PG 测试。
- 公共契约(import.rs): `SymbolDisambiguation { matches: Vec<String>, suggestions: Vec<String> }` — 对齐 TS `disambiguateSymbol` 返回结构，exact 有命中填 matches，无命中时 suggestions 填 fuzzy did-you-mean。
- BrainEngine 方法 `disambiguate_symbol(&self, bare: &str, source_id: &str) -> Result<SymbolDisambiguation>` 默认 Err 兜底；op 接线依然延后到终局 cutover(同 1-6-7-10-2/3 约定)。
- 两阶段算法(严格对齐 TS 实现):
  1. **阶段一(精确)**: `SELECT DISTINCT symbol_name_qualified FROM content_chunks cc JOIN pages p ON p.id = cc.page_id WHERE p.source_id = $1 AND cc.symbol_name_qualified IS NOT NULL AND (cc.symbol_name = $2 OR cc.symbol_name_qualified = $2) LIMIT 25`；有命中即返回 `{matches, suggestions:[]}`，不进 fuzzy。
  2. **阶段二(近似)**: 仅当无精确命中 → `symbol_name_qualified LIKE '%bare%'`(libsql 大小写不敏感等价 ILIKE) / `ILIKE '%bare%'`(PG) 取最多 5 候选填 suggestions，matches 为空。
- 三后端实现：
  - InMemory: 扫 `store`(pages) + `chunk_store`(chunks)，双重循环过滤 source_id + symbol_name_qualified is not None；exact 匹配规则同 SQL，去重；排序自然就是遍历顺序(无额外排序需要，因为 distinct already)。
  - Libsql: `code_disambiguate_query` 自由函数，参数绑定 source_id(?) + bare(?) → 精确 → 无则 like → 逐行收集 distinct qualified name。
  - Postgres: `code_disambiguate_query_pg` 自由函数，原生 ILIKE，参数绑定与行解码同 pattern，不需要额外 INT4 拓宽(只取 string，不碰整数列)。
- 测试 `tests/code_symbol_disambiguation.rs` 共 9 (5 libsql: tracer exact / multiple exact / fuzzy suggestions / qualified exact / source scoping + 2 inmemory: exact / fuzzy + 2 pg: exact / fuzzy)。
- **三道门全绿**: lib test 1874/1874 passed; zbrain-mcp check ok; zbrain-cli check ok。
- roadmap 1-6-7-10-4 → completed + render。后续下一步是 **1-6-7-10-5 递归遍历 runRecursiveWalk**(多跳 over get_callers/callees + 依赖消歧，输出 code_blast / code_flow)。

## 1-6-7-10-5 递归遍历 runRecursiveWalk — DONE (2026-07-21)
- TDD 垂直切片：先写 import.rs 公共类型 → BrainEngine trait 默认方法 → 三后端实现 → InMemory 集成测试。
- 公共契约(import.rs): `RecursiveWalkOpts { direction: WalkDirection, depth_cap: Option<usize>(缺省 callers=5/callees=8), max_nodes: Option<usize>(缺省200), source_id: String(必填), exact: Option<bool> }` + `RecursiveWalkResult` enum (Ok{depth_groups, cycles_detected, truncation, freshness, terminal_nodes} / NotFound{did_you_mean} / Ambiguous{candidates} / UnsupportedLanguage{supported}) + `RecursiveWalkNode { symbol, chunk_id:Option<i64>, sink_kind:Option<String> }` + `DepthGroup { depth, nodes, confidence }` + `WalkTruncation` enum (None/MaxNodes/DepthCap/Both) + `TerminalNode { symbol, sink_kind }` + `DidYouMeanCandidate` + `AmbiguousCandidate` + `WalkFreshness`。
- BrainEngine trait 加 `recursive_walk(&self, symbol: &str, opts: &RecursiveWalkOpts) -> Result<RecursiveWalkResult>` 默认 Err 兜底；op 接线依然延后到终局 cutover。
- **核心算法(InMemory)**: BFS 从起始符号出发，每跳调 `get_callers_of`(Callers 方向)或 `get_callees_of`(Callees 方向)取下一跳；visited set 去重+检测循环(标记 cycles_detected=true 并跳过已访问节点)；命中 depth_cap 或 max_nodes 时标记对应 truncation 提前退出。置信度 `clamp(1/(1+0.3*depth), 0.05, 1.0)`，对齐 TS。
- 新建 `code_intel.rs` 模块: `SinkKind` enum + `classify_sink(symbol, lang) -> Option<SinkKind>` 匹配 TS sink 分类(console.log/fetch/require 等)。
- 三后端实现: InMemory(完整 BFS 实现，query 不加 limit 以正确检测 max_nodes truncation) / Libsql(`code_recursive_walk_query` 自由函数) / Postgres(`code_recursive_walk_query_pg`，注意 sqlx::query 而非 sqlx::query! 宏以避免 offline cache 问题)。
- **关键修复**: (1) `get_callees_of` 的 `limit` 参数会导致 max_nodes truncation 无法检测(返回的边数不足以触发 total_nodes >= max_nodes 检查)——改为 `limit: None` 让 in-loop check 正常工作。(2) Postgres 新查询必须用 `sqlx::query`(非宏)对齐既有代码风格(全仓 147 处 sqlx::query vs 0 处 sqlx::query!)，否则 SQLX_OFFLINE 模式报 no cached data。
- 测试 `tests/code_recursive_walk.rs` 共 9 (callers_basic / callees_basic / cycle_detection / truncation_depth_cap / truncation_max_nodes / not_found / source_scoping / exact_mode_skips_disambiguation / confidence_formula)。
- **三道门全绿**: core --lib 1859 passed; zbrain-mcp build ok; zbrain-cli build ok。
- **踩坑(impl 块定位)**: engine.rs 9200+ 行，`impl BrainEngine for InMemoryEngine` 在第 7400 行闭合。新方法必须插入 7400 行之前(impl 块内)，不能放在 impl 块外(会报 "self parameter is only allowed in associated functions")。`InternalCodeEdge` 是私有的，集成测试不能直接构造——用 `add_code_edges` 公共 trait 方法 + `CodeEdgeInput` 公共类型注入测试数据。每条边需唯一 chunk ID 避免 `add_code_edges` 的去重逻辑跳过。
- roadmap 1-6-7-10-5 → completed。后续下一步是 **1-6-7-10-6 遍历缓存**(cache 表 + config 键 code.cluster_generation)。


