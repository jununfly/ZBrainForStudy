# 2026-07-13 ZBrain 工作日志

## Phase9 节点 1-1-3-3 完成（Jobs/Minions D 层运维方法）

TDD (grill-me 收敛 8 决策全 A → red-green) 实现 minion queue 四个运维方法，三后端全绿。

**交付：**
- `types.rs`：新增 `QueueStats{by_status:BTreeMap<String,i64>, by_type:Vec<QueueTypeStat>, queue_health:QueueHealth}` / `QueueTypeStat{name,total,completed,failed,dead,avg_duration_ms:Option<i64>}` / `QueueHealth{waiting,active,stalled}`
- `engine.rs` BrainEngine trait：新增 `pause_job` / `resume_job` / `prune_jobs(statuses, older_than_rfc3339: &str)` / `get_stats(since_rfc3339: &str)`（默认 Unsupported）
- 三后端（InMemory/libsql/postgres）全实现；`queue.rs` facade 4 薄封装（prune 默认 `[completed,dead,cancelled]`+now-30d，get_stats 默认 since=now-24h）
- `tests/minion_queue.rs`：contract_pause_resume/prune/prune_cascades_attachments/get_stats × 三后端 = 12 新测试，全量 **82 passed 含真实 PG(pg-embed)**

**关键技术点（经验）：**
1. **prune/get_stats cutoff 用 `&str`（RFC-3339）不用 epoch_ms**：因为比较的是 record 列（updated_at/created_at 是 ISO-8601 TEXT），字典序=时间序，两后端通用。
2. **prune 靠 DB `ON DELETE CASCADE` 清子表**，只 `DELETE minion_jobs`；InMemory 无 FK 手动 `retain` 模拟 CASCADE；`contract_prune_cascades_attachments` 守 `PRAGMA foreign_keys` 不被误关。
3. **get_stats 三方言**：PG 照抄 TS（`FILTER` + `EXTRACT(EPOCH...)*1000`），libsql 用 `SUM(CASE)` + `AVG(julianday差*86400000)`，InMemory 内存聚合 + `chrono::DateTime::parse_from_rfc3339` 算时长。
4. **PG `avg()` 返回 NUMERIC 不是 FLOAT8**：Rust `Option<f64>` decode 报 mismatched types → SQL 里 `(avg(...) FILTER(...))::double precision` 显式 cast 修复。这是 PG mirror 测试再次揪出 SQLite 掩盖问题的实例。
5. **stalled 定义**：`status='active' AND lock_until < now`。测试用**负 lock duration**（`claim_job("t3", -1, ...)`）可靠制造过期锁（沿用 handle_stalled 先例）。

**KNOWN-GAPS 登记 G28**：`pause_job` 不能暂停 `waiting-children` 父 job。这是**共有设计边界**（TS `pauseJob` WHERE 同样 `IN ('waiting','active','delayed')`，不含 waiting-children），非 Rust 独有缺口。代码锚点在 InMemory pause 注释加了 `// registered in docs/plans/KNOWN-GAPS.md (G28)` 双向指针。

**roadmap 收尾**：1-1-3-3 → completed；父链 1-1-3 与 1-1 三子节点全完成随之置 completed。渲染 ZJ_ROADMAP.md（注意：render 必须从 zbrain 项目根 cwd 运行，因 metadata.md_file 是相对路径 `.workbuddy/roadmaps/ZJ_ROADMAP.md`；从 skill 目录运行会误写到 skill 下）。

**下一步候选**：Phase9 的 1-2（MinionWorker + supervisor）；或 G23（postgres search_pages）等其他 part 缺口。按 roadmap 推进。

## Phase9 节点 1-2（MinionWorker + supervisor）grill-me 设计拷问（进行中）

用 grill-me + roadmap-driven 对 1-2 做设计决策拷问。已探明 Rust 侧零骨架（`crates/zbrain-core/src/minions/` 仅 attachments/mod/queue/types），TS 三文件为黄金参照（worker.ts 37KB / supervisor.ts 22KB / child-worker-supervisor.ts 14KB，spawn 独立子进程非 fork）。

**已落盘 4 决策（roadmap JSON node 1-2）：**
- Q1 并发运行时：worker 是 runtime-aware 消费层坦然用 tokio（JoinSet/CancellationToken/time::interval），zbrain-core 保持 runtime 中立（仅 tokio::sync 不启 rt-*），只提供数据层 + MinionHandler 纯 trait。
- Q2 crate 落点：新开 `zbrain-worker` crate，依赖 zbrain-core + 启 tokio rt-multi-thread，worker+supervisor 同住；zbrain-cli `jobs work`(1-7) 做 thin wrapper。
- Q3 契约形状：`MinionHandler` = `#[async_trait] trait{ async fn handle(&self,ctx:&MinionJobContext)->Result<Value> }` 放 core；`MinionJobContext` 做 concrete struct 忠实映射 TS（持 `Arc<dyn BrainEngine>`+job_id+数据快照+双 `CancellationToken`(signal/shutdown)+5 异步能力方法委托 engine 回写 DB）；`register(name, Arc<dyn MinionHandler>)` 存 HashMap。**关键判断：`tokio-util{sync}` 引入 core 不破坏 runtime 中立**——`CancellationToken` 本质 `Arc<AtomicBool>+Notify`，创建/取消/is_cancelled 不需 executor，只有 `.cancelled().await` 需要（那发生在 worker 层）。
- Q4 切分：**5 片单向依赖**——1-2-1 契约层(仅 core) → 1-2-2 队列消费核(串行 concurrency=1 先通闭环) → 1-2-3 并发池+锁续期+abort+graceful shutdown → 1-2-4 自监控外围(RSS/health/quiet 全可 flag 关) → 1-2-5 supervisor+child-spawn(依赖 1-2-3 可与 1-2-4 并行)。

**已建 5 个子节点**（1-2-1~1-2-5，mode=execute）并 render ZJ_ROADMAP.md。

- Q5 外围取舍(选 A)：**①lease-full 路径留桩**（依赖 1-3 rate-leases 未迁——1-2-3 fail 分支首版只做 普通失败/UnrecoverableError/退避重试，RateLeaseUnavailable 分支写 no-op 桩，实现到 1-2-3 时登记 KNOWN-GAPS，1-3 落地再接）；**②RSS** parse_rss_from_proc_status 纯函数照迁可单测，读取 Linux 走 /proc 非 Linux 返 None 降级，整体 flag 关（maxRssMb 默认 disabled）；**③quiet-hours defer 首版做**（不依赖未迁模块，quiet_hours 列已在，flag 可关）；**④self-health/unhealthy 用 `mpsc::Sender<UnhealthyReason>` 或 callback 替代 EventEmitter**（1-2-4 定义信号形状，1-2-5 消费）。总纲：①留桩，②③④首版做但全 flag 可关。

**grill 收敛完成（5/5 决策全落盘 + 1-2 notes 更新 + render）**。下一步预期用户发 `/zj-tdd 开始实现`，从 1-2-1 契约层起。

## ZJ_ROADMAP.md 整理（修 render 叠层烂摊子）

**问题**：`ZJ_ROADMAP.md` 里叠了 3 个 part 的 render 结果 + 3 套不同格式地层（23KB）——Part5 最老裸格式(`💡 Decision: undefined` 噪声、无 marker)、Part6 旧 `⚠️ ROADMAP_SECTION` marker、Part7 新 `ROADMAP_SECTION` marker。**根因**：Part5/6/7 三个 JSON 的 `metadata.md_file` 都误绑同一个 `ZJ_ROADMAP.md`，而 render 只认新 marker 做替换，命不中的旧地层永久残留。

**方案 A（每 part 独立 md）**：Part4/5/6 各 `link` 到独立 `ZBRAIN_TS_TO_RUST_PARTn_*.md` 并 render（对齐 Part1-3 已有模式；Part4 原本没绑，一并补齐）；`ZJ_ROADMAP.md` 直接 `rm` 后让 Part7 render 全新重建（render 对不存在文件走 `content=section` 纯新写，天然清掉叠层）。结果 ZJ_ROADMAP.md 23KB→7.7KB 只剩 Part7；验证 undefined 噪声 0、旧 ⚠️ marker 0。

## Roadmap 文件迁到 docs/plans（统一位置）

用户要求所有 roadmap `.json`+`.md` 统一放 `docs/plans/`（原 Part1-3 md 已在那，Part1-3 json + Part4-7 全部还在 `.workbuddy/roadmaps/`）。执行：`git mv` 全部 7 JSON + 4 md 到 docs/plans → 逐个 `link` 重绑 md_file 到 docs/plans 绝对路径 → 全部 render 验证。**清掉 Part6/7 JSON 的根级遗留脏 `md_file` key**（与 metadata.md_file 并存、指向旧 roadmaps 路径，CLI 不读但会误导，用 `d.pop('md_file')` 删）。删空的 `.workbuddy/roadmaps/` 目录。最终 docs/plans 下 7 json + 7 md 齐全，0 残留旧路径。约定已写入 MEMORY.md。

**roadmap CLI 用法坑（本次踩）**：`decide`/`add`/`tree`/`render` 的第一个 positional 是 **roadmap JSON 完整路径**（不是 roadmap 名，会被当相对路径在 cwd 找）；正确形如 `decide <绝对.json 路径> <node_id> <question> <answer>`。Bash 里 Windows 路径要用正斜杠 + 引号，否则反斜杠被 shell 吞掉。`update --status` 合法值是 `completed`/`in_progress`（不是 `done`）。

## 节点 1-2-1 完成（MinionWorker 契约层，TDD 8 behaviors 全绿）

新建 `crates/zbrain-core/src/minions/handler.rs`，纯契约层（无 poll loop/scheduler/supervisor，那是 1-2-2~1-2-5）。TDD red→green 8 个 behaviors 全绿（`cargo test -p zbrain-core --lib minions::handler` = 8 passed）。

**交付：**
- `MinionHandler` trait：`#[async_trait] async fn handle(&self, ctx:&MinionJobContext)->Result<Value>`，对象安全（测试验证 `HashMap<String, Arc<dyn MinionHandler>>` 注册+调用）。
- `MinionJobContext` concrete struct：只读快照 `id/name/data/attempts_made` + 双独立 `CancellationToken`(signal/shutdown) + 私有 `engine:Arc<dyn BrainEngine>`+`lock_token`。5 个 async 能力方法（update_progress/update_tokens/log/is_active/read_inbox）全委托 engine 对应 trait 方法，token-fenced。`engine`/`lock_token` 私有——handler 只能走方法，保证 lease fence 恒生效。
- `MinionWorkerOpts` struct + `Default`：13 字段对齐 TS `MinionWorkerOpts` 默认值（queue=default/concurrency=1/lock_duration=30000/stalled_interval=30000/max_stalled_count=1/poll=5000/max_rss_mb=None/rss_check=60000/health_check=60000/stall_warn=300000/stall_exit=600000/db_fail_exit=3/db_probe_timeout=10000）。注：`getRss`(TS 注入点)不进 contract struct，留给 1-2-4 做 worker 字段。
- `mod.rs` re-export `MinionHandler/MinionJobContext/MinionWorkerOpts`。

**踩坑修正**：behavior 1 断言 `attempts_made==1` 错——读 InMemory `claim_job` 确认 claim bump 的是 `attempts_started`，`attempts_made` 保持 0（改断言并注释说明契约）。`complete_job` result 参数是 `Option<&Value>` 需借用。

**下一步**：1-2-2 队列消费核（zbrain-worker crate，worker 主循环 promoteDelayed→claim→launchJob→executeJob→complete/fail，串行 concurrency=1 先通闭环，依赖 1-2-1）。

## Phase9 节点 1-2-2 完成（队列消费核）

TDD 实现队列消费核，新建 `crates/zbrain-worker` crate（workspace member #7）。

**交付**：
- `backoff.rs`：纯函数 `calculate_backoff(attempt, backoff_type, delay, jitter_range, rng)`，照搬 TS `backoff.ts`——Fixed 常量 / Exponential 2^(n-1)·delay / jitter 对称范围 clamp≥0。RNG 抽象可注入（确定性测试）。
- `worker.rs`：`MinionWorker` 深模块——核心 `process_next_job(&self) -> Result<ProcessOutcome{Processed(id)/Idle}>`，内含 `promote_delayed → claim(1) → dispatch(execute_job) → complete/fail`。`unrecoverable(msg)` helper（`StructuredError class="unrecoverable"` 约定）。result 映射忠实 TS（object/array 直存，标量包 `{"value": x}`，null→None）。
- `tests/worker_consume.rs`：9 个集成测试（空队列 idle / 无 handler 不 claim / 成功+object result / 标量包装 / null→none / retryable→delayed+backoff / unrecoverable→dead / attempts 耗尽→dead / promote-delayed→claim 闭环 FlipFlop）。
- 13 tests 全绿（4 backoff unit + 9 consume integration）。

**关键设计决策**：
- `UnrecoverableError` = `StructuredError{class:"unrecoverable"}` 约定，不破坏 1-2-1 的 `handle->Result<Value>` 契约。worker `execute_job` 检查 `err.class == UNRECOVERABLE_KIND` → 直接 dead，不走 backoff。
- result 映射忠实 TS 下游形状——`is_object()||is_array()` 直存，标量包 `{"value":x}`，`Value::Null`→None。
- `MinionWorker` 持 `Arc<dyn BrainEngine>` + `HashMap<String, Arc<dyn MinionHandler>>` + `MinionWorkerOpts`。`process_next_job` 先 `promote_delayed` 再 `claim_job(registered_names)`——claim 时传已注册 name 列表，未注册的 job 不被 claim（不 dead-letter，等 handler 注册）。
- `fail_job` 内部做 `attempts_made += 1`，worker 判断 `attempts_exhausted = job.attempts_made + 1 >= max_attempts`（fail_job 加之前快照的值 +1，对齐 TS L815）。
- 串行 concurrency=1，无并发池/续锁/超时/graceful shutdown（→1-2-3），无 RSS/health/quiet（→1-2-4）。

**下一步**：1-2-3 并发生命周期（并发池 concurrency>1 + 续锁 interval + per-job 超时 + graceful shutdown signal）。

## Phase9 节点 1-2-3 完成（并发生命周期）

TDD 实现并发池+续锁+超时+graceful shutdown，在 `zbrain-worker` crate 内扩展。

**交付**：
- `worker.rs` 新增 `run()` 方法：JoinSet 并发上限（claim up to concurrency slots）+ poll loop（idle→poll_interval, capacity→100ms）+ drain 30s（shutdown 后等 in-flight 完成）。对齐 TS `start()` L430-503。
- `worker.rs` 新增 `run_one_job()` 关联函数（spawnable，无 `&self`）：per-job lock renewal（`tokio::spawn` interval lockDuration/2，renew 失败→signal.cancel()）+ per-job timeout（`tokio::select!` handler vs signal.cancelled() vs sleep(timeout_ms)）+ signal/shutdown 分离。对齐 TS `launchJob` L613-671。
- `shutdown_token()` accessor：外部可 cancel shutdown 触发 drain。
- `handle_failure` 从 `&self` 方法提取为 `Self::` 关联函数（不引用 self，run_one_job 可复用）。
- `aborted_error(reason)` helper：构建 `StructuredError{class:"aborted", message:"aborted: timeout/signal"}`。
- `tests/worker_lifecycle.rs`：6 个集成测试（concurrency=2 并行 / concurrency=1 串行 / per-job timeout→dead / lock renewal 保活 / graceful shutdown drain / shutdown 停止 claim）。
- 19 tests 全绿（4 backoff + 9 consume + 6 lifecycle）。

**关键设计**：
- `run_one_job` 持有 owned data（`Arc<dyn BrainEngine>` + `Option<Arc<dyn MinionHandler>>` + `MinionJob` + `String` + 2×`CancellationToken` + `MinionWorkerOpts`），满足 `JoinSet::spawn` 的 `Send + 'static` 约束。
- lock renewal 用 `tokio::spawn` 独立任务（interval 循环），handler 完成后 `abort()`。select! 里同时监听 `signal.cancelled()`（lock-loss 触发）和 `sleep(timeout_ms)`（timeout 触发）。
- timeout 触发时先 `signal.cancel()`（让 handler 有机会 cooperative 退出）再返回 `aborted_error("timeout")`。
- 测试用 `multi_thread` flavor（lock renewal spawn 需要多线程调度）。
- opts 字段类型坑：`poll_interval_ms` / `lock_duration_ms` 是 `i64`，`Duration::from_millis` 需 `as u64` 转换。

**不在本片**（→1-2-4）：RSS watchdog / health check / stall detection / quiet-hours。→1-2-5：supervisor。

## Phase9 节点 1-2-4 完成（自监控外围）

TDD 实现 worker 自监控外围（RSS + quiet-hours + health check），44 全绿。

**交付：**
- `rss.rs`：`parse_rss_from_proc_status` + `get_accurate_rss` 纯函数（8 tests），手写解析（RssAnon+RssShmem kB→bytes），无 regex 依赖，reader 可注入
- `quiet_hours.rs`：`evaluate_quiet_hours` 纯函数（9 tests），midnight-wrap + normal window + fail-open，不绑 chrono-tz（hour 由调用方传）
- worker 自监控扩展（8 tests）：
  - `check_memory_limit()`：可注入 RSS reader，超过 max_rss_mb 即 cancel shutdown token（idempotent）
  - RSS periodic watchdog：在 run() 中 spawn 独立 task，max_rss_mb>0 时定时检查，超阈值→shutdown
  - Health check timer：在 run() 中 spawn 独立 task，health_check_interval_ms>0 时定期 DB probe（get_job(0)）+ stall detection（jobs_completed 不增长 + waiting jobs→UnhealthyReason::Stalled），通过 mpsc channel 汇报
  - `UnhealthyReason::DbDead` / `UnhealthyReason::Stalled` 枚举
  - 全可 flag 关：max_rss_mb=None/0 关 watchdog，health_check_interval_ms=0 关 health check

**设计决策：**
- `evaluate_quiet_hours` 不绑 chrono-tz：hour 由调用方传，测试时直接传 u32（0–23）
- DB probe 用 `engine.get_job(0)` 代替 TS 的 `executeRaw('SELECT 1')`（Rust trait 刻意无裸 SQL escape hatch）
- `jobs_completed: Arc<AtomicU64>` 从 run() 传入 run_one_job，每次 complete 递增——health check 用它追踪 stall

**下一步**：1-2-5 supervisor（子进程 spawn + MinionWorker 实例 + graceful restart 父进程）。
