## 2026-06-30
- 修复 QueryOperation 测试：更新 InMemoryEngine::new() -> default()，修复 put_page 签名，PageInput 使用结构初始化，修复测试断言使用正确的 camelCase 字段名
- 修复 CLI 配置测试：修复 pattern guard 中的移动语义问题（String 不可 Copy）
- 实现 Phase 1-4: Operations layer trust boundary：
  - 添加 RemoteMcpConfig 结构和 is_thin_client() 检测
- 完成 1-6-2 admin API layer：关闭 #70-#74（stats/agents/api_keys/requests/signout）
- 完成 1-6-2-2 agents/spend + jobs/watch：关闭 #75-#76，提交 b27cf4f
- 解决 #75-#76 TDD 实施，Web handler + admin_queries + libsql graceful degradation
- 完成 1-6-2-1 calibration subsystem（TDD）：
  - #77 zbrain-svg crate：34 tests，6 种公开函数（4 chart + escapeXml + svgEmpty），零依赖
  - #78 calibration_queries.ts：CalibrationQueries trait + InMemory/LibsqlEngine 实现，4 contract tests
  - #79 calibration API handlers：3 endpoints（profile/charts/pattern），SVG rendering，7 integration tests
  - 路线图 1-6-2、1-6-2-2、1-6-2-1 全部标记 completed
  - AppState 新增 calibration_queries 字段，CLI/测试全部更新
- 9 个设计决策记录于 roadmap 1-6-2-1 节点
- 提交修改到分支 (commit pending)
- 完成 1-6-2-1 calibration 子系统规划（grill-me + roadmap-driven）：
  - 窄切：只 port 3个admin端点 + SVG renderer + calibration_profiles查询
  - 8 个决策落盘到 roadmap JSON
  - 拆 3 个 issue：#77 zbrain-svg crate, #78 calibration_queries, #79 calibration API handlers
  - 全部标记 enhancement + ready-for-agent
- 实现 Phase 1-4-3: 共享 MCP dispatch_tool_call 路径：
  - 添加 ToolResult / ToolContent 结构体（1:1 对齐 TS dispatch.ts ToolResult shape）
  - OperationRegistry::dispatch_tool_call() 统一分发路径（错误在 body 中，符合 MCP spec）
  - CLI run_operation() 迁移到使用 dispatch_tool_call()
  - 6 个 TDD 测试全部通过，commit 9fc365b 推送到 rust-rewrite 分支
  - 实现 McpClient 模块，支持 OAuth token 缓存和远程工具调用
  - run_operation() 中集成 thin-client guard，local-only 操作在远程模式拒绝
  - Phase 1-4 完成
- 实现 Phase 1-5-1: Rust MCP tool definitions and serve-mcp command：
  - Operation::input_schema() 方法加到 TypedOperation/Operation trait
  - 8 个 Operation 各自实现 input_schema()（手写 JSON Schema，选路线 A）
  - zbrain-mcp crate 实现：McpToolDef, build_tool_defs(), StdioMcpServer
  - build_tool_defs() 过滤 local_only ops，不对外暴露破坏性操作
  - StdioMcpServer 处理 JSON-RPC 2.0: initialize/tools/list/tools/call
  - zbrain-cli 添加 serve-mcp 命令（--source flag）
  - 8 个 TDD 测试全部通过，commit 5539d31 推送到 rust-rewrite 分支
- 1-6-3 to-issues + triage（zj-to-issues → zj-triage）：
  - 拆解 3 个 tracer-bullet issue 全部发布：#80-#82
  - #80: OAuth client management（migration + OAuthQueries trait + 3 handlers）
  - #81: SSE live activity feed（broadcast channel + /admin/events）
  - #82: TS admin cleanup（删除被 Rust 替代的路由，blocked by #80 #81）
  - 全部 triage 为 enhancement + ready-for-agent
  - roadmap JSON updated with issue references

- Roadmap 同步 & 代码审计（grill-me + roadmap-driven）：
  - 发现 roadmap JSON 未同步：1-2 父节点 + 1-4-3 + 1-5-1 状态为 pending 但代码已提交，已修复
  - 发现 P0: serve-mcp tools/call 因 engine=None 完全不可用（除 no-engine ops 外）
  - 发现 P1: run_operation 硬编码 local_only 名单 vs trait 脱钩；MCP 暴露但不可用的 engine-dependent ops
  - 发现 P2: 7 个 operation 的 input_schema() 未实现
  - 用户选择方案 B：标记为 known-gap，defer 到 1-5-2 一起处理
  - 已更新 JSON + MD roadmap：1-5-1 标记 completed 并在 decisions 中记录 3 个 known gaps

- 1-5-2 grill + issue publish（grill-me → zj-to-issues → zj-triage）：
  - Q1 scope: expanded（engine wiring + rate limiting + audit hooks + input_schema fill + P1b fix）
  - Q2 engine: 构造器注入 Box<dyn Engine>，StdioMcpServer::new() 接收
  - Q3 rate limit: 全局 sliding window（AtomicU64 + timestamp）
  - Q4 audit: tracing crate 结构化日志
  - Q5 config: zbrain.yml mcp.rate_limit 字段，默认 60
  - Q6 P1b: 1-5-2 内修复，从 registry.is_local_only() 动态查询
  - 发布 4 个 issues：#59 engine wiring, #60 input schema fill, #61 P1b fix, #62 rate limiting + audit hooks（依赖 #59）
  - 1-5-2 → in_progress

- 1-5-2 TDD 验证 + 补测完成：#59-#62 代码已在 1-5-1 阶段实现，本阶段通过 TDD 验证现有覆盖率并补充缺失测试：
  - #59 Engine Wiring: 新增 `stdio_server_tools_call_engine_data_roundtrip`（引擎存入数据→MCP tools/call get_page 取回数据→验证往返）
  - #60 Input Schema Fill: 现有 `all_seven_tag_version_slug_ops_have_non_empty_schemas` 已覆盖全部 7 个 operation 的 required 字段和属性存在性，无需补充
  - #61 P1b Dynamic local_only: 新增 `dynamic_local_only_unknown_operation_defaults_to_false`（验证未知 operation 不回退为 local_only）
  - #62 Rate Limiting + Audit: 新增 `rate_limiter_window_resets_after_expiry`（验证滑动窗口过期后重置计数器并允许新请求）
  - 配套修复：libsql_engine_migrations.rs 适配新版 EngineConfig API（db_path→database_path, with_config→new+connect）、移除无用的 `>= 0` u64 比较、清理未使用 import
  - 测试结果：zbrain-cli 24/24 ✅, zbrain-mcp 14/14 ✅, zbrain-core 182/182 ✅（init_schema_is_idempotent 为预存 migration bug，与 1-5-2 无关）
  - 1-5-2 → completed，roadmap JSON 已更新
- 状态同步：GitHub issues #59-#62 全部 closed，1-5-2 roadmap JSON + MD 均更新为 completed，commit 27665c2

- 1-5-3 grill + issue publish（grill-me → zj-to-issues → zj-triage）：
  - Grilling 完成：策略选定 B（保守对应删除），只删 Rust 已有对等且无 TS 依赖方的文件
  - 删除清单：src/mcp/server.ts, rate-limit.ts, http-transport.ts, src/commands/serve.ts, call.ts, scripts/smoke-test-mcp.ts + 对应测试
  - 保留：dispatch.ts + tool-defs.ts（serve-http.ts 仍依赖，1-6 处理）
  - 裂解为 3 个 tracer-bullet issues：#63（标记保留文件，独立）、#64（删除核心 MCP 文件，阻塞 #65）、#65（删除 serve/call + cli 清理）
  - GitHub issues #63-#65 已发布，triage 标签：enhancement + ready-for-agent
  - 1-6-1 已预埋遗言：HTTP MCP 替代 serve-http.ts 后也要删 dispatch.ts/tool-defs.ts
  - commit cfc7663

- 1-5-3 TDD 执行完成（#63-#65）：
  - #63: dispatch.ts + tool-defs.ts 顶部添加 KEPT 删除提醒注释 ✅
  - #64: 删除 7 个文件（server.ts, rate-limit.ts, http-transport.ts, smoke-test-mcp.ts + 3 tests）✅
  - #65: 删除 serve.ts + call.ts + serve-stdio-lifecycle.test.ts；清理 cli.ts 中 6 处引用 ✅
  - 验证：cargo test 38/38 全绿（24 cli + 14 mcp），src/ 内无残留 import
  - GitHub issues #63-#65 全部 closed，1-5-3 → completed
  - commit b9144b0：14 files, +17/-2648

- 1-6 grill + issue publish（grill-me → zj-roadmap-driven → zj-to-issues → zj-triage）：
  - Grilling 完成 9 个决策（Q1-Q9），全部落盘到 roadmap JSON
  - Q1: 1-6-1/1-6-2 边界 → 基础设施 vs 业务拆分
  - Q2: Crate 结构 → 新 zbrain-web library crate
  - Q3: Server 启动 → zbrain-cli Commands::ServeHttp 调用 zbrain-web::run()
  - Q4: Admin auth 范围 → 核心（bootstrap token + login + requireAdmin），magic-link/sign-out 延后
  - Q5: 1-6-1 路由 → GET /health + /admin SPA 静态文件
  - Q6: MCP HTTP dispatch → 新增 1-6-4 子节点
  - Q7: 配置来源 → yml server section 为主，CLI 覆盖，最小化 env var
  - Q8: Engine 连接 → Arc<AppState{engine, sessions, config}>
  - Q9: TS 删除时机 → 1-6-4 完成后批量删除（serve-http.ts, dispatch.ts, tool-defs.ts, oauth-provider.ts, scope.ts）
  - 拆解为 4 个 tracer-bullet issues：#66-#69
  - #66: zbrain-web crate + health endpoint（无阻塞）
  - #67: admin auth（阻塞 #66）
  - #68: admin SPA serving（阻塞 #66）
  - #69: server config + CLI integration（阻塞 #66, #67, #68）
  - 全部 triage 为 enhancement + ready-for-agent，已发布到 GitHub
- #66-#69 TDD 实现完成：
  - #66: zbrain-web crate + /health endpoint
  - #67: admin auth (argon2 + session)
  - #68: admin SPA 静态文件服务
  - #69: ServerConfig + CLI ServeHttp 集成（本日核心改动）
  - 测试: cli 32/32, web 39/39, core 182/182, mcp 23/23
  - 已提交到 rust-rewrite 分支 (5d76590)，roadmap 1-6-1 → completed, GitHub issues #66-#69 全部 close
- 1-6-2 Grill 完成 (7 Q, 全部落盘)：
  - Q1: 18端点四层拆分 → A(12)/B→1-6-4(6)/C→1-6-2-1(3)/D→1-6-2-2(2)
  - Q2: 模块组织 → 方案A 按领域分 5 module
  - Q3: 数据访问 → 方案B 新建 AdminQueries trait (8方法)
  - Q4: 拆分 → 方案A 5个 tracer-bullet issue
  - Q5: AppState注入 → 方案A Arc<dyn AdminQueries> 独立字段
  - Q6: 路由路径 → 方案A 保持 /admin/api/*
  - Q7: trait方法 → 8个: get_stats/get_full_stats/check_health/list_agents/list_api_keys/create_api_key/revoke_api_key/list_requests
  - 1-6-2 → in_progress, 新增 1-6-2-1/1-6-2-2 子节点, 1-6-4 补充 B 层 note
  - 全 7 决策已渲染到 MD roadmap

- #69 TDD 实施完成（按 zj-tdd tracer bullet 流程）：
  - RED→GREEN #1: ServerConfig 配置解析（3 个测试：defaults / YAML 反序列化 / 部分覆盖默认）
  - RED→GREEN #2: CLI ServeHttp args 解析（4 个测试：无 flag / --port / --bind / 全部 flag）
  - RED→GREEN #3: 集成测试 serve_http_integration_health_and_spa（真实 HTTP: health 200 + admin SPA）
  - 改动文件：config.rs（+ServerConfig struct, +server 字段, +default fns）、lib.rs（+ServeHttpArgs, +Commands::ServeHttp, +run_serve_http_command）、Cargo.toml（+zbrain-web dep, +dev reqwest/tokio）
  - 测试结果：zbrain-cli 32/32 ✅, zbrain-web 39/39 ✅, zbrain-core 182/182 ✅, zbrain-mcp 23/23 ✅
  - 唯一失败：init_schema_is_idempotent（预存 migration bug，与本次无关）
  - cargo build -p zbrain-cli 成功
  - GitHub issue #69 closed, roadmap JSON + MD 1-6-1 → completed
- 1-6-2 to-issues + triage（zj-to-issues → zj-triage）：
  - 拆解 5 个 tracer-bullet issue 全部发布：#70-#74
  - #70: AdminQueries trait + session (blocker)
  - #71: requests (blocked by #70)
  - #72: api_keys (blocked by #70)
  - #73: agents (blocked by #70)
  - #74: stats (blocked by #70)
  - 全部 triage 为 enhancement + ready-for-agent，含完整 agent brief
  - roadmap JSON updated with issue references
- 完成 1-6-2 Layer A 全部 admin 业务路由实现与验证：
  - #70 AdminQueries trait (8 方法) + session/sign-out-everywhere
  - #74 stats: /stats, /health-indicators, /full-stats
  - #73 agents: /agents (OAuth + API key 联合查询 UNION)
  - #72 api_keys: GET/POST /api-keys, POST /api-keys/revoke (list/create/revoke)
  - #71 requests: GET /requests 分页+过滤 (source/method/status)
  - 所有 issue 已 closed，roadmap 1-6-2 status -> completed
  - zbrain-core: AdminQueries trait + libsql/InMemory 双实现
  - zbrain-web: admin/{mod,session,stats,agents,api_keys,requests}.rs + require_admin 中间件
  - 测试: 258 passed, 1 pre-existing failure (init_schema_is_idempotent)
  - 剩余子节点: 1-6-2-1 (calibration) + 1-6-2-2 (jobs/spend) pending

## 2026-06-30 下午: 1-6-2-2 grilled → issues #75 #76

通过 zj-grill-me + zj-roadmap-driven 完成 1-6-2-2 的决策树遍历：
- Q2: 一刀切两个端点 (agents/spend + jobs/watch)
- Q3: 扩 AdminQueries trait，不新建 OpsQueries
- Q4: PG→SQLite 手动翻译 (date_trunc→strftime, ANY→LIKE, ->>→json_extract)
- Q5: 5 子查询独立 graceful degradation，assembler 串行组装
- Q6: 不创建新 migration，表不存在时返回空/默认值
- Q7: admin/spend.rs + admin/watch.rs 两个独立文件
- Q8: 错误分类器放 watch.rs private sub-module
- Q9: 方法签名 confirm: list_agent_client_spend()→Vec<AgentClientSpend>, get_watch_snapshot()→WatchSnapshot
- Q10: TDD 顺序 (#75 first, #76 depends on #75)

拆成 2 个 GitHub issue:
- #75 "Port GET /admin/api/agents/spend to Rust" (ready-for-agent, 无阻塞)
- #76 "Port GET /admin/api/jobs/watch to Rust" (ready-for-agent, blocked by #75)

Roadmap JSON 也已更新 decisions + issues 引用。
- 修复 pre-existing `init_schema_is_idempotent` 测试失败：
  - 根因: libsql local mode 中，从同一 `Database::connect()` 获取的新连接无法看到前一次 `init_schema()` 提交的 WAL 变化（尽管独立 `Builder::new_local()` 可以）
  - 修复: 将版本读取从 `self.conn()` 改为 `read_rust_schema_version_fresh()`，该方法通过独立 `Builder::new_local()` 打开连接读取 `rust_schema_version` 表，绕过了 WAL 可见性问题
  - 结构调整: `init_schema` 先在独立连接中尝试读取版本号 → 若表不存在则 fallback 到 bootstrap 流程 → 仅当确实需要迁移时才打开 `self.conn()` 执行 SQL
  - LibsqlEngine 新增 `db_path: OnceLock<String>` 字段存储数据库路径
  - 同时修复: `BEGIN TRANSACTION`/`COMMIT`/`ROLLBACK` 从 `execute_batch` 改为 `conn.execute()`（避免潜在的事务边界问题）
  - 全部测试通过：0 failures，构建通过

- 完成 #75 #76 (1-6-2-2 agents/spend + jobs/watch) via TDD:
  - #75 agents/spend: AgentClientSpend type + AdminQueries::list_agent_client_spend() + InMemory stub + LibsqlEngine graceful degradation (PG→SQLite translated SQL, table-not-found→[]) + admin/spend.rs handler + 2 web tests (401 + 200/JSON)
  - #76 jobs/watch: WatchSnapshot + 5 nested types + AdminQueries::get_watch_snapshot() + InMemory stub + LibsqlEngine 5 independent sub-queries (queue_health, by_type, lease_pressure, error_clusters, budget_owners) + error classifier (13 clusters ported from TS) + admin/watch.rs handler + 2 web tests
  - 新增 test files: admin_queries_spend_contract.rs, admin_queries_watch_contract.rs, libsql_admin_spend.rs, libsql_admin_watch.rs
  - 全测试通过 0 fail, build 通过
  - 1-6-2-2 roadmap → completed
  - 剩余: 1-6-2-1 (calibration) 或 1-6-3 (React TS admin frontend) 或 1-6-4 (MCP HTTP dispatch)

- 完成 #80-#82 (1-6-3-1/2/3) via TDD red-green-refactor:
  - #80 OAuth client management: OAuthQueries trait (3 methods) + LibsqlEngine impl + InMemoryEngine stubs + migration 0009 + admin/oauth.rs handler (3 routes + 7 tests)
  - #81 SSE live activity feed: ActivityEvent type + broadcast channel + events.rs (4 tests) + require_admin middleware on SSE endpoint
  - #82 TS admin cleanup: 删除 serve-http.ts 中 OAuth 3 个 handler + SSE events endpoint + unused imports (normalizeScopesInput, validateTokenEndpointAuthMethod)
  - Kept: broadcastEvent/sseClients 在 TS 中保留（MCP dispatch 仍引用，将在 MCP dispatch 迁移时删除）
  - zbrain-web: 31/31 tests pass ✅, zbrain-core: all pass (仅 pre-existing binary permissions issue)
  - Route path discrepancy noted: Rust admin routes at /* (bare), TS at /admin/api/* — admin/mod.rs comment outdated
  - Roadmap 1-6-3 + 1-6-3-1/2/3 all → completed
