# 2026-08-12 工作日志

## 路线图优先级重排（1-1-5 真 LLM eval 族）
- **#63 真相更正**：从 git 历史 `39e14cd5` 取回 TS `runBrainstorm`，确认它是**完整 generator 移植**——缺失 4 引擎方法（list_prefix_sampled_pages / list_corpus_sample / get_embeddings_by_chunk_ids + search/ai 层 hybrid_search / embed_query）+ 需新建 orchestrator/domain-bank/judges 三模块 + search/embed 集成。原路线图节点 `1-1-5-5` 标「前置 port 3 引擎方法」**严重低估**，已更正；eval gate 3 轴 DISTANCE≥0.4 / USEFULNESS≥3.5 / GROUNDING=1.0（is_brainstorm_output 标记检测 Rust 已有）。
- **Q1 决策（A）**：推翻 2026-08-09 的 `#61→#63→#62`，翻转为 **`#61→#62→#63`**。理由：#62 复用 #59 cross_modal judge panel、零新引擎方法、无 generator，单会话可收口；#63 是多会话量级。
- **Q2 决策（是）**：修正 `1-1-5-5` 节点描述 + 给 `1-1-5-4` 补收敛说明。地图须反映真实范围。
- **焦点**：`1-1-5-3`（#61）标 completed（MVP 已交付）；`1-1-5-4`（#62）标 in_progress，**当前施工点 = #62**。harness `1-1-5-3-2` 仍 pending，作为独立 deferred 子节点。

## #62 MVP 实际落地（推翻 Q2 的「复用 cross_modal 收敛」假设）
- **实测真相**：#62 TS 源 `src/core/eval-contradictions/` 是 **18 文件完整子系统**（orchestrator/judge/calibration/cost-tracker/severity-classify/trends/cache/cross-source/date-filter/auto-supersession/judge-errors），427 行命令文件严重低估。它用**自有 query-conditioned judge**（非 cross_modal panel），且 deferred 部分依赖 Rust 缺失的 `hybrid_search`/`embed_query`/`listActiveTakesForPages` 引擎方法。
- **MVP 收敛收口**：只用**现有 takes 语料**配对（不依赖 hybrid_search retrieval），所以 MVP 自身零新引擎方法——这才是对「复用基础设施」的正确理解（复用 corpus，不是复用 cross_modal judge）。CLI 动词 `eval-suspected-contradictions`（run/trend/review，仅 run 实装）+ 6 类 verdict/severity 分类法 + judge-errors 一等公民 + 空语料诚实 Err。
- **已交付文件**：`crates/zbrain-core/src/eval/contradictions.rs`（新，CRLF 22906B）+ `eval/mod.rs`（pub mod）+ `zbrain-cli/src/lib.rs`（枚举/Args/dispatch/命令函数/RENAMED 重定向/2 解析测试）+ 从 UNPORTED 移除 suspected-contradictions。
- **验证**：`cargo test -p zbrain-core --lib contradictions` 4 passed（含空语料 Err、judge-error 计入分母、无 key 评分、surfaced finding）；`cargo test -p zbrain-cli --lib suspected_contradictions` 2 passed。CRLF 已对齐（contradictions.rs 由 LF 转 CRLF，numstat lib.rs 326/1 / mod.rs 1/0 局部安全）。
- **已修正路线图 `1-1-5-4` 注记**：删去错误的「复用 cross_modal 收敛」描述，改为 MVP 已交付内容 + deferred 依赖（hybrid_search/listActiveTakesForPages）+ TS 真实体量。诚实地图优先于早期乐观假设。
- **教训**：路线图的「收敛/复用」判断必须基于「MVP 实际用了什么」，而非「将来完整系统要什么」。#62 完整系统仍需 hybrid_search 等，但其 MVP 可绕过——这仍是收敛的，但原因不同（复用 corpus，非复用 judge panel）。

## 仓库健康（延续 2026-08-11 重建）
- zbrain / zbrain-clean 均 = `2fdcefd1`，fetch+pull 已对齐 origin/rust-rewrite（Already up to date），fsck 干净。
- 注意原生 git 不认 `/c/...` POSIX 路径 → 一律 `cd` 进目录再 `git`。

## #62 retrieval 配对延伸（PairingMode::Retrieval，hybrid_search 移植）
- 把 MVP 的 deferred retrieval 配对路径正式 port 进来，作为 #62 的延伸。`crates/zbrain-core/src/eval/contradictions.rs` 新增 `PairingMode::{Corpus,Retrieval}` + `RetrievalHit` + 纯函数 `build_retrieval_pairs`（cross 优先再 intra，respect cap，继承 query）；`run()` 按 `opts.pairing` 分派。
- TS 在 chunk 级配对（hybridSearch→chunk + listActiveTakesForPages）；Rust `hybrid_search` 已存在但返回**页面级** `SearchResult`，faithful 适配为页面级：RetrievalCross（page compiled_truth×page）+ RetrievalIntra（page compiled_truth×该页 takes，mirrors TS intra_page_chunk_take）。
- 引擎缺口用 `TakesListOpts.page_ids: Option<Vec<u64>>` 兜底（faithful port of TS `listActiveTakesForPages`），三后端全覆盖：libsql（`page_id IN (...)` 动态占位）、postgres（`$9::bigint[] ANY`）、InMemory（filter）。operation.rs 显式字面量补 `page_ids: None`（防止 E0063 缺字段）。
- CLI `eval-suspected-contradictions run` 新增 `--pairing retrieval|corpus`（默认 corpus）、`--queries <csv>`、`--top-k N`（默认 5）。
- 诚实降级：无 embedding provider 时 hybrid_search fail-open 降级 keyword-only，retrieval 模式离线可用；judge-errors 仍计入分母（不伪 PASS）。
- 验证（离线：`unset` 代理 + `--offline` + `CARGO_TARGET_DIR=.../zb_clean_target`）：contradictions 6 tests（含 2 retrieval 新测 `build_retrieval_pairs_builds_cross_and_intra` / `run_retrieval_pairs_without_api_key`）+ cli 1 解析新测 `eval_suspected_contradictions_retrieval_flags_parse`，全绿。CRLF numstat 无 N/N（contradictions.rs 351/25、lib.rs 62/2 等局部安全）。
- 提交 `5c8a5967` 已 push 到 `origin/rust-rewrite`。路线图 `1-1-5-4` json 注记 + md 当前施工段：retrieval 从 DEFERRED 移除、记入已 port；trend/review 仍 deferred。KNOWN-GAPS 无 #62 retrieval 专属条目（grep 命中均为 G58/G67/G74 等 incidental retrieval 字样），未动。
- 清理：移除会话期 scratch 目录 `.tmp_ts_src/`（TS 长记忆 eval 参考导出 + test_build.log），未纳入提交。

## zbrain-clean 备份 clone 废弃（用户决策）
- 用户决定删除 `zbrain-clean`（第二份 clone），不再保留；需要时从 `origin/rust-rewrite` 重新 `git clone`。
- **影响**：`docs/CROSS_OS_AGENT_GUIDE.md` §0 的"双 clone 互为健康基准 + 原地重建 zbrain"恢复流程已过时、不可再信。当前仅 `zbrain` 一个活跃工作副本；恢复靠重新克隆，而非 zbrain-clean。该约定已写入 MEMORY.md「其他」段。
- 注意：上面「仓库健康」条目（line 18）的 `zbrain / zbrain-clean 均 = 2fdcefd1` 已成为历史——zbrain 已前进到 `5c8a5967`，且 zbrain-clean 即将被删。
- 顺带把 `docs/CROSS_OS_AGENT_GUIDE.md` 的过时 §0/§2 恢复流程改写：移除对 zbrain-clean 的依赖，改为"单一 clone + 从 origin/rust-rewrite 重新克隆恢复"。提交 `19bc5527` 已 push（首次 push 因代理/SSH 抖动失败，unset 代理后重试成功）。diff 6/5 局部安全，行尾符保持 LF。

## 本地目录改名 zbrain → ZBrain（用户决策，自行操作）
- 用户要求本地目录 `/c/workspace/github/jununfly/zbrain` 改名为 `ZBrain`（大写 B，对齐 GitHub 仓库 slug `jununfly/ZBrain`）。范围仅限**本地目录**，不含 crate 名（Rust 约定必须小写、crates.io 不允许大写，不动）也不含 remote。
- **为何不让我在会话内 mv**：① Windows NTFS 大小写不敏感，`zbrain`→`ZBrain` 是纯大小写改名，需两步（先→临时名再→ZBrain）；② IDE 锁仓库根会 EBUSY；③ 本会话 cwd 与 `.workbuddy` 记忆路径绑定在该目录，改名会搞断会话。故由用户关 IDE 后在文件管理器/cmd 自行改名（同删 zbrain-clean）。
- **改名后待办（用户确认后我再做）**：① `docs/CROSS_OS_AGENT_GUIDE.md` 里硬编码的绝对路径 `/c/workspace/github/jununfly/zbrain` 需批量改为 `ZBrain`；② `Cargo.toml` 的 `repository = "https://github.com/jununfly/zbrain"` 小写 URL 可顺手改对（零风险）；③ 在 WorkBuddy 里用新路径 `ZBrain` 重新打开项目。
- 注：`Cargo.toml` 的 `repository` 字段小写 URL 与 crate 名 `zbrain-*` 均按 Rust 惯例保持小写，无需对齐大小写。
