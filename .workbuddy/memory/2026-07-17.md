# 2026-07-17

## 1-6-4-6 whoknows 命令迁移完成（Task 125, commit 1193412）

TS `src/commands/whoknows.ts` 专家路由命令迁入 Rust。用户拍板**方案B**（扩 SearchOpts 求精确 parity，非命令层取巧）。

**改动点：**
- `engine.rs` `SearchOpts` 新增 `types: Option<Vec<String>>`（page-type 白名单）+ `disable_salience_boost`/`disable_recency_boost: bool`（gate 掉 fuse_and_boost 的 salience/recency boost，对齐 TS `salience:'off',recency:'off'`）。
- `fuse_and_boost` 两处改造：①候选索引处按 `opts.types` 过滤（三后端共享，不碰各后端 search_pages SQL，budget 与 TS SQL 级过滤等价）；②两个 boost stage 用 `!opts.disable_*` 包裹，关闭时跳过 `get_salience_scores`/`get_effective_dates` round-trip。
- 所有显式枚举的 `SearchOpts{}` literal（operation.rs 两处 + engine.rs test 模块 10 处）改为 `..Default::default()`。
- 新 `whoknows.rs`：`rank_candidates`（纯函数，expertise=ln(1+raw)×recency_decay×salience_factor，recency 180d 半衰期 floor 0.1，salience 居中 0.5）+ `find_experts`（search 带 boosts-off + type 过滤 → 批量取 salience/effective_date → rank）+ CLI `zbrain whoknows [--json|--explain]`。
- `iso8601_to_unix_ms` 升为 `pub(crate)` 供 whoknows 复用日期解析。

**测试：** core lib 10 纯单测 + libsql 6 + postgres 4 集成全绿；共享 search 复归 14（libsql 8 + postgres 6）全绿，未扰动 1-6-4-2 的 search-revival 投资。core lib 1547 全绿。

**端到端 smoke：** `--help` 正确；空 brain → 人类 "(no person or company pages match...)" / JSON `[]`，无 panic。

**roadmap：** `1-6-4-6` completed + decide 记录方案B决策；render md。

**KNOWN-GAPS：** G49（capture `--type` flag 是 content-type 语义却被盖章进 page_type，person/company 落为 "note"——`lib.rs:2500` + `2534-2538` 解析分支，影响创建专家页供给，独立于 whoknows）+ G50（whoknows 两处 parity 差异：①Rust 按 boosted score 截 innerLimit，TS 按裸分，大网可忽略；②Rust 用 DEFAULT_TYPES=[person,company]，TS 从 active pack `expertTypesFromPack` 派生，schema-pack 消费者未迁）。

**踩坑/经验：**
- postgres 集成测试首次跑报 pg-embed 二进制下载被代理拦（502 cert mismatch），重试后通过——是偶发网络问题非代码问题；务必先 `use zbrain_core::engine::BrainEngine` 才能调 `put_page` trait 方法 + `&dyn BrainEngine` 强转。
- capture `--type` 是 pre-existing 正交 bug，**不在本任务范围**，未修（仅登记 G49）。用户指令"继续未完成"聚焦 whoknows 迁移，已闭环。

## 1-6-4-7 integrity check 命令迁移完成（commit 12fb322）

TS `src/commands/integrity.ts` 的 `check` 只读扫描子命令迁入 Rust。选它因 Explore agent 评估为**最干净孤儿命令**：零 AI 网关耦合、引擎原语全齐（list_all_page_refs + get_page + Page.compiled_truth/frontmatter）、近机械 150 行 port。

- **`crates/zbrain-core/src/integrity.rs`**：纯函数 `find_bare_tweet_hits`（围栏 ```/~~~ 跳过 + tweet-URL 邻近跳过 + `via X` guard）+ `find_external_links`（MD link 捕获）+ `scan_integrity`（枚举 + `validate:false` 祖父跳过 + `--type` slug 前缀过滤 + top_pages 汇总），结果/命中结构 camelCase serde。
- **CLI** `zbrain integrity [--json|--type|--limit]`，引擎接线镜像 whoknows（home PGLite + connect）。
- **测试**：core 12 单测（detector 边界）+ libsql 6 + postgres 4 集成全绿；core lib 1557 不变。
- **端到端 smoke**：空 brain→`Scanned 0`；播种页→1 bare+1 external；cited tweet 行正确跳过；JSON 良构。再次证明命令在生产后端**活**（非 trait 默认 unsupported 死路）。

**踩坑/经验：**
- `regex` crate **不支持 look-around**（look-ahead/look-behind）→ TS `via X(?!\s*/)` 的负面前瞻必须在代码里 guard（`phrase=="via X"` 且匹配后字符非 `/`）。`(?i)` 大小写、`(?:...)` 非捕获组都支持。
- `crate::Result` 是 `std::result::Result<T, StructuredError>`（error.rs:140），zbrain-core **无 `anyhow` 直接依赖**——异步 fn 返回类型用 `crate::Result` 而非 `anyhow::Result`。`Page` 定义在 `engine.rs:70`（非 types.rs），`PageRef` 在 types.rs:144。
- `integrity check` 路径被 `zbrain doctor` 采样 integrity 信号调用，本次迁移顺带解锁该信号（之前 TS getPage 路径）。

**KNOWN-GAPS：** G51（integrity 仅迁 check，`auto`/`review`/`reset-progress` 押后——依赖未迁 resolver SDK `x_handle_to_tweet`/`url_reachable` + BrainWriter 逃生舱禁令区 G39）。已登记。

**Parity_GATE 提示：** 1-6-4 下 1-6-4-4（features auto-fix，blocked 于 embed --stale + extract links/timeline CLI）、1-6-5（PARITY_GATE）仍 pending。下一个干净孤儿命令候选（按 Explore 评估）：storage(b+) / publish(b) / resolvers(c+ 需新 resolver 子系统) / check-resolvable(c+ 需 skill-tree 静态分析)。recall/forget 因依赖 AI 网关 + 4 个 facts 读方法，暂不适合。

## 1-6-4-4 features auto-fix 设计 grill 完成（zj-grill-me + zj-roadmap-driven）

用两 skill 对阻塞节点 1-6-4-4 做了完整设计 grill，落地 5 决策并重构节点。

**地形发现（探查代码，非凭空）：**
- Rust `features` 纯引擎+CLI(--json) 已完整（1-6-4-1~3），唯缺 `execute_auto_fix`；`--auto-fix` 故意未接。
- **关键反转**：Rust 存储已重架构为 page-level——`libsql.rs:4384/4466` 明确 "NO content_chunks / timeline_entries tables"；`pages.embedding` 是每页一个 BLOB 向量(G24)，`get_health().missing_embeddings` 数 null-embedding 的**页**；timeline 是 `pages.timeline` TEXT 列；`upsert_chunks`/`get_chunks_for_page` 在 libsql/postgres 完全未实现(仅InMemory桩)→ `import_from_content` 调 `upsert_chunks` 但未落地，真实库导入不持久化 chunk(#110残骸)。`add_links_batch` 已实现。
- 故 TS 的 "embed --stale(重嵌chunk)" 在 Rust 应理解为"重嵌页面"，且 features recs 已基于 page-level。

**5 决策（roadmap 1-6-4-4 已 decide + 渲染）：**
- Q1 范围：引擎地基先行（本刀只补引擎原语，库函数+CLI接线拆子节点）。
- Q2 建模：Rust-native page-level（embed --stale=枚举null页→EmbeddingClient→put_page回写；extract timeline=追加pages.timeline TEXT；extract links=现有add_links_batch）。chunk-level(建content_chunks/timeline_entries表+chunk embed+chunk搜索重架构+health重算+存量回填)定为**独立epic**，不塞进本刀。
- Q3 嵌回机制：专用引擎方法 `list_stale_pages`(SQL WHERE embedding IS NULL)+`put_page_embedding`(单条UPDATE)。
- Q4 timeline：智能 trait 方法 `add_timeline_entry(slug,source_id,date,summary,detail)` (get_page→解析→插入→put_page)。
- Q5 结构：地基=父节点本体 + 4 行为子节点 1-6-4-4-1 embed_stale / 1-6-4-4-2 extract_links / 1-6-4-4-3 extract_timeline / 1-6-4-4-4 features --auto-fix CLI接线。**不做独立 embed/extract CLI**。

**节点状态：** 1-6-4-4 标签改为 "features auto-fix 引擎地基 (page-level: list_stale_pages + put_page_embedding + add_timeline_entry + link 解析器)"，status=in_progress，mode=exploit；4 子节点 pending。

**下一步：** 本刀(1-6-4-4 地基)实现 = 三引擎方法(list_stale_pages / put_page_embedding / add_timeline_entry，三后端)+ link 解析器纯模块。完成后转 1-6-4-4-1..4 库函数与CLI接线。

**经验/铁律：** roadmap 用 roadmap_cli.py（禁手改JSON/md）；Grill: 能读代码不凭空问——本刀靠探查戳穿了"缺chunk原语"的误设，发现是 page-level 重架构。路径用引号Windows反斜杠绕过 Git Bash 路径转换坑。

## 1-6-4-4 引擎地基 TDD 开始（垂直切片）

用 /zj-tdd 启动地基实现。规划 4 个行为垂直切片（一个行为→一个失败测试→一个实现→重复）：
1. `list_stale_pages() -> Result<Vec<Page>>`（枚举 live+null-embedding 页）
2. `put_page_embedding(slug, source_id, Vec<u8>) -> Result<()>`（单列表 UPDATE）
3. `set_page_timeline(slug, source_id, String)`（外科 UPDATE）+ `add_timeline_entry(slug, source_id, entry)`（默认:get_page→解析→追加→set）+ 纯 parse/format_timeline
4. `extract_markdown_links(text) -> Vec<(text,url)>` 纯函数（移植 TS link-extraction）

**Cycle 1 状态（进行中）：** trait 方法已加(默认 Unsupported)；InMemory + libsql + postgres 三后端已实现；inmemory/libsql 集成测试全绿(3 passed)。postgres 集成测试后台跑（首次下载 pg-embed 二进制，较慢）。测试文件：tests/{inmemory,libsql,postgres}_features_foundation.rs。

## 1-6-4-4 引擎地基 DONE（TDD 垂直切片，2026-07-17）
地基全部完成并三后端验证。新增：
- trait 方法（engine.rs）：list_stale_pages / put_page_embedding(slug,source_id,Vec<u8>) / set_page_timeline / add_timeline_entry（默认 get_page→解析→追加→set_page_timeline）。
- 三后端实现：InMemory（直写 store）+ libsql（full_row_to_page + 外科 UPDATE pages SET embedding/timeline）+ postgres（row_to_page 同源 SQL）。
- 纯模块 markdown_links.rs：extract_markdown_links(content)->Vec<(name,relTarget)>，移植 TS extractMarkdownLinks（[text](x.md) + [[wiki]] 两语法，跳过 :// 外部URL，wiki 补 .md / 去 #anchor / 取 |display）。已 pub mod 注册。
- 测试：tests/{inmemory,libsql,postgres}_features_foundation.rs（2+4+3=9 passed）+ markdown_links 单测（9 passed）。
- 关键细节：put_page_embedding 取 Vec<u8>（f32-LE BLOB，与 Page.embedding 同构），调用方(embed --stale)负责 f32→bytes；TS 的 display name 保留 #anchor（仅 rel_target 去 #），md-first 排序——忠实移植非修复。
- 路线图 1-6-4-4 标记 completed；子节点 1-6-4-4-1..4 仍 pending（库函数+CLI 接线）。chunk-level 建模定为独立 epic。
- 未提交（待 commit）。

## 1-6-4-4-1 embed_stale 库函数 DONE（2026-07-17，TDD）
- 新模块 crates/zbrain-core/src/auto_fix.rs（pub mod 已注册），主函数 embed_stale(engine: &dyn BrainEngine, client: &EmbeddingClient, opts: &EmbedStaleOpts) -> Result<EmbedStaleResult{total,embedded,would_embed,skipped}>。
- 文本取 page.compiled_truth，空则 title 兜底，皆空则 skip（无嵌入文本不写）。支持 dry_run + source_id 过滤（list_stale_pages 后 retain 过滤）。
- 嵌入用 EmbeddingClient::embed() 得 Vec<f32>，f32 LE bytes 后 put_page_embedding 回写。EmbeddingError 无 From<StructuredError>，手动 map_err。
- 测试：auto_fix.rs 4 单测（ConstProvider 假 EmbeddingProvider，无网络）+ libsql_auto_fix_embed.rs 2 + postgres_auto_fix_embed.rs 2 = 8 passed，三后端全绿。
- 关键：EmbeddingConfig::default() dims=1280；假 provider 须返回 config.dimensions 长度向量否则 embed() 报 DimensionMismatch。MockEmbeddingProvider 是私有，测试自实现 EmbeddingProvider trait（pub）。
- 路线图 1-6-4-4-1 标 completed。下一步 1-6-4-4-2 extract_links（markdown_links 解析器已就绪→add_links_batch）。

## 1-6-4-4-2 extract_links 库函数 DONE（2026-07-17，TDD）
- auto_fix.rs 新增 extract_links(engine, &ExtractLinksOpts{slug:Option<String>}) -> Result<ExtractLinksResult{pages_processed,links_created,dangling}>。
- 流程：list_all_page_refs 建 slug->source 集合 → 枚举页面(slug 指定则单页，否则全部) → extract_markdown_links(compiled_truth) → resolve_link_slug(去 .md / 去 source: 前缀 / 查 slug 集合，page-level 无 fileDir，等价于 TS resolveSlug 空 fileDir) → 跳过悬空(无匹配slug)/自链接 → add_links_batch 批写(LINK_BATCH_SIZE=200)。link_type=None(link_source="markdown")。
- 测试：auto_fix.rs 5 单测(解析存在slug/跳过悬空/跳过自链接/单页scope/markdown语法) + libsql 2 + postgres 2。全绿。
- 后续 1-6-4-4-3 extract_timeline（待做：parse timeline entries + add_timeline_entry）、1-6-4-4-4 features --auto-fix CLI 接线。

## 1-6-4-4-3 extract_timeline 库函数 DONE（2026-07-17，TDD）
- auto_fix.rs 新增 extract_timeline(engine, &ExtractTimelineOpts{slug}) + 纯解析器 extract_timeline_entries(content)->Vec<TimelineEntry{date,summary}>。
- 移植TS两格式：bullet `- **YYYY-MM-DD** | Source — Summary`（取date+summary，丢source）、header `### YYYY-MM-DD — Title`（取date+title，丢detail），顺序与TS一致。LazyLock 两个 Regex。
- 每条格式化为 "{date} {summary}" 单行，对页面已有 timeline 行去重后 add_timeline_entry 追加（幂等，重跑 entries_added=0）。
- 测试：auto_fix.rs 4 单测 + libsql 2 + postgres 2。全绿。累计 auto_fix 共 25 测试(单测13+libsql6+pg6)。
- 剩 1-6-4-4-4 features --auto-fix CLI 接线（execute_auto_fix + accepted 记账 + --json auto_fix_results）。三个库函数 embed_stale/extract_links/extract_timeline 均已就绪。

## 1-6-4-4-4 features --auto-fix CLI 接线 DONE（2026-07-17）
- crates/zbrain-cli/src/lib.rs：FeaturesArgs 加 `auto_fix: bool`；run_features_command 重写加 auto-fix 分支（调 run_auto_fix + auto_fixable recs 记入 offers.accepted 账本 + --json 输出加 `auto_fix` 字段 + human 打印 links/timeline/embedded 统计）；新增 AutoFixResults{embedding_enabled,embedded,links_created,timeline_entries_added} + run_auto_fix(engine) 助手（调 extract_links/extract_timeline + EmbeddingClient::from_env()→embed_stale，无 key 则 skip）。
- E2E smoke（真实 zbrain.exe 子进程 + 播种 libsql brain）：RUN1 --json → links_created=1, timeline_entries_added=1, embedded=0, embedding_enabled=false；RUN2 human 模式 exit 0 无 panic；RUN3 幂等重跑 links=0/timeline=0（不重复不崩）；feature-offers.json 账本记 missing-embeddings accepted。CLI Cargo 已启 features=["rerank","embedding"]，from_env 可用。
- 注意：extract_links 不自身去重、靠 add_links_batch 幂等/唯一约束；重跑靠 timeline 去重 + link 唯一约束不崩。embedding_enabled 路径（设 ZEROENTROPY_API_KEY）依赖真实网络 provider，CLI smoke 只验 fail-open 无 key 路径；embed 写入路径由 libsql/postgres 集成测试(fake provider)覆盖。
- 路线图 1-6-4-4-4 + 1-6-4-4 全 completed + render。1-6-4-4 整刀收口（4/4 子节点）。

## 1-6-4-4 整刀收口结论（2026-07-17）
1-6-4-4（features auto-fix，4/4 子节点全 completed）已收口。后续：1-6-5 PARITY_GATE pending；下一个干净孤儿命令候选（前序评估）：storage/publish/resolvers/check-resolvable。

## 1-6-4-8 storage status 命令迁移完成（2026-07-17，TDD + 干净孤儿）

选中 `storage` 作为下一个干净孤儿命令（候选排序 storage(b+) > publish(b) > resolvers(c+ 需新 resolver 子系统) > check-resolvable(c+ 需 skill-tree 静态分析)）。

**改动点：**
- `crates/zbrain-core/src/storage_status.rs`（新建，pub mod 已注册 lib.rs）：忠实移植 TS `src/commands/storage.ts` 的 `status` 子命令。
  - 类型：`StorageConfig`/`StorageTier`(DbTracked/DbOnly/Unspecified)/`PageCountsByTier`/`DiskUsageByTier`/`MissingFile`/`StorageStatusResult`/`DiskFileEntry`。
  - 配置解析：`parse_storage_yaml` 手写行扫描（忠实 TS 零依赖解析器，非 serde_yaml，避免 gray-matter 沉默失败）+ `normalize_storage_config`（补 `git_tracked`/`supabase_only` 弃用别名）+ `validate_storage_config`（overlap + 缺尾斜杠 warning）+ `normalize_and_validate_storage_config`（自动补 / + overlap 抛 StorageConfigError）+ `matches_tier_dir`（路径段精确前缀，防 `media/xerox` 误匹配）+ `get_storage_tier` + `load_storage_config`。
  - `walk_brain_repo`（walkdir 跳过 dot-dir/node_modules，slug=相对路径去 `.md` + `\`→`/`）+ `get_storage_status(engine, repo_path)`（list_pages 三后端全实现）+ `format_storage_status_json`/`format_storage_status_human`/`format_bytes`。
  - 单测 21 个全绿（config parser 9 + walk 1 + status 3 + formatter 4 + overlap 1 + tiers 1 + no-repo 1 + missing-files 1）。**踩坑**：tempfile 默认 `.tmp` 前缀目录被 dot-dir 过滤整棵排除 → 改用 `tempfile::Builder::new().prefix("zbs_").tempdir()`；`validate_storage_config` 不归一化故 overlap 需精确串匹配（测两者要同串）；serde pretty 输出 `"db_tracked": 1` 含空格。
- `crates/zbrain-cli/src/lib.rs`：`Commands::Storage(StorageArgs)`（subcommand default "status" / `--repo` / `--json`）+ `run_storage_command`（home Libsql 引擎 → EngineKind::Libsql 才 stderr tty 警告 tiering-limited → `--repo` 否则 `config.sync.default_repo` → `get_storage_status` → json/human；unknown subcommand `anyhow::bail!` exit 1）。

**E2E smoke（真实 zbrain.exe 子进程 + 播种 libsql brain + temp repo）：** 4 页（notes/kept, archive/lost, other/free, people/alice）+ zbrain.yml(db_tracked: notes/, db_only: archive/) + archive/lost.md 故意缺失。
- human：Total 4 / DB tracked 1 / DB only 1 / Unspecified 2 / Disk DB tracked 14B("kept note body")+Unspecified 19B("free body"9+"alice body"10) / Missing Files: archive/lost / 配置段正确。
- JSON：total_pages 4、pages_by_tier{db_tracked:1,db_only:1,unspecified:2}、missing_files=[{slug:"archive/lost",expected_path:...}]、disk_usage_by_tier{db_tracked:14,db_only:0,unspecified:19}、warnings:[] 全对。
- 边界：`storage bogus` → "Unknown storage subcommand: bogus" exit 1。

**关键发现（E2E 踩坑，重要）：** `storage` 硬编码 `zbrain_home().join("brain.pglite")`；而 put-page 等用 `resolve_database_path(config.database_url)`（默认 `sqlite://~/.zbrain/zbrain.db`）。若无 config，`storage` 读 brain.pglite、`put-page` 读 zbrain.db → **两库不一致**。经 `zbrain init --pglite` 后 config.database_url 指向 brain.pglite 才一致。E2E 须先 `init --pglite` 引导 schema 迁移（否则 "no such table: pages"），且 `ZBRAIN_HOME` 必须传 **Windows 原生路径**（如 `C:\Users\...`）而非 Git Bash `/c/Users/...`，否则 home_root 字面化找不到 .zbrain/config。

**路线图：** 1-6-4-8 + 1-6-4-8-1..4 全 completed + render md。clean-orphan 策略验证成功（零新依赖、零 AI/网络、list_pages 三后端复用）。

**下一步候选（按 Explore 评估剩余）：** publish(b，需新增 markdown 渲染器 + AES-GCM/PBKDF2 两类 crate) / resolvers(c+，依赖未迁 resolver SDK) / check-resolvable(c+，依赖未迁 skill-tree 静态分析)。

## 1-6-4-9 publish 命令迁移完成（2026-07-17，TDD + 干净孤儿）

`publish` 是三个候选(resolvers/check-resolvable 被未迁底层 core 阻塞)中**唯一当下可迁项**。用户拍板**「Rust 渲染」方案**：服务端 pulldown-cmark 渲染成静态 HTML，去掉 TS 的 marked.js 浏览器端渲染（非 vendor marked）。

**新增 crate（打破 clean-orphan 惯例）：** `aes-gcm` + `pbkdf2`(+`hmac`) + `pulldown-cmark`（zbrain-core/Cargo.toml；rand/sha2/base64/regex 已存在）。

**改动点：**
- `crates/zbrain-core/src/publish.rs`（新建，pub mod 已注册 lib.rs）：
  - `make_shareable`（正则清洗流水线，忠实 TS `src/commands/publish.ts` 顺序：frontmatter→`[Source:...]`→confirmation→cross-link 留显示文本→`See also:`→Timeline→折叠空行→trim）。**parity 校验**：`\s*\[Source:...\]` 对 `"See [Source: x] more"` 输出 `"See more"`（JS 同），最初断言误写 "Seemore" 已改。
  - `extract_title`（取 raw H1，与 TS 一致 frontmatter 用 `---` 非 `#`）、`render_markdown`（pulldown-cmark）、`encrypt_content`/`decrypt_content`（AES-256-GCM + PBKDF2 100k SHA-256，**wire format 与 TS 兼容**：salt16/iv12/ct||authTag base64）、`generate_password`（16 位去歧义字符集）、`sanitize_html`（去 script/iframe/object/embed/form + on* 属性 + javascript:）、`generate_html`（静态 HTML 模板，密码 overlay + `__SALT/__IV/__CT` 变量 + 内嵌 DECRYPT_JS 直接 `innerHTML=decrypt(...)` 无 marked）。
  - 单测 **17 全绿**（content strip 7 + title 2 + render 1 + encrypt/decrypt 3 + password 1 + generate_html 2 + xss 1）。**踩坑**：Rust 2021 保留 `"#` 前缀字符串字面量 → 测试中 `"# Hello"`/String::from("#") 需改用 `push('#')`；raw string `r#"..."#` 闭合误写 `"` 导致吞掉后续常量（CSS/PASSWORD_CSS/SANITIZE_JS/DECRYPT_JS 四处）；`replace` 用 `Regex::new().replace_all()`；`generate_password` 需 `use rand::Rng` 取 `.gen()`。
- `crates/zbrain-cli/src/lib.rs`：`Commands::Publish(PublishArgs)`（input required / `--password` `num_args=0..=1,default_missing_value=""` / `--title` / `--out`）+ `run_publish_command`（读 input→make_shareable→extract_title(raw 或 --title)→render_markdown→`--password` 无值 generate_password(16) 自生成并打印、有值原样、缺则明文→encrypt_content(已渲染HTML)→generate_html→写文件(默认 `<stem>.html` 同目录，`create_dir_all` 父目录)）。

**E2E smoke（真实 zbrain.exe）：**
- 明文：渲染 `<h1>Quarterly Update</h1>`/`<strong>`、bracketed `[Source:...]` 剥离、confirmation 红挡为 "Confirmation: on file"、无 marked.js、无 `__SALT`、无 inline handler 全过。
- 加密：`__SALT`/`__IV`/`__CT` + password overlay + 无 marked。**node 按同 AES-256-GCM+PBKDF2 解密 `__CT` 得到 `<h1>...</h1><p>...<ul>...` 已渲染 HTML**（非原始 markdown）→ 证明 wire 兼容 + 「加密已渲染 HTML」设计成立。
- 边缘：`--password`(无值) 自生成密码打印、`--out`+`--title` 覆盖生效、缺输入文件 exit 1 全过。

**有意分叉（用户选定）：** 加密明文是**已渲染 HTML**（非 TS 的清洗后 markdown），浏览器侧只 `innerHTML=sanitizeHtml(decrypt(...))`，全程无 JS 渲染器；wire format 与 TS `encryptContent` 兼容故内嵌 DECRYPT_JS 无需改。

**路线图：** 1-6-4-9 + 1-6-4-9-1..4 全 completed + 4 decisions（渲染方式/加密明文/新增crate/密码标题语义）+ render md（depth=2 树中 1-6-4 显示 `[x]` completed，叶子不展开）。

**下一个候选：** `resolvers`(c+，依赖未迁 Resolver SDK，含网络 X API resolver) / `check-resolvable`(c+，依赖未迁 skill-tree 静态分析 core + `--fix` 写回 SKILL.md 非只读) 仍阻塞；`publish` 已收口，clean-orphan 命令线暂告段落。

## resolvers 迁移设计 grill 完成（zj-grill-me + zj-roadmap-driven，2026-07-17）

用两 skill 对 `resolvers` 命令（1-6-4-10）做了完整设计 grill，落地 7 决策并建 4 子节点。

**地形发现（探查代码，非凭空）：**
- `resolvers` 命令(195 行, list/describe) 本身轻量，但阻塞在 `src/core/resolvers/*` 整体(~1109 行: interface 158 + registry 151 + url-reachable 332 + x-api/handle-to-tweet 442 + index 26)。Rust 侧**零** resolver SDK 基础。
- **url-reachable 可直接复用** `zbrain-core/src/url_safety.rs::is_internal_url`（已忠实 port TS `integrations.ts` 的 SSRF 防护，含 IPv4/IPv6/RFC1918/metadata/CGNAT/hex绕过）——不重迁。这同时解锁 integrity G51 的 auto 路径。
- `reqwest` 已是 `zbrain-core` 的 **optional** 依赖（openai/anthropic/embedding/rerank feature 后用），且仓库已有 **trait+mock+feature-gated live** 模式（embedding.rs::HttpEmbeddingProvider / rerank_client.rs / chat.rs::MockLlmClient）——x-api 照此建 HttpClient trait 即可。

**7 决策（roadmap 1-6-4-10 已 decide + 4 子节点 + render）：**
- Q1 选哪个命令：resolvers（check-resolvable ~2300 行 + --fix 改写用户文件风险高，留后续）。
- Q2 切片深度：**全量迁**（含 x-api，不 defer）。x-api 网络路径沙箱无 token 不可真跑，靠注入 HttpClient trait + MockHttpClient 离线测试。
- Q3 网络架构：**trait 在 core + live 在 core feature**（新 `resolvers` feature 复用已有 optional reqwest；`register_builtin_resolvers()` feature 开启时建 ReqwestHttpClient，测试用 `with_client(mock)` 注入；token 在 resolve() 内读 `ctx.secret("X_API_BEARER_TOKEN")`）。
- Q4 模块布局：`zbrain-core/src/resolvers/{mod,interface,registry,url_reachable,x_api,http}.rs`，lib.rs 加 `pub mod resolvers`。
- Q5 CLI parity：1:1 镜像 `zbrain resolvers list [--json] [--cost free|rate-limited|paid] [--backend <b>]` + `describe <id>`；unknown subcommand exit 1。
- Q6 测试：TDD 垂直切片，忠实 port TS `tests/unit/resolvers.test.ts`(717 行) 离线部分（registry 契约/url-reachable SSRF/x-api 纯函数+错误码），全用 MockHttpClient；CI 防漂移锚点同步清空。
- Q7 注册/secret/feature：总是注册两个 resolver；`ResolverContext.secret(name)` 由闭包提供（运行时 env+config，测试注入）；无 token → ResolverError(Config)；模块常编译，仅 live 路径在 `resolvers` feature 后。

**节点状态：** 1-6-4-10 (in_progress, exploit) + 1-6-4-10-1(SDK核心)/-2(url-reachable)/-3(x-api)/-4(CLI) 均 pending。第一轮起刀顺序：**1-6-4-10-1 SDK 核心(tracer bullet)** → 2 url-reachable(复用 is_internal_url) → 3 x-api(打分纯函数先 + HttpClient trait + ReqwestHttpClient feature) → 4 CLI 接线 + E2E。

**经验/铁律：** grill 能读代码不凭空问——本刀靠探查戳穿"需重迁 SSRF"误设（url_safety 已就绪）、确认 reqwest optional 现状，避免无谓新增依赖。check-resolvable 仍留待 resolver 线稳定后或单独开。

**更正（Q7 笔误）：** 上条「无 token → ResolverError(Config)」是错的——TS `ResolverErrorCode` 无 `config` 变体；x-api 缺 token 实际映射 `ResolverError(Unavailable)`（TS 测试 `tests/unit/resolvers.test.ts:461` 断言 'unavailable'），`available()` 亦返回 false。roadmap 1-6-4-10 已补一条更正 decision 覆盖。

## 1-6-4-10-1 Resolver SDK 核心 DONE（2026-07-17，tracer bullet）

`resolvers` 第一刀：迁 SDK 核心（interface + registry + 类型 + ResolverError），零新依赖、零网络、零 CLI 改动。

**新增文件：** `crates/zbrain-core/src/resolvers/{mod.rs, interface.rs, registry.rs}`，`lib.rs` 加 `pub mod resolvers`。

**设计要点（忠实 port TS `src/core/resolvers/{interface,registry,index}.ts`）：**
- 类型擦除到 JSON 边界：`Resolver` trait 的 `input`/`value` 用 `serde_json::Value`（镜像 TS `Resolver<unknown, unknown>` 擦除），注册表存 `Arc<dyn Resolver>`。`ResolverRequest { input, context, timeout_ms }` / `ResolverResult { value, confidence, source, fetched_at: chrono::DateTime<Utc>, cost_estimate, raw }`。
- `ResolverContext { config, logger: Arc<dyn ResolverLogger>, request_id, remote, deadline, secret: Arc<dyn Fn(&str)->Option<String>+Send+Sync> }` — `secret` 闭包使 resolver 完全离线可测（测试注入返回 token 的闭包）。`ResolverLogger` trait 全默认 no-op + `NoopResolverLogger`。**`ResolverContext` 派生 `Clone` 但不 `Debug`**（`Arc<dyn Fn>`/`Arc<dyn ResolverLogger>` 非 Debug）→ `ResolverRequest` 也只能派生 `Clone`（测试里 `get(...).unwrap_err()` 会要 `Ok=Arc<dyn Resolver>: Debug` 失败，改 `match` 提取错误，不靠 unwrap_err）。
- `ResolverError { code: ResolverErrorCode, message, resolver_id, cause }` + `ResolverErrorCode` 9 变体（NOT_FOUND/ALREADY_REGISTERED/UNAVAILABLE/TIMEOUT/RATE_LIMITED/AUTH/SCHEMA/ABORTED/UPSTREAM，as_str 与 TS 完全相同，无 config）。手动 `Display`+`Error`（与 url_safety/rerank 一致）。
- `ResolverRegistry`：`register(&mut self)`（空 id→Schema / 重复→AlreadyRegistered）/ `get`（NotFound）/ `has` / `list(filter?)`（按 cost/backend 过滤 + id 升序）/ `resolve`（get→available(Unavailable)→resolve）/ `clear` / `size`。全局单例 `get_default_registry()` 返回 `MutexGuard<'static, ResolverRegistry>`（`OnceLock<Mutex<...>>`，因可变+共享只能返回 guard；测试用 reset_default_registry() 复位验证持久化）。

**测试：** `registry.rs` 13 单测全绿（starts_empty / register_get_has / duplicate-Schema / empty_id-Schema / get_not_found / clear / list_sorted / filter_by_cost / filter_by_backend / resolve_returns / resolve_not_found / resolve_unavailable / default_registry_persists）。core lib 编译无 error（仅既有 29 warning 非本刀引入）。

**下一步：** 1-6-4-10-2 url-reachable（复用 `url_safety::is_internal_url`，HEAD+逐跳重定向 SSRF 重校验+AbortSignal→aborted）。

## 1-6-4-10-2 url-reachable resolver DONE（2026-07-17，TDD）

`resolvers` 第二刀：迁 `url-reachable` HEAD-check resolver（332 行 TS），零新依赖，复用已有 `url_safety::is_internal_url`。

**新增文件：** `crates/zbrain-core/src/resolvers/{url_reachable.rs, http.rs, dns.rs}`；`interface.rs` 加 `abort: Arc<Notify>` 字段（Q7 原定 CancellationToken，实测改用 `tokio::sync::Notify`，更简单且无新依赖）；`mod.rs` 声明三新模块；`url_safety.rs` 加 `is_private_addr(IpAddr)->bool`（DNS-rebinding 防御用）。`Cargo.toml`：`tokio` 加 `macros` feature；新增 `resolvers` feature 门控 live `ReqwestHttpClient`/`LiveDnsResolver`。

**设计要点（忠实 port TS `builtin/url-reachable.ts`）：**
- `UrlReachableResolver { client: Arc<dyn HttpClient>, dns: Arc<dyn DnsResolver> }`，类型擦除边界同 SDK。
- **SSRF 不重迁**：直接调 `url_safety::is_internal_url(url)` 挡 internal/private/metadata/非 http(s)；逐跳重定向对新 URL 重新校验。
- **DNS-rebinding 防御**（port TS `checkDnsRebinding`）：对 hostname 解析 A/AAAA，逐条 `is_private_addr` 命中即阻断；IP literal 跳过 DNS。
- **传输 seam**：`HttpClient` trait（`send(HttpRequest)->Result<HttpResponse,HttpClientError>`，method=Head/Get/Post + timeout + abort: Option<Arc<Notify>>）+ `MockHttpClient`（always-compiled：`fixed_status` / 闭包 / error）+ `HttpResponse{status,headers,body}`。live `ReqwestHttpClient`（feature `resolvers`，`redirect=Policy::none` 手动跟链，select 兼听 abort）。`DnsResolver` trait + `MockDnsResolver` + `LiveDnsResolver`（`tokio::net::lookup_host`，零新依赖）。
- **HEAD→GET 回退**：405/501 重试用 GET（同 hop）。3xx 手动跟链 max 5 hops，每跳 SSRF+DNS 重校验。2xx/3xx→reachable=true，4xx/5xx→reachable=false 且 reason="HTTP {status}"。fetch 错误→reachable=false confidence=1 reason="fetch error:..."。
- **abort 落地（实测修正）**：`resolve()` 最开头用 `biased tokio::select { _ = notified => true, _ = ready(()) => false }` 先查 `req.context.abort.notified()`，预触发立即 `ResolverError(Aborted)`（忠实 TS `checkReachable` 开头检查；且避免 `MockHttpClient` 同步 ready 的 transport future 与 abort 在 `tokio::select!` 公平性下竞态——裸 select 随机选 send_fut 导致 abort 信号被吞）。逐跳循环内仍 select 兼听 abort 处理 in-flight 取消。`ResolverContext::new()` 默认 abort=独立未触发 Notify。

**测试：** `url_reachable.rs` 18 单测全绿（contract_id_cost_backend / available_is_true / blocks_localhost_ssrf / blocks_rfc1918 / blocks_aws_metadata / blocks_non_http_scheme / empty_url_throws_schema / status_200_reachable / status_404_unreachable_with_reason / head_405_falls_back_to_get / follows_redirect_to_external / blocks_redirect_to_internal_per_hop / network_failure_reachable_false_confidence_one / abort_signal_yields_aborted / dns_rebinding_{skips_ip_literals,unparseable_url_null,dns_failure_null,private_target_blocked}）。加 SDK 13 → **resolvers 共 31 全绿**；`url_safety` 31 测试无回归；默认 + `--features resolvers` 两 build 均通过（live 传输层编译 OK，为 slice 3 铺路）。

**踩坑：** 测试 `abort_signal_yields_aborted` 初版用 `resolver(200)` 返回的 notify 与 `input()` 内部另建 ctx 的 notify 是**两个不同实例** → notify_one 触发错对象 → abort 不生效 → reachable=true 误判。修：测试自建 ctx 并注入同源 `Arc<Notify>`。且裸 `tokio::select!` 公平性让 mock 同步 ready 的 send 赢了 abort 竞态 → 加 biased 开头检查根治。

**路线图：** 1-6-4-10-2 completed + 2 decision（abort 落地 / DNS-rebinding 防御）+ render。`--features resolvers` 验证 live 传输层编过（修正 `to_response` 非 async 却 `.await` 的 E0728 + `and_then(to_response)` 误用 → 改 `match fut.await`）。

**下一步：** 1-6-4-10-3 x-api（handle-to-tweet）：纯函数 computeBackoffMs/available/打分分桶先落 + `HttpClient` trait 已就绪 + `ReqwestHttpClient` feature 已编译过；`register_builtin_resolvers()` 可待两 resolver 都就绪后落地（slice 4 或 slice 3 末）。

## 1-6-4-10-3 x-api handle-to-tweet resolver DONE（2026-07-17，TDD）

`resolvers` 第三刀：迁 `x_handle_to_tweet` X API v2 recent-search resolver（442 行 TS），复用 slice 2 的 `HttpClient`/`HttpResponse` 传输层，零新依赖。

**新增/改文件：** `crates/zbrain-core/src/resolvers/x_api.rs`（新建，pub mod 已注册 mod.rs）；`registry.rs` 加 `register_builtin_resolvers(http: Arc<dyn HttpClient>, dns: Arc<dyn DnsResolver>)`（常编译，注册 url_reachable + x_handle_to_tweet 两 resolver，对应 TS commands/resolvers.ts）；`http.rs` 给 `HttpRequest` 加 `#[derive(Clone)]` + `HttpClientError` 加 `Display`（x_api 用 `{e}` 格式化）；`mod.rs` 加 `pub use x_api::XHandleToTweetResolver`。

**设计要点（忠实 port TS `builtin/x-api/handle-to-tweet.ts`）：**
- `XHandleToTweetResolver { client: Arc<dyn HttpClient> }`；`token(ctx)` 解析顺序 = `config.x_api_bearer_token` 优先 → `secret("X_API_BEARER_TOKEN")` 闭包（永不上日志）。`available()` = token.is_some()。`resolve()` 无 token → `ResolverError(Unavailable)`（订正 Q7 笔误，非 Config）。
- 输入校验：`HANDLE_RE = ^[A-Za-z0-9_]{1,15}$`（OnceLock 编译一次），不符 → `Schema`。`maxCandidates` 默认 10 夹 [1,25]。
- query 构建：`from:{handle}` + `sanitize_keywords(keywords)`（剥 X 运算符 `from:/to:/lang:/...` + shell 元字符，cap 200）→ `url.query_pairs_mut` 编码追加 `query`/`max_results`/`tweet.fields`，无字符串插值。
- 重试环：`for attempt in 0..=MAX_RETRIES_ON_429(2)`（3 次）；每次 `HttpClient::send(HttpRequest{Get, Authorization: Bearer, timeout, abort})`；429 且 attempt<2 → `compute_backoff_ms(&r, now)` 后 `sleep_backoff`（abort-aware select）→ continue；transport err → Aborted→`ResolverError(Aborted)`，否则 Upstream。
- 终态：401/403→Auth；429(退完)→RateLimited；!2xx→Upstream(含 body 前 200 字)；否则 parse `data[]` → 空则 confidence=0 candidates=[]。
- 打分：`score_match`（tokenize + STOP_WORDS 过滤 len<=2，命中率 [0,1]，无 keywords→0.5）→ candidates 按 score 降序 → `compute_confidence`（单候选 0.85/0.9/0.6；2-4 候选 margin 0.85/0.7/0.5；5+ 候选 dominant 0.75 / ambiguous 0.4），与 TS `zbrain integrity --auto` 三桶（>=0.8 auto / 0.5-0.8 review / <0.5 skip）对齐。
- **`confidence<0.5` 时 top 字段(url/tweet_id/text/created_at)整体省略 key**（匹配 TS `value.url=undefined`），而非置 JSON null——初版置 null 致 `many_ambiguous` 测试 `value.url` 为 `Some(Null)` 失败，改为条件插 Map key 修复。

**测试：** `x_api.rs` 29 单测全绿（available×3 / 输入校验×3 / zero+single_strong+single_weak+many_ambiguous / 401/403/500/429重试 / 注入防御 / score_match×3 / confidence×7 / backoff×3 / schemas）。加 SDK 13 + url_reachable 18 → **resolvers 共 60 全绿**；**zbrain-core 全 lib 1677 passed 0 failed**（--features resolvers build 亦通过，live 传输层就绪）。

**路线图：** 1-6-4-10-3 completed + 2 decision（x-api 打分/注入/退避落地 / register_builtin_resolvers 落点）+ render。

**下一步：** 1-6-4-10-4 CLI 接线（`zbrain resolvers list [--json|--cost|--backend] + describe <id>`，启动时注入 `ReqwestHttpClient` + `LiveDnsResolver` 调 `register_builtin_resolvers`）+ E2E smoke。

## 1-6-4-10-4 resolvers CLI 接线 DONE（2026-07-17，1:1 parity TS）

`resolvers` 第四刀（收口刀）：CLI 接线 + E2E smoke，1-6-4-10 整刀完成。

**改动点（仅 CLI，core 在 slice 1-3 已就绪）：**
- `crates/zbrain-cli/Cargo.toml`：zbrain-core 依赖加 `features = ["rerank", "embedding", "resolvers"]`（开 live `ReqwestHttpClient`+`TokioDnsResolver`）。
- `crates/zbrain-cli/src/lib.rs`：`Commands::Resolvers(ResolversArgs)`（subcommand 用 `Option<ResolversSub>`：无子命令→help exit 0）+ dispatch 分支 + `run_resolvers_command` + `print_resolvers_table` + `print_resolvers_help`。
  - `run_resolvers_command` 每次调用在 `get_default_registry()` 上 `register_builtin_resolvers(Arc::new(ReqwestHttpClient::new()), Arc::new(TokioDnsResolver))`（幂等，重复 id 忽略）。
  - `list`：`--json`（手写 JSON `{id,cost,backend,description,hasInputSchema,hasOutputSchema}` 绕开 ResolverSummary 的 Serialize 需求）/ `--cost`（free|rate-limited|paid，非法→exit 1）/ `--backend`（backend 字符串过滤）。`describe <id>`：无 id→exit 1；查 registry.get 后 `ResolverContext::new()` + `available().await`（await 前 `drop(registry)` 释放 MutexGuard 避免 !Send 跨 await）；打印 ID/Cost/Backend/Description/Available/Input&Output schema（pretty JSON）。
- **注意**：x_handle_to_tweet 的 `cost()` 返回 `RateLimited`（非 paid，Rust 自洽）；无 token 时 `available()=false`（describe 显示 "no (check env/config)"），url_reachable `available()=true`。

**E2E smoke（真实 zbrain.exe 子进程）全过：**
- `resolvers list` → 表头 ID/COST/BACKEND/DESCRIPTION + 2 resolver（url_reachable free head-check / x_handle_to_tweet rate-limited x-api-v2）+ "2 resolvers registered."。
- `resolvers list --json` → 2 元素数组字段正确。
- `resolvers list --cost free` → 仅 url_reachable（1）；`--cost paid` → "No resolvers registered."（空）；`--cost bogus` → stderr 错误 + exit 1。
- `resolvers list --backend x-api-v2` → 仅 x_handle_to_tweet；`--json --cost rate-limited` → 仅 x_handle_to_tweet JSON。
- `resolvers describe url_reachable` → available yes + schemas；`describe x_handle_to_tweet` → available no + schemas（无 token）；`describe does_not_exist` → "Resolver not found" + exit 1。
- `resolvers`（无子命令）→ help exit 0；`resolvers frobnicate`（未知子命令）→ clap 拒绝 exit 2（非零，TS 是 exit 1 的轻微偏差，clap 生态一致，已知）。

**路线图：** 1-6-4-10-4 + 1-6-4-10 全 completed + render。1-6-4-10 整刀收口（4/4 子节点：SDK 核心 13 / url-reachable 18 / x-api 29 / CLI 接线）。resolvers 全部 resolvers 模块共 60 单测 + zbrain-core 全 lib 1677 passed。

**下一步候选（resolvers 线闭环后）：** `check-resolvable`（仍阻塞于 skill-tree 静态分析 core ~2300 行 + `--fix` 改写用户 SKILL.md 风险高）或回看其他干净命令；integrity G51 的 auto 路径现可借 `register_builtin_resolvers` 复用 resolver。

## anomalies 迁移设计 grill 完成（zj-grill-me + zj-roadmap-driven，2026-07-17）

用户二选一：「check-resolvable（阻塞+高风险）」或「回看其他干净孤儿命令」。探查后 grill，用户选 **anomalies**。

**地形发现（探查代码，非凭空）：**
- `check-resolvable` 命令(333 行) 轻量，但核心 `src/core/check-resolvable.ts`(662) 拉入整片静态分析子系统：routing-eval 396 / dry-fix 575 / skill-trigger-index 252 / filing-audit 240 / skill-manifest 146 / resolver-filenames 60 / repo-root 274 ≈ **2905 行**，且 `--fix` 会改写用户 `SKILL.md` → 阻塞+高风险，确认不干净。
- `anomalies`(110 行 CLI) 路由 `engine.findAnomalies(...)`，但 **Rust `BrainEngine` trait 无此方法**（minion 注释 "engine method not yet ported"）。故 anomalies 须**先迁引擎方法再迁 CLI**，是 bounded 垂直切片。
- `findAnomalies` 算法干净分层：纯统计 `meanStddev`/`computeAnomaliesFromBuckets`/`cohortKey`（在 `src/core/cycle/anomaly.ts`，易测易迁）+ SQL（pglite-engine/postgres-engine/operations 三处，产出 baseline buckets + today counts 喂纯函数）。
- `check-update`(179) 虽最干净（纯函数离线可测、仅依赖 detectInstallMethod~25 行），但用户选 anomalies。
- Rust **已有 thin-client 基建**（`config::is_thin_client` + `mcp_client::call_remote_tool`），anomalies CLI 可镜像 TS 本地 engine + thin-client 回退。

**3 决策（roadmap 1-6-4-11 已 decide + render）：**
- Q1 选哪个命令：**anomalies**（check-resolvable 留后；check-update 干净但用户选 anomalies）。先迁 `engine.find_anomalies` 再迁 CLI。
- Q2 后端范围：**三后端全迁**（Libsql + InMemory + Postgres），SQL 按方言重写。Libsql 用 recursive date-series CTE + strftime + json_group_array；Postgres 用 generate_series/date_trunc/array_agg；InMemory 在 Rust 内存直接计算。对齐 Part4 search / Phase7 PG mirror 惯例。
- Q3 thin-client：镜像 TS，本地 engine path + thin-client 经 `call_remote_tool(cfg,'find_anomalies',...)` 回退，零新建基建。

**节点状态：** 新建 `1-6-4-11`(in_progress, exploit) + 4 子节点：
- 1-6-4-11-1 纯统计函数 port（meanStddev/computeAnomaliesFromBuckets/cohortKey）+ 单测 【in_progress，起刀点】
- 1-6-4-11-2 find_anomalies 引擎方法（BrainEngine trait + Libsql SQL 方言 + InMemory 计算）
- 1-6-4-11-3 find_anomalies Postgres 后端 SQL
- 1-6-4-11-4 anomalies CLI 接线（flags + thin-client 回退 + 输出）+ 删 TS 命令

**起刀顺序：** 1-6-4-11-1 纯统计函数（零 DB、零网络、全离线可测，tracer bullet）→ 2 引擎方法 Libsql+InMemory → 3 Postgres → 4 CLI + E2E + 删 TS。

**待验证（删 TS 步骤）：** cycle/anomaly.ts 的 `computeAnomaliesFromBuckets` 是否除引擎方法外另有 importer（若有则迁完不能删）；src/commands/anomalies.ts 有 operations.ts op-shadow（cliHints name='anomalies' 调 engine.findAnomalies），删 TS CLI 命令前须过 PARITY_GATE（零 src 引用 + 零 test 引用 + 真 Rust 覆盖非 stub）。

**经验/铁律：** grill 能读代码不凭空问——本刀靠探查戳穿"anomalies 是干净独立命令"的直觉（实际需先迁引擎方法），并确认 check-resolvable 真正 blockers（~2905 行静态分析 + --fix 写回）。anomalies 的 SQL 方言差异（PG generate_series vs SQLite 递归 CTE）是主要工作量。
